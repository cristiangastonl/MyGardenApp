---
phase: 19-pet-toxicity
plan: 07
type: execute
wave: 4
depends_on: ["19-00", "19-01", "19-02", "19-03", "19-04", "19-05", "19-06"]
files_modified: []
autonomous: false
requirements:
  - TOX-01
  - TOX-02
  - TOX-03
  - TOX-04
  - TOX-05
  - TOX-06

must_haves:
  truths:
    - "All file-content sentinels in smoke-phase19 are at PASS (no SKIP, no FAIL)"
    - "Phase 18 cross-phase regression sentinels remain PASS"
    - "User has reviewed and either run the device-test checklist OR explicitly approved Option B deferral to v1.2 backlog memory"
    - "All 6 Phase 19 requirement IDs (TOX-01..06) reach closing PASS state at the file-content/i18n-parity surface"
  artifacts: []
  key_links: []
---

<objective>
TOX-01..06 closing manual gate: a checkpoint:human-verify task that consolidates Phase 19's automation-green gate (file-content + tsc + i18n parity + smoke-phase19 + smoke-phase18) with a manual device-test checklist. User runs the checklist on iOS + Android dev clients OR explicitly approves Option B deferral to the v1.2 device-test backlog memory file (per Phase 18 precedent).

This plan is `autonomous: false` per Phase 13/14/18 manual-gate convention. The executor pauses before the checkpoint for user input. After approval (or deferral), Phase 19 closes at the code level.

Per CLAUDE.md milestone-end batching pattern: physical device verification (Pressable inside Gesture.Race, scroll-to-section animation smoothness, etc.) is batched into the v1.2 milestone-end submission gate alongside Phase 13 / 14 / 18 deferred device-tests.

Purpose: Surface manual-only verifications that no smoke runner can exercise (RNGH gesture system, RN ScrollView scrollTo timing, banner z-order). Provide a deferral lane consistent with Phase 18 Option B precedent.

Output: Phase 19 closure decision (run-now or defer) recorded; if defer, 14-block device-test checklist appended to v1_2_test_backlog memory file; SUMMARY.md captures user's explicit signal.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-pet-toxicity/19-CONTEXT.md
@.planning/phases/19-pet-toxicity/19-RESEARCH.md
@.planning/phases/19-pet-toxicity/19-VALIDATION.md
@.planning/phases/19-pet-toxicity/19-00-SUMMARY.md
@.planning/phases/19-pet-toxicity/19-01-SUMMARY.md
@.planning/phases/19-pet-toxicity/19-02-SUMMARY.md
@.planning/phases/19-pet-toxicity/19-03-SUMMARY.md
@.planning/phases/19-pet-toxicity/19-04-SUMMARY.md
@.planning/phases/19-pet-toxicity/19-05-SUMMARY.md
@.planning/phases/19-pet-toxicity/19-06-SUMMARY.md
@.planning/STATE.md
@CLAUDE.md
@/Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-checkpoint automation gate — confirm full suite green before pausing for human-verify</name>
  <files></files>
  <read_first>
    .planning/phases/19-pet-toxicity/19-00-SUMMARY.md through 19-06-SUMMARY.md (verify each plan's PASS/SKIP/FAIL counts and confirm all SKIPs flipped to PASS)
  </read_first>
  <action>
**Goal:** Run the full automation gate end-to-end. Document the green output. Do NOT proceed to the manual checkpoint if any command fails.

**Step 1 — Run full suite in sequence:**

```bash
npx tsc --noEmit
npm run check:i18n-keys
npm run smoke:phase19
npm run smoke:phase18
```

Expected:
- `npx tsc --noEmit` exits 0
- `npm run check:i18n-keys` exits 0
- `npm run smoke:phase19` exits 0 with `[Phase 19 smoke] PASS=N FAIL=0 SKIP=0` (zero SKIP — all flipped by Plans 19-01..06)
- `npm run smoke:phase18` exits 0 (cross-phase regression preserved)

**Step 2 — Verify catalog count + ASPCA citation density:**

```bash
grep -c "petToxicity:" src/data/plantDatabase.ts                  # MUST be exactly 118
grep -c "https://www.aspca.org" src/data/plantDatabase.ts         # MUST be >= 80 (CRIT-2 audit density)
grep -c "// LATAM-specific" src/data/plantDatabase.ts             # MUST be >= 3 (LATAM unknowns documented)
```

**Step 3 — Verify component-level integration:**

```bash
grep -c "PetToxicityBadge" src/components/PlantCard.tsx                 # MUST be >= 2 (import + JSX)
grep -c "MascotasContent\|onSectionLayout('mascotas')" src/components/MyPlantDetailModal.tsx   # MUST be >= 1
grep -c "petSafeOnly" src/screens/OnboardingScreen.tsx                  # MUST be >= 3
grep -c "hasAnyToxicityWarning\|toxicityBannerData" src/components/AddPlantModal.tsx   # MUST be >= 1
```

**Step 4 — If any command fails, HALT and surface failure to user.** Do NOT proceed to Task 2 (manual checkpoint) until automation is fully green. Failure recovery: identify which plan introduced the regression, follow gap-closure pattern (e.g., reopen Plan 19-04 if Mascotas section was incomplete).

**Step 5 — On success, document the green state:**

Capture the full suite output in this plan's SUMMARY for handoff to user before manual checkpoint.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:i18n-keys && npm run smoke:phase19 && npm run smoke:phase18</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run check:i18n-keys` exits 0
    - `npm run smoke:phase19` exits 0 with `SKIP=0` (verifiable: `npm run smoke:phase19 2>&1 | grep -E "PASS=[0-9]+ FAIL=0 SKIP=0"`)
    - `npm run smoke:phase18` exits 0
    - `grep -c "petToxicity:" src/data/plantDatabase.ts` returns exactly 118
    - `grep -c "https://www.aspca.org" src/data/plantDatabase.ts` >= 80
    - `grep -c "// LATAM-specific" src/data/plantDatabase.ts` >= 3
    - `grep -c "PetToxicityBadge" src/components/PlantCard.tsx` >= 2
    - `grep -c "MascotasContent\|onSectionLayout('mascotas')" src/components/MyPlantDetailModal.tsx` >= 1
    - `grep -c "petSafeOnly" src/screens/OnboardingScreen.tsx` >= 3
    - `grep -c "hasAnyToxicityWarning\|toxicityBannerData" src/components/AddPlantModal.tsx` >= 1
  </acceptance_criteria>
  <done>Automation gate fully green. Output documented. Ready for human-verify checkpoint in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: MANUAL CHECKPOINT — iOS + Android device verification of Phase 19 surface (or Option B deferral to v1.2 backlog)</name>
  <files></files>
  <read_first>
    .planning/phases/19-pet-toxicity/19-07-SUMMARY.md (Task 1 automation gate output — verify all green BEFORE engaging user),
    /Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md (existing v1.2 device-test backlog — Phase 19 entries append here on Option B deferral),
    .planning/phases/18-plantcard-cleanup-mood-emoji/18-05-SUMMARY.md (Phase 18 manual deferral precedent — pattern to mirror)
  </read_first>
  <action>
PAUSE for user input. Present the 14-item device-test checklist below to the user. Wait for approval signal before proceeding. Do NOT execute any code beyond presenting the checklist + waiting for the resume-signal.
  </action>
  <verify>
    <automated>echo "MANUAL CHECKPOINT — awaiting user resume-signal"</automated>
  </verify>
  <acceptance_criteria>
    - User has provided one of: "approved" / "approved (Option B)" / specific issue report
    - On "approved": Phase 19 SUMMARY documents user's signal verbatim
    - On "approved (Option B)": v1_2_test_backlog.md memory file appended with the 14-item Phase 19 checklist (with hard-fail vs soft-fail classifier per Phase 18 precedent)
    - On issue report: gap-closure plan opened OR issue deferred per user direction
  </acceptance_criteria>
  <done>User signal received and recorded; Phase 19 closes at code level (with or without device-deferral).</done>
  <what-built>
    Phase 19 Pet Toxicity full surface:
    - **TOX-01:** ToxLevel + PetToxicityEntry types; petToxicity helper module with absence === 'unknown' discipline
    - **TOX-02:** All 118 catalog entries classified (cats + dogs) against ASPCA with source URL citations; per-entry symptom arrays for caution/toxic entries in EN + ES plants.json; LATAM 'unknown' entries documented
    - **TOX-03:** PetToxicityBadge component (Pressable + emoji + colored stripe); rendered in PlantCard headerRight as cluster (cat then dog) gated to 'toxic'/'caution' only; tap routes to MyPlantDetailModal scrolled to Mascotas section
    - **TOX-04:** 5th 🐾 Mascotas section in MyPlantDetailModal (always visible); per-state copy for all 4 ToxLevel × 2 species combinations; symptoms list with graceful degradation; initialSection prop with scrollTo-section mechanism (Pitfall 2 mitigation: 50ms defer)
    - **TOX-05:** OnboardingScreen pet-safe Switch with AND-style filter chain + empty-state fallback; AddPlantModal toxicity warning banner (passive informational, severity-aware background, non-blocking, tap → Mascotas)
    - **TOX-06:** check-i18n-keys.mjs extended with conditional petToxicity.symptoms validation; common.json toxicity.* namespace polished; voseo regression preserved
    - All Phase 19 sentinels at PASS in smoke-phase19; smoke-phase18 cross-phase regression preserved.
  </what-built>
  <how-to-verify>
    Build and run the app on iOS dev client (or Expo Go) AND Android dev client (or Expo Go). Verify the 14-item checklist below. Each item maps to a TOX-XX requirement and a Pitfall flagged in RESEARCH.md that file-content sentinels CANNOT exercise.

    **Block A — TOX-03 PlantCard badge gestures (Pitfall 3 from RESEARCH.md):**
    1. Long-press a card with toxicity badge for ≥500ms → bottom-sheet menu opens (Phase 18 long-press), badge tap NOT fired.
    2. Swipe-left a card with toxicity badge >25% → reveal-action delete shown (Phase 18 swipe), badge tap NOT fired.
    3. Brief tap on the cat badge (no drift, no hold) → MyPlantDetailModal opens scrolled to Mascotas section. Repeat with dog badge — same modal opens to same section.
    4. Brief tap on the cat badge with thumb drift >15px → ACCEPTABLE if Pan wins the race (Pitfall 3 documented trade-off). Card-body tap then opens modal at top; user scrolls to Mascotas. Either result is acceptable for shipping.

    **Block B — TOX-03 visual hierarchy (Pitfall 7 from RESEARCH.md):**
    5. On a card with both mood emoji AND cat+dog badges visible (e.g., a toxic plant in warning health state), confirm:
       - Mood emoji on image-overlay (solid colored circle) reads as "how plant feels"
       - Toxicity badges in headerRight (emoji + thin colored stripe) read as "fixed safety fact"
       - The two are visually distinct enough that the user does NOT confuse them at-a-glance.

    **Block C — TOX-04 Mascotas section + scroll-to-anchor (Pitfall 2 from RESEARCH.md):**
    6. Tap a toxicity badge → modal opens, ScrollView lands AT the Mascotas section (not scrolling there manually). Scroll-to animation feels natural (~250ms).
    7. Tap card body (not the badge) on a non-toxic plant → modal opens at TOP, no auto-scroll.
    8. Open modal on cinta (safe-safe) → Mascotas section shows single line "Segura para gatos y perros ✓".
    9. Open modal on a LATAM-unknown entry (e.g., ceibo) → Mascotas section shows two lines "No verificada para esta especie en LATAM 🤷" (one per species).
    10. Open modal on potus (toxic-toxic) → Mascotas section shows two lines with "Tóxica para gatos." + "Síntomas:" + bullet list, same for dogs.

    **Block D — TOX-05 OnboardingScreen filter:**
    11. Walk through onboarding to the catalog browse → toggle pet-safe Switch ON → confirm:
        - All categories: only safe-safe plants visible (no unknowns, no toxics)
        - Aromáticas + pet-safe ON: oregano (caution) HIDDEN, basil + romero (safe) VISIBLE
        - Empty state appears for any category with zero pet-safe plants; suggestion shows a different category name.

    **Block E — TOX-05 AddPlantModal banner:**
    12. Identify potus → AddPlantModal opens → red banner at top reads "⚠️ Tóxica para gatos y perros". Tap banner → MyPlantDetailModal opens to Mascotas section.
    13. Identify cinta → AddPlantModal opens → NO banner renders. Add normally.
    14. Identify oregano (caution-caution if classification confirmed) → AddPlantModal opens → yellow banner reads "⚠️ Precaución para gatos y perros" (or species-appropriate cautionSingle/mixed copy).

    **Cross-language voseo correctness (TOX-06):**
    Run app in ES locale → confirm all 14 items above show ES voseo-compliant copy. Switch to EN → confirm parallel English wording. No Castilian forms (`tienes` / `puedes` / etc.).

    **Phase 18 cross-phase regression:**
    On the same cards used above: verify swipe-to-delete still works (Phase 18 CARD-01); long-press menu still works (Phase 18 CARD-02); mood emoji still always visible (Phase 18 GAM-03); Toast still appears for delete-undo flow.

    **If any item fails:** Surface as a Phase 19 device-test bug. Fix in a gap-closure plan or defer to v1.2 backlog memory.

    **Option B deferral (if user prefers — per Phase 18 precedent):**
    User responds with "approved" + "Option B" / "defer to backlog". Executor:
    1. Append a "Phase 19 device-test checklist" section to `/Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md` with all 14 items above + classifier (hard-fail = Block A items 1-3, Block C item 6 / soft-fail = visual hierarchy, empty state, voseo audit).
    2. Preserve user's explicit approval signal in this plan's SUMMARY's "Decisions Made" section.
    3. Close Phase 19 at the code level (file-content gates + smoke runners green).
  </how-to-verify>
  <resume-signal>Type "approved" (full run completed, all items pass) OR "approved (Option B)" (defer to v1.2 backlog) OR describe specific issues + which items failed.</resume-signal>
</task>

</tasks>

<verification>
- [ ] Full automation suite green (Task 1 confirmed)
- [ ] User has provided explicit approval signal (Task 2)
- [ ] If Option B chosen: device-test checklist appended to v1_2_test_backlog.md memory file
- [ ] If issues reported: gap-closure plan opened (or deferred per user direction)
</verification>

<success_criteria>
1. Automation gate fully green at checkpoint entry
2. User responds with explicit approval signal
3. If Option B: 14-item device-test checklist appended to v1_2_test_backlog.md with hard-fail/soft-fail classifier
4. Phase 19 closes at code level — all 6 TOX-XX requirement IDs reach PASS at file-content surface
5. v1.2 milestone-end submission gate accumulates Phase 19 deferred items alongside Phase 13/14/18 deferrals
</success_criteria>

<output>
After completion, create `.planning/phases/19-pet-toxicity/19-07-SUMMARY.md` documenting:
- Pre-checkpoint automation gate output (PASS counts, exit codes for tsc / check:i18n-keys / smoke:phase19 / smoke:phase18)
- User's explicit approval signal verbatim
- If Option B: backlog memory append confirmation + classifier breakdown
- Phase 19 closure status (CLOSED at code level / partially open with gap-closure pending)
- Final tally: 6 requirement IDs, 7 plans, total tasks, total commits, ~minutes execution time
- Cross-phase regression confirmation (Phase 18 sentinels still PASS)
- Carry-forward note: Phase 20 (FERT) inherits the EducationalSection pattern + initialSection precedent for fertilize task surfaces
</output>
