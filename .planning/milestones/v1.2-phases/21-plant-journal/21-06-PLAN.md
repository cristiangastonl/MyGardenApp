---
phase: 21-plant-journal
plan: 06
type: execute
wave: 5
depends_on: ["21-05"]
files_modified: []
autonomous: false
requirements: [JOURNAL-01, JOURNAL-02, JOURNAL-03, JOURNAL-04, JOURNAL-05]
must_haves:
  truths:
    - "All automated gates pass (tsc + check:i18n-keys + smoke 18/19/20/21) before checkpoint"
    - "User has visibility into 14-item Blocks A–E device-test checklist for Phase 21"
    - "User decision lane between Option A (run device tests now) vs Option B (defer to v1.2 backlog memory per Phase 18-05 / 19-07 / 20-10 precedent)"
    - "If Option B selected: 14-item checklist appended verbatim to v1_2_test_backlog.md memory"
    - "Phase 21 closes at code level upon checkpoint resolution"
    - "Block A item A3 cites the Plan 21-01 __DEV__ payload-bytes log (Important 9) for quantitative verification — log magnitude tells tester whether photos are stored as base64 (~50KB growth) or filesystem refs (<1KB growth)"
  artifacts:
    - path: ".planning/phases/21-plant-journal/21-06-SUMMARY.md"
      provides: "Closing summary documenting automation-gate result + Option A/B decision + 14-item checklist disposition"
  key_links:
    - from: "Phase 21 closure"
      to: "v1_2_test_backlog.md memory (if Option B selected)"
      via: "checklist append per milestone-end batching precedent"
      pattern: "Phase 21|Plant Journal"
    - from: "Block A.A3 manual test"
      to: "Plan 21-01 __DEV__ payload-bytes instrumentation (Important 9)"
      via: "Metro console grep for `[useStorage] AsyncStorage payload bytes:`"
      pattern: "AsyncStorage payload bytes"
---

<objective>
Wave 5 closing manual-checkpoint plan. Two-step structure mirroring Phase 19-07 + 20-10 verbatim:

**Step 1 (autonomous Task 1):** Re-verify all 6 automation gates (npx tsc --noEmit + npm run check:i18n-keys + npm run smoke:phase18..21) exit 0 at plan start time. This is the "we're code-complete" gate.

**Step 2 (checkpoint:human-action Task 2):** Present the 14-item Blocks A–E manual device-test checklist to the user. User chooses Option A (run device tests now — block plan completion until all 14 items resolved) or Option B (defer to v1_2_test_backlog.md memory per Phase 18-05 / 19-07 / 20-10 milestone-end batching precedent — plan closes immediately at code level).

Purpose: Phase 21 closes at code level. Manual device verification is either executed now or queued for the end-of-v1.2 batch test (consistent with the 3 prior phases' precedent). The orchestrator can then proceed to Phase 22 planning.

Output: 0 source files modified. 1 SUMMARY.md authored. STATE.md / ROADMAP.md updated to reflect Phase 21 closure.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-plant-journal/21-CONTEXT.md
@.planning/phases/21-plant-journal/21-RESEARCH.md § Validation Architecture Manual-Only Verifications
@.planning/phases/21-plant-journal/21-VALIDATION.md § Manual-Only Verifications
@.planning/phases/19-pet-toxicity/19-07-SUMMARY.md (precedent — Option A/B decision pattern)
@.planning/phases/20-fertilization-subsystem/20-10-SUMMARY.md (precedent — Option B selected; backlog memory append pattern)

<interfaces>
**Automation-gate commands** (from RESEARCH § Validation Architecture):
1. `npx tsc --noEmit` — type-check passes
2. `npm run check:i18n-keys` — plants.json EN+ES parity passes (118 catalog ids, no Phase 21 catalog changes)
3. `npm run smoke:phase18` — Phase 18 STRICT cross-phase regression
4. `npm run smoke:phase19` — Phase 19 STRICT cross-phase regression
5. `npm run smoke:phase20` — Phase 20 STRICT cross-phase regression
6. `npm run smoke:phase21` — all JOURNAL-01..05 PASS + Phase 18/19/20 STRICT cross-phase regression PASS

**14-item Blocks A–E checklist** (derived from VALIDATION.md § Manual-Only Verifications, expanded per Phase 19-07 / 20-10 precedent):

**Block A — Data integrity (3 hard-fail items)**
- A1: Add a journal entry (no photo) → restart app → entry survives, appears in reverse-chrono order
- A2: Add 5 entries across 3 days → restart app → date headers show "Hoy" / "Ayer" / "Hace 2 días" correctly
- A3: Add entry with photo → restart app → photo renders correctly from documentDirectory URI. **Inspect Metro console for `[useStorage] AsyncStorage payload bytes: N` log (Plan 21-01 Important 9 instrumentation); size after adding a photo should grow by <1KB (filesystem ref), NOT ~50KB+ (base64 inline). If growth is ~50KB, FAIL — photo is being serialized inline, not stored as filesystem reference.**

**Block B — Bottom-sheet + gesture flow (6 hard-fail items)**
- B1: Tap "+ Nueva entrada" → BottomSheetModal opens at 60% snap on iOS AND Android
- B2: Tap "Guardar" with NO text + NO photo + NO tag → entry committed; Toast "Entrada guardada 📔" visible above sheet
- B3: Tap "📷 Cámara" → permission prompt first run → grant → camera opens → preview tile shows
- B4: Tap "🖼️ Galería" → gallery picker opens → photo selected → preview tile shows; tap "Quitar" X → preview clears
- B5: Select careTag chip → highlighted; tap selected chip again → deselects (single-select toggle)
- B6: Long-press JournalEntryRow → BottomSheetModal "Eliminar entrada" → Alert.alert confirm → entry + photo file deleted

**Block C — Animation + visual (1 soft-fail item)**
- C1: Diario section collapsed by default on modal open; tap header → 180ms smooth collapse-expand on iOS AND Android (Pitfall 9 — verify Android does NOT have height-0 stuck state)

**Block D — Cascade cleanup (3 hard-fail items)**
- D1: Add 3 entries with photos to plant P → delete plant P → verify `documentDirectory/journal/${plantId}/` directory is gone
- D2: Re-add a plant (different id) → no leftover photos in journal/ dir from previously deleted plants
- D3: tsc + smoke 18/19/20/21 still green AFTER manual delete cascade test

**Block E — Z-order + multi-window (1 soft-fail item)**
- E1: With JournalQuickAddSheet open AND PaywallModal trigger fires (simulate) → sheet dismisses gracefully via useDismissOnPaywall → no Z-order glitch

**Total: 14 items** — 12 hard-fail (A1-3, B1-6, D1-3) + 2 soft-fail (C1, E1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automation-gate re-verification (the "code-complete" gate)</name>
  <files></files>
  <read_first>
    - .planning/phases/21-plant-journal/21-VALIDATION.md § Sampling Rate
    - .planning/phases/19-pet-toxicity/19-07-SUMMARY.md (precedent — automation gate before manual checkpoint)
    - .planning/phases/20-fertilization-subsystem/20-10-SUMMARY.md (precedent — same pattern)
  </read_first>
  <action>
    Run the full Phase 21 automation suite. ALL 6 commands MUST exit 0 before proceeding to Task 2:

    ```bash
    npx tsc --noEmit
    npm run check:i18n-keys
    npm run smoke:phase18
    npm run smoke:phase19
    npm run smoke:phase20
    npm run smoke:phase21
    ```

    Record results in working memory for the SUMMARY:
    - `tsc --noEmit`: PASS / FAIL
    - `check:i18n-keys`: PASS / FAIL (also note current catalog count — should still be 118)
    - `smoke:phase18`: PASS=N FAIL=0 SKIP=0
    - `smoke:phase19`: PASS=N FAIL=0 SKIP=0
    - `smoke:phase20`: PASS=N FAIL=0 SKIP=0
    - `smoke:phase21`: PASS=N FAIL=0 SKIP=0 (all JOURNAL-01..05 sentinels PASS + Phase 18/19/20 STRICT cross-phase regression PASS)

    **If ANY automation gate FAILS:** do NOT proceed to Task 2. Instead, document the failure in the SUMMARY and surface to the orchestrator for a remediation plan (`/gsd:plan-phase 21 --gaps`). This is the same fail-fast pattern from Phase 19-07 / 20-10.

    **If ALL automation gates PASS:** proceed to Task 2 (manual checkpoint).
  </action>
  <acceptance_criteria>
    - All 6 commands listed exit 0
    - `npm run smoke:phase21` output shows `PASS=N FAIL=0 SKIP=0` (no remaining SKIPs after Plan 21-05)
    - Phase 18/19/20 STRICT cross-phase regression all PASS
    - No catalog drift introduced (check:i18n-keys still reports 118 ids)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run check:i18n-keys && npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20 && npm run smoke:phase21</automated>
  </verify>
  <done>All 6 automation gates green. Phase 21 is code-complete; ready for manual checkpoint decision.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Manual device-test checkpoint — Option A (run now) vs Option B (defer to v1.2 backlog)</name>
  <files></files>
  <read_first>
    - .planning/phases/21-plant-journal/21-06-SUMMARY.md (Task 1 automation-gate output — verify all green BEFORE engaging user)
    - /Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md (existing v1.2 device-test backlog — Phase 21 entries append here on Option B deferral)
    - .planning/phases/19-pet-toxicity/19-07-SUMMARY.md (Phase 19 manual gate closure pattern — Option A vs Option B)
    - .planning/phases/20-fertilization-subsystem/20-10-SUMMARY.md (Phase 20 manual gate closure pattern — Option B selected; memory append timestamp)
    - .planning/phases/21-plant-journal/21-VALIDATION.md § Manual-Only Verifications
  </read_first>
  <action>
    **Present the 14-item Blocks A–E checklist to the user verbatim** and ask them to choose Option A (run now) or Option B (defer to v1.2 backlog). Claude pauses for user input — DO NOT proceed with any automation in this task.

    **What was built (summary for user):**
    All 5 JOURNAL-01..05 requirement IDs are CLOSED at code level:
    - JOURNAL-01: `AppData.journals?: Record<string, JournalEntry[]>` + migration default `{}` + Plan 21-01 `__DEV__` payload-bytes instrumentation (Important 9)
    - JOURNAL-02: `JournalEntry` type + 4 journalService.ts functions using modern Paths/File/Directory API
    - JOURNAL-03: `addJournalEntry` + `deleteJournalEntry` actions in useStorage; `journals` exposed on public StorageContextType (Blocker 2 — single atomic 3-site mutation)
    - JOURNAL-04: Diario 6th section in MyPlantDetailModal + JournalSection + JournalQuickAddSheet + JournalEntryRow + Toast "Entrada guardada 📔" at screen-level via Approach B (Important 6) + ModalSectionId 'diario' extension via locked import path (Important 8)
    - JOURNAL-05: NO premium-gate at any journal-read site (negative-grep sentinel PASS)

    All 6 automation gates green (Task 1 of this plan). Phase 18 / 19 / 20 STRICT cross-phase regression PASS.

    **14-item Blocks A–E manual device-test checklist** (12 hard-fail + 2 soft-fail):

    **Block A — Data integrity (3 hard-fail)**
    - **A1**: Add a journal entry (no photo) → restart app → entry survives, appears in reverse-chrono order
    - **A2**: Add 5 entries across 3 days → restart app → date headers show "Hoy" / "Ayer" / "Hace 2 días" correctly
    - **A3**: Add entry with photo → restart app → photo renders from documentDirectory URI. **Inspect Metro console for `[useStorage] AsyncStorage payload bytes: N` log (Plan 21-01 Important 9 instrumentation); size after adding a photo should grow by <1KB (filesystem ref), NOT ~50KB+ (base64 inline). If growth is ~50KB+, FAIL — photo is being serialized inline, not stored as filesystem reference.**

    **Block B — Bottom-sheet + gesture flow (6 hard-fail)**
    - **B1**: Tap "+ Nueva entrada" → BottomSheetModal opens at 60% snap on iOS AND Android
    - **B2**: Tap "Guardar" with NO text + NO photo + NO tag → entry committed; Toast "Entrada guardada 📔" visible above sheet (screen-level Toast via Approach B — Important 6)
    - **B3**: Tap "📷 Cámara" → permission prompt first run → grant → camera opens → preview tile shows
    - **B4**: Tap "🖼️ Galería" → gallery picker opens → photo selected → preview tile shows; tap "Quitar" X → preview clears
    - **B5**: Select careTag chip → highlighted; tap selected chip again → deselects (single-select toggle)
    - **B6**: Long-press JournalEntryRow → BottomSheetModal "Eliminar entrada" → Alert.alert confirm → entry + photo file deleted

    **Block C — Animation + visual (1 soft-fail)**
    - **C1**: Diario section collapsed by default on modal open; tap header to expand → 180ms smooth collapse-expand on iOS AND Android (Pitfall 9 — verify Android does NOT have height-0 stuck-state bug)

    **Block D — Cascade cleanup (3 hard-fail)**
    - **D1**: Add 3 entries with photos to plant P → delete plant P → verify `documentDirectory/journal/${plantId}/` directory is gone (iOS: Files app; Android: `adb shell ls $EXTERNAL_STORAGE/Android/data/com.mygardencare.app/files/journal/`)
    - **D2**: Re-add a plant (different id) → confirm no leftover photos in journal/ dir from previously deleted plants
    - **D3**: Run `npx tsc --noEmit && npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20 && npm run smoke:phase21` AFTER manual delete cascade test → all still green

    **Block E — Z-order + multi-window (1 soft-fail)**
    - **E1**: With JournalQuickAddSheet open AND a PaywallModal trigger fires (simulate via __DEV__ flow if available) → sheet dismisses gracefully via `useDismissOnPaywall` hook → no Z-order glitch

    **Total: 14 items** — 12 hard-fail (A1-3, B1-6, D1-3) + 2 soft-fail (C1, E1)

    ---

    **Choose ONE option:**

    **Option A — Run all 14 items now on device.**
    User runs the checklist on iOS dev client + Android dev client. Reports back pass/fail per item. If any hard-fail item fails, plan re-enters with `/gsd:plan-phase 21 --gaps`. If all hard-fail items pass (12/12) and at least one of the 2 soft-fail items is acceptable, Phase 21 closes fully.

    **Option B — Defer to v1.2 backlog memory** (per Phase 18-05 / 19-07 / 20-10 milestone-end batching precedent).
    The 14-item checklist is appended verbatim to `v1_2_test_backlog.md` memory with section heading `## Phase 21 — Plant Journal (deferred YYYY-MM-DD)` + 12-hard / 2-soft classifier. Phase 21 closes at code level immediately; device tests run as part of end-of-v1.2 batch alongside Phase 18 38-item + Phase 19 14-item + Phase 20 14-item + 69-entry image upload backlog before App Store / Google Play submission.

    **Resume signals:**
    - `option-a` — I will run the 14-item checklist now and report results.
    - `option-b` — Defer to v1.2 backlog memory; close Phase 21 at code level.
    - Or describe a specific item that needs adjustment.

    **If user selects Option A:**
    Walk through each of the 14 items. Record PASS/FAIL in SUMMARY. If any hard-fail item FAILS, surface to orchestrator for `/gsd:plan-phase 21 --gaps` remediation. Plan completes only after all hard-fail items resolved.

    **If user selects Option B:**
    Append the 14-item checklist verbatim to `/Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md` with section heading + 12-hard / 2-soft classifier. Plan completes immediately at code level.
  </action>
  <acceptance_criteria>
    - User responds with `option-a` OR `option-b` (or a specific item adjustment request).
    - If Option A: all 12 hard-fail items report PASS; soft-fail items acceptable; SUMMARY records per-item PASS/FAIL.
    - If Option B: 14-item checklist appended verbatim to `v1_2_test_backlog.md` memory with `## Phase 21 — Plant Journal (deferred <date>)` heading + 12-hard / 2-soft classifier; SUMMARY records timestamp of append.
    - `.planning/phases/21-plant-journal/21-06-SUMMARY.md` exists and documents the decision lane taken.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/21-plant-journal/21-06-SUMMARY.md</automated>
  </verify>
  <done>User has resolved the Option A vs Option B checkpoint. Phase 21 closes at code level either way; manual device-test results either captured in SUMMARY (A) or queued to v1_2_test_backlog.md memory (B).</done>
</task>

</tasks>

<verification>
**If Option A selected:**
- All 12 hard-fail items report PASS
- ≥0 of 2 soft-fail items report acceptable (UX-level judgment)
- If any hard-fail FAILS: re-enter via `/gsd:plan-phase 21 --gaps`

**If Option B selected:**
- 14-item checklist appended to `v1_2_test_backlog.md` memory with `## Phase 21 — Plant Journal` heading + classifier
- Phase 21 closes at code level immediately
- SUMMARY documents Option B selection + memory append timestamp
</verification>

<success_criteria>
**Phase 21 closure (code level):**
- [x] All 6 automation gates green at plan-start time (Task 1)
- [x] All JOURNAL-01..05 sentinels PASS in `smoke-phase21.cjs`
- [x] Phase 18/19/20 STRICT cross-phase regression all PASS
- [x] User has resolved the Option A/B checkpoint
- [x] If Option B: 14-item checklist queued in v1_2_test_backlog.md memory
- [x] `.planning/phases/21-plant-journal/21-06-SUMMARY.md` written
- [x] Phase 21 ready for orchestrator-spawned verifier to confirm closure invariants
- [x] Block A.A3 references Important 9 `__DEV__` payload-bytes instrumentation (quantitative verification)
</success_criteria>

<output>
After completion, create `.planning/phases/21-plant-journal/21-06-SUMMARY.md` documenting:
- Automation-gate results (6 commands exit codes + smoke runner PASS/FAIL/SKIP counts)
- 14-item checklist verbatim (with A3 referencing the __DEV__ payload-bytes log)
- User's Option A vs Option B decision
- If Option B: timestamp of v1_2_test_backlog.md append
- Phase 21 closure status: CLOSED at code level

Then update `.planning/STATE.md` `Last activity` line and progress table:
- Add Phase 21 row: `| 21. Plant Journal | v1.2 | 7/7 | Complete | <date> |`
- Update `current_plan: 7 of 7`, `status: closed`, `stopped_at: Completed 21-06-PLAN.md`

Update `.planning/ROADMAP.md` Phase 21 entry to `[x]` with completion date.
</output>
