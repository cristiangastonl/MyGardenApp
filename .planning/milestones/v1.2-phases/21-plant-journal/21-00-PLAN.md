---
phase: 21-plant-journal
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/smoke-phase21.cjs
  - package.json
  - .gitignore
  - src/types/index.ts
  - src/services/journalService.ts
  - src/components/plant-detail/JournalSection.tsx
  - src/components/plant-detail/JournalQuickAddSheet.tsx
  - src/components/plant-detail/JournalEntryRow.tsx
  - src/i18n/locales/en/common.json
  - src/i18n/locales/es/common.json
autonomous: true
requirements: [JOURNAL-01, JOURNAL-02, JOURNAL-03, JOURNAL-04, JOURNAL-05]
must_haves:
  truths:
    - "Wave 0 scaffold lands ALL Phase 21 file paths so later waves only modify existing files (no NEW files)"
    - "`npm run smoke:phase21` exits 0 with PASS scaffold + SKIP placeholders for JOURNAL-01..05 + STRICT cross-phase regression for Phase 18/19/20"
    - "`npx tsc --noEmit` exits 0 with new type skeletons in place"
    - "i18n parity skeleton has ≥22 `journal.*` keys present in BOTH en/common.json and es/common.json (covers ALL Wave 3 t() call sites — Wave 3 components use BARE t() with no ?? fallbacks)"
    - "JOURNAL-04.Toast.entrada-guardada-wired sentinel uses STRICT proximity regex requiring BOTH `journalToastVisible` AND `journal.savedToast` in the same file within 500 chars (Blocker B + Warning E — proves the journal-specific Toast actually wired, not a false-positive on the existing Phase 18 swipe-undo `toastVisible`)"
  artifacts:
    - path: "scripts/smoke-phase21.cjs"
      provides: "Three-tier smoke runner (PASS scaffold + JOURNAL-01..05 SKIP placeholders + Phase 18/19/20 STRICT regression) + STRICT i18n-namespace-exists sentinels + Blocker B/Warning E tight proximity Toast sentinel"
      min_lines: 160
    - path: "src/types/index.ts"
      provides: "CareTag union + JournalEntry interface + AppData.journals?: optional field"
      contains: "CareTag"
    - path: "src/services/journalService.ts"
      provides: "Skeleton signatures for pickJournalPhoto/saveJournalPhoto/deleteJournalPhoto/deleteJournalDirectory"
    - path: "src/components/plant-detail/JournalSection.tsx"
      provides: "Skeleton component (default export returning empty View)"
    - path: "src/components/plant-detail/JournalQuickAddSheet.tsx"
      provides: "Skeleton component"
    - path: "src/components/plant-detail/JournalEntryRow.tsx"
      provides: "Skeleton component"
    - path: "src/i18n/locales/en/common.json"
      provides: "journal.* namespace skeleton with ≥22 keys (covers all Wave 3 t() call sites)"
    - path: "src/i18n/locales/es/common.json"
      provides: "journal.* namespace skeleton with ≥22 keys (voseo); EN+ES parity"
  key_links:
    - from: "package.json scripts"
      to: "scripts/smoke-phase21.cjs"
      via: "npm run smoke:phase21"
      pattern: "smoke:phase21.*node scripts/smoke-phase21.cjs"
    - from: ".gitignore"
      to: "scripts/.tmp-phase21/"
      via: "gitignore entry for behavioral-asserts tmp directory"
      pattern: "scripts/\\.tmp-phase21/"
---

<objective>
Wave 0 Nyquist scaffold for Phase 21 (Plant Journal). Lands the smoke runner, npm script, .gitignore entry, type skeletons (`CareTag` + `JournalEntry` + `AppData.journals?`), service skeleton (`journalService.ts` with no-op signatures), 3 component skeletons (JournalSection / JournalQuickAddSheet / JournalEntryRow returning empty Views), and i18n skeleton (≥22 `journal.*` keys EN+ES parity — covers EVERY `t('journal.X')` call site Wave 3 will introduce, so Wave 3 components use BARE `t()` calls with NO `??` fallbacks).

Purpose: Every Phase 21 modification site exists on disk before Wave 1 begins. Later waves modify existing files only — never create new ones. The smoke runner ships with SKIP placeholders for JOURNAL-01..05 that flip to PASS as later plans land real implementations. STRICT sentinels for `journal` namespace existence + key-count threshold lock the parity invariant from Wave 0. The Toast-wiring sentinel (Blocker B + Warning E) uses a tight proximity regex requiring BOTH `journalToastVisible` (distinct from the existing Phase 18 `toastVisible`) AND `journal.savedToast` in the same file — so it CAN'T false-positive on Phase 18 swipe-undo state already shipped in PlantsScreen.tsx:135 and TodayScreen.tsx:249.

Output: 10 files (1 new smoke runner + 1 npm script + 1 .gitignore line + 7 new source files / 2 modified i18n JSONs). All exit 0 on `npm run smoke:phase21 && npx tsc --noEmit`.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-plant-journal/21-CONTEXT.md
@.planning/phases/21-plant-journal/21-RESEARCH.md
@.planning/phases/21-plant-journal/21-VALIDATION.md

<interfaces>
Reference smoke runner (fork verbatim):
- scripts/smoke-phase20.cjs (174 lines) — assertion harness lines 23-44; readSafe + getNested helpers lines 38-44; Phase 18/19 STRICT regression lines 140-159

Existing types (insertion site src/types/index.ts):
- AppData interface lines 138-154 (per RESEARCH); add CareTag/JournalEntry above + journals? field inside AppData

photoService.ts (template for journalService skeleton signatures only):
```typescript
export async function pickJournalPhoto(source: 'camera' | 'gallery'): Promise<string | null>;
export async function saveJournalPhoto(plantId: string, entryId: string, imageUri: string): Promise<string>;
export async function deleteJournalPhoto(plantId: string, entryId: string): Promise<void>;
export async function deleteJournalDirectory(plantId: string): Promise<void>;
```

**Existing collision context (Blocker B — informational for Wave 0 sentinel design):**
- `src/screens/PlantsScreen.tsx:135` already declares `const [toastVisible, setToastVisible] = useState(false);` for Phase 18 CARD-01 swipe-undo Toast (rendered at lines 472-480)
- `src/screens/TodayScreen.tsx:249` declares the identical state for the same Phase 18 feature (rendered around line 681)
- Phase 21 Wave 3 will add a SECOND, DISTINCT Toast for journal-saved feedback. To avoid TS duplicate-declaration errors AND to make the Wave 0 Toast sentinel meaningful, the new state MUST use a different identifier — locked as `journalToastVisible` / `setJournalToastVisible`. The Wave 0 smoke sentinel below requires this exact identifier alongside `journal.savedToast` in the same file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Smoke runner + npm script + .gitignore + i18n skeleton (≥22 keys EN+ES — covers ALL Wave 3 t() sites)</name>
  <files>scripts/smoke-phase21.cjs, package.json, .gitignore, src/i18n/locales/en/common.json, src/i18n/locales/es/common.json</files>
  <read_first>
    - scripts/smoke-phase20.cjs (full 174 lines — fork verbatim)
    - .planning/phases/21-plant-journal/21-VALIDATION.md (Per-Task Verification Map + Wave 0 Requirements §)
    - .planning/phases/21-plant-journal/21-RESEARCH.md § Validation Architecture (lines 814-885)
    - package.json `scripts` section (insertion site for smoke:phase21)
    - .gitignore (insertion site for `scripts/.tmp-phase21/`)
    - src/i18n/locales/en/common.json (top-level structure — where to insert `journal` namespace)
    - src/i18n/locales/es/common.json (top-level structure — voseo precedent)
    - src/screens/PlantsScreen.tsx line 135 (existing Phase 18 `toastVisible` declaration — proves the Blocker B collision is real and the Wave 0 sentinel design needs `journalToastVisible` proximity, not the loose `toastVisible` match)
    - src/screens/TodayScreen.tsx line 249 (mirror declaration)
  </read_first>
  <action>
    Create `scripts/smoke-phase21.cjs` by forking `scripts/smoke-phase20.cjs` verbatim. **PRESERVE the existing assertion harness helpers verbatim** — `assert`, `assertSkippable`, `readSafe`, `getNested` at smoke-phase20.cjs lines 23-44 are copied as-is. Do NOT replace `assertSkippable` with `assert` for the JOURNAL-* SKIP placeholders — those placeholders MUST stay skippable so they flip on demand as later waves land real impls.

    Modify the fork as follows:

    1. **Header comment block** (lines 1-16): replace "Phase 20 Fertilization Subsystem" with "Phase 21 Plant Journal" and "FERT-01..07" with "JOURNAL-01..05"; replace "Phase 18 + 19" cross-phase mention with "Phase 18 + 19 + 20".

    2. **Read source files block** (after the existing reads, line ~60): add these new reads:
    ```js
    const journalServiceSrc = readSafe('src/services/journalService.ts') || '';
    const journalSectionSrc = readSafe('src/components/plant-detail/JournalSection.tsx') || '';
    const journalQuickAddSrc = readSafe('src/components/plant-detail/JournalQuickAddSheet.tsx') || '';
    const journalEntryRowSrc = readSafe('src/components/plant-detail/JournalEntryRow.tsx') || '';
    const useStorageSrc = readSafe('src/hooks/useStorage.tsx') || '';
    const plantsScreenSrc = readSafe('src/screens/PlantsScreen.tsx') || '';
    const todayScreenSrc = readSafe('src/screens/TodayScreen.tsx') || '';
    const enCommonSrc = readSafe('src/i18n/locales/en/common.json') || '{}';
    const esCommonSrc = readSafe('src/i18n/locales/es/common.json') || '{}';
    const enCommon = JSON.parse(enCommonSrc);
    const esCommon = JSON.parse(esCommonSrc);
    ```

    3. **REMOVE ALL FERT-* and Phase 20 SKIP placeholders** (lines ~70-137 in smoke-phase20.cjs).

    4. **ADD Phase 21 W0 (scaffold) PASSes** — STRICT (use `assert`, not `assertSkippable`). Includes new i18n-namespace-exists + key-count sentinels per Important 5:
    ```js
    // ─── W0 Scaffold (STRICT PASS — files MUST exist after Wave 0) ───
    assert(readSafe('scripts/smoke-phase21.cjs') !== null, 'W0.scaffold.smoke-runner-exists');
    assert(/smoke:phase21/.test(readSafe('package.json') || ''), 'W0.scaffold.npm-script-exists');
    assert(/scripts\/\.tmp-phase21\//.test(readSafe('.gitignore') || ''), 'W0.scaffold.gitignore-tmp-dir');
    assert(/export type CareTag\s*=/.test(typesSrc), 'W0.scaffold.types.CareTag-union');
    assert(/export interface JournalEntry\b/.test(typesSrc), 'W0.scaffold.types.JournalEntry');
    assert(/journals\?:\s*Record<string,\s*JournalEntry\[\]>/.test(typesSrc), 'W0.scaffold.types.AppData.journals-field');
    assert(journalServiceSrc.includes('pickJournalPhoto') && journalServiceSrc.includes('saveJournalPhoto') && journalServiceSrc.includes('deleteJournalPhoto') && journalServiceSrc.includes('deleteJournalDirectory'), 'W0.scaffold.journalService.4-signatures');
    assert(journalSectionSrc.length > 0, 'W0.scaffold.JournalSection-file-exists');
    assert(journalQuickAddSrc.length > 0, 'W0.scaffold.JournalQuickAddSheet-file-exists');
    assert(journalEntryRowSrc.length > 0, 'W0.scaffold.JournalEntryRow-file-exists');

    // ─── W0 i18n namespace presence + key-count thresholds (STRICT — Important 5) ───
    assert(enCommon && enCommon.journal && esCommon && esCommon.journal, 'W0.scaffold.i18n-namespace-exists');
    // Count top-level + nested keys recursively to satisfy threshold (6 careTag.* + 3 dateLabel.* + 1 error.* + scalars)
    function countLeafKeys(obj) {
      if (obj == null || typeof obj !== 'object') return 0;
      let n = 0;
      for (const k of Object.keys(obj)) {
        const v = obj[k];
        if (v && typeof v === 'object') n += countLeafKeys(v);
        else n += 1;
      }
      return n;
    }
    assert(countLeafKeys(enCommon.journal) >= 22, 'W0.scaffold.i18n-key-count-en');
    assert(countLeafKeys(esCommon.journal) >= 22, 'W0.scaffold.i18n-key-count-es');
    ```

    5. **ADD JOURNAL-01..05 SKIP placeholders** (use `assertSkippable`; return `undefined` for SKIP, `true` for PASS):
    ```js
    // ─── JOURNAL-01 AppData.journals migration default — SKIP at W0, PASS after 21-01 ───
    assertSkippable(() => /data\.journals\s*\|\|\s*\{\}/.test(useStorageSrc) || undefined, 'JOURNAL-01.useStorage.load-path-default');
    assertSkippable(() => /dataRef\.current\.journals\s*=/.test(useStorageSrc) || undefined, 'JOURNAL-01.useStorage.dataRef-init');
    // StorageState interface MUST be extended with journals field so snapshotFromRef's generic accepts d.journals (Blocker 3)
    assertSkippable(() => /journals:\s*Record<string,\s*JournalEntry\[\]>/.test(useStorageSrc) || undefined, 'JOURNAL-01.useStorage.StorageState-extension');
    // snapshotFromRef MUST list journals in its returned AppData (Blocker 3)
    assertSkippable(() => /journals:\s*d\.journals/.test(useStorageSrc) || undefined, 'JOURNAL-01.useStorage.snapshotFromRef-includes-journals');
    // __DEV__ payload-size instrumentation log (Important 9)
    assertSkippable(() => /AsyncStorage payload bytes/.test(useStorageSrc) || undefined, 'JOURNAL-01.useStorage.payload-size-log');

    // ─── JOURNAL-02 CareTag 6-literal union + journalService real impl — SKIP at W0 / PASS after 21-01/02 ───
    assertSkippable(() => /'riego'/.test(typesSrc) && /'fertilizar'/.test(typesSrc) && /'sol'/.test(typesSrc) && /'poda'/.test(typesSrc) && /'problema'/.test(typesSrc) && /'otro'/.test(typesSrc) || undefined, 'JOURNAL-02.types.CareTag-6-literals');
    // Tightened regex (Minor 10): require ${Paths.document.uri} template-literal interpolation, not just bare mention
    assertSkippable(() => /\$\{Paths\.document\.uri\}[^]*journal/.test(journalServiceSrc) || undefined, 'JOURNAL-02.journalService.Paths-document-uri');
    assertSkippable(() => journalServiceSrc.includes('manipulateAsync') && /compress:\s*0\.7/.test(journalServiceSrc) && /width:\s*1080/.test(journalServiceSrc) || undefined, 'JOURNAL-02.journalService.compression-pipeline');
    assertSkippable(() => journalServiceSrc.includes('new Directory') && journalServiceSrc.includes('new File') || undefined, 'JOURNAL-02.journalService.modern-API');

    // ─── JOURNAL-03 useStorage actions — SKIP at W0, PASS after 21-03 ───
    assertSkippable(() => /addJournalEntry:\s*\(plantId:\s*string,\s*entry:\s*JournalEntry\)/.test(useStorageSrc) || undefined, 'JOURNAL-03.useStorage.addJournalEntry-interface');
    assertSkippable(() => /deleteJournalEntry:\s*\(plantId:\s*string,\s*entryId:\s*string\)/.test(useStorageSrc) || undefined, 'JOURNAL-03.useStorage.deleteJournalEntry-interface');
    assertSkippable(() => (useStorageSrc.match(/addJournalEntry/g) || []).length >= 4 || undefined, 'JOURNAL-03.useStorage.addJournalEntry-4-sites');
    // Important 7: 3-name surface STRICT-ish gate for downstream pre-flight
    assertSkippable(() => {
      if (useStorageSrc.length === 0) return undefined;
      const hasJournals = /\bjournals\b/.test(useStorageSrc);
      const hasAdd = /\baddJournalEntry\b/.test(useStorageSrc);
      const hasDel = /\bdeleteJournalEntry\b/.test(useStorageSrc);
      // Only flip to PASS once all three appear AND at least one is in the public interface (StorageActions or StorageContextType)
      const inInterface = /addJournalEntry:\s*\(/.test(useStorageSrc) && /deleteJournalEntry:\s*\(/.test(useStorageSrc);
      if (!hasJournals && !hasAdd && !hasDel) return undefined;
      return hasJournals && hasAdd && hasDel && inInterface;
    }, 'JOURNAL-03.useStorage.public-interface-exposes-3-names');
    assertSkippable(() => useStorageSrc.includes('deleteJournalDirectory') || undefined, 'JOURNAL-04.useStorage.deletePlant-calls-deleteJournalDirectory');
    assertSkippable(() => /delete\s+newJournals\[id\]|delete\s+newJournals\[plantId\]/.test(useStorageSrc) || undefined, 'JOURNAL-04.useStorage.deletePlant-removes-journals-map-entry');

    // ─── JOURNAL-04 ModalSectionId + Diario section + BottomSheetModal — SKIP at W0, PASS after 21-04 ───
    assertSkippable(() => /'que-hacer'\s*\|\s*'donde'\s*\|\s*'por-que'\s*\|\s*'tus-ajustes'\s*\|\s*'mascotas'\s*\|\s*'diario'/.test(modalSrc) || undefined, 'JOURNAL-04.ModalSectionId.diario-extension');
    assertSkippable(() => modalSrc.includes("onSectionLayout('diario')") || undefined, 'JOURNAL-04.modal.diario-section-layout');
    assertSkippable(() => modalSrc.includes('JournalSection') || undefined, 'JOURNAL-04.modal.JournalSection-rendered');
    assertSkippable(() => /snapPoints=\{\[['"]60%['"]\]\}/.test(journalQuickAddSrc) || undefined, 'JOURNAL-04.JournalQuickAddSheet.60-percent-snap');
    assertSkippable(() => journalQuickAddSrc.includes('launchCameraAsync') || journalQuickAddSrc.includes('photoCamera') || undefined, 'JOURNAL-04.JournalQuickAddSheet.camera-button');
    assertSkippable(() => journalQuickAddSrc.includes('launchImageLibraryAsync') || journalQuickAddSrc.includes('photoGallery') || undefined, 'JOURNAL-04.JournalQuickAddSheet.gallery-button');
    assertSkippable(() => /careTag/.test(journalQuickAddSrc) || undefined, 'JOURNAL-04.JournalQuickAddSheet.careTag-chips');
    assertSkippable(() => journalEntryRowSrc.includes('onLongPress') || /Gesture\.LongPress/.test(journalEntryRowSrc) || undefined, 'JOURNAL-04.JournalEntryRow.long-press');

    // ─── Toast wiring (Blocker B + Warning E TIGHTENED) ───
    // Phase 18 CARD-01 already shipped `toastVisible` in BOTH PlantsScreen.tsx:135 and TodayScreen.tsx:249
    // for swipe-undo Toast. A loose `toastVisible >= 2` or bare `savedToast` substring match would
    // false-positive against the existing Phase 18 state.
    // STRICT proximity regex: BOTH `journalToastVisible` (distinct journal-specific identifier) AND
    // `journal.savedToast` (i18n key literal) MUST appear in the same file within 500 chars, in
    // either order. This proves the journal-specific Toast wiring actually landed.
    assertSkippable(() => {
      if (plantsScreenSrc.length === 0 && todayScreenSrc.length === 0) return undefined;
      const proximityRe = /journalToastVisible[\s\S]{0,500}journal\.savedToast|journal\.savedToast[\s\S]{0,500}journalToastVisible/;
      const inPlants = proximityRe.test(plantsScreenSrc);
      const inToday = proximityRe.test(todayScreenSrc);
      // Both consumers MUST carry the wiring (Plan 21-04 Task 4 mandates both).
      if (!inPlants && !inToday) return undefined;
      return inPlants && inToday;
    }, 'JOURNAL-04.Toast.entrada-guardada-wired');

    // Important 8: ModalSectionId import path locked — both consumers MUST import ModalSectionId (>=2 occurrences = import + usage)
    assertSkippable(() => (plantsScreenSrc.match(/ModalSectionId/g) || []).length >= 2 || undefined, 'JOURNAL-04.PlantsScreen.ModalSectionId-widened');
    assertSkippable(() => (todayScreenSrc.match(/ModalSectionId/g) || []).length >= 2 || undefined, 'JOURNAL-04.TodayScreen.ModalSectionId-widened');

    // ─── JOURNAL-05 negative-grep (NO premium-gate at journal-read sites) — SKIP at W0, PASS after 21-05 ───
    assertSkippable(() => {
      if (journalSectionSrc.length === 0) return undefined;
      const hasGate = /usePremiumGate|isPremium|canReadJournal/.test(journalSectionSrc) || /usePremiumGate|isPremium|canReadJournal/.test(journalEntryRowSrc);
      return hasGate ? false : true;
    }, 'JOURNAL-05.negative-grep.no-premium-gate-at-read-sites');

    // ─── i18n parity (≥22 keys — covers ALL Wave 3 t() sites) — SKIP per-key at W0, PASS after Wave 0 task 1 lands JSON skeletons ───
    const requiredJournalKeys = [
      'journal.header', 'journal.emptyState', 'journal.addEntry', 'journal.savedToast',
      'journal.photoCamera', 'journal.photoGallery',
      'journal.careTag.riego', 'journal.careTag.fertilizar', 'journal.careTag.sol',
      'journal.careTag.poda', 'journal.careTag.problema', 'journal.careTag.otro',
      'journal.deleteConfirm',
      'journal.dateLabel.today', 'journal.dateLabel.yesterday', 'journal.dateLabel.daysAgo',
      // Blocker 4: 6 additional keys used by Wave 3 components (bare t() — no ?? fallbacks)
      'journal.save', 'journal.cancel', 'journal.delete', 'journal.deleteEntry',
      'journal.textPlaceholder', 'journal.error.photoSaveFailed',
    ];
    requiredJournalKeys.forEach(k => {
      assertSkippable(() => {
        const enHas = getNested(enCommon, k) !== undefined;
        const esHas = getNested(esCommon, k) !== undefined;
        if (!enHas && !esHas) return undefined;
        return enHas && esHas;
      }, `i18n.parity.${k}`);
    });
    ```

    **Sentinel design notes (Blocker B + Warning E):**
    - The OLD sentinel design (`grep -c "toastVisible" >= 2` and `savedToast` substring anywhere) is DELETED — it was a false-positive trap because both screens already declare `toastVisible` for Phase 18 swipe-undo.
    - The NEW sentinel uses the distinct `journalToastVisible` identifier (locked in Plan 21-04 Task 4) AND requires `journal.savedToast` proximity within 500 chars in the SAME file. This proves the journal-specific Toast block landed.
    - The sentinel requires BOTH screens to carry the wiring (`inPlants && inToday`) — matches the Plan 21-04 Task 4 mandate that both PlantsScreen and TodayScreen own the journal Toast state.

    6. **CROSS-PHASE STRICT REGRESSION** (keep Phase 18 + 19 from smoke-phase20.cjs lines 140-159 verbatim; ADD Phase 20):
    ```js
    // Phase 20 FERT-03 5-site discriminator preserved
    assert(plantLogicSrc.includes("'fertilize'"), 'CROSS.FERT-03.plantLogic.fertilize-emit');
    assert(schedulerSrc.includes("'fertilize'"), 'CROSS.FERT-03.scheduler.fertilize-filter');
    // Phase 20 FERT-06 FertilizeCard preserved
    assert(readSafe('src/components/plant-detail/FertilizeCard.tsx') !== null, 'CROSS.FERT-06.FertilizeCard.FILE-NOT-DELETED');
    // Phase 20 FERT-07 i18n parity extension preserved
    const checkScript2 = readSafe('scripts/check-i18n-keys.mjs') || '';
    assert(/fertilizer\.(industrial|homemade)/.test(checkScript2), 'CROSS.FERT-07.checkScript.fertilizer-extension-preserved');
    ```

    7. **Footer report** (lines 161-174): replace "Phase 20 smoke" → "Phase 21 smoke"; replace "Plans 20-01..09" → "Plans 21-01..05".

    **package.json**: insert AFTER the `"smoke:phase20"` line (preserve alphabetical-by-phase ordering):
    ```json
    "smoke:phase21": "node scripts/smoke-phase21.cjs",
    ```

    **.gitignore**: append at end of file (after existing `.tmp-phase20/` if present, else after `# Native` block):
    ```
    scripts/.tmp-phase21/
    ```

    **en/common.json**: add new top-level key `"journal"` (alphabetically positioned — typically near top with other namespaces). Insert these 22 keys verbatim (Blocker 4: extends from prior 16-key set by 6 Wave 3 keys — `save`, `cancel`, `delete`, `deleteEntry`, `textPlaceholder`, `error.photoSaveFailed`):
    ```json
    "journal": {
      "header": "Journal",
      "emptyState": "Add your first entry 📔",
      "addEntry": "+ New entry",
      "savedToast": "Entry saved 📔",
      "photoCamera": "📷 Camera",
      "photoGallery": "🖼️ Gallery",
      "careTag": {
        "riego": "Watering 💧",
        "fertilizar": "Fertilizing 🌱",
        "sol": "Sun ☀️",
        "poda": "Pruning ✂️",
        "problema": "Problem ⚠️",
        "otro": "Other 📝"
      },
      "deleteConfirm": "Delete this entry?",
      "dateLabel": {
        "today": "Today",
        "yesterday": "Yesterday",
        "daysAgo": "{{count}} days ago"
      },
      "save": "Save",
      "cancel": "Cancel",
      "delete": "Delete",
      "deleteEntry": "Delete entry",
      "textPlaceholder": "Write your note...",
      "error": {
        "photoSaveFailed": "Couldn't save the photo"
      }
    }
    ```

    **es/common.json**: matching keyset with voseo (NEVER tuteo "tu"; use "vos"/"agregá"):
    ```json
    "journal": {
      "header": "Diario",
      "emptyState": "Agregá tu primera entrada 📔",
      "addEntry": "+ Nueva entrada",
      "savedToast": "Entrada guardada 📔",
      "photoCamera": "📷 Cámara",
      "photoGallery": "🖼️ Galería",
      "careTag": {
        "riego": "Riego 💧",
        "fertilizar": "Fertilizar 🌱",
        "sol": "Sol ☀️",
        "poda": "Poda ✂️",
        "problema": "Problema ⚠️",
        "otro": "Otro 📝"
      },
      "deleteConfirm": "¿Eliminar esta entrada?",
      "dateLabel": {
        "today": "Hoy",
        "yesterday": "Ayer",
        "daysAgo": "Hace {{count}} días"
      },
      "save": "Guardar",
      "cancel": "Cancelar",
      "delete": "Eliminar",
      "deleteEntry": "Eliminar entrada",
      "textPlaceholder": "Escribí tu nota...",
      "error": {
        "photoSaveFailed": "No pudimos guardar la foto"
      }
    }
    ```
  </action>
  <acceptance_criteria>
    - `node scripts/smoke-phase21.cjs` exits 0 (FAIL=0). Output contains `[Phase 21 smoke] PASS=` line.
    - `node -e "process.exit(require('./package.json').scripts['smoke:phase21'] ? 0 : 1)"` exits 0
    - `grep -q 'scripts/\.tmp-phase21/' .gitignore` exits 0
    - `node -e "const en=require('./src/i18n/locales/en/common.json'); const es=require('./src/i18n/locales/es/common.json'); const flat=['header','emptyState','addEntry','savedToast','photoCamera','photoGallery','deleteConfirm','save','cancel','delete','deleteEntry','textPlaceholder']; flat.forEach(k=>{if(!en.journal[k]||!es.journal[k])throw new Error('missing '+k)}); ['riego','fertilizar','sol','poda','problema','otro'].forEach(k=>{if(!en.journal.careTag[k]||!es.journal.careTag[k])throw new Error('missing careTag.'+k)}); ['today','yesterday','daysAgo'].forEach(k=>{if(!en.journal.dateLabel[k]||!es.journal.dateLabel[k])throw new Error('missing dateLabel.'+k)}); if(!en.journal.error?.photoSaveFailed||!es.journal.error?.photoSaveFailed)throw new Error('missing error.photoSaveFailed')"` exits 0
    - Smoke runner output contains `W0.scaffold.i18n-namespace-exists` PASS + `W0.scaffold.i18n-key-count-en` PASS + `W0.scaffold.i18n-key-count-es` PASS
    - **Blocker B/Warning E enforcement:** the smoke source `scripts/smoke-phase21.cjs` contains the literal `journalToastVisible` identifier (proves the tight proximity regex was applied — `grep -q "journalToastVisible" scripts/smoke-phase21.cjs` exits 0); it does NOT contain the OLD loose pattern (`grep -E 'toastVisible[^"]*>=\\s*2' scripts/smoke-phase21.cjs` returns 0 matches)
    - **JOURNAL-04.Toast.entrada-guardada-wired sentinel is SKIP at Wave 0** (PlantsScreen + TodayScreen don't yet contain `journalToastVisible`) — verifies the false-positive bug is fixed
    - Phase 18/19/20 STRICT regression in smoke-phase21.cjs all PASS (zero FAIL on cross-phase sentinels)
    - `npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20` all exit 0 (Wave 0 doesn't break prior phases)
    - All 4 `assert`/`assertSkippable`/`readSafe`/`getNested` helpers preserved verbatim from smoke-phase20.cjs lines 23-44 (visual diff check)
  </acceptance_criteria>
  <verify>
    <automated>node scripts/smoke-phase21.cjs && npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20</automated>
  </verify>
  <done>Phase 21 smoke runner forked from smoke-phase20.cjs with JOURNAL-01..05 SKIPs + Phase 18/19/20 STRICT regression + i18n-namespace + key-count STRICT sentinels + TIGHTENED `journalToastVisible`-proximity Toast sentinel (Blocker B + Warning E — no false-positive on existing Phase 18 toastVisible). package.json scripts entry; .gitignore line; i18n skeleton with ≥22 keys EN+ES parity (covers ALL Wave 3 t() call sites so Wave 3 uses bare t() with no ?? fallbacks). All cross-phase smoke runners remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type skeletons (CareTag + JournalEntry + AppData.journals?) + 4 source-file skeletons</name>
  <files>src/types/index.ts, src/services/journalService.ts, src/components/plant-detail/JournalSection.tsx, src/components/plant-detail/JournalQuickAddSheet.tsx, src/components/plant-detail/JournalEntryRow.tsx</files>
  <read_first>
    - src/types/index.ts (full file — current AppData shape around lines 138-154; Note/Reminder shape lines ~100-104 as precedent)
    - src/services/photoService.ts (full 101 lines — API surface to mirror for journalService skeleton signatures)
    - src/components/plant-detail/FertilizeCard.tsx (skeleton-style precedent for Wave 0 component shells)
    - .planning/phases/21-plant-journal/21-RESEARCH.md § Pattern 1 (AppData Extension) lines 144-171 + § Pattern 4 (journalService) lines 286-368
  </read_first>
  <behavior>
    - Test 1: `npx tsc --noEmit` exits 0 with new types in place
    - Test 2: `npm run smoke:phase21` flips W0.scaffold.* assertions from missing → PASS
    - Test 3: `JournalEntry.id` is string (not number); `date` is string; text/photoUri/careTag are optional
    - Test 4: `CareTag` union has EXACTLY 6 literals: 'riego' | 'fertilizar' | 'sol' | 'poda' | 'problema' | 'otro'
    - Test 5: `AppData.journals?` is optional (with `?:`), typed as `Record<string, JournalEntry[]>`
  </behavior>
  <action>
    **src/types/index.ts**: Insert the following BEFORE the existing `AppData` interface (per RESEARCH Pattern 1, lines 144-171):
    ```typescript
    /** v1.2 Phase 21 (JOURNAL-02) — care-tag taxonomy. 6 predefined values, single-select. */
    export type CareTag = 'riego' | 'fertilizar' | 'sol' | 'poda' | 'problema' | 'otro';

    /** v1.2 Phase 21 (JOURNAL-02) — single per-plant journal entry.
     *  All fields except `id` and `date` are optional → empty entry with just the date is valid.
     *  `photoUri` points to a file in `${Paths.document.uri}journal/${plantId}/${id}.jpg` — NEVER base64.
     *  `date` is ISO string ("YYYY-MM-DD" — matches Plant.lastWatered format).
     *  `id` is generated via `Date.now().toString()` (matches app-wide convention). */
    export interface JournalEntry {
      id: string;
      date: string;
      text?: string;
      photoUri?: string;
      careTag?: CareTag;
    }
    ```

    Then locate the `AppData` interface (currently ends with `climateOverride?: ClimateOverride;` per RESEARCH). Insert IMMEDIATELY BEFORE the closing `}`:
    ```typescript
      /** v1.2 Phase 21 (JOURNAL-01). Additive optional; absence/empty = no entries. Keyed by plant.id. */
      journals?: Record<string, JournalEntry[]>;
    ```

    **src/services/journalService.ts** (NEW skeleton — real impl in Plan 21-02):
    ```typescript
    /**
     * src/services/journalService.ts — Phase 21 (JOURNAL-02) skeleton.
     *
     * Real implementation lands in Plan 21-02 (modern Paths/File/Directory API
     * mirroring src/services/photoService.ts:1-101, with 1080px @ 0.7 JPEG compression
     * and per-plant subdirectory layout under documentDirectory/journal/<plantId>/<entryId>.jpg).
     *
     * Skeleton purpose: file exists for tsc + smoke runner gating at Wave 0.
     * All functions return null / no-op to keep tsc green.
     *
     * Note: the Paths.document.uri TEMPLATE-LITERAL interpolation lands in the real impl
     * (Plan 21-02). The W0 smoke gate `JOURNAL-02.journalService.Paths-document-uri` regex
     * requires `${Paths.document.uri}` interpolation (not a bare mention), so this skeleton
     * intentionally does NOT include that pattern.
     */
    import { Paths } from 'expo-file-system';

    void Paths; // satisfy unused-import; real impl uses Paths.document.uri

    export async function pickJournalPhoto(source: 'camera' | 'gallery'): Promise<string | null> {
      void source;
      return null;
    }

    export async function saveJournalPhoto(plantId: string, entryId: string, imageUri: string): Promise<string> {
      void plantId; void entryId;
      return imageUri;
    }

    export async function deleteJournalPhoto(plantId: string, entryId: string): Promise<void> {
      void plantId; void entryId;
    }

    export async function deleteJournalDirectory(plantId: string): Promise<void> {
      void plantId;
    }
    ```

    **src/components/plant-detail/JournalSection.tsx** (NEW skeleton):
    ```typescript
    /**
     * src/components/plant-detail/JournalSection.tsx — Phase 21 (JOURNAL-04) skeleton.
     * Real impl in Plan 21-04: collapsible (EducationalSection composition) + FlatList timeline +
     * empty-state CTA + "+ Nueva entrada" header button.
     */
    import React from 'react';
    import { View } from 'react-native';
    import type { JournalEntry } from '../../types';

    interface JournalSectionProps {
      plantId: string;
      entries: JournalEntry[];
      onAddEntry: () => void;
      onDeleteEntry: (entryId: string) => void;
    }

    export default function JournalSection(props: JournalSectionProps): React.ReactElement {
      void props;
      return <View />;
    }
    ```

    **src/components/plant-detail/JournalQuickAddSheet.tsx** (NEW skeleton):
    ```typescript
    /**
     * src/components/plant-detail/JournalQuickAddSheet.tsx — Phase 21 (JOURNAL-04) skeleton.
     * Real impl in Plan 21-04: BottomSheetModal with snapPoints=['60%'], text input,
     * Cámara/Galería buttons, 6 careTag chips, Guardar/Cancelar footer.
     */
    import React from 'react';
    import { View } from 'react-native';
    import type { CareTag, JournalEntry } from '../../types';

    interface JournalQuickAddSheetProps {
      plantId: string;
      visible: boolean;
      onDismiss: () => void;
      onSave: (entry: JournalEntry) => void;
    }

    export default function JournalQuickAddSheet(props: JournalQuickAddSheetProps): React.ReactElement {
      void props;
      const _careTag: CareTag | undefined = undefined;
      void _careTag;
      return <View />;
    }
    ```

    **src/components/plant-detail/JournalEntryRow.tsx** (NEW skeleton):
    ```typescript
    /**
     * src/components/plant-detail/JournalEntryRow.tsx — Phase 21 (JOURNAL-04) skeleton.
     * Real impl in Plan 21-04: date header ("Hoy"/"Ayer"/"Hace N días") + careTag chip
     * + 80x80 photo thumbnail + text body + long-press → BottomSheetModal delete option.
     */
    import React from 'react';
    import { View } from 'react-native';
    import type { JournalEntry } from '../../types';

    interface JournalEntryRowProps {
      entry: JournalEntry;
      plantId: string;
      onDelete: (entryId: string) => void;
    }

    export default function JournalEntryRow(props: JournalEntryRowProps): React.ReactElement {
      void props;
      return <View />;
    }
    ```
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `grep -c "export type CareTag = 'riego' | 'fertilizar' | 'sol' | 'poda' | 'problema' | 'otro'" src/types/index.ts` returns 1
    - `grep -c "export interface JournalEntry" src/types/index.ts` returns 1
    - `grep -E "journals\?:\s*Record<string,\s*JournalEntry\[\]>" src/types/index.ts` returns 1 match
    - `grep -c "pickJournalPhoto\|saveJournalPhoto\|deleteJournalPhoto\|deleteJournalDirectory" src/services/journalService.ts` returns ≥4
    - `test -f src/components/plant-detail/JournalSection.tsx -a -f src/components/plant-detail/JournalQuickAddSheet.tsx -a -f src/components/plant-detail/JournalEntryRow.tsx` exits 0
    - `node scripts/smoke-phase21.cjs` shows W0.scaffold.types.* assertions all PASS (not SKIP)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && node scripts/smoke-phase21.cjs</automated>
  </verify>
  <done>Types skeletons (CareTag union + JournalEntry interface + AppData.journals? field) committed; 4 source-file skeletons exist (journalService + 3 components) with tsc green. Wave 0 W0.scaffold.* PASSes flip on; JOURNAL-* placeholders remain SKIP (will flip in later waves).</done>
</task>

</tasks>

<verification>
- `node scripts/smoke-phase21.cjs` exits 0; output shows PASS count > 0 + SKIP count for JOURNAL-01..05 + Phase 18/19/20 cross-phase regression all PASS + W0.scaffold.i18n-namespace-exists + W0.scaffold.i18n-key-count-en/es PASS.
- `npx tsc --noEmit` exits 0 with new types in place.
- `npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20 && npm run smoke:phase21` all exit 0.
- `grep -c "scripts/\.tmp-phase21/" .gitignore` returns 1.
- **Blocker B/Warning E proof:** `grep -q "journalToastVisible" scripts/smoke-phase21.cjs` exits 0; the OLD loose `toastVisible >= 2` pattern is GONE from the smoke source.
</verification>

<success_criteria>
Wave 0 is COMPLETE when:
- [x] `scripts/smoke-phase21.cjs` exists, runs, exits 0
- [x] `package.json` has `"smoke:phase21"` script entry
- [x] `.gitignore` has `scripts/.tmp-phase21/` entry
- [x] `src/types/index.ts` has `CareTag` union + `JournalEntry` interface + `AppData.journals?:` field
- [x] `src/services/journalService.ts` exists with 4 exported async signatures
- [x] 3 component skeletons exist returning empty View
- [x] EN + ES `common.json` have full `journal.*` namespace (≥22 keys parity — covers Wave 3 bare-t() call sites)
- [x] STRICT i18n-namespace-exists + key-count sentinels PASS in smoke-phase21.cjs
- [x] STRICT `journalToastVisible`-proximity Toast sentinel (Blocker B + Warning E) lands in smoke source; no false-positive on existing Phase 18 `toastVisible`
- [x] All Phase 18/19/20 cross-phase STRICT regression sentinels still PASS in smoke-phase21.cjs
- [x] `npx tsc --noEmit` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/21-plant-journal/21-00-SUMMARY.md` per template.
</output>
</output>
