---
phase: 20-fertilization-subsystem
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/smoke-phase20.cjs
  - package.json
  - .gitignore
  - src/i18n/locales/en/common.json
  - src/i18n/locales/es/common.json
  - src/types/index.ts
  - src/components/plant-detail/FertilizeCard.tsx
  - src/utils/plantLogic.ts
autonomous: true
requirements: [FERT-01, FERT-02, FERT-03, FERT-04, FERT-05, FERT-06, FERT-07]
must_haves:
  truths:
    - "smoke-phase20 runner exists and exits 0 with PASS scaffold + SKIP placeholders for FERT-01..07 + STRICT cross-phase regression sentinels for Phase 18 + Phase 19"
    - "npm run smoke:phase20 script wires the runner"
    - ".gitignore covers scripts/.tmp-phase20/ for future ts.transpileModule stubs"
    - "i18n skeleton keys land in BOTH en/es common.json with parity (tasks.fertilize, notifications.fertilize, plantCard.fertilize, plantDetailModal.water/fertilize/fertilizeEvery/fertilizeDormant, settings.fertilizeReminders/Subtitle)"
    - "FertilizeCard skeleton component file exists and exports a no-op (returns null) so Wave 0 baseline does not render it"
    - "plantLogic exports getSeasonalFertilizeInterval + getNextFertilizeDate skeletons returning null until Plan 20-02 lands real implementation"
    - "Type extensions land in src/types/index.ts (Plant.fertilizeSchedule, PlantDBEntry.fertilizeIntervalWarm/Cold + fertilizer, Task discriminator '| fertilize', NotificationSettings.fertilizeReminders) — additive optional, tsc-green"
  artifacts:
    - path: scripts/smoke-phase20.cjs
      provides: three-tier smoke runner (PASS scaffold + SKIP→PASS placeholders + STRICT cross-phase sentinels)
      min_lines: 200
    - path: src/components/plant-detail/FertilizeCard.tsx
      provides: skeleton component (returns null) — real impl in Plan 20-04
    - path: src/utils/plantLogic.ts
      provides: skeleton helpers getSeasonalFertilizeInterval + getNextFertilizeDate (return null) — real impl in Plan 20-02
    - path: src/types/index.ts
      provides: type extensions for FERT-01/02/03/05 + 07 (additive optional)
  key_links:
    - from: package.json
      to: scripts/smoke-phase20.cjs
      via: scripts.smoke:phase20
      pattern: "smoke:phase20.*node scripts/smoke-phase20.cjs"
    - from: scripts/smoke-phase20.cjs
      to: src/components/PlantCard.tsx
      via: STRICT cross-phase regression sentinels (Phase 18 + 19 surfaces preserved)
      pattern: "CROSS\\.GAM-04|CROSS\\.TOX-03|CROSS\\.health-no-fertilize-axis"
---

<objective>
Wave 0 Nyquist gate. Lay down the entire Phase 20 verification surface BEFORE any implementation lands: smoke runner with three-tier sentinel pattern (PASS scaffold + SKIP→PASS placeholders for FERT-01..07 + STRICT cross-phase regression sentinels for Phase 18 PlantCard 5-element layout + Phase 19 toxicity surfaces), npm script, .gitignore stub directory, i18n skeleton keys with EN+ES parity, FertilizeCard skeleton component (returns null), plantLogic helper skeletons (return null), and additive-optional type extensions. After this plan: `node scripts/smoke-phase20.cjs` exits 0 with the SKIP queue documenting every Phase 20 surface that subsequent plans will flip to PASS.

Purpose: locks the verification contract first, then the implementation plans flip SKIPs to PASSes by editing source files — the smoke runner is NEVER edited again after Wave 0 (Phase 14-00 / 19-00 precedent).
Output: 8 files modified; smoke baseline established at PASS=scaffold-and-cross-phase, FAIL=0, SKIP=Phase-20-feature-set; tsc-green.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-fertilization-subsystem/20-CONTEXT.md
@.planning/phases/20-fertilization-subsystem/20-RESEARCH.md
@.planning/phases/20-fertilization-subsystem/20-VALIDATION.md

<interfaces>
<!-- Existing types in src/types/index.ts that this plan EXTENDS -->

```typescript
// CURRENT (src/types/index.ts:107-112) — Task discriminator union
export interface Task {
  type: "water" | "sun" | "outdoor" | "check_soil";
  icon: string;
  label: string;
  plantId: string;
}

// CURRENT NotificationSettings (src/types/index.ts:283-289)
export interface NotificationSettings {
  enabled: boolean;
  morningReminder: boolean;
  morningTime: string;
  weatherAlerts: boolean;
  careReminders: boolean;
}

// CURRENT Plant interface — gains optional fertilizeSchedule
// CURRENT PlantDBEntry interface — gains fertilizeIntervalWarm/Cold + fertilizer
```

<!-- Reference: scripts/smoke-phase19.cjs three-tier pattern -->
<!-- assert(cond, label) — STRICT (PASS or FAIL) -->
<!-- assertSkippable(condFn, label) — returns undefined → SKIP, true → PASS, false → FAIL -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/smoke-phase20.cjs three-tier runner + add npm script + .gitignore stub dir</name>
  <files>scripts/smoke-phase20.cjs, package.json, .gitignore</files>
  <read_first>
    - scripts/smoke-phase19.cjs (full file — template to fork; harness `assert`/`assertSkippable`/`readSafe`/`getNested`; 257 LOC)
    - package.json (`scripts` block — see how `smoke:phase19` is wired; line ~10-30)
    - .gitignore (existing scripts/.tmp-phaseNN/ entries from Phase 11/12/14/15/19)
    - .planning/phases/20-fertilization-subsystem/20-RESEARCH.md §"Code Examples → Example 3: smoke-phase20.cjs Skeleton" (full skeleton lines 862-984)
    - .planning/phases/20-fertilization-subsystem/20-VALIDATION.md §"Per-Task Verification Map" (sentinel labels)
  </read_first>
  <action>
Create `scripts/smoke-phase20.cjs` as a CommonJS file, forking `scripts/smoke-phase19.cjs` structure verbatim. Use the EXACT skeleton from `20-RESEARCH.md` §"Code Examples → Example 3" (lines 862-984), with these explicit sentinel labels:

**Harness (verbatim from smoke-phase19.cjs):**
```js
let pass = 0, fail = 0, skip = 0;
const errors = [], skips = [];
function assert(cond, label) { if (cond) pass++; else { fail++; errors.push(`FAIL: ${label}`); } }
function assertSkippable(condFn, label) {
  try {
    const cond = condFn();
    if (cond === undefined) { skip++; skips.push(`SKIP: ${label}`); }
    else if (cond) pass++;
    else { fail++; errors.push(`FAIL: ${label}`); }
  } catch (e) { skip++; skips.push(`SKIP: ${label} (threw: ${e.message})`); }
}
function readSafe(rel) { const abs = path.resolve(ROOT, rel); return fs.existsSync(abs) ? fs.readFileSync(abs, 'utf8') : null; }
```

**Read these source files ONCE at top of script (use `readSafe`):**
- src/types/index.ts → typesSrc
- src/utils/plantLogic.ts → plantLogicSrc
- src/utils/notificationScheduler.ts → schedulerSrc
- src/utils/plantHealth.ts → plantHealthSrc
- src/components/DayDetail.tsx → dayDetailSrc
- src/components/DayDetailModal.tsx → dayDetailModalSrc
- src/components/MonthCalendar.tsx → monthCalendarSrc
- src/components/PlantCard.tsx → plantCardSrc
- src/components/MyPlantDetailModal.tsx → modalSrc
- src/components/plant-detail/FertilizeCard.tsx → fertilizeCardSrc
- src/screens/SettingsScreen.tsx → settingsScreenSrc
- src/data/plantDatabase.ts → dbSrc
- src/i18n/locales/en/common.json → enCommonSrc
- src/i18n/locales/es/common.json → esCommonSrc

**STRICT W0.scaffold sentinels (PASS at Wave 0 baseline — these are the gates Task 2/3/4 of THIS plan must satisfy):**
```js
assert(readSafe('scripts/smoke-phase20.cjs') !== null, 'W0.scaffold.runner-self-exists');
assert(typesSrc.includes('fertilizeSchedule?:'), 'W0.scaffold.types.Plant-fertilizeSchedule');
assert(typesSrc.includes('fertilizeIntervalWarm?:'), 'W0.scaffold.types.PlantDBEntry-fertilizeIntervalWarm');
assert(typesSrc.includes('fertilizeIntervalCold?:'), 'W0.scaffold.types.PlantDBEntry-fertilizeIntervalCold');
assert(/type:\s*['"]industrial['"]\s*\|\s*['"]homemade['"]\s*\|\s*['"]both['"]/.test(typesSrc), 'W0.scaffold.types.PlantDBEntry-fertilizer');
assert(typesSrc.includes('"fertilize"'), 'W0.scaffold.types.Task-discriminator-extended');
assert(typesSrc.includes('fertilizeReminders?:'), 'W0.scaffold.types.NotificationSettings-fertilizeReminders');
assert(readSafe('src/components/plant-detail/FertilizeCard.tsx') !== null, 'W0.scaffold.FertilizeCard.file-exists');
assert(plantLogicSrc.includes('export function getSeasonalFertilizeInterval'), 'W0.scaffold.helpers.getSeasonalFertilizeInterval-exported');
assert(plantLogicSrc.includes('export function getNextFertilizeDate'), 'W0.scaffold.helpers.getNextFertilizeDate-exported');
// i18n skeleton parity — keys must exist in BOTH locales
const enJSON = enCommonSrc ? JSON.parse(enCommonSrc) : {};
const esJSON = esCommonSrc ? JSON.parse(esCommonSrc) : {};
assert(getNested(enJSON, 'tasks.fertilize') && getNested(esJSON, 'tasks.fertilize'), 'W0.scaffold.i18n.tasks.fertilize-parity');
assert(getNested(enJSON, 'notifications.fertilize') && getNested(esJSON, 'notifications.fertilize'), 'W0.scaffold.i18n.notifications.fertilize-parity');
assert(getNested(enJSON, 'plantCard.fertilize') && getNested(esJSON, 'plantCard.fertilize'), 'W0.scaffold.i18n.plantCard.fertilize-parity');
assert(getNested(enJSON, 'plantDetailModal.water') && getNested(esJSON, 'plantDetailModal.water'), 'W0.scaffold.i18n.plantDetailModal.water-parity');
assert(getNested(enJSON, 'plantDetailModal.fertilize') && getNested(esJSON, 'plantDetailModal.fertilize'), 'W0.scaffold.i18n.plantDetailModal.fertilize-parity');
assert(getNested(enJSON, 'plantDetailModal.fertilizeEvery') && getNested(esJSON, 'plantDetailModal.fertilizeEvery'), 'W0.scaffold.i18n.plantDetailModal.fertilizeEvery-parity');
assert(getNested(enJSON, 'plantDetailModal.fertilizeDormant') && getNested(esJSON, 'plantDetailModal.fertilizeDormant'), 'W0.scaffold.i18n.plantDetailModal.fertilizeDormant-parity');
assert(getNested(enJSON, 'settings.fertilizeReminders') && getNested(esJSON, 'settings.fertilizeReminders'), 'W0.scaffold.i18n.settings.fertilizeReminders-parity');
assert(getNested(enJSON, 'settings.fertilizeRemindersSubtitle') && getNested(esJSON, 'settings.fertilizeRemindersSubtitle'), 'W0.scaffold.i18n.settings.fertilizeRemindersSubtitle-parity');
```

**SKIP→PASS placeholders for FERT-01..07 (Plans 20-01..09 will flip these to PASS):**
```js
// FERT-02 catalog content — SKIP at Wave 0, PASS as Plans 20-06/07/08 land
assertSkippable(() => {
  const matches = (dbSrc.match(/fertilizeIntervalWarm:\s*\d+/g) || []).length;
  if (matches === 0) return undefined;
  return matches >= 100;
}, 'FERT-02.catalog.fertilizeIntervalWarm-coverage');

// FERT-03 5-site discriminator sweep — SKIP at Wave 0, PASS after Plan 20-03
assertSkippable(() => plantLogicSrc.includes('type: "fertilize"') || undefined, 'FERT-03.plantLogic.emit-branch');
assertSkippable(() => /case "fertilize"/.test(dayDetailSrc) || undefined, 'FERT-03.dayDetail.discriminator');
assertSkippable(() => /task\.type === ['"]fertilize['"]/.test(dayDetailModalSrc) || undefined, 'FERT-03.dayDetailModal.discriminator');
assertSkippable(() => monthCalendarSrc.includes('hasFertilize') || undefined, 'FERT-03.monthCalendar.dot-indicator');
assertSkippable(() => schedulerSrc.includes('fertilizeTasks') || undefined, 'FERT-03.scheduler.body-filter');
assertSkippable(() => /case "fertilize"|task\.type === ['"]fertilize['"]/.test(readSafe('src/components/TaskButton.tsx') || '') || /fertilize/i.test(readSafe('src/components/TaskButton.tsx') || '') || undefined, 'FERT-03.TaskButton.fertilize-render');

// FERT-04 cadence math — SKIP at Wave 0, PASS after Plan 20-02 lands real impl
assertSkippable(() => /export function getNextFertilizeDate[^]*?\bcatalogEntry\b/.test(plantLogicSrc) && !plantLogicSrc.includes('return null; // skeleton') || undefined, 'FERT-04.helper.getNextFertilizeDate-real-impl');
assertSkippable(() => /export function getSeasonalFertilizeInterval[^]*?\bseason\b/.test(plantLogicSrc) && !plantLogicSrc.includes('return null; // skeleton') || undefined, 'FERT-04.helper.getSeasonalFertilizeInterval-real-impl');
assertSkippable(() => /season === ['"]cold['"][^]*?return null|fertilizeIntervalCold/.test(plantLogicSrc) || undefined, 'FERT-04.helper.cold-season-null');
assertSkippable(() => /while\s*\(\s*next\s*<\s*today\s*\)\s*next\s*=\s*addDays/.test(plantLogicSrc) || undefined, 'FERT-04.helper.catch-up-clip');

// FERT-05 Settings toggle + opt-in gate — SKIP at Wave 0, PASS after Plan 20-05
assertSkippable(() => settingsScreenSrc.includes('fertilizeReminders') || undefined, 'FERT-05.settings.toggle-rendered');
assertSkippable(() => /notifSettings\?\.fertilizeReminders\s*===\s*true|notifSettings\.fertilizeReminders\s*===\s*true/.test(schedulerSrc) || undefined, 'FERT-05.scheduler.opt-in-gate');
assertSkippable(() => {
  const useNotifSrc = readSafe('src/hooks/useNotifications.ts') || '';
  return /fertilizeReminders:\s*false/.test(useNotifSrc) || undefined;
}, 'FERT-05.useNotifications.default-OFF');

// FERT-06 PlantCard + modal two-column — SKIP at Wave 0, PASS after Plans 20-04
assertSkippable(() => plantCardSrc.includes('onFertilizeDone') || undefined, 'FERT-06.PlantCard.fertilize-task-row');
assertSkippable(() => modalSrc.includes('careCardsRow') || modalSrc.includes('FertilizeCard') || undefined, 'FERT-06.MyPlantDetailModal.two-column-layout');
assertSkippable(() => /initialExpanded\?:\s*['"]fertilize['"]|initialExpanded\?:\s*'fertilize'/.test(modalSrc) || undefined, 'FERT-06.MyPlantDetailModal.initialExpanded-prop');
assertSkippable(() => fertilizeCardSrc.includes('useSharedValue') && fertilizeCardSrc.includes('useDerivedValue') || undefined, 'FERT-06.FertilizeCard.reanimated-primitives');
assertSkippable(() => /COLLAPSE_DURATION\s*=\s*180|duration:\s*180/.test(fertilizeCardSrc) || undefined, 'FERT-06.FertilizeCard.180ms-tuning');

// FERT-07 i18n parity gate extension — SKIP at Wave 0, PASS after Plan 20-09
assertSkippable(() => {
  const checkScript = readSafe('scripts/check-i18n-keys.mjs') || '';
  if (checkScript.includes('fertilizer.industrialRecommendation') || checkScript.includes('fertilizer.homemadeRecommendation')) return true;
  return undefined;
}, 'FERT-07.checkScript.fertilizer-conditional-extension');
```

**STRICT cross-phase regression sentinels (these are PASS-or-FAIL, never SKIP — they protect Phase 18 + Phase 19 surfaces):**
```js
// Phase 18 GAM-04 — PlantHealthBadge preserved in modal
assert(modalSrc.includes('PlantHealthBadge'), 'CROSS.GAM-04.healthBadge.MODAL-USAGE-PRESERVED');
assert(readSafe('src/components/PlantHealthBadge.tsx') !== null, 'CROSS.GAM-04.healthBadge.FILE-NOT-DELETED');
// Phase 18 CARD-01 — Gesture.Pan/GestureDetector preserved in PlantCard
assert(/Gesture\.Pan|GestureDetector/.test(plantCardSrc), 'CROSS.CARD-01.plantcard.gesture-pan-preserved');
// Phase 18 GAM-03 — mood emoji preserved (5-element budget)
assert(plantCardSrc.includes('moodEmoji') && plantCardSrc.includes('moodBadge'), 'CROSS.GAM-03.plantcard.mood-emoji-preserved');
// Phase 18 Toast primitive preserved
assert(readSafe('src/components/Toast.tsx') !== null, 'CROSS.Toast.FILE-NOT-DELETED');
// Phase 19 TOX-03 — PetToxicityBadge usage in PlantCard preserved
assert(plantCardSrc.includes('PetToxicityBadge'), 'CROSS.TOX-03.plantcard.toxicity-badges-preserved');
// Phase 19 TOX-04 — Mascotas section preserved in modal
assert(modalSrc.includes('MascotasContent') || modalSrc.includes('emoji="🐾"'), 'CROSS.TOX-04.modal.mascotas-section-preserved');
// Phase 19 TOX-04 — initialSection prop preserved on modal
assert(modalSrc.includes('initialSection?: ModalSectionId'), 'CROSS.TOX-04.modal.initialSection-preserved');
// Phase 19 TOX-06 — petToxicity.symptoms parity gate preserved in check script
const checkScript = readSafe('scripts/check-i18n-keys.mjs') || '';
assert(/petToxicity[^]*?symptoms/.test(checkScript), 'CROSS.TOX-06.checkScript.symptoms-extension-preserved');
// Phase 20 health-axis no-op (Success Criterion 5) — plantHealth MUST NOT mention 'fertilize'
assert(!plantHealthSrc.includes("'fertilize'") && !plantHealthSrc.includes('"fertilize"'), 'CROSS.health-no-fertilize-axis');
```

**Report block (verbatim from smoke-phase19.cjs):**
```js
console.log('');
if (skips.length > 0) {
  console.log('--- SKIPS (will flip to PASS as Plans 20-01..09 land) ---');
  skips.forEach(s => console.log('  ' + s));
}
if (errors.length > 0) {
  console.error('--- FAILURES ---');
  errors.forEach(e => console.error('  ' + e));
}
console.log(`[Phase 20 smoke] PASS=${pass} FAIL=${fail} SKIP=${skip}`);
process.exit(fail > 0 ? 1 : 0);
```

**package.json** — add INSIDE the existing `scripts` object, alphabetically next to `smoke:phase19`:
```json
"smoke:phase20": "node scripts/smoke-phase20.cjs"
```
Use Edit tool to insert ONE line; do NOT touch other scripts.

**.gitignore** — add ONE new line (after existing `scripts/.tmp-phase19/` line):
```
scripts/.tmp-phase20/
```

After this task: `node scripts/smoke-phase20.cjs` exits with FAIL count > 0 (because Tasks 2 and 3 of THIS plan haven't landed yet — that's expected and correct; Tasks 2/3 will close the W0.scaffold.* gates). The runner is committed once and is NEVER edited again after this plan.
  </action>
  <verify>
    <automated>node scripts/smoke-phase20.cjs; node -e "const s = require('./package.json').scripts; if (!s['smoke:phase20']) process.exit(1)"; grep -q 'scripts/.tmp-phase20/' .gitignore</automated>
  </verify>
  <done>scripts/smoke-phase20.cjs exists with three-tier sentinel structure; npm run smoke:phase20 invokes it; .gitignore covers scripts/.tmp-phase20/. Runner exits non-zero (expected — Tasks 2+3 will close the W0.scaffold gates within this plan).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add additive-optional type extensions in src/types/index.ts (Plant.fertilizeSchedule, PlantDBEntry.fertilizeIntervalWarm/Cold + fertilizer, Task discriminator '| fertilize', NotificationSettings.fertilizeReminders)</name>
  <files>src/types/index.ts</files>
  <behavior>
    - W0.scaffold.types.Plant-fertilizeSchedule sentinel passes (Plant interface contains `fertilizeSchedule?:`)
    - W0.scaffold.types.PlantDBEntry-fertilizeIntervalWarm + fertilizeIntervalCold sentinels pass
    - W0.scaffold.types.PlantDBEntry-fertilizer sentinel passes (regex `type: 'industrial' | 'homemade' | 'both'`)
    - W0.scaffold.types.Task-discriminator-extended sentinel passes (Task type union contains `"fertilize"`)
    - W0.scaffold.types.NotificationSettings-fertilizeReminders sentinel passes
    - `npx tsc --noEmit` exits 0 (additive optional → no breaking changes)
  </behavior>
  <read_first>
    - src/types/index.ts (full file — see existing Plant interface ~lines 49-104, PlantDBEntry ~192-246, Task ~107-112, NotificationSettings ~283-289)
    - .planning/phases/20-fertilization-subsystem/20-RESEARCH.md §"Code Examples → Example 1: Type Extensions" (lines 783-833 — exact JSDoc + field shapes)
  </read_first>
  <action>
Make 4 edits to `src/types/index.ts`:

**Edit 1 — Add `FertilizeSchedule` interface ABOVE the `Plant` interface declaration:**
```typescript
/**
 * v1.2 Phase 20 (FERT-01) — per-plant fertilize schedule. Additive optional.
 * Absence means no fertilize task emission and no health-score penalty.
 * `lastFertilized` is ISO date "YYYY-MM-DD" (matches Plant.lastWatered format).
 * `intervalDays` is season-agnostic when set per-plant (overrides catalog warm/cold).
 */
export interface FertilizeSchedule {
  intervalDays: number;
  lastFertilized?: string;
}
```

**Edit 2 — Inside the `Plant` interface, add the new optional field at the end (after the last existing field, before the closing `}`):**
```typescript
  /** v1.2 Phase 20 (FERT-01). Additive optional; absence means no fertilize task emission. */
  fertilizeSchedule?: FertilizeSchedule;
```

**Edit 3 — Inside the `PlantDBEntry` interface, append three new optional fields at the end (after the last existing field, before the closing `}`):**
```typescript
  /** v1.2 Phase 20 (FERT-02). Days between fertilizations in warm season. Absence skips emission. */
  fertilizeIntervalWarm?: number;
  /** v1.2 Phase 20 (FERT-02). Days between fertilizations in cold season. null = dormant (no emission). */
  fertilizeIntervalCold?: number | null;
  /** v1.2 Phase 20 (FERT-07). Fertilizer type + per-locale recommendation copy (industrial NPK ratios + homemade recipes; NEVER brand names — FERT-07 lock). */
  fertilizer?: {
    type: 'industrial' | 'homemade' | 'both';
    industrialRecommendation?: string;
    homemadeRecommendation?: string;
  };
```

**Edit 4 — Extend the `Task` discriminator union and `NotificationSettings`:**
```typescript
// CHANGE — Task interface
export interface Task {
  type: "water" | "sun" | "outdoor" | "check_soil" | "fertilize"; // FERT-03
  icon: string;
  label: string;
  plantId: string;
}

// CHANGE — NotificationSettings: add fertilizeReminders?: boolean as the LAST field before closing brace
export interface NotificationSettings {
  enabled: boolean;
  morningReminder: boolean;
  morningTime: string;
  weatherAlerts: boolean;
  careReminders: boolean;
  /** v1.2 Phase 20 (FERT-05). Default OFF; opt-in fertilize axis in morning body. */
  fertilizeReminders?: boolean;
}
```

**Constraints:**
- ALL fields are additive optional (`?:`) — no existing call sites need updating.
- Do NOT touch any other interface in this file.
- Preserve all existing JSDoc comments.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "fertilizeSchedule?:" src/types/index.ts && grep -q "fertilizeIntervalWarm?:" src/types/index.ts && grep -q "fertilizeIntervalCold?:" src/types/index.ts && grep -qE "type:\s*'industrial'\s*\|\s*'homemade'\s*\|\s*'both'" src/types/index.ts && grep -q '"fertilize"' src/types/index.ts && grep -q "fertilizeReminders?:" src/types/index.ts</automated>
  </verify>
  <done>Type extensions land additively; tsc-green; W0.scaffold.types.* sentinels in smoke-phase20 transition from FAIL to PASS.</done>
</task>

<task type="auto">
  <name>Task 3: Create FertilizeCard skeleton + plantLogic helper skeletons + i18n skeleton keys (EN+ES parity)</name>
  <files>src/components/plant-detail/FertilizeCard.tsx, src/utils/plantLogic.ts, src/i18n/locales/en/common.json, src/i18n/locales/es/common.json</files>
  <read_first>
    - src/components/plant-detail/EducationalSection.tsx (full file — pattern reference for FertilizeCard real impl in Plan 20-04; the skeleton just needs to compile)
    - src/utils/plantLogic.ts (full file — see existing getSeasonalInterval + getNextWaterDate at lines 16-49 for shape mirror)
    - src/i18n/locales/en/common.json (lines 299-330 plantCard namespace, 792-817 plantDetailModal namespace, 835-838 tasks namespace, 848-870 notifications namespace, 175-177 settings.careReminders entries)
    - src/i18n/locales/es/common.json (mirror locations — locale parity required)
    - .planning/phases/20-fertilization-subsystem/20-RESEARCH.md §"Validation Architecture → Wave 0 Gaps" (lines 1056-1071 — exact i18n key list)
  </read_first>
  <action>
Make 4 file edits:

**Edit 1 — Create `src/components/plant-detail/FertilizeCard.tsx` (skeleton — returns null):**
```typescript
// src/components/plant-detail/FertilizeCard.tsx
//
// v1.2 Phase 20 (FERT-06) — Two-column water | fertilize layout inside the existing
// 🌿 ¿Qué hacer? section of MyPlantDetailModal. Skeleton at Wave 0; real implementation
// (Reanimated v4 collapse + tap-to-expand + recipe rendering) lands in Plan 20-04.
//
// At Wave 0 baseline this component returns null — it is imported nowhere yet, but the
// file must exist for smoke-phase20.cjs W0.scaffold.FertilizeCard.file-exists assertion
// AND the FERT-06.FertilizeCard.reanimated-primitives SKIP placeholder.

import React from 'react';
import type { PlantDBEntry } from '../../types';
import type { WaterSeason } from '../../utils/seasonality';

export interface FertilizeCardProps {
  strictDbEntry: PlantDBEntry | null;
  season: WaterSeason;
  /** One-shot signal from MyPlantDetailModal initialExpanded prop. */
  defaultExpanded?: boolean;
  /** Style override for half-width vs full-width layout (computed by caller). */
  style?: any;
}

export function FertilizeCard(_props: FertilizeCardProps): React.ReactElement | null {
  // Skeleton — returns null until Plan 20-04 lands real impl with Reanimated v4 collapse.
  return null;
}
```

**Edit 2 — Append two skeleton functions to the END of `src/utils/plantLogic.ts`:**
```typescript

/**
 * v1.2 Phase 20 (FERT-04) — SKELETON. Returns null until Plan 20-02 lands real impl.
 *
 * Real impl will: per-plant Plant.fertilizeSchedule.intervalDays wins over catalog;
 * cold-season catalogEntry.fertilizeIntervalCold === null → return null (no emission);
 * else return season-bucketed catalog interval.
 */
export function getSeasonalFertilizeInterval(
  _plant: import('../types').Plant,
  _catalogEntry: import('../types').PlantDBEntry | null,
  _season: WaterSeason
): number | null {
  return null; // skeleton
}

/**
 * v1.2 Phase 20 (FERT-04) — SKELETON. Returns null until Plan 20-02 lands real impl.
 *
 * Real impl will: mirror getNextWaterDate advance-loop (catch-up clip → ONE task on due-day);
 * if no lastFertilized, return today; if intervalDays null/<=0, return null.
 */
export function getNextFertilizeDate(
  _plant: import('../types').Plant,
  _catalogEntry: import('../types').PlantDBEntry | null,
  _today: Date,
  _season: WaterSeason
): Date | null {
  return null; // skeleton
}
```

**Edit 3 — Add i18n keys to `src/i18n/locales/en/common.json` (use Edit tool, multiple targeted insertions to preserve existing JSON structure):**

In the `plantCard` namespace (around line 302, after `"outdoor": "Put outside"`):
```json
    "fertilize": "Fertilize",
```

In the `plantDetailModal` namespace (around line 816, after `"pets": "Pets"` — add as the new last property of plantDetailModal; remember to add a comma to the previous line):
```json
    "water": "Water",
    "fertilize": "Fertilize",
    "fertilizeEvery": "Fertilize every {{days}} days ({{season}})",
    "fertilizeDormant": "Dormant — no fertilizer needed in cold season"
```

In the `tasks` namespace (around line 837, after `"checkSoilBody": "..."` — add as new last properties):
```json
    "fertilize": "Fertilize {{name}}"
```

In the `notifications` namespace (around line 868, after `"followUpBody"` — add as new last property):
```json
    "fertilize": "Fertilize:"
```

In the `settings` namespace (around line 177, after `"careRemindersSubtitle"` — add two new properties):
```json
    "fertilizeReminders": "Fertilize reminders",
    "fertilizeRemindersSubtitle": "Get notified when plants need fertilizing",
```

**Edit 4 — Add MIRROR keys to `src/i18n/locales/es/common.json` (voseo for ES; same insertion points):**

`plantCard` namespace (after `"outdoor": "..."`):
```json
    "fertilize": "Fertilizar",
```

`plantDetailModal` namespace (as new last properties of plantDetailModal):
```json
    "water": "Regar",
    "fertilize": "Fertilizar",
    "fertilizeEvery": "Fertilizá cada {{days}} días ({{season}})",
    "fertilizeDormant": "Dormante — no fertilizar en frío"
```

`tasks` namespace (as new last property after `"checkSoilBody"`):
```json
    "fertilize": "Fertilizar {{name}}"
```

`notifications` namespace (as new last property):
```json
    "fertilize": "Fertilizá:"
```

`settings` namespace (after `"careRemindersSubtitle"`):
```json
    "fertilizeReminders": "Recordatorios de fertilización",
    "fertilizeRemindersSubtitle": "Te avisamos cuándo abonar tus plantas",
```

**JSON validity:** every insert must respect existing comma rules (add a comma to the previous line if needed; no trailing comma after the last property of an object). After this task: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/common.json','utf8')); JSON.parse(require('fs').readFileSync('src/i18n/locales/es/common.json','utf8'));"` exits 0.

**Voseo register check (ES):** "Fertilizar {{name}}" is infinitive (matches existing `tasks.checkSoil` register "Chequear tierra — {{name}}"); "Fertilizá:" notification body uses voseo imperative (matches existing `notifications.water: "regar"` neutral verb). "Te avisamos cuándo abonar tus plantas" uses voseo "te avisamos" (no "te avisaremos" / no "le avisamos").
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/common.json','utf8')); JSON.parse(require('fs').readFileSync('src/i18n/locales/es/common.json','utf8'));" && npx tsc --noEmit && node scripts/smoke-phase20.cjs</automated>
  </verify>
  <done>FertilizeCard.tsx skeleton exists; plantLogic.ts exports getSeasonalFertilizeInterval + getNextFertilizeDate skeletons; 9 i18n key pairs land with EN+ES parity. smoke-phase20.cjs exits 0 with all W0.scaffold.* sentinels PASSing and FERT-* SKIP placeholders queued. tsc-green. JSON valid.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
- `npx tsc --noEmit` exits 0
- `node scripts/smoke-phase20.cjs` exits 0 with PASS = all W0.scaffold.* + all CROSS.* sentinels (~25); FAIL = 0; SKIP = ~20 placeholders for FERT-01..07 features
- `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/common.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/es/common.json','utf8'))"` exits 0
- `npm run check:i18n-keys` still PASSes (no new fertilizer.* fields in plants.json yet — extension lands in Plan 20-09)
- `npm run smoke:phase18 && npm run smoke:phase19` still PASS (cross-phase regression preserved)
</verification>

<success_criteria>
- Wave 0 Nyquist gate green: smoke runner authored ONCE, never edited after this plan
- Cross-phase regression sentinels protect Phase 18 (PlantHealthBadge, Gesture.Pan, mood emoji, Toast) and Phase 19 (PetToxicityBadge, Mascotas section, initialSection prop, check-i18n-keys symptoms extension)
- Type extensions are additive optional — zero existing call sites need updates
- i18n skeleton keys land with locale parity verified at Wave 0
- FertilizeCard + plantLogic helpers exist as skeletons (returns null) — real impls land in Plans 20-02 and 20-04 by editing these files in place
</success_criteria>

<output>
After completion, create `.planning/phases/20-fertilization-subsystem/20-00-SUMMARY.md`
</output>
