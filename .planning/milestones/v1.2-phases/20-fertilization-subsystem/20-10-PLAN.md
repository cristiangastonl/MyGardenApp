---
phase: 20-fertilization-subsystem
plan: 10
type: execute
wave: 6
depends_on: ["20-00", "20-01", "20-02", "20-03", "20-04", "20-05", "20-06", "20-07", "20-08", "20-09"]
files_modified: []
autonomous: false
requirements: [FERT-01, FERT-02, FERT-03, FERT-04, FERT-05, FERT-06, FERT-07]
must_haves:
  truths:
    - "Automation gate green BEFORE manual checkpoint runs: npx tsc --noEmit exits 0 + npm run check:i18n-keys exits 0 + node scripts/smoke-phase20.cjs exits 0 with all FERT-*.* + CROSS.* sentinels PASS, zero SKIP, zero FAIL + npm run smoke:phase18 + npm run smoke:phase19 still PASS"
    - "User exercises the 14-item device-test checklist (Blocks A-E pattern from Phase 19-07-SUMMARY.md) on iOS dev client AND Android dev client"
    - "User chooses Option A (full run-now → all checklist items pass on both platforms; phase CLOSES with 0 deferrals) OR Option B (defer manual gate to v1.2 device-test backlog memory per Phase 18-05 / Phase 19-07 milestone-end batching pattern; phase CLOSES at code level despite device-only deferrals)"
    - "All manual-only verifications listed in 20-VALIDATION.md §Manual-Only Verifications get exercised: two-column layout height-equalization (FERT-06); 180ms tap-to-expand snappiness (FERT-06); initialExpanded auto-expand vs initialSection scroll race (Pitfall 1, FERT-06); PlantCard fertilize TaskButton tap doesn't fire long-press menu OR swipe (Pitfall 3, FERT-06); fertilize push notification appears in morning body when toggle ON (FERT-05); voseo register naturalness across 118 catalog entries (FERT-07)"
  artifacts: []
  key_links: []
---

<objective>
Wave 6 manual checkpoint — closes Phase 20 by exercising all manual-only verifications across iOS + Android dev clients. Mirrors the 14-item Blocks A-E pattern from Phase 19-07-SUMMARY.md (manual gate closure pattern). The checkpoint supports two completion paths per Phase 18-05 / 19-07 precedent: Option A (run-now, all 14 items pass on both platforms) OR Option B (defer to v1.2 backlog memory).

Purpose: closes the FERT-XX requirements at the device level. Code-side gates (smoke runners + tsc + check-i18n-keys) all green; this plan verifies the device-side surfaces (animation snappiness, gesture composition coexistence, notification body wording, voseo register naturalness) that automation cannot probe.
Output: 0 source files modified; metadata-only commit captures SUMMARY + STATE + ROADMAP updates atomically (mirrors Phase 19-07 closure pattern).
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-fertilization-subsystem/20-CONTEXT.md
@.planning/phases/20-fertilization-subsystem/20-RESEARCH.md
@.planning/phases/20-fertilization-subsystem/20-VALIDATION.md
@.planning/phases/19-pet-toxicity/19-07-SUMMARY.md
@.planning/phases/18-plantcard-cleanup-mood-emoji/18-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automation gate — verify all code-side gates green BEFORE manual checkpoint</name>
  <files></files>
  <read_first>
    - .planning/phases/20-fertilization-subsystem/20-VALIDATION.md (Per-Task Verification Map — every Plans 20-00..09 sentinel listed)
    - .planning/phases/19-pet-toxicity/19-07-SUMMARY.md (Phase 19 automation gate format precedent)
  </read_first>
  <action>
**Run the full automation gate sequence and capture output for the SUMMARY:**

```bash
echo "=== Phase 20 Automation Gate ==="
echo "--- tsc ---"
npx tsc --noEmit
echo "--- check:i18n-keys ---"
npm run check:i18n-keys
echo "--- smoke:phase20 ---"
node scripts/smoke-phase20.cjs
echo "--- smoke:phase18 (cross-phase regression) ---"
npm run smoke:phase18
echo "--- smoke:phase19 (cross-phase regression) ---"
npm run smoke:phase19
echo "=== End Automation Gate ==="
```

**Required outputs:**
- `npx tsc --noEmit` exits 0 (no errors)
- `npm run check:i18n-keys` exits 0 with `[check:i18n-keys] PASS — 118 catalog ids verified across en/es plants.json`
- `node scripts/smoke-phase20.cjs` exits 0 with `[Phase 20 smoke] PASS=<all sentinels> FAIL=0 SKIP=0` (all SKIPs flipped to PASS by Plans 20-01..09)
- `npm run smoke:phase18` exits 0 with `PASS=<count> FAIL=0` (Phase 18 cross-phase preserved)
- `npm run smoke:phase19` exits 0 with `PASS=<count> FAIL=0` (Phase 19 cross-phase preserved)

**If ANY gate fails, STOP and report:** the manual checkpoint MUST NOT run on a red automation gate. Defer to a gap-closure plan (Plans 20-N1, 20-N2, ...) following the Phase 19 19-07 precedent of running automation-gate-first-then-manual.

**Capture the exact pass/fail/skip counts in this task's verification output.** They go into `20-10-SUMMARY.md`'s Automation Gate section verbatim.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:i18n-keys && node scripts/smoke-phase20.cjs && npm run smoke:phase18 && npm run smoke:phase19</automated>
  </verify>
  <done>All 5 automation gates green; counts captured for SUMMARY; manual checkpoint cleared to proceed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual device-test checkpoint — 14-item Blocks A-E checklist (iOS + Android)</name>
  <files></files>
  <read_first>
    - .planning/phases/20-fertilization-subsystem/20-10-SUMMARY.md (Task 1 automation gate output — verify all green BEFORE engaging user)
    - /Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md (existing v1.2 device-test backlog — Phase 20 entries append here on Option B deferral)
    - .planning/phases/19-pet-toxicity/19-07-SUMMARY.md (Phase 19 manual gate closure pattern — Option A vs Option B)
    - .planning/phases/18-plantcard-cleanup-mood-emoji/18-05-SUMMARY.md (Phase 18 manual deferral precedent)
  </read_first>
  <action>
PAUSE for user input. Present the 14-item device-test checklist (in `<how-to-verify>` below) to the user verbatim. Wait for the resume-signal before proceeding. Do NOT execute any code beyond presenting the checklist + waiting for user response.

On user response:
- **"approved" / "Option A":** Capture user's signal verbatim in 20-10-SUMMARY.md "Decisions Made" section; phase 20 closes with ZERO items deferred.
- **"defer to v1.2 backlog" / "Option B":** Append the 14-item Phase 20 checklist to `/Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md` with hard-fail vs soft-fail classifier per Phase 18-05 protocol (Block A/B/D = hard-fail; Block C/E = soft-fail). Phase 20 closes at code level; deferred items remain ship-blocker for v1.2 store submission, NOT for Phase 20 closure.
- **Specific item failure (e.g., "B4 fails on Android"):** Document failure in 20-10-SUMMARY.md "Issues Found" section; trigger gap-closure plan creation per `/gsd:plan-phase --gaps` flow if user requests.
  </action>
  <verify>
    <automated>echo "MANUAL CHECKPOINT — awaiting user resume-signal"</automated>
  </verify>
  <what-built>
The complete Phase 20 fertilization subsystem:
- New 'fertilize' task type (5-site discriminator sweep across plantLogic / notificationScheduler / DayDetail / DayDetailModal / MonthCalendar / TaskButton)
- Per-plant fertilizeSchedule + per-catalog fertilizeIntervalWarm/Cold + fertilizer.{type, industrialRecommendation, homemadeRecommendation} types
- Cadence math (getNextFertilizeDate + getSeasonalFertilizeInterval) with cold-season dormancy + catch-up clipping
- PlantCard mode='tasks' fertilize TaskButton (5-element budget preserved alongside Phase 18 mood emoji + Phase 19 toxicity badges)
- MyPlantDetailModal `🌿 ¿Qué hacer?` two-column water | fertilize layout with collapse-to-single-column graceful degradation
- FertilizeCard tap-to-expand recipe reveal (180ms Reanimated v4 collapse — matches in-repo EducationalSection tuning, NOT 250ms)
- initialExpanded='fertilize' auto-expand-on-arrival prop wiring (PlantsScreen + TodayScreen → MyPlantDetailModal → FertilizeCard)
- Settings → Notifications → "Fertilize reminders" Switch (default OFF; toggle ON wires fertilize body-line into morning notification)
- 118 catalog entries × EN+ES fertilizer recipe content (per-category framing: tropicales/aromáticas/huerta both; suculentas industrial-only; exterior/frutales both with high-P / high-K differentiation)
- check-i18n-keys.mjs extended with conditional fertilizer sub-field parity validation
  </what-built>
  <how-to-verify>
**Pre-test setup:**
1. Pull latest, rebuild dev clients on iOS + Android (`npx expo start --dev-client` and physical device install).
2. Seed test data: in dev menu, add 4 plants:
   - Plant A: identify as "Anthurium" (interior, both industrial+homemade fertilizer, 21d warm)
   - Plant B: identify as "Jade" (suculenta, industrial-only, 60d warm, null cold)
   - Plant C: identify as "Tomate" (huerta, both, 14d warm)
   - Plant D: custom plant (no databaseId — should NEVER show fertilize task or content)

**14-item checklist — execute on BOTH iOS dev client AND Android dev client. Mark each PASS / FAIL.**

### Block A: PlantCard fertilize TaskButton (FERT-06 mode='tasks')
1. **A1 — Card budget preserved:** Open Today screen with all 4 plants. PlantCard for A/B/C still shows: image + name + 1 task (or none) + water badge + mood emoji = 5 elements; no layout overflow. Plant D (custom) shows NO fertilize task indicator. **Expected: PASS on both.**
2. **A2 — Fertilize TaskButton renders when due:** Force a fertilize-due-today by editing Plant A's fertilizeSchedule.lastFertilized to (today − 21 days) via dev menu OR by setting it to undefined (never fertilized = due today). Reload — Plant A's tasks row now shows the 🌱 "Fertilizar" button alongside any water/sun/outdoor buttons. **Expected: PASS.**
3. **A3 — TaskButton tap marks done (Pitfall 3 — gesture coexistence):** Tap the 🌱 TaskButton on Plant A. ONLY mark-done fires (button shows done state with strikethrough). Phase 18 long-press menu does NOT open; horizontal swipe gesture does NOT trigger. **Expected: PASS on both.**

### Block B: MyPlantDetailModal two-column layout (FERT-06 modal side)
4. **B1 — Two-column water + fertilize:** Open detail for Plant A (anthurium). The `🌿 ¿Qué hacer?` section renders TWO side-by-side mini-cards: 💧 Regar (left, water content) + 🌱 Fertilizá cada 21 días (cálido) (right, fertilize header). Cards are EQUAL HEIGHT (no awkward gap). **Expected: PASS on both.**
5. **B2 — Single-column fallback (water only):** Open detail for Plant D (custom — no databaseId, no fertilize content). The `🌿 ¿Qué hacer?` section renders only the water info OR a placeholder if no water content either; NO empty fertilize card; NO awkward right-side gap. **Expected: PASS on both.**
6. **B3 — Single-column fallback (fertilize only):** Edit Plant A in dev menu to remove careAction (water content). Reopen detail — fertilize card spans full row width; no awkward left-side gap. **Expected: PASS on both.**
7. **B4 — Tap-to-expand 180ms snappy:** Tap the fertilize card header. Body slides open in ~180ms with `Easing.out(Easing.cubic)` — feels snappy, not sluggish. Industrial recipe (🧪) and homemade recipe (🏡) are now visible. Tap again — collapses smoothly. **Expected: PASS on both.**
8. **B5 — Suculenta industrial-only (no homemade):** Open detail for Plant B (jade). Tap the fertilize card. Body shows ONLY the 🧪 industrial recommendation; NO 🏡 homemade line. **Expected: PASS on both.**
9. **B6 — Cold-season dormancy:** Switch device locale climate or seed Plant A with climateOverride='cold'. Reopen detail — fertilize card shows "🌱 Dormante — no fertilizar en frío" (no frequency teaser, no expand chevron). **Expected: PASS on both.**

### Block C: PlantCard → modal auto-expand routing (Pitfall 1 — FERT-06 race)
10. **C1 — initialExpanded='fertilize' auto-expand:** From Today screen, tap Plant A's CARD BODY (not the TaskButton). Modal opens scrolled to top of `🌿 ¿Qué hacer?` section AND fertilize card is ALREADY expanded on arrival (recipes visible without manual tap). No animation jank, no half-rendered content during scroll. **Expected: PASS on both.**

### Block D: Settings toggle + push notifications (FERT-05)
11. **D1 — Toggle visible + default OFF:** Open Settings → Notifications. The "🌱 Recordatorios de fertilización" Switch row is visible BETWEEN "Recordatorios de cuidado" and the test button. The Switch is OFF on first install. **Expected: PASS on both.**
12. **D2 — Toggle ON + morning body line:** Toggle the fertilize Switch ON. Tap "Enviar prueba" — observe the test notification body. (Note: live morning reminder requires waiting for scheduled time; the test notification is the proxy here.) Then advance device clock to 08:00 OR re-trigger morning reminder via dev menu — body should include "Fertilizá: 1 planta" (or similar) when Plant A is fertilize-due-today. **Expected: PASS on both.**
13. **D3 — Toggle OFF blocks fertilize body:** Toggle fertilize Switch OFF. Re-trigger morning reminder. Body should INCLUDE water/sun/outdoor lines for due plants but EXCLUDE fertilize line. **Expected: PASS on both.**

### Block E: Voseo register quality (FERT-07 native-speaker review)
14. **E1 — Voseo register sample (10 random entries):** Open detail for 10 random plants spanning all 6 categories. Read the fertilizer recipe text (industrial AND homemade) in ES locale. Verify:
    - Imperatives use voseo: "Diluí 1:4", "Aplicá", "Sumá", "Regá", "Fertilizá" — NOT "Diluye", "Aplica", "Riega", "Fertiliza" (Castilian)
    - No "tienes que" / "debes" / "puedes" — only voseo "tenés que" / "podés" / "querés" if conditional language is used
    - Per-entry mechanism citation feels distinct (not generic boilerplate)
    - LATAM-friendly homemade ingredients ("té de cáscara de banana", "lombricompuesto", "cenizas de madera", "harina de hueso") — NO Castilian "ceniza de leña" or other regionalisms
    **Expected: PASS on both.**

**After all 14 items: choose closure path:**

- **Option A (run-now full pass):** All 14 items pass on iOS AND Android. Reply: `approved` or `Option A`. Phase 20 closes with ZERO items deferred.
- **Option B (defer to v1.2 backlog):** Some items deferred for batched device-testing at v1.2 milestone end (mirror Phase 18-05 / 19-07 precedent). Reply: `defer to v1.2 backlog` or `Option B`. Phase 20 closes at code level; deferred items appended to `v1_2_test_backlog.md` memory file with full unrun checklist + classifier (hard-fail vs soft-fail per Phase 18-05 protocol).
- **Issues found:** Reply with specific item IDs (e.g., "B4 fails on Android — animation janky") for gap-closure planning.
  </how-to-verify>
  <resume-signal>Reply: `approved` (Option A — full run, all 14 pass), `defer to v1.2 backlog` (Option B — milestone-end batching), OR specific item failures (e.g., "B4 fails on Android" — triggers gap-closure plan).</resume-signal>
  <done>User signal received and recorded; Phase 20 closes at code level (with or without device-deferral).</done>
</task>

</tasks>

<verification>
After Task 1 (automation gate):
- All 5 commands (tsc, check:i18n-keys, smoke:phase20, smoke:phase18, smoke:phase19) exit 0
- smoke-phase20 reports zero SKIP, zero FAIL
- Cross-phase regression preserved

After Task 2 (manual checkpoint):
- User responds with Option A, Option B, or specific failures
- Option A: 14 items pass on both iOS + Android dev clients
- Option B: failed/deferred items appended to v1_2_test_backlog.md memory file with classifier
- Specific failures: gap-closure plan triggered (Plans 20-N1+)
</verification>

<success_criteria>
- Automation gate green BEFORE manual checkpoint runs (Task 1 ordering enforced)
- 14-item Blocks A-E checklist covers all FERT-XX manual-only verifications from 20-VALIDATION.md
- Two completion paths supported (Option A run-now / Option B defer-to-backlog) — mirrors Phase 18-05 / 19-07 precedent
- Voseo register sampling discipline (Block E) catches Castilian regression even after pre-commit grep gates pass
- Phase 20 closure pattern reusable for Phases 21-24 manual checkpoint plans
</success_criteria>

<output>
After completion, create `.planning/phases/20-fertilization-subsystem/20-10-SUMMARY.md`. Phase 20 closes — update `.planning/STATE.md` (current_plan: 10 of 10, status: "Phase 20 CLOSED ...") and `.planning/ROADMAP.md` (Phase 20 progress 10/10 Complete).
</output>
