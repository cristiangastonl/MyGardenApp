---
phase: 20-fertilization-subsystem
plan: 06
type: execute
wave: 4
depends_on: ["20-00"]
files_modified:
  - src/data/plantDatabase.ts
  - src/i18n/locales/en/plants.json
  - src/i18n/locales/es/plants.json
autonomous: true
requirements: [FERT-02, FERT-07]
must_haves:
  truths:
    - "All 67 catalog entries in categories interior + aromaticas + huerta gain `fertilizeIntervalWarm: <number>`, `fertilizeIntervalCold: <number | null>`, AND `fertilizer: { type, industrialRecommendation?, homemadeRecommendation? }` per the per-category framing matrix from RESEARCH §Pattern 12 — locale parity authored from start"
    - "Per-category framing — interior tropicales (44 entries): type='both', NPK 20-20-20 industrial + lombricompuesto/té de cáscara/té de compost homemade; aromáticas (13 entries): type='homemade' default (industrial optional), té de compost / harina de hueso for floración; huerta (10 entries): type='both', NPK 10-10-10 + lombricompuesto + té de cáscara de banana"
    - "Per-entry rationales DISTINCT — no copy-paste industrial OR homemade text across entries (Phase 14-07 + 15-01 + 16-01 + 17-01 zero-copy-paste discipline)"
    - "char-limit-from-draft applied — every industrialRecommendation AND homemadeRecommendation drafted ≤ 120 chars from start (Phase 15-01 first-try-zero-trim discipline)"
    - "voseo pre-sweep applied — `grep -cE '\\btienes\\b|\\bpuedes\\b|\\bdebes\\b|\\bquieres\\b|\\bsu\\s' src/i18n/locales/es/plants.json` returns the SAME baseline as before this plan (no NEW Castilian/3rd-person forms introduced)"
    - "locale parity preserved — every entry that declares `fertilizer.industrialRecommendation` in plantDatabase.ts has the corresponding plants.json key in BOTH en AND es; same for homemadeRecommendation"
    - "FERT-02.catalog.fertilizeIntervalWarm-coverage SKIP placeholder remains SKIP after this plan (mid-band: 67 of 118 ≈ 57%, below the ≥100 threshold) — flips to PASS after Plan 20-08 lands the final batch reaching 118"
    - "tsc-green; npm run check:i18n-keys passes (Plan 20-09 will EXTEND check-i18n-keys.mjs to validate fertilizer.industrialRecommendation/homemadeRecommendation conditional parity — but this plan must already produce locale-parity content so Plan 20-09's extension PASSes immediately)"
  artifacts:
    - path: src/data/plantDatabase.ts
      provides: 67 entries with fertilizeIntervalWarm/Cold + fertilizer fields populated (interior + aromaticas + huerta)
      contains: "fertilizeIntervalWarm:"
    - path: src/i18n/locales/en/plants.json
      provides: 67 entries × fertilizer.industrialRecommendation/homemadeRecommendation translation keys
      contains: "industrialRecommendation"
    - path: src/i18n/locales/es/plants.json
      provides: 67 entries × voseo-imperative fertilizer text (mirror of EN)
      contains: "industrialRecommendation"
  key_links:
    - from: src/i18n/locales/{en,es}/plants.json
      to: src/data/plantDatabase.ts (fertilizer.industrialRecommendation key)
      via: getTranslatedPlant fallback resolution chain (Phase 14 EDU-02 i18n indirection pattern)
      pattern: "fertilizer.*industrialRecommendation"
---

<objective>
Wave 4 Batch A — author FERT-07 catalog content for the 67 entries in categories `interior` (44) + `aromaticas` (13) + `huerta` (10). Apply the per-category framing matrix from RESEARCH §Pattern 12: tropicales lean homemade-first with NPK 20-20-20 industrial alternative; aromáticas default homemade-only; huerta both with phase-of-growth differentiation.

Purpose: lands ~67/118 of FERT-07 content in a single category-grouped batch (Phase 14-04/06/07 rhythm). Plan 20-07 ships the suculentas batch; Plan 20-08 ships exterior + frutales + remaining; together they close FERT-07.
Output: 3 files modified; 67 entries × ~2 fertilizer recipe fields × 2 locales = ~270 strings; tsc-green; locale parity verified.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-fertilization-subsystem/20-CONTEXT.md
@.planning/phases/20-fertilization-subsystem/20-RESEARCH.md
@.planning/phases/20-fertilization-subsystem/20-00-PLAN.md
@.planning/phases/20-fertilization-subsystem/20-01-PLAN.md

<interfaces>
<!-- PlantDBEntry shape (Plan 20-00 Task 2 added these optional fields): -->

```typescript
interface PlantDBEntry {
  // ... existing fields
  fertilizeIntervalWarm?: number;
  fertilizeIntervalCold?: number | null; // null = dormant (no emission in cold season)
  fertilizer?: {
    type: 'industrial' | 'homemade' | 'both';
    industrialRecommendation?: string;
    homemadeRecommendation?: string;
  };
}
```

<!-- plants.json shape — mirror the catalog field paths under each entry's id key -->

```json
{
  "potus": {
    "name": "Potus",
    "tip": "...",
    "description": "...",
    "problems": [...],
    // existing v1.1 + EDU fields
    // NEW (Phase 20 FERT-07):
    "fertilizer": {
      "industrialRecommendation": "<EN text>",
      "homemadeRecommendation": "<EN text>"
    }
  }
}
```

<!-- Per-category framing matrix from RESEARCH §Pattern 12 — see "Per-category default framing matrix" table for tone/content guidance -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Batch A — 44 interior + 13 aromáticas + 10 huerta entries (catalog + i18n EN + ES)</name>
  <files>src/data/plantDatabase.ts, src/i18n/locales/en/plants.json, src/i18n/locales/es/plants.json</files>
  <read_first>
    - src/data/plantDatabase.ts (full file ~5900 LOC — focus: every entry where `category: "interior"` OR `"aromaticas"` OR `"huerta"`. Use `grep -nE "category: \"(interior|aromaticas|huerta)\"" src/data/plantDatabase.ts` to enumerate exact line numbers; expect 67 matches)
    - src/i18n/locales/en/plants.json (mirror entry shape — every catalog id has a top-level key; new fertilizer.* keys nest inside)
    - src/i18n/locales/es/plants.json (mirror)
    - .planning/phases/20-fertilization-subsystem/20-RESEARCH.md §"Architecture Pattern 12 — Catalog Content Authoring Playbook" (lines 654-682) and §"Per-category default framing matrix" table
    - .planning/phases/14-educational-detail-modal/14-04-SUMMARY.md (Phase 14 interior batch — same author rhythm precedent)
    - .planning/phases/15-catalog-wave-a-interior-tropicals/15-01-SUMMARY.md (interior content authoring for Wave A — what they cover)
    - CLAUDE.md §Internationalization (voseo: regá/sacá/podés/abonará for ES; never tú/tu/tienes/debes/usted/su)
  </read_first>
  <action>
**Authoring discipline (mandatory — Phase 14/15/16/17 lineage):**
1. **char-limit-from-draft** — every `industrialRecommendation` and `homemadeRecommendation` ≤ 120 chars from initial draft. Reject any draft >120 chars before mirroring to JSON.
2. **voseo pre-sweep** — before commit, run `grep -cE "\\btienes\\b|\\bpuedes\\b|\\bdebes\\b|\\bquieres\\b" src/i18n/locales/es/plants.json` and confirm count is identical to baseline (`git show HEAD:src/i18n/locales/es/plants.json | grep -cE ...`). If new hits appear, reword. Common false-positives: `\btoca\b` (verb form), `\bten\b` (matches `tenés` — not Castilian), `\briego se\b` (3rd-person reflexive — reword to `el riego va`).
3. **locale parity from start** — author EN + ES + catalog defaults in the SAME edit pass per entry. Don't author 67 EN strings and then 67 ES strings — interleave.
4. **distinct per-entry rationale** — every entry's recipe text cites a per-species mechanism or sub-typology. Zero copy-paste.

**Step 1 — Enumerate target entries.** Run `grep -nE 'category: "(interior|aromaticas|huerta)"' src/data/plantDatabase.ts` → expect 67 line numbers. Confirm count = 67 before proceeding.

**Step 2 — Per-category framing baselines (apply per-entry, with distinct mechanism citation):**

| Sub-category | type | fertilizeIntervalWarm | fertilizeIntervalCold | Industrial framing template | Homemade framing template |
|--------------|------|------------------------|-------------------------|------------------------------|----------------------------|
| Interior aroide (anthurium, monstera, costilla-adan, espatifilo, singonio, aglaonema, difenbaquia, alocasia, caladium, filodendro, potus) | 'both' | 21 | null | "NPK 20-20-20 diluido 1:4, cada 21d en cálido" + per-entry mechanism note | "Té de cáscara de banana cada 21d (alto K para hojas exuberantes)" |
| Interior helecho (helecho-boston, helecho-nido) | 'both' | 14 | null | "NPK equilibrado 10-10-10 diluido 1:4 cada 14d, esporofito alta demanda" | "Té de compost diluido cada 14d, agua humifera epífita" |
| Interior palmera (palmera-areca, palmera-kentia, palmera-interior) | 'both' | 28 | null | "NPK 15-5-15 diluido 1:4 cada 28d, K para fronda firme" | "Cáscara de banana molida en superficie cada 30d" |
| Interior foliage especial (calathea, maranta, fitonia, croton, peperomia, pilea, tradescantia, begonia-rex) | 'both' | 21 | null | per-pigment / per-mechanism (anthocyanins/carotenoids/marantáceas/CAM); diluído 1:4 cada 21d | té de compost cada 21d / lombricompuesto líquido |
| Interior CAM/suculenta light (sansevieria, zamioculca, jade, cola-burro, sansevieria-cilindrica) | 'industrial' | 60 | null | "NPK 5-10-10 baja-N cada 60d, CAM bajo demanda" | (omit homemadeRecommendation — homemade composts demasiado N para CAM) |
| Interior tropicales misc (ficus, ficus-lyrata, hiedra, dracaena, cheflera, arbol-dinero, anthurium, cinta, orquidea, yuca, cactus-navidad-en-interior — verify category) | 'both' | 21-30 | null | NPK 20-20-20 OR 15-15-15 per species need; diluído 1:4 | té de compost / cáscara de banana / lombricompuesto |
| Aromáticas (lavanda x3, romero, romero-rastrero, menta, ciboulette, cilantro, perejil, salvia-officinalis, eneldo, stevia, albahaca) | 'homemade' | 14 | 30 | (rare — only when soil exhausted, NPK 5-10-5 cada 30d) | "Té de compost cada 14d, harina de hueso al inicio de floración" |
| Huerta (tomate, tomate-cherry, lechuga, zanahoria, rúcula, espinaca, pimiento, calabaza-zucchini, frutilla, pepino — verify list) | 'both' | 14 | 30 | "NPK 10-10-10 fase vegetativa cada 14d; cambiar a 5-15-5 en fructificación" | "Lombricompuesto + cáscara de banana cada 14d; cenizas para K en fructificación" |

**Step 3 — For each of the 67 entries**, apply the appropriate baseline above, then:
1. Edit `src/data/plantDatabase.ts` to add `fertilizeIntervalWarm`, `fertilizeIntervalCold`, and `fertilizer: { type, industrialRecommendation, homemadeRecommendation }` to the entry object. Place the new fields between existing `nutrients` and `lightLevel` fields (or at the end of the object if those don't exist), preserving alphabetical/grouped order with neighboring entries. The `industrialRecommendation` + `homemadeRecommendation` strings in plantDatabase.ts are the **default fallback** (matches Phase 14 EDU-02 i18n indirection pattern).
2. Add the corresponding `fertilizer.industrialRecommendation` + `fertilizer.homemadeRecommendation` keys to BOTH `src/i18n/locales/en/plants.json` AND `src/i18n/locales/es/plants.json` under the entry's top-level id key. ES uses voseo imperatives ("Diluí 1:4 cada 21d" / "Aplicá NPK 10-10-10 cada 14d" / "Sumá té de cáscara de banana cada 21d").
3. Per-entry mechanism citation MUST be DISTINCT — e.g.:
   - **anthurium**: "NPK 20-20-20 diluído 1:4 cada 21d en cálido (epífita aroide alta demanda K para spathes)"
   - **monstera**: "NPK 20-20-20 diluído 1:4 cada 21d en cálido (hemiepífita demanda P para fenestraciones)"
   - **calathea**: "NPK 15-15-15 diluído 1:4 cada 21d (marantácea sensitive — diluir más en verano)"
   - **lavanda-angustifolia**: "Casi nada — té de compost cada 30d primavera/verano (mediterránea suelo magro)"
   - **tomate**: "NPK 10-10-10 fase vegetativa cada 14d; cambiar a 5-15-5 (alto P) cuando ves primeras flores (Solanácea)"
   - ...repeat with distinct mechanism per entry; zero generic template re-use.

**Step 4 — Verification before commit:**
1. `npx tsc --noEmit` exits 0
2. `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/plants.json','utf8')); JSON.parse(require('fs').readFileSync('src/i18n/locales/es/plants.json','utf8'));"` exits 0 (JSON valid)
3. `npm run check:i18n-keys` passes (existing v1.1 + EDU + TOX-06 gates intact)
4. ES voseo regex baseline preserved: `grep -cE "\btienes\b|\bpuedes\b|\bdebes\b|\bquieres\b" src/i18n/locales/es/plants.json` returns the SAME count as before this plan
5. Locale parity check: every entry that has `fertilizer.industrialRecommendation` in plantDatabase.ts also has it in both en/es plants.json. Use a one-shot helper script:
   ```bash
   node -e "
   const db = require('./src/data/plantDatabase').PLANT_DATABASE;
   const en = require('./src/i18n/locales/en/plants.json');
   const es = require('./src/i18n/locales/es/plants.json');
   for (const e of db) {
     if (e.fertilizer?.industrialRecommendation) {
       if (!en[e.id]?.fertilizer?.industrialRecommendation) console.error('EN missing industrial: '+e.id);
       if (!es[e.id]?.fertilizer?.industrialRecommendation) console.error('ES missing industrial: '+e.id);
     }
     if (e.fertilizer?.homemadeRecommendation) {
       if (!en[e.id]?.fertilizer?.homemadeRecommendation) console.error('EN missing homemade: '+e.id);
       if (!es[e.id]?.fertilizer?.homemadeRecommendation) console.error('ES missing homemade: '+e.id);
     }
   }
   "
   ```
6. Char-limit verification: `node -e "const en=require('./src/i18n/locales/en/plants.json'); const es=require('./src/i18n/locales/es/plants.json'); for(const id in en){ for(const k of ['industrialRecommendation','homemadeRecommendation']){ const t=en[id]?.fertilizer?.[k]; if(t && t.length>120) console.error('EN '+id+'.'+k+' '+t.length+' chars'); const t2=es[id]?.fertilizer?.[k]; if(t2 && t2.length>120) console.error('ES '+id+'.'+k+' '+t2.length+' chars'); } }"`

**Step 5 — Atomic commit message** (single commit per task — GSD baseline): `feat(20-06): FERT-07 Batch A — 67 interior+aromáticas+huerta entries with fertilizer content (EN+ES)`

**Forward-compat note:** After this batch, `FERT-02.catalog.fertilizeIntervalWarm-coverage` SKIP remains SKIP (67 < 100 threshold). It flips to PASS in Plan 20-08 when the running count reaches 118. The mid-band SKIP behavior is by design — mirrors Phase 15 / 16 / 17 catalog batch precedent.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/plants.json','utf8')); JSON.parse(require('fs').readFileSync('src/i18n/locales/es/plants.json','utf8'));" && npm run check:i18n-keys && test "$(grep -c 'fertilizeIntervalWarm:' src/data/plantDatabase.ts)" -ge 60 && node scripts/smoke-phase20.cjs && npm run smoke:phase18 && npm run smoke:phase19</automated>
  </verify>
  <done>67 catalog entries (interior + aromaticas + huerta) gain fertilizer content; locale parity preserved; voseo regex baseline preserved; tsc-green; check-i18n-keys passes; FERT-02 catalog count grows to ~67 (SKIP remains SKIP — flips to PASS after Plan 20-08 reaches 118).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `node scripts/smoke-phase20.cjs` exits 0 (FERT-02.catalog.* SKIP remains SKIP — by design until Plan 20-08)
- `npm run check:i18n-keys` PASSes (existing gates respect the new entries)
- `npm run smoke:phase18 && npm run smoke:phase19` still PASS
- Locale parity: every catalog `fertilizer.industrialRecommendation` has matching plants.json key in BOTH en/es
- Char limit: zero entries exceed 120 chars in any locale
</verification>

<success_criteria>
- char-limit-from-draft + voseo pre-sweep + locale parity from start + distinct per-entry rationales — Phase 14/15/16/17 muscle memory applied
- Per-category framing matrix from RESEARCH §Pattern 12 honored
- Zero copy-paste rationales across the 67 entries
- v1.1 + EDU + TOX existing keysets preserved
</success_criteria>

<output>
After completion, create `.planning/phases/20-fertilization-subsystem/20-06-SUMMARY.md`
</output>
