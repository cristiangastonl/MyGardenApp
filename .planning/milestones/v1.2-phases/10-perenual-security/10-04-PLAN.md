---
phase: 10-perenual-security
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - SEC-05
gap_closure: false

must_haves:
  truths:
    - "CLAUDE.md edge function list now includes `get-plant-care` alongside identify-plant, diagnose-plant, chat-diagnosis, waitlist"
    - "CLAUDE.md Supabase Edge Functions section has a deploy command example for get-plant-care: `source .envrc && supabase functions deploy get-plant-care`"
    - "CLAUDE.md Secrets section lists PERENUAL_API_KEY alongside the existing PLANTNET_API_KEY and GEMINI_API_KEY"
    - "CLAUDE.md Pre-Submit/Security area has a grep guard note: `EXPO_PUBLIC_PERENUAL_API_KEY` should never appear in `.env*` files post-Phase-10"
    - "PROJECT.md Key Decisions table contains a v1.2 row noting the Perenual key migration: 'Perenual API key moved server-side via get-plant-care edge function (was leaked in client bundle since v1.0; rotated as part of v1.2)'"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated developer instructions reflecting the get-plant-care edge function, deploy command, secret name, and pre-submit grep guard"
      contains: "get-plant-care"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row documenting the v1.2 Perenual security migration"
      contains: "Perenual"
  key_links:
    - from: "CLAUDE.md (Edge functions list line)"
      to: "supabase/functions/get-plant-care/index.ts"
      via: "Documentation list entry — future Claude sessions reading CLAUDE.md will know the function exists"
      pattern: "get-plant-care"
    - from: "CLAUDE.md (Secrets section)"
      to: "Supabase project secrets store"
      via: "Documents that PERENUAL_API_KEY is a Supabase secret, not a client env var"
      pattern: "PERENUAL_API_KEY"
---

<objective>
Update the two project-level documentation files to reflect the Perenual security migration so future Claude sessions (and any human onboarding) start with the correct mental model: Perenual is server-side now, the key lives in Supabase secrets, the deploy command exists, and `EXPO_PUBLIC_PERENUAL_API_KEY` is a forbidden string in committed files.

Purpose: SEC-05 is the documentation requirement. Without it, the next time Claude reads CLAUDE.md it will not know that `get-plant-care` is a deployable edge function, and may inadvertently try to re-add the client-side key when working on Phase 11 (Perenual data quality improvements). PROJECT.md Key Decisions table is the long-term institutional memory — the entry there means the v1.2 architecture decision is preserved past this milestone.

Output: 2 modified docs files (CLAUDE.md gets ~5 line additions across 3 sections; PROJECT.md gets 1 row added to the Key Decisions table). No code changes.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-perenual-security/10-CONTEXT.md
@.planning/phases/10-perenual-security/10-01-SUMMARY.md
@.planning/phases/10-perenual-security/10-02-SUMMARY.md
@.planning/phases/10-perenual-security/10-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update CLAUDE.md — edge function list + deploy command + secrets + pre-submit grep guard</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (FULL FILE — focus: "Edge functions in `supabase/functions/`" line in the Supabase Configuration section; "Secrets:" line listing PLANTNET_API_KEY + GEMINI_API_KEY; the existing edge function deploy example in the Commands section near top: `source .envrc && supabase functions deploy identify-plant`)
    - .planning/phases/10-perenual-security/10-CONTEXT.md (Documentation SEC-05 section: lists exact insertion points + content)
    - .planning/phases/10-perenual-security/10-01-SUMMARY.md (confirms the edge function name string is `get-plant-care` verbatim)
  </read_first>
  <action>
    Four targeted edits to `CLAUDE.md`. Use the Edit tool with surgical `old_string`/`new_string` pairs — do NOT rewrite the file wholesale.

    **Edit 1 — Edge function list (Supabase Configuration section).**

    Find the existing line (verbatim from current CLAUDE.md):

    ```
    - Edge functions in `supabase/functions/`: `identify-plant`, `diagnose-plant`, `chat-diagnosis`, `waitlist`
    ```

    Replace with:

    ```
    - Edge functions in `supabase/functions/`: `identify-plant`, `diagnose-plant`, `chat-diagnosis`, `waitlist`, `get-plant-care`
    ```

    (Single comma + space + `\`get-plant-care\`` appended at the end. Preserve the leading `- ` and the surrounding section context exactly.)

    **Edit 2 — Secrets section.**

    Find the existing line:

    ```
    - Secrets: `PLANTNET_API_KEY`, `GEMINI_API_KEY` configured in Supabase dashboard
    ```

    Replace with:

    ```
    - Secrets: `PLANTNET_API_KEY`, `GEMINI_API_KEY`, `PERENUAL_API_KEY` configured in Supabase dashboard (set via `source .envrc && supabase secrets set PERENUAL_API_KEY=<value>`)
    ```

    **Edit 3 — Commands section (deploy command example).**

    Find the existing block (in the Commands section near the top of the file):

    ```
    # Supabase Edge Functions
    source .envrc && supabase functions deploy identify-plant
    source .envrc && supabase secrets set KEY=value
    ```

    Replace with:

    ```
    # Supabase Edge Functions
    source .envrc && supabase functions deploy identify-plant
    source .envrc && supabase functions deploy get-plant-care
    source .envrc && supabase secrets set KEY=value
    ```

    (Adds the `get-plant-care` deploy line between the existing identify-plant deploy and the secrets-set example.)

    **Edit 4 — Security section (grep guard note).**

    Find the existing Security section at the bottom of the file:

    ```
    ## Security

    **Never commit** `.env` or `.envrc` (both in .gitignore). These contain Supabase keys and CLI tokens.
    ```

    Replace with:

    ```
    ## Security

    **Never commit** `.env` or `.envrc` (both in .gitignore). These contain Supabase keys and CLI tokens.

    **Pre-submit grep guard (Phase 10 SEC-01):** `EXPO_PUBLIC_PERENUAL_API_KEY` MUST NOT appear in `.env`, `.env.example`, `app.json`, or anywhere under `src/`. The Perenual API key lives ONLY in Supabase secrets (`PERENUAL_API_KEY`) and is accessed server-side via `Deno.env.get` in `supabase/functions/get-plant-care/index.ts`. Verify with: `grep -rc "EXPO_PUBLIC_PERENUAL_API_KEY" src/ .env .env.example app.json` — every line must show count 0. The pre-Phase-10 leaked key has been rotated as of v1.2.
    ```

    Do NOT touch any other section (About, Architecture, Feature Flags, Design System, i18n, App Store, Build & Submit). Do NOT alter the existing Stack list or Privacy Manifest section.
  </action>
  <verify>
    <automated>test $(grep -c "get-plant-care" CLAUDE.md) -ge 3 && test $(grep -c "PERENUAL_API_KEY" CLAUDE.md) -ge 2 && test $(grep -c "EXPO_PUBLIC_PERENUAL_API_KEY" CLAUDE.md) -ge 1 && echo "CLAUDE.md updated"</automated>
  </verify>
  <done>
    - `grep -c "get-plant-care" CLAUDE.md` ≥ 3 (edge function list + deploy command + Security section reference to the source file path)
    - `grep -c "PERENUAL_API_KEY" CLAUDE.md` ≥ 2 (Secrets section + Security grep-guard note)
    - `grep -c "EXPO_PUBLIC_PERENUAL_API_KEY" CLAUDE.md` ≥ 1 (Security grep-guard documents the forbidden client env var)
    - `grep -c "supabase functions deploy get-plant-care" CLAUDE.md` ≥ 1 (Commands section deploy example present)
    - `grep -c "supabase secrets set PERENUAL_API_KEY" CLAUDE.md` ≥ 1 (Secrets section command pattern present)
    - File is still well-formed Markdown (no broken code fences): `grep -c '^```' CLAUDE.md` is even (every code fence opens AND closes)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add v1.2 Perenual security row to PROJECT.md Key Decisions table</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (focus: the "Key Decisions" table — search for `## Key Decisions` heading; the table format follows `| Date | Decision | Rationale | ... |` columns. Use the LAST row's exact column structure as the template for the new row.)
    - .planning/phases/10-perenual-security/10-CONTEXT.md (PROJECT.md update note: "add Key Decisions row noting the v1.0→v1.2 evolution: 'Perenual key moved server-side (was leaked in client bundle since v1.0; rotated as part of v1.2)'")
    - .planning/phases/10-perenual-security/10-03-SUMMARY.md (confirms the rotation happened or was deferred — the row text should match reality)
  </read_first>
  <action>
    Append a single row to the Key Decisions table in `.planning/PROJECT.md`.

    First, READ the file and find the `## Key Decisions` table. The table will have a header row (column names) and existing rows from v1.0/v1.1 milestones. Match the EXACT column structure of the last row when adding the new one — pipe alignment, column count, etc.

    For example, if the existing table looks like:

    ```markdown
    ## Key Decisions

    | Date | Decision | Rationale |
    |------|----------|-----------|
    | 2026-03-19 | Local-first AsyncStorage (no cloud sync MVP) | Ship faster; auth deferred |
    | 2026-04-30 | climateOverride additive-optional schema field | Avoids schema bump for v1.1 |
    | ... |
    ```

    Append (after the LAST existing row, before any blank line / next section):

    ```markdown
    | 2026-05-02 | Perenual API key moved server-side via get-plant-care edge function | Was leaked in client bundle since v1.0 (EXPO_PUBLIC_PERENUAL_API_KEY); rotated as part of v1.2 Phase 10. Key now lives ONLY in Supabase secrets, accessed via Deno.env.get. |
    ```

    Adjust the column count + delimiters to MATCH the existing table's actual schema (it may have more or fewer columns than the example above — read the file first and replicate exactly).

    If the table currently has more columns (e.g., a "Phase" or "Owner" column), populate them appropriately:
    - Phase column → `Phase 10`
    - Owner column → leave blank or `Solo dev` (whatever convention exists)

    Do NOT modify any existing row. Do NOT alter the table header or section heading. Do NOT touch any other section of PROJECT.md.

    Special-case: IF the Key Decisions table does NOT exist yet in PROJECT.md (unlikely — it was referenced in the Phase 5 SUMMARY tracking), CREATE the section using this scaffold AT THE END of the file:

    ```markdown

    ## Key Decisions

    | Date | Decision | Rationale |
    |------|----------|-----------|
    | 2026-05-02 | Perenual API key moved server-side via get-plant-care edge function | Was leaked in client bundle since v1.0 (EXPO_PUBLIC_PERENUAL_API_KEY); rotated as part of v1.2 Phase 10. Key now lives ONLY in Supabase secrets, accessed via Deno.env.get. |
    ```

    But strongly prefer the append-row path — verify the table exists FIRST.
  </action>
  <verify>
    <automated>test $(grep -c "Perenual" .planning/PROJECT.md) -ge 1 && test $(grep -c "get-plant-care" .planning/PROJECT.md) -ge 1 && test $(grep -c "Key Decisions" .planning/PROJECT.md) -ge 1 && echo "PROJECT.md updated"</automated>
  </verify>
  <done>
    - `grep -c "Perenual" .planning/PROJECT.md` ≥ 1 (new Key Decisions row added)
    - `grep -c "get-plant-care" .planning/PROJECT.md` ≥ 1 (edge function name documented)
    - `grep -c "Key Decisions" .planning/PROJECT.md` ≥ 1 (section exists)
    - The new row's date column shows `2026-05-02` (today's date per system-reminder)
    - The new row's rationale references both the leak AND the rotation (not just the structural move)
    - No existing Key Decisions row was modified or deleted (defensive — verify by counting rows before + after; +1 expected)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
grep -c "get-plant-care" CLAUDE.md \
  && grep -c "PERENUAL_API_KEY" CLAUDE.md \
  && grep -c "EXPO_PUBLIC_PERENUAL_API_KEY" CLAUDE.md \
  && grep -c "Perenual" .planning/PROJECT.md \
  && grep -c "get-plant-care" .planning/PROJECT.md
```

Expected:
- CLAUDE.md `get-plant-care` count: ≥ 3
- CLAUDE.md `PERENUAL_API_KEY` count: ≥ 2
- CLAUDE.md `EXPO_PUBLIC_PERENUAL_API_KEY` count: ≥ 1 (in the Security section grep-guard note)
- PROJECT.md `Perenual` count: ≥ 1
- PROJECT.md `get-plant-care` count: ≥ 1

Cross-phase reverification (the goal-backward must-haves for the entire phase):

```bash
# Source-level — Plan 10-02 deliverable
grep -rc "EXPO_PUBLIC_PERENUAL_API_KEY" src/

# Edge function source — Plan 10-01 deliverable
test -f supabase/functions/get-plant-care/index.ts && grep -c "Deno.env.get('PERENUAL_API_KEY')" supabase/functions/get-plant-care/index.ts

# Client invoke — Plan 10-02 deliverable
grep -c "supabase.functions.invoke('get-plant-care'" src/services/plantKnowledgeService.ts

# Env files — Plan 10-02 deliverable
grep -c "EXPO_PUBLIC_PERENUAL_API_KEY" .env .env.example

# TSC project-wide
npx tsc --noEmit
```

Expected: src grep 0, edge function file present + Deno.env.get count ≥ 1, invoke count === 1, env grep 0+0, tsc exit 0.
</verification>

<success_criteria>
1. CLAUDE.md edge function list includes `get-plant-care`.
2. CLAUDE.md Commands section has a `supabase functions deploy get-plant-care` example.
3. CLAUDE.md Secrets section documents `PERENUAL_API_KEY`.
4. CLAUDE.md Security section has the pre-submit grep guard for `EXPO_PUBLIC_PERENUAL_API_KEY`.
5. PROJECT.md Key Decisions table has a new row dated 2026-05-02 documenting the Perenual key migration.
6. No other section of CLAUDE.md or PROJECT.md was modified.
7. `npx tsc --noEmit` still exits 0 (defensive — docs changes shouldn't break tsc but verify regardless).
8. All 5 SEC-* requirements (SEC-01..05) are now closed (assuming Plan 10-03 Task 2 was `proceed`; if `abort`, SEC-04 is deferred and that fact is documented in 10-03-SUMMARY.md).
</success_criteria>

<output>
After completion, create `.planning/phases/10-perenual-security/10-04-SUMMARY.md` documenting:
- CLAUDE.md edits made (cite the 4 sections + paste the new lines)
- PROJECT.md Key Decisions row added (paste verbatim)
- Final phase status: which SEC-* requirements closed, any deferrals
- Cross-phase reverification grep results pasted

Then create `.planning/phases/10-perenual-security/10-SUMMARY.md` (phase-level summary aggregating all 4 plans):
- Phase 10 outcome: Perenual API key successfully migrated server-side, rotated, documented
- All 4 plans + their commit hashes (or `pending` if not yet committed)
- All 4 ROADMAP success criteria status (#1 grep, #2 live identify, #3 logs no errors, #4 key rotated)
- Roll-up of any deviations
</output>
