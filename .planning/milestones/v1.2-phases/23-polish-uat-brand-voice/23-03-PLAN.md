---
phase: 23-polish-uat-brand-voice
plan: 03
type: execute
wave: 3
depends_on:
  - "23-polish-uat-brand-voice/00"
  - "23-polish-uat-brand-voice/02"
files_modified:
  - assets/illustrations/empty-plants.png
  - assets/illustrations/empty-calendar.png
  - assets/illustrations/empty-explore.png
  - src/screens/PlantsScreen.tsx
  - src/screens/CalendarScreen.tsx
  - src/screens/ExploreScreen.tsx
autonomous: true
requirements:
  - POLISH-07
  - POLISH-08
must_haves:
  truths:
    - "3 PNG illustration files exist under assets/illustrations/ (empty-plants.png, empty-calendar.png, empty-explore.png) — each ≥ 5 KB, valid PNG"
    - "PlantsScreen.tsx EmptyState component uses Image require('../../assets/illustrations/empty-plants.png') and renders t('emptyState.plants.title') + t('emptyState.plants.cta')"
    - "ExploreScreen.tsx EmptyState component uses Image require('../../assets/illustrations/empty-explore.png') and renders t('emptyState.explore.title') + t('emptyState.explore.cta')"
    - "CalendarScreen.tsx gains a NEW EmptyState component (none currently per RESEARCH §Finding 5 / §Pitfall 7) rendered when plants.length === 0 BELOW the MonthCalendar; uses Image require('../../assets/illustrations/empty-calendar.png') and renders t('emptyState.calendar.title') + t('emptyState.calendar.cta')"
    - "All 3 EmptyState JSX use existing theme tokens (colors.green, colors.bgPrimary, colors.textPrimary, colors.textSecondary post-darkening) — NO new theme tokens introduced"
    - "POLISH-08 negative-grep remains PASS — no samplePlants/mockPlants/seedPlants/demoPlants/firstLaunchPlants arrays introduced in src/"
    - "smoke-phase23.cjs POLISH-07.illustrated-empty-states placeholder flips SKIP → PASS"
  artifacts:
    - path: "assets/illustrations/empty-plants.png"
      provides: "Planta-en-maceta illustration for PlantsScreen empty state (POLISH-07)"
      min_bytes: 5000
    - path: "assets/illustrations/empty-calendar.png"
      provides: "Calendario-sin-tasks illustration for CalendarScreen empty state (POLISH-07)"
      min_bytes: 5000
    - path: "assets/illustrations/empty-explore.png"
      provides: "Lupa/búsqueda illustration for ExploreScreen empty state (POLISH-07)"
      min_bytes: 5000
    - path: "src/screens/PlantsScreen.tsx"
      provides: "EmptyState swap — emoji icon replaced with <Image> illustration; i18n keys swap to emptyState.plants.*"
      contains: "empty-plants.png"
    - path: "src/screens/CalendarScreen.tsx"
      provides: "NEW EmptyState component (none before per Pitfall 7) — rendered when plants.length === 0"
      contains: "empty-calendar.png"
    - path: "src/screens/ExploreScreen.tsx"
      provides: "EmptyState swap — emoji icon replaced with <Image> illustration; i18n keys swap to emptyState.explore.*"
      contains: "empty-explore.png"
  key_links:
    - from: "src/screens/PlantsScreen.tsx EmptyState"
      to: "assets/illustrations/empty-plants.png + emptyState.plants.* i18n keys (landed in Plan 23-00)"
      via: "Image require + t() consumers"
      pattern: "empty-plants\\.png"
    - from: "src/screens/CalendarScreen.tsx (NEW empty branch)"
      to: "assets/illustrations/empty-calendar.png + emptyState.calendar.* i18n keys"
      via: "Conditional render when plants.length === 0 BELOW MonthCalendar"
      pattern: "empty-calendar\\.png"
    - from: "src/screens/ExploreScreen.tsx EmptyState"
      to: "assets/illustrations/empty-explore.png + emptyState.explore.* i18n keys"
      via: "Image require + t() consumers"
      pattern: "empty-explore\\.png"
---

<objective>
Wave 3 (empty states) — close POLISH-07 (3 illustrated empty states) AND verify POLISH-08 (no sample-plant arrays in src/) as a STRICT negative-grep gate already PASS at baseline.

POLISH-07 (Task 1 — assets): Generate 3 PNG illustrations under assets/illustrations/ for the three empty states. CONTEXT.md locked motifs: planta-en-maceta-vacía (PlantsScreen), calendario-sin-tasks (CalendarScreen), lupa-sobre-hoja (ExploreScreen). PNG path chosen per RESEARCH §Finding 10 (react-native-svg NOT in package.json — PNG renders via RN built-in <Image> with zero new deps). Each PNG ≥ 5 KB minimum (rough quality floor). RESEARCH §Pattern 4 (PNG @1x/@2x/@3x render path; single @3x source acceptable for simple illustrations).

POLISH-07 (Task 2 — screen JSX): Insert/swap EmptyState components in PlantsScreen.tsx (swap from emoji 🌿 to Image), ExploreScreen.tsx (swap from emoji to Image), and CalendarScreen.tsx (ADD new EmptyState — Pitfall 7 — none exists currently). All 3 EmptyState components consume the emptyState.{plants,calendar,explore}.{title,cta} i18n keys landed in Plan 23-00 + finalized voseo copy from Plan 23-00 ES + EN. No new theme tokens.

POLISH-08 (verification-only, STRICT sentinel): Already PASS at Wave 0 baseline per RESEARCH §Finding 12 — this plan just confirms it stays PASS post-JSX edits (no introduction of `samplePlants` / `mockPlants` / `seedPlants` / `demoPlants` / `firstLaunchPlants` arrays). Plan does NOT add a separate task for POLISH-08; the smoke-phase23 STRICT negative-grep ALREADY enforces it on every run.

Atomic commits: 1 for assets (3 PNG files in single commit), 1 for screen JSX (3 screen edits). NO commit specifically for POLISH-08 (verification-only, already PASS).

Purpose: Replace blank/emoji empty states with branded illustrations + motivating voseo CTAs; lock POLISH-08 as a permanent CI guard against future "test event syndrome" regression.
Output: 6 file changes (3 new PNGs + 3 screen JSX edits) in 2 atomic commits; smoke-phase23 SKIP count goes from 1 to 0 (all POLISH-01..08 PASS).
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-polish-uat-brand-voice/23-CONTEXT.md
@.planning/phases/23-polish-uat-brand-voice/23-RESEARCH.md
@.planning/phases/23-polish-uat-brand-voice/23-VALIDATION.md
@.planning/phases/23-polish-uat-brand-voice/23-00-SUMMARY.md
@.planning/phases/23-polish-uat-brand-voice/23-02-SUMMARY.md

# Reference implementations
@src/screens/PlantsScreen.tsx
@src/screens/CalendarScreen.tsx
@src/screens/ExploreScreen.tsx
@src/i18n/locales/es/common.json
@src/i18n/locales/en/common.json
@src/theme.ts
@scripts/smoke-phase23.cjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: POLISH-07 (assets) — generate 3 PNG illustrations under assets/illustrations/</name>
  <files>assets/illustrations/empty-plants.png, assets/illustrations/empty-calendar.png, assets/illustrations/empty-explore.png</files>
  <read_first>
    - .planning/phases/23-polish-uat-brand-voice/23-RESEARCH.md §Pattern 4 (PNG asset render path lines 319-360) + §Finding 10 (no react-native-svg dep — PNG only path at lines 876-886) + §Pitfall 6 (NO SVG attempted at runtime — RN cannot render Svg JSX without react-native-svg installed at lines 477-485)
    - .planning/phases/23-polish-uat-brand-voice/23-CONTEXT.md "Empty-state illustrations" decision block (lines 18-31) — locks per-screen motifs + theme tokens only
    - src/theme.ts (post-Plan 23-02 — confirm colors.green #5b9a6a + colors.bgPrimary #f5f0e6 + colors.textPrimary #2d3a2e + colors.textSecondary #6f6450 — these 4 tokens are the SOLE palette for illustration fills)
    - .gitignore (confirm scripts/.tmp-phase23/ is ignored — landed in Plan 23-00 Task 2)
  </read_first>
  <acceptance_criteria>
    - `ls assets/illustrations/empty-plants.png` returns the file path
    - `ls assets/illustrations/empty-calendar.png` returns the file path
    - `ls assets/illustrations/empty-explore.png` returns the file path
    - Each PNG size ≥ 5000 bytes — verify with `du -b assets/illustrations/empty-plants.png | awk '{print $1}'` returns ≥ 5000, same for the other two
    - Each PNG has valid header — verify with `file assets/illustrations/empty-plants.png | grep -c "PNG image data"` returns 1, same for the other two
    - `assets/illustrations/` directory exists and is git-tracked via the 3 PNG files (no .gitkeep needed)
    - No SVG source files committed (sources, if any, live under scripts/.tmp-phase23/ which is gitignored)
    - `npx tsc --noEmit` exits 0 (no JSX touched yet — sanity check)
    - smoke-phase23.cjs still shows POLISH-07.illustrated-empty-states as SKIP (Task 2 JSX wiring still pending)
  </acceptance_criteria>
  <action>
    Three approaches in priority order — pick whichever runs cleanly in the current env. ALL produce 3 valid PNG files at `assets/illustrations/empty-{plants,calendar,explore}.png`.

    APPROACH A — Claude SVG-to-PNG (PREFERRED):

      1. Ensure `assets/illustrations/` directory exists: `mkdir -p assets/illustrations`
      2. Write 3 SVG sketches at scripts/.tmp-phase23/ (gitignored):
         - scripts/.tmp-phase23/empty-plants.svg: pot silhouette (colors.green stroke) + small sprout/seedling growing out, soft shadow. ~30-40 LOC. 600×600 viewBox.
         - scripts/.tmp-phase23/empty-calendar.svg: 4×4 grid calendar with 3 cells having green checkmarks (signaling "all done"). ~30 LOC. 600×600 viewBox.
         - scripts/.tmp-phase23/empty-explore.svg: magnifying glass at 45° angle hovering over a stylized leaf. ~30 LOC. 600×600 viewBox.
         All fills/strokes use ONLY: colors.green #5b9a6a, colors.bgPrimary #f5f0e6, colors.textPrimary #2d3a2e, colors.textSecondary #6f6450.
      3. Convert each SVG to PNG at 600×600 via one of:
         - `npm i -D sharp` then small node script: `require('sharp')('scripts/.tmp-phase23/empty-plants.svg').resize(600, 600).png().toFile('assets/illustrations/empty-plants.png')` (repeat for 3 motifs).
         - System `convert` (ImageMagick): `convert scripts/.tmp-phase23/empty-plants.svg -resize 600x600 assets/illustrations/empty-plants.png`.
      4. Verify each output PNG: `file <path>` reports "PNG image data" + `du -b <path>` ≥ 5000.

    APPROACH B — emoji-as-PNG fallback (if APPROACH A's sharp/convert unavailable):

      1. Ensure `assets/illustrations/` exists.
      2. Write scripts/.tmp-phase23/gen-emoji-pngs.cjs that uses `@napi-rs/canvas` (or `canvas` if already installed) to render emojis on a 600×600 canvas:
         - empty-plants.png: 🪴 at 240pt centered on #f5f0e6 bg
         - empty-calendar.png: 📅 at 240pt centered on #f5f0e6 bg
         - empty-explore.png: 🔍 at 240pt centered on #f5f0e6 bg
      3. If neither @napi-rs/canvas nor canvas is installable, switch to APPROACH C.

    APPROACH C — minimal data-URI PNG (last resort if A and B both unavailable):

      1. Write 3 hand-crafted minimal PNG files (≥ 5KB each) via node's built-in zlib + buffer — emit a single-color or 4-color gradient PNG that meets the size/header gates. Acceptable as v1.2 placeholder; v1.3 may upgrade to hand-drawn.
      2. Document the fallback in 23-03-SUMMARY.md and propose a v1.3 hand-illustrate ticket.

    Commit: `feat(23-03): add 3 PNG illustrations for empty states (POLISH-07 assets)`

    DO NOT commit anything under scripts/.tmp-phase23/ (the directory is gitignored from Plan 23-00 Task 2).
  </action>
  <verify>
    <automated>ls -la assets/illustrations/empty-plants.png assets/illustrations/empty-calendar.png assets/illustrations/empty-explore.png && file assets/illustrations/empty-plants.png && file assets/illustrations/empty-calendar.png && file assets/illustrations/empty-explore.png && du -b assets/illustrations/empty-plants.png assets/illustrations/empty-calendar.png assets/illustrations/empty-explore.png</automated>
    Expected: 3 files listed; each `file` reports "PNG image data"; each `du -b` shows ≥ 5000 bytes. smoke-phase23 POLISH-07 still SKIP (Task 2 pending).
  </verify>
  <done>
    - 3 PNG files at assets/illustrations/ — each ≥ 5 KB, valid PNG header
    - No SVG source files committed (scripts/.tmp-phase23/ gitignored)
    - 1 atomic commit for assets
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: POLISH-07 (screen JSX) — swap PlantsScreen + ExploreScreen EmptyState to Image illustrations; ADD CalendarScreen EmptyState branch</name>
  <files>src/screens/PlantsScreen.tsx, src/screens/CalendarScreen.tsx, src/screens/ExploreScreen.tsx</files>
  <read_first>
    - src/screens/PlantsScreen.tsx (full file — locate EmptyState component at line ~365-373 with emoji 🌿 + current t('plants.emptyTitle') / t('plants.emptyText') consumers; locate FlatList ListEmptyComponent prop at line ~383)
    - src/screens/ExploreScreen.tsx (full file — locate EmptyState component at line ~164-172 with current t('explore.noResults') / t('explore.noResultsText') consumers; locate FlatList ListEmptyComponent at line ~184)
    - src/screens/CalendarScreen.tsx (full file — confirm NO EmptyState exists currently per RESEARCH §Pitfall 7; locate MonthCalendar render site + plants array access to decide insertion point)
    - .planning/phases/23-polish-uat-brand-voice/23-RESEARCH.md §Finding 5 (per-screen existing patterns at lines 783-790) + §Pattern 4 (PNG asset render path at lines 319-360) + §Example 6 (PlantsScreen EmptyState diff at lines 657-680) + §Pitfall 6 (NO SVG — PNG only) + §Pitfall 7 (CalendarScreen has NONE — must ADD; render BELOW MonthCalendar grid when plants.length === 0)
    - src/i18n/locales/{en,es}/common.json (confirm emptyState.{plants,calendar,explore}.{title,cta} namespace landed in Plan 23-00)
    - src/theme.ts (use colors.green, colors.bgPrimary, colors.textPrimary, colors.textSecondary (post-#6f6450 darken); spacing.lg for marginBottom)
  </read_first>
  <acceptance_criteria>
    - `grep -F "require('../../assets/illustrations/empty-plants.png')" src/screens/PlantsScreen.tsx` returns 1 (or relative path equivalent — `..` segment count matches PlantsScreen.tsx depth)
    - `grep -F "require('../../assets/illustrations/empty-calendar.png')" src/screens/CalendarScreen.tsx` returns 1
    - `grep -F "require('../../assets/illustrations/empty-explore.png')" src/screens/ExploreScreen.tsx` returns 1
    - PlantsScreen EmptyState references `t('emptyState.plants.title')` AND `t('emptyState.plants.cta')` — verify with: `grep -cE "emptyState\\.plants\\.(title|cta)" src/screens/PlantsScreen.tsx` returns ≥ 2
    - CalendarScreen EmptyState references `t('emptyState.calendar.title')` AND `t('emptyState.calendar.cta')` — `grep -cE "emptyState\\.calendar\\.(title|cta)" src/screens/CalendarScreen.tsx` returns ≥ 2
    - ExploreScreen EmptyState references `t('emptyState.explore.title')` AND `t('emptyState.explore.cta')` — `grep -cE "emptyState\\.explore\\.(title|cta)" src/screens/ExploreScreen.tsx` returns ≥ 2
    - CalendarScreen now has an EmptyState JSX block (NEW per Pitfall 7) — verify with: `grep -E "plants\\.length\\s*===\\s*0" src/screens/CalendarScreen.tsx` returns ≥ 1 (the empty conditional gate)
    - No new theme tokens introduced — `grep -cE "newToken|illustrationColor" src/theme.ts` returns 0; existing tokens unchanged
    - POLISH-08 STRICT negative-grep STILL PASS (no `samplePlants`/`mockPlants`/`seedPlants`/`demoPlants`/`firstLaunchPlants` arrays introduced) — verified by smoke-phase23 STRICT block
    - `npx tsc --noEmit` exits 0
    - `npm run check:i18n-keys` still passes 118 catalog ids
    - `node scripts/smoke-phase23.cjs` shows POLISH-07.illustrated-empty-states PASS (was SKIP) AND POLISH-08 still PASS
    - All Phase 18-22 cross-phase STRICT sentinels still PASS (no regression)
  </acceptance_criteria>
  <action>
    Three screen edits:

    EDIT A — src/screens/PlantsScreen.tsx

    1. Ensure `Image` is imported from `react-native` (likely already present; add to import line if missing).

    2. Replace the existing EmptyState component (at ~line 365-373):

    Before (approximate shape):
      const EmptyState = () => (
        <View style={styles.emptyState}>
          <Text style={styles.emptyIcon}>🌿</Text>
          <Text style={styles.emptyTitle}>{t('plants.emptyTitle')}</Text>
          <Text style={styles.emptyText}>{t('plants.emptyText')}</Text>
        </View>
      );

    After:
      const EmptyState = () => (
        <View style={styles.emptyState}>
          <Image
            source={require('../../assets/illustrations/empty-plants.png')}
            style={styles.emptyIllustration}
            resizeMode="contain"
            accessibilityIgnoresInvertColors
          />
          <Text style={styles.emptyTitle}>{t('emptyState.plants.title')}</Text>
          <Text style={styles.emptyText}>{t('emptyState.plants.cta')}</Text>
        </View>
      );

    3. Add the `emptyIllustration` style entry to the StyleSheet.create block:
       `emptyIllustration: { width: 180, height: 180, marginBottom: spacing.lg, alignSelf: 'center' },`
       (Remove the existing `emptyIcon` style entry if no longer referenced.)

    4. Confirm the relative path `../../assets/illustrations/empty-plants.png` is correct given PlantsScreen is at `src/screens/PlantsScreen.tsx` (two-level up to project root). Test with `require.resolve` if uncertain.

    EDIT B — src/screens/ExploreScreen.tsx

    1. Same pattern as PlantsScreen. Locate EmptyState at ~line 164-172.

    2. Replace the body with the Image-based version using `require('../../assets/illustrations/empty-explore.png')` + `t('emptyState.explore.title')` + `t('emptyState.explore.cta')`.

    3. Add `emptyIllustration` style entry (same shape as PlantsScreen).

    EDIT C — src/screens/CalendarScreen.tsx (ADD NEW — currently has NO EmptyState per Pitfall 7)

    1. Inside the main JSX return, AFTER the MonthCalendar render, add a conditional render:
       {plants.length === 0 && (
         <View style={styles.emptyState}>
           <Image
             source={require('../../assets/illustrations/empty-calendar.png')}
             style={styles.emptyIllustration}
             resizeMode="contain"
             accessibilityIgnoresInvertColors
           />
           <Text style={styles.emptyTitle}>{t('emptyState.calendar.title')}</Text>
           <Text style={styles.emptyText}>{t('emptyState.calendar.cta')}</Text>
         </View>
       )}

    2. Add styles to the existing StyleSheet.create block (or create if absent):
       emptyState: { alignItems: 'center', paddingHorizontal: spacing.lg, paddingTop: spacing.xl },
       emptyIllustration: { width: 180, height: 180, marginBottom: spacing.lg, alignSelf: 'center' },
       emptyTitle: { fontSize: 18, fontFamily: 'PlayfairDisplay_700Bold', color: colors.textPrimary, textAlign: 'center', marginBottom: spacing.sm },
       emptyText: { fontSize: 14, fontFamily: 'DMSans_400Regular', color: colors.textSecondary, textAlign: 'center' },

    3. Ensure `Image`, `View`, `Text` are imported from `react-native`; ensure `useTranslation` is hooked up (likely already present — CalendarScreen has i18n). Ensure `plants` is accessible (likely already via `useStorage()` — confirm by reading the existing CalendarScreen pattern).

    4. Render position: BELOW the MonthCalendar grid (Pitfall 7 lock — do NOT replace the grid; show the illustration as an addendum when zero plants exist). Per-day "no tasks scheduled" empty state inside DayDetailModal is OUT OF SCOPE and unchanged.

    POLISH-08 GUARD (passive): Do NOT introduce any `samplePlants`, `mockPlants`, `seedPlants`, `demoPlants`, or `firstLaunchPlants` array in ANY of the 3 screens. The smoke-phase23 STRICT negative-grep will catch any accidental introduction at commit time.

    Optional (CLEANUP): Old i18n keys `plants.emptyTitle`, `plants.emptyText`, `explore.noResults`, `explore.noResultsText` may now be orphan (no consumer). LEAVE THEM IN PLACE for v1.2 (RESEARCH §"Deprecated/outdated" notes this is optional). Phase 24 DOCS may add a cleanup pass.

    Commit message: `feat(23-03): wire 3 illustrated empty states in PlantsScreen + CalendarScreen + ExploreScreen (POLISH-07)`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run check:i18n-keys && node scripts/smoke-phase23.cjs</automated>
    Expected: tsc clean. check:i18n-keys passes 118 catalog ids. smoke-phase23 POLISH-07.illustrated-empty-states flips SKIP → PASS. POLISH-08 STRICT still PASS. All Phase 18-22 cross-phase sentinels still PASS. SKIP count drops from 1 to 0 — all POLISH-01..08 PASS (POLISH-04 manual-only, not in smoke sentinels).
  </verify>
  <done>
    - 3 screens render Image illustrations from assets/illustrations/empty-*.png
    - PlantsScreen + ExploreScreen swap from emoji to Image; CalendarScreen ADDS new EmptyState block (Pitfall 7)
    - All 3 use emptyState.{screen}.{title,cta} i18n keys (landed Plan 23-00)
    - No new theme tokens
    - POLISH-08 STRICT negative-grep STILL PASS (no source-array regression introduced)
    - tsc + check:i18n-keys + smoke-phase23 all green
    - smoke-phase23 SKIP count = 0 (all POLISH-01..08 PASS, except POLISH-04 manual-only)
    - Atomic commit landed for POLISH-07 JSX
  </done>
</task>

</tasks>

<verification>
- `node scripts/smoke-phase23.cjs` → exit 0; POLISH-07 PASS; POLISH-08 STRICT still PASS; all Phase 18-22 cross-phase sentinels PASS
- `npx tsc --noEmit` → exit 0
- `npm run lint:voseo` → exit 0 (STRICT — preserved from Plan 23-02)
- `npm run check:i18n-keys` → PASS 118 catalog ids
- 2 atomic commits: feat(23-03) POLISH-07 assets, feat(23-03) POLISH-07 JSX
- POLISH-08 verification-only — already PASS at baseline; preserved through this plan (no source arrays introduced)
</verification>

<success_criteria>
- 3 illustrated empty states render with PNG illustrations + CONTEXT.md-locked voseo copy on PlantsScreen / CalendarScreen / ExploreScreen
- CalendarScreen gains a new EmptyState branch (was missing per Pitfall 7) — rendered below MonthCalendar when plants.length === 0
- Existing theme tokens reused; no design-system token sprawl
- POLISH-08 negative-grep PASS preserved (zero sample-plant arrays introduced)
- smoke-phase23 reaches 0 SKIP (all automated POLISH sentinels PASS; POLISH-04 manual-only deferred to Plan 23-04)
- 2 atomic commits per RESEARCH §"Established Patterns"
</success_criteria>

<output>
After completion, create `.planning/phases/23-polish-uat-brand-voice/23-03-SUMMARY.md` per template.
</output>
