---
phase: 23-polish-uat-brand-voice
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/smoke-phase23.cjs
  - scripts/voseo-lint.mjs
  - package.json
  - .gitignore
  - src/i18n/locales/en/common.json
  - src/i18n/locales/es/common.json
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
  - POLISH-04
  - POLISH-05
  - POLISH-06
  - POLISH-07
  - POLISH-08
must_haves:
  truths:
    - "scripts/smoke-phase23.cjs exists and runs (exit code 0 with non-zero PASS at baseline)"
    - "scripts/voseo-lint.mjs exists as skeleton (BANNED list + walkValues + exit codes); STRICT body lands in Plan 23-02"
    - "npm run smoke:phase23 + npm run lint:voseo wired in package.json"
    - "emptyState.{plants,calendar,explore}.{title,cta} namespace exists in EN + ES (6 keys per locale, placeholder values acceptable)"
    - "POLISH-01..07 placeholders SKIP at baseline and flip to PASS as Plans 23-01/02/03 land code"
    - "POLISH-08 STRICT negative-grep is PASS at baseline (no sample/seed/demo/mock/firstLaunch plant arrays in src/)"
    - "Cross-phase STRICT sentinels for Phase 18 + 19 + 20 + 21 + 22 are preserved verbatim (regression gate)"
  artifacts:
    - path: "scripts/smoke-phase23.cjs"
      provides: "Three-tier smoke runner (PASS scaffold + SKIP placeholders for POLISH-01/02/03/05/06/07 + STRICT POLISH-08 negative-grep + STRICT cross-phase Phase 18-22 regression + STRICT voseo-lint subprocess)"
      min_lines: 280
    - path: "scripts/voseo-lint.mjs"
      provides: "Voseo lint skeleton — BANNED list (empty placeholder), walkValues, exit codes; STRICT body deferred to Plan 23-02"
      min_lines: 60
    - path: "package.json"
      provides: "smoke:phase23 + lint:voseo npm scripts"
      contains: 'smoke:phase23'
    - path: ".gitignore"
      provides: "scripts/.tmp-phase23/ ignore line (parity with Phase 19/20/21/22)"
      contains: "scripts/.tmp-phase23/"
    - path: "src/i18n/locales/en/common.json"
      provides: "emptyState namespace with 6 EN keys (plants/calendar/explore × title/cta)"
      contains: 'emptyState'
    - path: "src/i18n/locales/es/common.json"
      provides: "emptyState namespace with 6 ES keys (voseo placeholder values)"
      contains: 'emptyState'
  key_links:
    - from: "scripts/smoke-phase23.cjs"
      to: "scripts/voseo-lint.mjs"
      via: "child_process spawnSync STRICT subprocess invocation"
      pattern: "voseo-lint\\.mjs"
    - from: "scripts/smoke-phase23.cjs"
      to: "src/utils/plantLogic.ts, src/data/plantDatabase.ts, src/components/PlantIdentifier/IdentificationResults.tsx, src/theme.ts"
      via: "readSafe + regex sentinels (SKIP until later plans land code)"
      pattern: "OUTDOOR_TYPE_IDS|resolveTypeIdForPicker|textSecondary"
    - from: "scripts/smoke-phase23.cjs"
      to: "src/ tree"
      via: "STRICT negative-grep on samplePlants|mockPlants|seedPlants|demoPlants|firstLaunchPlants ARRAYS"
      pattern: "\\b(samplePlants|mockPlants|seedPlants|demoPlants|firstLaunchPlants)\\b"
    - from: "scripts/smoke-phase23.cjs"
      to: "src/ tree (Phase 18-22 invariants)"
      via: "STRICT cross-phase regression — forks Phase 22 cross-phase block VERBATIM"
      pattern: "Phase18|Phase19|Phase20|Phase21|Phase22"
---

<objective>
Wave 0 Nyquist scaffold for Phase 23 (Polish — UAT Fixes + Brand Voice).

Lands `scripts/smoke-phase23.cjs` (three-tier runner forked from `scripts/smoke-phase22.cjs`), the voseo-lint skeleton (BANNED list empty + walkValues + exit codes; STRICT body deferred to Plan 23-02), two new npm scripts (`smoke:phase23` + `lint:voseo`), the `.gitignore` entry for `scripts/.tmp-phase23/`, and the `emptyState.{plants,calendar,explore}.{title,cta}` namespace skeleton in EN + ES common.json (6 keys per locale, CONTEXT.md-locked voseo values — final JSX wiring lands in Plan 23-03).

The smoke runner ships three tiers:
1. STRICT W0 scaffold PASS — files exist + npm scripts wired + i18n keys present
2. SKIP-to-PASS placeholders for POLISH-01 (OUTDOOR_TYPE_IDS Set), POLISH-02 (45 outdoor:false entries), POLISH-03 (resolveTypeIdForPicker), POLISH-05 (textSecondary != #8a7e6b + WCAG 4.5:1 dual-bg), POLISH-06 (4 action button voseo+emoji literals + voseo-lint exit 0), POLISH-07 (3 PNG illustrations + 3 EmptyState JSX inserts + 6+6 i18n keys finalized)
3. STRICT POLISH-08 negative-grep + STRICT cross-phase regression preserving Phase 18 + 19 + 20 + 21 + 22 invariants verbatim (forked from smoke-phase22.cjs cross-phase block)

POLISH-08 STRICT negative-grep MUST PASS at Wave 0 baseline (RESEARCH §Finding 12: zero violations expected). POLISH-04 is manual-only (no automated sentinel; verified in Plan 23-04 device gate).

Runner is NEVER modified after Wave 0 lands (Phase 14-00 / 19-00 / 20-00 / 21-00 / 22-00 precedent). Later plans flip SKIPs to PASSes by landing concrete code/JSX/data.

Purpose: Lock the validation contract before any source change lands. Smoke runner is the deterministic gate per VALIDATION.md.
Output: 6 file changes (2 new scripts, 1 npm-script edit, 1 .gitignore edit, 2 i18n JSON edits).
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-polish-uat-brand-voice/23-CONTEXT.md
@.planning/phases/23-polish-uat-brand-voice/23-RESEARCH.md
@.planning/phases/23-polish-uat-brand-voice/23-VALIDATION.md

# Reference implementations
@scripts/smoke-phase22.cjs
@src/i18n/locales/en/common.json
@src/i18n/locales/es/common.json
@.gitignore
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/smoke-phase23.cjs forked from smoke-phase22.cjs</name>
  <files>scripts/smoke-phase23.cjs</files>
  <read_first>
    - scripts/smoke-phase22.cjs (full file — fork target; preserves CommonJS harness, readSafe/assert/assertSkippable/getNested helpers, and three-tier pattern with Phase 18+19+20+21 cross-phase block at the tail)
    - .planning/phases/23-polish-uat-brand-voice/23-RESEARCH.md §Pattern 5 (three-tier smoke runner structure lines 361-396) + §Example 7 (POLISH-08 negative-grep at lines 704-714) + §Finding 6 (WCAG contrast table lines 794-810)
    - .planning/phases/23-polish-uat-brand-voice/23-VALIDATION.md Per-Task Verification Map (W0/W1/W2/W3 sentinels lines 38-50)
    - src/utils/plantLogic.ts lines 1-100 (POLISH-01 emit site at line 81-83; OUTDOOR_TYPE_IDS sentinel target — NOT modified by this task)
    - src/data/plantDatabase.ts (POLISH-02 sentinel: outdoor:true vs outdoor:false counts per category)
    - src/components/PlantIdentifier/IdentificationResults.tsx (POLISH-03 sentinel target — current indoor-flag derivation at lines 53, 100)
    - src/theme.ts lines 1-14 (POLISH-05 sentinel: textSecondary current value #8a7e6b at line 11 + TODO comment at line 13)
  </read_first>
  <acceptance_criteria>
    - File scripts/smoke-phase23.cjs exists
    - `grep -c "POLISH-01.outdoor-task-gate" scripts/smoke-phase23.cjs` returns ≥ 1
    - `grep -c "POLISH-08.negative-grep" scripts/smoke-phase23.cjs` returns ≥ 1
    - `grep -c "wcagAA\|relativeLuminance" scripts/smoke-phase23.cjs` returns ≥ 1 (WCAG helper present)
    - `grep -c "voseo-lint.mjs" scripts/smoke-phase23.cjs` returns ≥ 1 (subprocess invocation)
    - `grep -cE "PHASE 18|PHASE 19|PHASE 20|PHASE 21|PHASE 22" scripts/smoke-phase23.cjs` returns ≥ 5 (cross-phase block preserved)
    - `node scripts/smoke-phase23.cjs; echo $?` returns 0 (exit code zero at baseline)
    - Runner output shows PASS count > 0 and FAIL count = 0 at baseline; SKIP count ≥ 6 (POLISH-01/02/03/05/06-button/06-lint/07 placeholders)
    - `wc -l scripts/smoke-phase23.cjs` returns ≥ 280
  </acceptance_criteria>
  <action>
    Fork `scripts/smoke-phase22.cjs` verbatim. Apply these edits:

    A. Header rename: smoke-phase22 → smoke-phase23; objective comment swapped to describe Phase 23 POLISH-01..08 scope.

    B. TIER 1 — STRICT W0 scaffold PASS (5+ assertions):
       - assert(fs.existsSync(path.resolve(ROOT, 'scripts/smoke-phase23.cjs')), 'W0.scaffold.smoke-runner-exists')
       - assert(fs.existsSync(path.resolve(ROOT, 'scripts/voseo-lint.mjs')), 'W0.scaffold.voseo-lint-exists')
       - assert(pkgJson.scripts && pkgJson.scripts['smoke:phase23'], 'W0.scaffold.npm-script-smoke')
       - assert(pkgJson.scripts && pkgJson.scripts['lint:voseo'], 'W0.scaffold.npm-script-voseo')
       - assert(/scripts\/\.tmp-phase23\//.test(gitignoreSrc), 'W0.scaffold.gitignore-tmp')
       - i18n parity: assert getNested(enJson, 'emptyState.plants.title') && getNested(enJson, 'emptyState.plants.cta') && getNested(enJson, 'emptyState.calendar.title') && getNested(enJson, 'emptyState.calendar.cta') && getNested(enJson, 'emptyState.explore.title') && getNested(enJson, 'emptyState.explore.cta') — same for ES — assertion id 'W0.scaffold.i18n-emptyState-12keys'

    C. TIER 2 — SKIP→PASS placeholders (use assertSkippable from Phase 22 helpers):
       - POLISH-01: read plantLogicSrc = readSafe('src/utils/plantLogic.ts'); assertSkippable( /const OUTDOOR_TYPE_IDS[\s\S]*?=[\s\S]*?new Set\(\s*\[\s*['"]exterior['"]\s*,\s*['"]frutales['"]\s*\]\s*\)/.test(plantLogicSrc) && /OUTDOOR_TYPE_IDS\.has\(p\.typeId\)/.test(plantLogicSrc) , 'POLISH-01.outdoor-task-gate' )
       - POLISH-02: write a tiny .tmp-phase23/count-outdoor.cjs at runtime that loads src/data/plantDatabase.ts content as string and counts entries where category matches /"(exterior|frutales)"/ and outdoor: false; assertSkippable returning undefined when total outdoor:false count < 45 (PASS at >= 45 — RESEARCH §Finding 2 expects 28 exterior + 7 frutales + 10 outdoor-aromaticas = 45). Assertion id 'POLISH-02.catalog-outdoor-false'.
       - POLISH-03: identSrc = readSafe('src/components/PlantIdentifier/IdentificationResults.tsx'); assertSkippable( /function resolveTypeIdForPicker/.test(identSrc) || /selectedPlant\?\.category/.test(identSrc) , 'POLISH-03.category-over-indoor-flag' )
       - POLISH-05: themeSrc = readSafe('src/theme.ts'); match = themeSrc.match(/textSecondary:\s*['"](#[0-9a-fA-F]{6})['"]/); hex = match ? match[1].toLowerCase() : null; assertSkippable( hex && hex !== '#8a7e6b' && wcagAA(hex, '#f5f0e6') >= 4.5 && wcagAA(hex, '#fffdf8') >= 4.5 , 'POLISH-05.textSecondary-wcag-aa' ). Embed wcagAA(fgHex, bgHex) helper near top of file (~30 LOC): hexToRgb → linearize → relativeLuminance → contrastRatio = (L_light + 0.05) / (L_dark + 0.05).
       - POLISH-06 button literals: esCommon = readSafe('src/i18n/locales/es/common.json'); assertSkippable( /"water"\s*:\s*"Regá ahora 💧"/.test(esCommon) && /"outdoor"\s*:\s*"Sacalo afuera 🌳"/.test(esCommon) && /"fertilize"\s*:\s*"Fertilizá 🌱"/.test(esCommon) && /"sunLabel"\s*:\s*"[^"]*☀️[^"]*"/.test(esCommon) , 'POLISH-06.action-button-voseo-emoji' )
       - POLISH-06 lint: const r = require('child_process').spawnSync('node', [path.resolve(ROOT, 'scripts/voseo-lint.mjs')], { encoding: 'utf8' }); assertSkippable( r.status === 0 , 'POLISH-06.voseo-lint-exit-0' ) — SKIPs while skeleton BANNED is empty (still exits 0, but the assertion treats "skeleton" state as SKIP via second-check: assertSkippable( r.status === 0 && !/skeleton — BANNED list empty/.test(r.stdout || ''), ... ). When Plan 23-02 lands STRICT body, the stdout flag disappears → flips to PASS.
       - POLISH-07: plantsSrc = readSafe('src/screens/PlantsScreen.tsx'); calSrc = readSafe('src/screens/CalendarScreen.tsx'); expSrc = readSafe('src/screens/ExploreScreen.tsx'); assertSkippable( fs.existsSync(path.resolve(ROOT, 'assets/illustrations/empty-plants.png')) && fs.existsSync(path.resolve(ROOT, 'assets/illustrations/empty-calendar.png')) && fs.existsSync(path.resolve(ROOT, 'assets/illustrations/empty-explore.png')) && /empty-plants\.png/.test(plantsSrc) && /empty-calendar\.png/.test(calSrc) && /empty-explore\.png/.test(expSrc) , 'POLISH-07.illustrated-empty-states' )

    D. TIER 2.5 — STRICT POLISH-08 negative-grep (NEVER SKIP — RESEARCH §Finding 12 says PASS at baseline):
       - Define SAMPLE_PLANT_RE = /\b(samplePlants|mockPlants|seedPlants|demoPlants|firstLaunchPlants)\b/.
       - Walk src/ recursively (mirror smoke-phase22.cjs walkSrcForStreakTokens pattern). For each .ts/.tsx file content, accumulate matches; collect file:line in samplePlantViolations[].
       - assert(samplePlantViolations.length === 0, 'POLISH-08.negative-grep.no-sample-plant-arrays') — STRICT PASS at baseline.

    E. TIER 3 — STRICT cross-phase regression (preserve Phase 18 + 19 + 20 + 21 + 22 invariants VERBATIM):
       - Copy the entire `// === PHASE 18 STRICT ===` … through `// === PHASE 22 STRICT ===` block from scripts/smoke-phase22.cjs (it includes Phase 18 mood emoji + Toast + Gesture.Pan + JOURNAL-04 ModalSectionId.diario; Phase 19 PetToxicityBadge + MascotasContent + check-i18n-keys symptoms; Phase 20 fertilize emit + scheduler filter + FertilizeCard + checkScript fertilizer; Phase 21 AppData.journals + journalService + JournalSection; Phase 22 waterPlant/sunPlant/outdoorPlant useStorage actions + triggerHaptic + gamificationToastVisible 3-screen wiring).
       - DO NOT MODIFY these blocks. Cross-phase sentinels are STRICT — failure blocks the gate.

    F. Final exit: console.log PASS/SKIP/FAIL counts; process.exit(fail > 0 ? 1 : 0).

    Target file size: ~280-350 LOC (smoke-phase22.cjs is ~245 LOC; Phase 23 adds 8 POLISH placeholders + WCAG helper + voseo-lint subprocess + walkSrcForSamplePlantArrays).
  </action>
  <verify>
    <automated>node scripts/smoke-phase23.cjs</automated>
    Expected at Wave 0 baseline: exit code 0. Output reports PASS count > 0, SKIP count ≥ 6 (POLISH-01/02/03/05/06-button/06-lint/07), FAIL = 0. Phase 18-22 cross-phase STRICT block all PASS. POLISH-08 STRICT negative-grep PASS.
  </verify>
  <done>
    - scripts/smoke-phase23.cjs exists, runs, exits 0 at baseline
    - Three-tier structure (W0 scaffold STRICT, POLISH-01..07 SKIP, POLISH-08 + cross-phase 18-22 STRICT) present
    - Output shows PASS > 0, SKIP ≥ 6, FAIL = 0
    - File NOT modified again after this task
  </done>
</task>

<task type="auto">
  <name>Task 2: Create voseo-lint skeleton + wire 2 npm scripts + .gitignore + 6-key emptyState i18n namespace EN+ES</name>
  <files>scripts/voseo-lint.mjs, package.json, .gitignore, src/i18n/locales/en/common.json, src/i18n/locales/es/common.json</files>
  <read_first>
    - .planning/phases/23-polish-uat-brand-voice/23-RESEARCH.md §Pattern 3 (voseo-lint impl reference at lines 252-312) + §Pitfall 4 (`tu` possessive vs `tú` pronoun discrimination at lines 458-466) + §Finding 7 (17 existing violations table at lines 818-839 — KNOWN STATE; this task does NOT fix them, only ships skeleton)
    - .planning/phases/23-polish-uat-brand-voice/23-CONTEXT.md "Voseo lint enforcement" decision block (lines 32-47)
    - package.json scripts block (current state — 22 npm scripts including smoke:phase18-22)
    - .gitignore (current — confirm scripts/.tmp-phaseXX pattern; Phase 22 added scripts/.tmp-phase22/)
    - src/i18n/locales/en/common.json (current — confirm top-level namespace shape; emptyState namespace will be NEW)
    - src/i18n/locales/es/common.json (current — same)
  </read_first>
  <acceptance_criteria>
    - scripts/voseo-lint.mjs exists and has shebang `#!/usr/bin/env node` on line 1
    - `grep -c "const BANNED" scripts/voseo-lint.mjs` returns ≥ 1
    - `grep -c "walkValues" scripts/voseo-lint.mjs` returns ≥ 1
    - `grep -c "process.exit" scripts/voseo-lint.mjs` returns ≥ 2 (parse-error + violations + success paths)
    - `node scripts/voseo-lint.mjs; echo $?` returns 0 (skeleton — empty BANNED list)
    - `node -e "console.log(require('./package.json').scripts['smoke:phase23'])"` prints `node scripts/smoke-phase23.cjs`
    - `node -e "console.log(require('./package.json').scripts['lint:voseo'])"` prints `node scripts/voseo-lint.mjs`
    - `grep -c "scripts/\.tmp-phase23/" .gitignore` returns ≥ 1
    - `node -e "const j=require('./src/i18n/locales/en/common.json'); console.log(Object.keys(j.emptyState||{}).sort().join(','))"` prints `calendar,explore,plants`
    - `node -e "const j=require('./src/i18n/locales/es/common.json'); console.log(Object.keys(j.emptyState||{}).sort().join(','))"` prints `calendar,explore,plants`
    - Locale parity (EN keys === ES keys under emptyState): diff of `Object.keys(en.emptyState.plants).sort()` against ES equivalent is empty (all 3 subtrees have title+cta in both)
    - `npx tsc --noEmit` exits 0
    - `npm run check:i18n-keys` still passes (118 catalog ids)
  </acceptance_criteria>
  <action>
    Five surgical edits:

    Edit 1 — Create scripts/voseo-lint.mjs (skeleton; STRICT body lands in Plan 23-02). Content sketch:

      Line 1: shebang `#!/usr/bin/env node`
      Lines 2-15: header JSDoc describing Phase 23 POLISH-06 skeleton vs strict-body-in-23-02 split
      Lines 16-20: imports — `import fs from 'node:fs'; import path from 'node:path'; import { fileURLToPath } from 'node:url';`
      Lines 21-25: `const __filename = fileURLToPath(import.meta.url); const __dirname = path.dirname(__filename); const ROOT = path.resolve(__dirname, '..'); const ES_DIR = path.resolve(ROOT, 'src/i18n/locales/es');`
      Lines 26-30: `const BANNED = [];` (empty array with comment: "Plan 23-02 populates per RESEARCH §Pattern 3 lines 263-274")
      Lines 31-33: `const WHITELIST_KEYS = new Set();` (empty set with comment: "Plan 23-02 finalizes — see RESEARCH §Open Question 2 + §Pitfall 4")
      Lines 34-35: `let fail = 0; const violations = [];`
      Lines 36-53: function walkValues(obj, keyPath) — recursive walk that emits violations[] when BANNED regex matches AND keyPath not in WHITELIST_KEYS
      Lines 54-58: existsSync guard on ES_DIR → process.exit(2) if missing
      Lines 59-72: for each .json file under ES_DIR — readFileSync + JSON.parse (process.exit(2) on parse error) + walkValues(json, basename-without-ext)
      Lines 73-79: if fail > 0 → console.error + process.exit(1); else `console.log('voseo-lint: PASS (skeleton — BANNED list empty until Plan 23-02)'); process.exit(0);`

      Note the literal string "skeleton — BANNED list empty until Plan 23-02" — smoke-phase23.cjs POLISH-06.voseo-lint-exit-0 sentinel uses this stdout-substring to discriminate SKIP (skeleton) from PASS (strict body shipped).

    Edit 2 — package.json `"scripts"` map: add two entries (preserve alphabetic-ish order alongside existing smoke:phase22):

      "lint:voseo": "node scripts/voseo-lint.mjs",
      "smoke:phase23": "node scripts/smoke-phase23.cjs",

      Insert immediately after the existing `"smoke:phase22"` line. Preserve trailing commas / JSON validity.

    Edit 3 — .gitignore: append a single line (idempotent — grep before appending to avoid duplicates):

      scripts/.tmp-phase23/

    Edit 4 — src/i18n/locales/en/common.json: add NEW top-level namespace `emptyState` (insert at end of file before the closing `}` — preserve trailing-comma style of the existing last namespace). EN values:

      "emptyState": {
        "plants": { "title": "Your garden is waiting", "cta": "Add your first plant" },
        "calendar": { "title": "No tasks today", "cta": "Enjoy the break" },
        "explore": { "title": "Explore +100 plants for your home", "cta": "Browse species" }
      }

    Edit 5 — src/i18n/locales/es/common.json: add NEW top-level namespace `emptyState` with CONTEXT.md-locked voseo values (final copy; Plan 23-03 only wires JSX consumers):

      "emptyState": {
        "plants": { "title": "Tu jardín está esperando 🌱", "cta": "Agregá tu primera planta" },
        "calendar": { "title": "No hay tareas hoy ☀️", "cta": "Disfrutá del descanso" },
        "explore": { "title": "Explorá +100 plantas para tu hogar", "cta": "Buscar especies" }
      }

    Locale parity check (manual): the 3 sub-namespaces + 2 keys each = 6 leaf paths must match EN ↔ ES exactly. Run jq path comparison after edit.

    Voseo verification: `node scripts/voseo-lint.mjs` MUST exit 0 (skeleton BANNED is empty so no possible match). Plan 23-02 takes responsibility for filling BANNED + fixing 17 pre-existing violations atomically.
  </action>
  <verify>
    <automated>npm run smoke:phase23 && npm run lint:voseo && npx tsc --noEmit && npm run check:i18n-keys</automated>
    Expected: smoke-phase23 exits 0 (POLISH-08 STRICT + cross-phase 18-22 PASS; POLISH-01/02/03/05/06/07 SKIP). voseo-lint exits 0 (skeleton). tsc clean. check:i18n-keys passes 118 catalog ids.
  </verify>
  <done>
    - scripts/voseo-lint.mjs exists and exits 0 (skeleton with empty BANNED)
    - package.json has both `smoke:phase23` and `lint:voseo` npm scripts
    - .gitignore contains `scripts/.tmp-phase23/`
    - EN + ES common.json both have `emptyState.{plants,calendar,explore}.{title,cta}` (12 leaf keys total)
    - Locale parity preserved (6 paths each, identical structure)
    - All 4 automation commands green (smoke, voseo, tsc, check:i18n-keys)
  </done>
</task>

</tasks>

<verification>
- `node scripts/smoke-phase23.cjs` → exit 0 with PASS > 0, SKIP ≥ 6 (POLISH-01/02/03/05/06-button/06-lint/07), FAIL = 0
- `node scripts/voseo-lint.mjs` → exit 0 (skeleton)
- `npx tsc --noEmit` → exit 0
- `npm run check:i18n-keys` → PASS 118 catalog ids
- Cross-phase Phase 18+19+20+21+22 STRICT sentinels all PASS (no regression introduced)
</verification>

<success_criteria>
- Wave 0 Nyquist gate established: 6 new/edited files (2 new scripts + 2 i18n JSON edits + 1 npm-script edit + 1 .gitignore edit)
- Smoke runner ships three tiers (W0 scaffold STRICT + POLISH SKIP placeholders + POLISH-08/cross-phase STRICT)
- Voseo-lint skeleton callable as `npm run lint:voseo`; STRICT body deferred to Plan 23-02
- emptyState namespace seeded in EN + ES with locked voseo copy; JSX consumers wired in Plan 23-03
- POLISH-08 negative-grep PASS at baseline (zero source change needed; verification-only)
- Cross-phase regression for Phase 18-22 PRESERVED VERBATIM (no edits to those sentinel blocks)
</success_criteria>

<output>
After completion, create `.planning/phases/23-polish-uat-brand-voice/23-00-SUMMARY.md` per template (`/Users/gaston/.claude/get-shit-done/templates/summary.md`).
</output>
