---
phase: 23-polish-uat-brand-voice
plan: 02
type: execute
wave: 2
depends_on:
  - "23-polish-uat-brand-voice/00"
files_modified:
  - src/theme.ts
  - scripts/voseo-lint.mjs
  - src/i18n/locales/es/common.json
  - src/i18n/locales/en/common.json
autonomous: true
requirements:
  - POLISH-05
  - POLISH-06
must_haves:
  truths:
    - "colors.textSecondary in src/theme.ts is #6f6450 (RESEARCH §Finding 6 — 5.12:1 on bgPrimary, 5.72:1 on card, both pass WCAG AA 4.5:1)"
    - "TODO comment about textSecondary AA failure at theme.ts line ~13 is removed"
    - "scripts/voseo-lint.mjs BANNED array populated per RESEARCH §Pattern 3 + §Pitfall 4 (\\btú\\b banned as accented pronoun; \\btu\\b unaccented possessive NOT banned)"
    - "WHITELIST_KEYS contains at minimum settings.locationDescription + alerts.sunDayToday (RESEARCH §Open Question 2)"
    - "All 17 pre-existing voseo violations in es/common.json (RESEARCH §Finding 7 table) are fixed inline"
    - "4 action button i18n values in es/common.json updated to voseo+emoji: plantCard.water → 'Regá ahora 💧'; plantCard.outdoor → 'Sacalo afuera 🌳'; plantCard.fertilize → 'Fertilizá 🌱'; plantCard.sunLabel preserves {{hours}} interpolation and adds ☀️"
    - "EN parity preserved (Water now 💧, Put outside 🌳, Fertilize 🌱, Sun ☀️ ({{hours}}h))"
    - "npm run lint:voseo exits 0 STRICT after Plan 23-02 (voseo skeleton tag removed from stdout)"
    - "node scripts/smoke-phase23.cjs POLISH-05 + POLISH-06.action-button + POLISH-06.voseo-lint all flip SKIP → PASS"
  artifacts:
    - path: "src/theme.ts"
      provides: "textSecondary darkened to #6f6450 (POLISH-05)"
      contains: "#6f6450"
    - path: "scripts/voseo-lint.mjs"
      provides: "STRICT body — populated BANNED list + WHITELIST_KEYS + skeleton-stdout-tag removed"
      contains: "BANNED"
      min_lines: 90
    - path: "src/i18n/locales/es/common.json"
      provides: "4 action button voseo+emoji values + 17 pre-existing violations fixed (POLISH-06)"
      contains: "Regá ahora"
    - path: "src/i18n/locales/en/common.json"
      provides: "4 EN action button emoji values (POLISH-06 EN parity)"
      contains: "Water now"
  key_links:
    - from: "src/theme.ts colors.textSecondary"
      to: "All UI text consumers (PlantCard subtitle, MyPlantDetailModal labels, etc.)"
      via: "RN StyleSheet color token"
      pattern: "textSecondary:\\s*['\\\"]#6f6450['\\\"]"
    - from: "scripts/voseo-lint.mjs"
      to: "src/i18n/locales/es/common.json + es/plants.json"
      via: "JSON.parse + walkValues with BANNED regex array + WHITELIST_KEYS bypass"
      pattern: "BANNED"
    - from: "scripts/smoke-phase23.cjs POLISH-06.voseo-lint-exit-0 sentinel"
      to: "scripts/voseo-lint.mjs"
      via: "child_process spawnSync; exit code 0 AND stdout NOT containing 'skeleton — BANNED list empty'"
      pattern: "voseo-lint.mjs"
---

<objective>
Wave 2 (WCAG AA + voseo brand voice) — close 2 polish requirements.

POLISH-05 (Task 1): Darken colors.textSecondary from #8a7e6b (3.50:1 on bgPrimary — FAIL AA) to #6f6450 (5.12:1 on bgPrimary + 5.72:1 on card — both PASS AA with comfortable margin per RESEARCH §Finding 6 computed-contrast table). Remove the obsolete TODO comment at theme.ts line ~13. Single-token swap; CONTEXT.md design-system lock honored ("no new theme tokens").

POLISH-06 (Task 2 — composite, atomic): Land the STRICT voseo-lint body (populate BANNED regex array per RESEARCH §Pattern 3 + §Pitfall 4 with `\btú\b` accented-pronoun discrimination; add WHITELIST_KEYS for legitimate 3rd-person 'se usa' / 'hoy toca' per RESEARCH §Open Question 2); fix all 17 pre-existing voseo violations in src/i18n/locales/es/common.json per RESEARCH §Finding 7 table; update 4 action button i18n values to voseo+emoji per CONTEXT.md lock; mirror 4 EN action button updates with emoji (no voseo for EN). The skeleton stdout tag 'skeleton — BANNED list empty until Plan 23-02' is removed so smoke-phase23 POLISH-06.voseo-lint-exit-0 flips SKIP → PASS.

Hardcoded ES strings in plantLogic.ts:74/79/82 (RESEARCH §Pitfall 3) are OUT OF SCOPE for this plan — CONTEXT.md voseo-lint scope is `src/i18n/locales/es/*.json` only. They remain hardcoded in v1.2; Phase 24 DOCS may surface for v1.3.

Atomic commits: 1 per requirement (POLISH-05 alone; POLISH-06 as a single composite — lint body + 17 violation fixes + 4 button updates + EN parity).

Purpose: Close UAT #4 (textSecondary AA failure) and lock brand-voice discipline (voseo lint becomes a STRICT gate enforceable on every future plan).
Output: 4 file edits across 2 atomic commits; smoke-phase23 SKIP count goes from 3 to 1 (POLISH-07 remains for Plan 23-03).
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-polish-uat-brand-voice/23-CONTEXT.md
@.planning/phases/23-polish-uat-brand-voice/23-RESEARCH.md
@.planning/phases/23-polish-uat-brand-voice/23-VALIDATION.md
@.planning/phases/23-polish-uat-brand-voice/23-00-SUMMARY.md

# Reference implementations
@src/theme.ts
@scripts/voseo-lint.mjs
@src/i18n/locales/es/common.json
@src/i18n/locales/en/common.json
@scripts/smoke-phase23.cjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: POLISH-05 — darken colors.textSecondary to #6f6450 + remove obsolete TODO</name>
  <files>src/theme.ts</files>
  <read_first>
    - src/theme.ts lines 1-20 (current textSecondary value #8a7e6b at line 11; AA-failure TODO at line 13; textMuted at line 14 = #6a604f for hierarchy reference)
    - .planning/phases/23-polish-uat-brand-voice/23-RESEARCH.md §Finding 6 (WCAG contrast table — #6f6450 = 5.12 vs bgPrimary, 5.72 vs card, hierarchy preserved at lines 794-810) + §Example 4 (theme.ts diff at lines 610-621) + §Pitfall 5 (large-text exception — no call-site audit needed; darkening is strictly better)
    - .planning/phases/23-polish-uat-brand-voice/23-CONTEXT.md "POLISH-05 exact new hex value" (line 65 — research computes; lock is "passes 4.5:1 on both #f5f0e6 AND #fffdf8")
  </read_first>
  <acceptance_criteria>
    - `grep -E "textSecondary:\\s*['\\\"]#6f6450['\\\"]" src/theme.ts` returns ≥ 1 line
    - `! grep -E "textSecondary:\\s*['\\\"]#8a7e6b['\\\"]" src/theme.ts` returns 0 (old value gone)
    - `! grep -F "textSecondary #8a7e6b also fails AA" src/theme.ts` returns 0 (obsolete TODO comment removed)
    - `grep -E "textMuted:\\s*['\\\"]#6a604f['\\\"]" src/theme.ts` still returns 1 (textMuted UNCHANGED — hierarchy preservation; textSecondary #6f6450 is lighter than textMuted #6a604f)
    - `npx tsc --noEmit` exits 0
    - `node scripts/smoke-phase23.cjs` shows POLISH-05.textSecondary-wcag-aa as PASS (smoke runner's wcagAA helper confirms both contrasts ≥ 4.5)
  </acceptance_criteria>
  <action>
    Single small edit to src/theme.ts (lines 9-14 area):

    Before:
    ```typescript
      // Text
      textPrimary: '#2d3a2e',
      textSecondary: '#8a7e6b',
      // textMuted: darkened from #a89e8b → #6a604f to meet WCAG AA contrast on bgPrimary (#f5f0e6) and card (#fffdf8). Was ~3.1:1 → now ~4.9:1.
      // NOTE (v1.2): textSecondary #8a7e6b also fails AA on bgPrimary (~3.1:1). Audit + fix in v1.2 polish phase.
      textMuted: '#6a604f',
    ```

    After:
    ```typescript
      // Text
      textPrimary: '#2d3a2e',
      textSecondary: '#6f6450', // POLISH-05 (Phase 23) — darkened from #8a7e6b (3.50:1 on bgPrimary — FAIL AA) to #6f6450 (5.12:1 on bgPrimary, 5.72:1 on card — both PASS AA per WCAG 2.1).
      // textMuted: darkened from #a89e8b → #6a604f to meet WCAG AA contrast on bgPrimary (#f5f0e6) and card (#fffdf8). Was ~3.1:1 → now ~4.9:1.
      textMuted: '#6a604f',
    ```

    Net change: one hex value swap on the textSecondary line + replace the two-line WCAG comment block with a single inline comment after textSecondary. Do NOT modify textPrimary, textMuted, or any other color token. Do NOT alter the textMuted line at all.

    Commit message: `fix(23-02): darken colors.textSecondary to #6f6450 for WCAG AA (POLISH-05)`
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/smoke-phase23.cjs</automated>
    Expected: tsc clean. smoke-phase23 POLISH-05.textSecondary-wcag-aa flips SKIP → PASS (wcagAA helper computes 5.12 on bgPrimary, 5.72 on card — both ≥ 4.5). All other sentinels unchanged.
  </verify>
  <done>
    - textSecondary darkened to #6f6450 in src/theme.ts
    - Obsolete TODO comment removed; concise WCAG note inline
    - textMuted UNCHANGED (hierarchy preserved)
    - tsc clean; smoke-phase23 POLISH-05 sentinel flips SKIP → PASS
    - Atomic commit landed for POLISH-05
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: POLISH-06 — populate STRICT voseo-lint + fix 17 pre-existing violations + 4 action button voseo+emoji updates (composite, atomic)</name>
  <files>scripts/voseo-lint.mjs, src/i18n/locales/es/common.json, src/i18n/locales/en/common.json</files>
  <read_first>
    - scripts/voseo-lint.mjs (current skeleton landed in Plan 23-00 — BANNED array empty, WHITELIST_KEYS empty Set, skeleton-stdout-tag present)
    - src/i18n/locales/es/common.json (current — locate all 17 pre-existing violations per RESEARCH §Finding 7 table; locate plantCard.water/sunLabel/outdoor/fertilize at their current paths)
    - src/i18n/locales/en/common.json (current — same keys, EN parity)
    - .planning/phases/23-polish-uat-brand-voice/23-RESEARCH.md §Pattern 3 (voseo-lint full reference at lines 252-312) + §Pitfall 4 (`\btú\b` accented vs `\btu\b` possessive at lines 458-466) + §Finding 7 (FULL 17-violation table at lines 818-839 with exact key paths + replacements) + §Finding 8 (4 action button keys at lines 845-867) + §Open Question 1 (sunLabel hours interpolation — Option B recommended: "Sol ☀️ ({{hours}}h)" preserves {{hours}}) + §Open Question 2 (whitelist for settings.locationDescription + alerts.sunDayToday)
    - .planning/phases/23-polish-uat-brand-voice/23-CONTEXT.md "Voseo lint enforcement" + "Action button copy variants" decision blocks (lines 32-47)
  </read_first>
  <acceptance_criteria>
    - `grep -cE "const BANNED\\s*=" scripts/voseo-lint.mjs` returns 1
    - `grep -E "\\\\btú\\\\b" scripts/voseo-lint.mjs` matches the accented form pattern (Pitfall 4 — accented only)
    - `! grep -E "\\\\btu\\\\b[^|]*BANNED" scripts/voseo-lint.mjs` returns 0 (unaccented `tu` NOT in BANNED — possessive use is legitimate)
    - `grep -c "WHITELIST_KEYS" scripts/voseo-lint.mjs` returns ≥ 2 (declaration + check inside walkValues)
    - `grep -E "settings\\.locationDescription|alerts\\.sunDayToday" scripts/voseo-lint.mjs` returns ≥ 2 lines (RESEARCH §Open Question 2 whitelist)
    - `! grep -F "skeleton — BANNED list empty" scripts/voseo-lint.mjs` returns 0 (skeleton stdout tag removed)
    - BANNED list contains at minimum: tienes, puedes, debes, quieres, eres, riega, saca, fertiliza, agrega, usa, toca, elige, mira, escucha, mueve, haz, tú (accented), usted, ustedes (verify with `grep -cE "\\\\b(tienes|puedes|debes|quieres|eres|riega|saca|fertiliza|agrega|usa|toca|elige|mira|escucha|mueve|haz|tú|usted|ustedes)\\\\b" scripts/voseo-lint.mjs` returns ≥ 18)
    - `node scripts/voseo-lint.mjs; echo $?` returns 0 (clean PASS — all 17 pre-existing violations fixed)
    - es/common.json has 4 voseo+emoji action button values: `grep -F '"water": "Regá ahora 💧"' src/i18n/locales/es/common.json` returns 1; same for `"outdoor": "Sacalo afuera 🌳"` and `"fertilize": "Fertilizá 🌱"`; sunLabel has ☀️ AND {{hours}} both present (regex: `grep -E '"sunLabel":[^,]*☀️[^,]*\\{\\{hours\\}\\}' src/i18n/locales/es/common.json` returns ≥ 1)
    - en/common.json EN parity: `grep -F '"water": "Water now 💧"' src/i18n/locales/en/common.json` returns 1; same for `"outdoor": "Put outside 🌳"` and `"fertilize": "Fertilize 🌱"`; sunLabel ES emoji ☀️ present + {{hours}} preserved
    - Pre-existing violations fixed verifiable by lint exit 0: e.g. `"Elige tu idioma"` → `"Elegí tu idioma"`, `"Riega cada {{days}} días..."` → `"Regá cada {{days}} días..."`, `"Toca un día"` → `"Tocá un día"`, etc. — full list per RESEARCH §Finding 7 table
    - `npx tsc --noEmit` exits 0
    - `npm run check:i18n-keys` still passes 118 catalog ids (no schema break)
    - `node scripts/smoke-phase23.cjs` shows POLISH-06.action-button-voseo-emoji AND POLISH-06.voseo-lint-exit-0 BOTH as PASS (were SKIP)
    - All Phase 18-22 cross-phase STRICT sentinels still PASS
  </acceptance_criteria>
  <action>
    Composite atomic edit (single commit for POLISH-06):

    EDIT A — populate scripts/voseo-lint.mjs BANNED + WHITELIST_KEYS + remove skeleton-stdout tag

    Replace the empty `const BANNED = [];` block with the populated regex array per RESEARCH §Pattern 3 (lines 263-274) AND §Pitfall 4 (accent discrimination). Final BANNED contents:

    ```javascript
    /**
     * Banned forms — word-bounded. Castilian/formal Spanish forbidden in es-AR voseo register.
     *
     * IMPORTANT (Pitfall 4): only `\btú\b` (accented pronoun) is banned. The unaccented
     * `tu` is a legitimate possessive ("tu jardín está esperando"). Spanish keyboards
     * may produce either form; the regex requires the accent to flag a violation.
     */
    const BANNED = [
      // Castilian 2nd-person verbs (tuteo)
      /\btienes\b/i, /\bpuedes\b/i, /\bdebes\b/i, /\bquieres\b/i, /\beres\b/i,
      // Castilian imperatives (have voseo equivalents)
      /\briega\b/i, /\bsaca\b/i, /\bpode\b/i, /\bcorta\b/i,
      /\bfertiliza\b/i, /\bagrega\b/i, /\bven\b/i, /\bmira\b/i, /\bescucha\b/i,
      /\belige\b/i, /\busa\b/i, /\btoca\b/i, /\bmueve\b/i, /\bhaz\b/i,
      // Pronouns (NOT possessive 'tu' — see JSDoc above)
      /\btú\b/i,
      // Formal 3rd-person
      /\busted\b/i, /\bustedes\b/i,
    ];
    ```

    Replace the empty `WHITELIST_KEYS = new Set();` with the populated set covering RESEARCH §Open Question 2 + 3rd-person legitimate uses:

    ```javascript
    /**
     * Whitelist — i18n key paths where banned forms appear as legitimate 3rd-person
     * verb forms, NOT Castilian imperatives. Documented in RESEARCH §Open Question 2.
     *
     * - settings.locationDescription: "Tu ubicación se usa para..." (reflexive 3rd-person)
     * - alerts.sunDayToday: "Hoy toca sacarla al sol" (impersonal 3rd-person "it's time to")
     */
    const WHITELIST_KEYS = new Set([
      'common.settings.locationDescription',
      'common.alerts.sunDayToday',
    ]);
    ```

    Note: keyPath in walkValues is built as `${basename}.${k1}.${k2}...` so the file is prefixed. Since the file is `common.json`, the prefix is `common.`. Verify by reading the walkValues implementation in the skeleton and confirming the produced key path.

    Update the final success log line — remove `(skeleton — BANNED list empty until Plan 23-02)`:

    Before:
    ```javascript
    console.log('voseo-lint: PASS (skeleton — BANNED list empty until Plan 23-02)');
    ```

    After:
    ```javascript
    console.log('voseo-lint: PASS');
    ```

    EDIT B — fix 17 pre-existing voseo violations in src/i18n/locales/es/common.json (RESEARCH §Finding 7 table):

    Apply each replacement EXACTLY as listed in RESEARCH §Finding 7. Examples (NOT exhaustive — RESEARCH lists all 17):
    - `onboarding.languageStep`: "Elige tu idioma" → "Elegí tu idioma"
    - `settings.languageDescription`: "Elige el idioma de la aplicación." → "Elegí el idioma de la aplicación."
    - `fab.chooseKnown`: "Elige de plantas conocidas" → "Elegí de plantas conocidas"
    - `plants.tipText`: "Usa el botón + para agregar..." → "Usá el botón + para agregar..."
    - `fab.useCamera`: "Usa la cámara o galería" → "Usá la cámara o galería"
    - `today.emptyGardenText`: "Agrega tu primera planta..." → "Agregá tu primera planta..."
    - `paywall.unlimitedPlantsDesc`: "...agrega todas las que quieras" → "...agregá todas las que quieras"
    - `plants.emptyText`: "Toca el botón + ..." → "Tocá el botón + ..." (Note: this key may be DEPRECATED by POLISH-07 in Plan 23-03; for now fix it OR leave as legacy. Recommendation: fix it now to be safe.)
    - `calendar.instructions`: "Toca un día para ver las tareas..." → "Tocá un día para ver las tareas..."
    - `weatherAlerts.tapToUnlock`: "Toca para desbloquear" → "Tocá para desbloquear"
    - `health.waterEvery`: "Riega cada {{days}} días..." → "Regá cada {{days}} días..."
    - `health.overdueWaterSuggestion`: "Riega la planta lo antes posible..." → "Regá la planta lo antes posible..."
    - `alerts.heatWave`: "...riega temprano o al atardecer" → "...regá temprano o al atardecer"
    - `wateringRec.extremeHeat`: "...riega temprano..." → "...regá temprano..."
    - `wateringRec.advanceOne`: "Riega temprano o al atardecer" → "Regá temprano o al atardecer"
    - `wateringRec.advanceMany`: "Riega temprano {{count}} plantas" → "Regá temprano {{count}} plantas"

    Re-read RESEARCH §Finding 7 for the canonical list. NOTE: `settings.locationDescription` ("se usa" reflexive) and `alerts.sunDayToday` ("hoy toca" impersonal) are legitimate 3rd-person — DO NOT MODIFY them; they are in WHITELIST_KEYS.

    EDIT C — update 4 action button i18n values in src/i18n/locales/es/common.json (RESEARCH §Finding 8):

    Locate the `"plantCard": { ... }` namespace in es/common.json. Apply:
    - `"water": "Regar"` → `"water": "Regá ahora 💧"`
    - `"sunLabel": "Sol ({{hours}}h)"` → `"sunLabel": "Sol ☀️ ({{hours}}h)"` (Open Question 1 — Option B: emoji-only, preserve hours)
    - `"outdoor": "Sacar afuera"` → `"outdoor": "Sacalo afuera 🌳"`
    - `"fertilize": "Fertilizar"` → `"fertilize": "Fertilizá 🌱"`

    If `plantDetailModal.water` / `plantDetailModal.fertilize` exist as separate keys (RESEARCH §Finding 8 footer), also update them to the same voseo+emoji values for consistency.

    EDIT D — mirror 4 EN action button values in src/i18n/locales/en/common.json (NO voseo for EN — emoji only):

    Locate `"plantCard": { ... }` in en/common.json. Apply:
    - `"water": "Water"` → `"water": "Water now 💧"`
    - `"sunLabel": "Sun ({{hours}}h)"` → `"sunLabel": "Sun ☀️ ({{hours}}h)"`
    - `"outdoor": "Put outside"` → `"outdoor": "Put outside 🌳"`
    - `"fertilize": "Fertilize"` → `"fertilize": "Fertilize 🌱"`

    Locale parity check: EN and ES key trees must remain identical (no new keys introduced; only value updates). Verify with `npm run check:i18n-keys` post-edit.

    Commit message: `feat(23-02): land STRICT voseo-lint + fix 17 pre-existing violations + 4 action button voseo+emoji (POLISH-06)`
  </action>
  <verify>
    <automated>npm run lint:voseo &amp;&amp; npx tsc --noEmit &amp;&amp; npm run check:i18n-keys &amp;&amp; node scripts/smoke-phase23.cjs</automated>
    Expected: voseo-lint exits 0 STRICT (all 17 violations fixed; BANNED is populated; whitelist allows 'se usa'/'hoy toca'). tsc clean. check:i18n-keys passes 118 catalog ids. smoke-phase23 shows POLISH-06.action-button-voseo-emoji AND POLISH-06.voseo-lint-exit-0 BOTH PASS (were SKIP). POLISH-05 also PASS from Task 1.
  </verify>
  <done>
    - scripts/voseo-lint.mjs has populated BANNED + WHITELIST_KEYS; skeleton stdout tag removed; exits 0
    - All 17 pre-existing voseo violations in es/common.json fixed inline
    - 4 ES action button values use voseo + emoji per CONTEXT.md lock (Regá ahora 💧 / Sol ☀️ / Sacalo afuera 🌳 / Fertilizá 🌱)
    - 4 EN action button values use emoji parity (Water now 💧 / Sun ☀️ / Put outside 🌳 / Fertilize 🌱)
    - voseo-lint + tsc + check:i18n-keys + smoke-phase23 all green
    - POLISH-06 placeholders flip SKIP → PASS
    - Atomic commit landed for POLISH-06
  </done>
</task>

</tasks>

<verification>
- `node scripts/smoke-phase23.cjs` → exit 0; POLISH-05 + POLISH-06.action-button + POLISH-06.voseo-lint all PASS. SKIP count drops from 3 to 1 (POLISH-07 remaining for Plan 23-03)
- `npm run lint:voseo` → exit 0 (STRICT)
- `npx tsc --noEmit` → exit 0
- `npm run check:i18n-keys` → PASS 118 catalog ids
- Phase 18-22 cross-phase STRICT sentinels all PASS
- POLISH-08 STRICT negative-grep still PASS (no source arrays introduced)
- 2 atomic commits in git log: fix(23-02) POLISH-05, feat(23-02) POLISH-06
</verification>

<success_criteria>
- WCAG AA contrast achieved on textSecondary against both #f5f0e6 (bgPrimary) AND #fffdf8 (card) with 5.12+ ratio
- Voseo brand-voice discipline locked at CI gate; future plans cannot introduce Castilian/formal forms in es/*.json without lint failure
- 4 action button labels now read with voseo + emoji ("Regá ahora 💧" etc.) on both PlantCard and DayDetailModal surfaces (via shared i18n keys)
- 17 pre-existing voseo violations in es/common.json eliminated atomically alongside lint impl
- 2 atomic commits per RESEARCH §"Established Patterns"
</success_criteria>

<output>
After completion, create `.planning/phases/23-polish-uat-brand-voice/23-02-SUMMARY.md` per template.
</output>
