---
phase: 14-educational-detail-modal
plan: 06
type: execute
wave: 5
depends_on:
  - "14-05"
files_modified:
  - src/data/plantDatabase.ts
  - src/i18n/locales/en/plants.json
  - src/i18n/locales/es/plants.json
autonomous: true
requirements:
  - EDU-03
gap_closure: false

must_haves:
  truths:
    - "All 9 AROMATICAS entries (albahaca, ciboulette, cilantro, menta, oregano, perejil, romero, romero-rastrero, tomillo) gain the 5 EDU-02 educational fields in src/data/plantDatabase.ts"
    - "All 9 HUERTA entries (frutilla, lechuga, pepino, pimiento, rucula, tomate-cherry, tomatera, zanahoria, zapallito) gain the 5 EDU-02 educational fields"
    - "Total: 18 entries × 5 fields × 2 locales — locale parity verified by `npm run check:i18n-keys`"
    - "Aromaticas-specific tone: cosecha (harvest) + pellizcá (pinch) + mantené humedad + Mediterráneo (lavandas analog) — voseo throughout"
    - "Huerta-specific tone: trasplante post-helada + tutorá (stake) + cosecha por escalera (succession harvest) + mulch + sol pleno — voseo throughout"
    - "whyRationale cites: aromáticas → Mediterráneo / Tropical / Frigorial origin; huerta → annual cycle / fruit-set photoperiod / nutrient demand / day-length sensitivity"
    - "Per-field char limits respected (same as Plan 14-04/05)"
    - "Voseo grep guard: no NEW Castilian forms introduced vs Plan 14-05 baseline"
    - "`npm run check:i18n-keys` exits 0 with 54 cumulative entries × 5 fields × 2 locales validated (15 interior + 21 exterior + 18 from this plan)"
    - "`npx tsc --noEmit` exits 0; `node scripts/smoke-phase14.mjs` reports 0 FAILs"
  artifacts:
    - path: "src/data/plantDatabase.ts"
      provides: "5 educational fields populated on 18 aromaticas+huerta entries"
      contains: "whyRationale"
    - path: "src/i18n/locales/en/plants.json"
      provides: "≤90 new EN strings"
      contains: "harvest|pinch|mulch"
    - path: "src/i18n/locales/es/plants.json"
      provides: "≤90 new ES voseo strings; locale parity"
      contains: "cosechá|pellizcá|tutorá"
  key_links:
    - from: "src/data/plantDatabase.ts (aromaticas + huerta entries)"
      to: "src/i18n/locales/{en,es}/plants.json (per-entry 5-key blocks)"
      via: "Validator extension from Plan 14-01 — when entry declares a field, validator demands matching key in both locales"
      pattern: "whyRationale|placementRecommended|careAction"
---

<objective>
Wave 3c — content authoring for 18 entries across two CLOSELY-RELATED categories: AROMATICAS (9 entries) + HUERTA (9 entries). Both categories share thematic authoring patterns (cosecha-driven care, voseo imperatives like cosechá / pellizcá / tutorá, photoperiod-aware whyRationale), so they batch into a single plan. Sequential after Plan 14-05 because of plantDatabase.ts + plants.json file conflict.

The 18 entry IDs:
- **Aromaticas (9):** albahaca, ciboulette, cilantro, menta, oregano, perejil, romero, romero-rastrero, tomillo
- **Huerta (9):** frutilla, lechuga, pepino, pimiento, rucula, tomate-cherry, tomatera, zanahoria, zapallito

Authoring distinction from prior plans:
- **Aromaticas** are harvested-for-leaf (or stem); careAction often references "Pellizcá las puntas para ramificación" and "Cosechá hojas exteriores frescas". Origin lexicon: Mediterráneo (oregano / romero / tomillo), Frigorial (cilantro / perejil / ciboulette), Tropical (albahaca), Húmedo subtropical (menta).
- **Huerta** are annuals or biennials with seasonal lifecycles: tomatera + tomate-cherry + pepino + pimiento + zapallito are cucurbits/solanaceous summer crops; lechuga + rucula are cool-season leafy greens; frutilla is a perennial-ish; zanahoria is a biennial root.

Output: 3 modified files. After this plan, 54/64 entries (84%) have full EDU-02/03 content (15 + 21 + 18).
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-educational-detail-modal/14-CONTEXT.md
@.planning/phases/14-educational-detail-modal/14-RESEARCH.md
@.planning/phases/14-educational-detail-modal/14-VALIDATION.md
@.planning/phases/14-educational-detail-modal/14-01-PLAN.md
@.planning/phases/14-educational-detail-modal/14-04-PLAN.md
@.planning/phases/14-educational-detail-modal/14-05-PLAN.md
@CLAUDE.md
@src/data/plantDatabase.ts
@src/i18n/locales/es/plants.json
@src/i18n/locales/en/plants.json

<interfaces>
<!-- Same content shape as Plans 14-04/05. Aromaticas + Huerta tone delta. -->

```typescript
// Example aromaticas entry (albahaca):
{
  careAction: {
    fixed: "Regá cada 3-4 días en cálido. Pellizcá las puntas para ramificación y evitar floración temprana.",
  },
  placementRecommended: "Sol parcial — 4-6 horas de luz directa, mejor mañanera.",
  placementAlternatives: ["Maceta en balcón soleado", "Cantera de hierbas con romero y tomillo"],
  placementAvoid: "Sol intenso del mediodía sin riego frecuente — se marchita rápido.",
  whyRationale: "Originaria de regiones tropicales del sudeste asiático, su crecimiento depende de día largo y calor. Florecer la convierte en amarga; por eso se pellizca para mantener producción foliar.",
}

// Example huerta entry (tomatera):
{
  careAction: {
    fixed: "Regá profundo cada 2 días en cálido (suelo seco mata el fruto). Tutorá tallos antes de fructificar.",
  },
  placementRecommended: "Pleno sol — 6-8 horas. Suelo profundo con compost.",
  placementAlternatives: ["Maceta de 30+ litros con tutor", "Cantera con tutorado vertical"],
  placementAvoid: "Heladas tardías — la planta no las tolera; sembrá tras última helada.",
  whyRationale: "Anual de origen andino (Sudamérica), domesticada hace ~7000 años. Su fructificación depende del calor (≥18°C nocturno) y la fotoperíodo. Riego inconsistente causa rajado del fruto.",
}
```

The 18 entry IDs (alphabetical within each category):
- Aromaticas: albahaca, ciboulette, cilantro, menta, oregano, perejil, romero, romero-rastrero, tomillo
- Huerta: frutilla, lechuga, pepino, pimiento, rucula, tomate-cherry, tomatera, zanahoria, zapallito
</interfaces>

<existing_patterns>
<!-- Aromaticas + Huerta voseo lexicon. -->

Cosecha verbs (voseo):
- Cosechá / Recolectá (harvest)
- Pellizcá las puntas (pinch tips)
- Cortá (cut/prune)
- Sembrá (sow)
- Trasplantá (transplant)
- Tutorá (stake)
- Aboná / Compostá / Fertilizá (feed)
- Aireá (aerate soil)
- Mulch (mulch — same word in ES; alternative: "acolchá con paja")

Climate / cycle vocabulary:
- "anual" / "bienal" / "perenne" — lifecycle
- "estación cálida" / "fría" — water-mode-aligned
- "después de la última helada" — frost-safety
- "fotoperíodo" / "día corto/largo" — flowering trigger (cilantro is short-day, lechuga is long-day)
- "polinización" / "fructificación" — fruit-set
</existing_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 5 EDU-02 fields × 18 entries (9 aromaticas + 9 huerta) × 2 locales + matching catalog defaults</name>
  <files>src/data/plantDatabase.ts, src/i18n/locales/en/plants.json, src/i18n/locales/es/plants.json</files>
  <read_first>
    - src/data/plantDatabase.ts lines 425-580 + 914-1110 + 1220-1270 (the 18 aromaticas + huerta entries; multi-section due to v1.1 + Phase 8 + extra additions)
    - src/i18n/locales/es/plants.json (existing 18 aromaticas + huerta entry blocks)
    - src/i18n/locales/en/plants.json (existing 18 aromaticas + huerta entry blocks)
    - .planning/phases/14-educational-detail-modal/14-04-PLAN.md + 14-05-PLAN.md (full authoring procedure — apply same procedure here)
    - .planning/phases/14-educational-detail-modal/14-04-SUMMARY.md + 14-05-SUMMARY.md (after prior plans complete — quality lessons)
    - CLAUDE.md §"Internationalization (i18n)"
    - scripts/check-i18n-keys.mjs (validator extension from Plan 14-01)
  </read_first>
  <action>
    **Same authoring procedure as Plans 14-04 + 14-05**, applied to 18 entries across two categories. Author each entry's 5 fields in plantDatabase.ts (catalog defaults), mirror to en/plants.json + es/plants.json with locale parity, voseo for ES.

    **Aromaticas-specific authoring (9 entries):**
    - **careAction.fixed** typically references both watering AND harvesting: "Regá cada N días; pellizcá las puntas semanalmente para mantener compactez."
    - **placementRecommended** is mostly "Sol pleno" or "Sol parcial — luz mañanera prioridad" (most aromaticas need 4-8h sun).
    - **placementAvoid** for aromaticas: heat stress without water (albahaca), excess winter cold (oregano in Patagonia), waterlogging (romero, tomillo Mediterranean preferences).
    - **whyRationale** by origin:
      - Mediterráneo (oregano, romero, romero-rastrero, tomillo): drought-adapted, oils protect from heat, well-drained soil critical
      - Frigorial / templado (cilantro, perejil, ciboulette): cool-season; cilantro bolts in heat (short day)
      - Tropical (albahaca): warm-season annual; flowers go bitter
      - Subtropical húmedo (menta): aggressive runner roots; needs containment
    - **Special: romero vs romero-rastrero** distinct whyRationale (rastrero is the prostrate variety — "menos altura, mismo origen mediterráneo, más usada como cubresuelo").

    **Huerta-specific authoring (9 entries):**
    - **careAction.fixed** typically references PROFOUND watering + STAKING + FRUIT-set:
      - tomatera/tomate-cherry: "Regá profundo cada 2 días en cálido. Tutorá tallos. Fertilizá durante fructificación con NPK alto en potasio."
      - lechuga/rucula: "Regá frecuente y suave; cosecha hojas exteriores (corte de hoja escalera)."
      - frutilla: "Regá al pie (no las hojas — hongos); mulch con paja para evitar contacto del fruto con tierra."
      - zanahoria: "Regá uniforme — siembras en línea; aclará plantines a 5cm cada uno."
    - **placementRecommended** for huerta: "Pleno sol — 6-8 horas mínimo" (most warm-season crops). Lechuga/rucula are exceptions: "Sol parcial en verano para evitar bolting."
    - **placementAvoid** for huerta:
      - Solanáceas (tomatera/tomate-cherry/pimiento) + cucurbitáceas (pepino/zapallito): heladas tardías = muerte; soil-borne pests (no rotación)
      - Cool-season (lechuga/rucula): pleno sol del verano = bolting + amargor
      - Frutilla: contacto con tierra húmeda (hongo Botrytis)
    - **whyRationale** by ecology:
      - tomatera/tomate-cherry/pimiento: solanáceas andinas; need ≥18°C nocturno + alto fósforo para fructificar
      - pepino/zapallito: cucurbitáceas; polinización por insectos crítica; flores macho y hembra separadas
      - lechuga/rucula: brassicáceas (rucula) + asteráceas (lechuga) — long-day → bolt; cosechan ANTES de florecer
      - zanahoria: bienal; raíz formada en año 1, flor en año 2 (no se deja florecer en huerta)
      - frutilla: rosáceas; estolones (runners) propagación; perenne pero productividad cae año 3+
      - rucula: rapidísima (4-6 sem ciclo); brassicáceas, sabor picante por glucosinolatos

    All other implementation rules from Plans 14-04/05 apply: voseo discipline, per-field char limits, sub-field independence, locale parity, no Castilian regression, JSON syntax preserved.

    Critical implementation requirements:
    - **Cumulative validator state.** After this plan, 54 entries declare 5 new fields each in en + es.
    - **Cosecha lexicon for both categories.** Both aromaticas and huerta share harvest-cycle care; reuse "cosechá / pellizcá / cortá" verbs across both batches.
    - **Solanáceas / cucurbitáceas / brassicáceas terminology in whyRationale.** Spanish-speaking gardeners recognize these family names; using them adds horticultural depth without academic stiffness.
    - **frutilla, tomate-cherry, tomatera nuance.** frutilla and tomate-cherry are NOT a "frutal" classification (they're huerta); their whyRationale should NOT cite "frutal cycle" — they're ANNUAL (tomate-cherry) or perennial-but-replanted-yearly (frutilla in cultivation).
    - **romero is in aromaticas TWICE** (romero, romero-rastrero) — distinct content per Plan 14-05 lavanda guidance.
    - **Voseo grep guard.** Same as 14-05.

    Do NOT:
    - Use exclamation marks.
    - Reference Phase 17+ entries (espinaca, eneldo, stevia, olivo, arandano).
    - Skip locale parity.
    - Use careAction.soilCheck for any aromaticas/huerta entry — none have waterMode === 'soil_check' in the catalog (verify by grep). All use careAction.fixed.
    - Author tomatera and tomate-cherry with identical strings — they ARE the same species pattern, but tomate-cherry has SMALLER fruit + different harvest cadence (cosechás casi diariamente vs semanalmente).
  </action>
  <verify>
    <automated>npm run check:i18n-keys && npx tsc --noEmit && node scripts/smoke-phase14.mjs && node -e "const j = require('./src/i18n/locales/es/plants.json'); const ids = ['albahaca','ciboulette','cilantro','menta','oregano','perejil','romero','romero-rastrero','tomillo','frutilla','lechuga','pepino','pimiento','rucula','tomate-cherry','tomatera','zanahoria','zapallito']; let n=0; for (const id of ids) { if (j[id]?.whyRationale) n++; } console.log('whyRationale-coverage:', n + '/18'); process.exit(n >= 16 ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run check:i18n-keys` exits 0 (cumulative 54 entries × 5 fields × 2 locales validated)
    - `npx tsc --noEmit` exits 0
    - `node scripts/smoke-phase14.mjs 2>&1 | grep -c "^FAIL"` returns 0
    - whyRationale coverage on 18 entries: ≥ 16/18 (≥89%)
    - careAction coverage on 18 entries: 18/18 (100%)
    - placementRecommended coverage on 18 entries: ≥ 14/18 (≥78%)
    - JSON syntax valid: `cat src/i18n/locales/{en,es}/plants.json | python3 -m json.tool > /dev/null` exits 0 for both
    - Locale parity: `node -e "const en = require('./src/i18n/locales/en/plants.json'); const es = require('./src/i18n/locales/es/plants.json'); const ids = ['albahaca','ciboulette','cilantro','menta','oregano','perejil','romero','romero-rastrero','tomillo','frutilla','lechuga','pepino','pimiento','rucula','tomate-cherry','tomatera','zanahoria','zapallito']; let drift = []; for (const id of ids) { const e = Object.keys(en[id] ?? {}).sort(); const s = Object.keys(es[id] ?? {}).sort(); if (JSON.stringify(e) !== JSON.stringify(s)) drift.push(id); } console.log('parity-drift:', drift); process.exit(drift.length === 0 ? 0 : 1)"` exits 0
    - Voseo grep guard: ES `grep -cE "\\b(riega|saca|puedes|quieres|mueve|toca|haz)\\b" src/i18n/locales/es/plants.json` count is unchanged from Plan 14-05 post-plan baseline
    - Cosecha lexicon present: `grep -cE "cosechá|pellizcá|tutorá|sembrá" src/i18n/locales/es/plants.json` ≥ 5
    - tomatera ≠ tomate-cherry rationale: `node -e "const j = require('./src/i18n/locales/es/plants.json'); const t = j['tomatera']?.whyRationale; const c = j['tomate-cherry']?.whyRationale; console.log('distinct:', t !== c); process.exit(t && c && t !== c ? 0 : 1)"` exits 0
    - romero ≠ romero-rastrero rationale: `node -e "const j = require('./src/i18n/locales/es/plants.json'); const r = j['romero']?.whyRationale; const rr = j['romero-rastrero']?.whyRationale; console.log('distinct:', r !== rr); process.exit(r && rr && r !== rr ? 0 : 1)"` exits 0
  </acceptance_criteria>
  <done>
    All 18 aromaticas + huerta catalog entries gain 5 new educational fields populated in plantDatabase.ts AND matching i18n keys in BOTH plants.json files (voseo for ES). Aromaticas-specific tone (Mediterráneo / Frigorial / Tropical / Subtropical origins; pellizcá / cosechá verbs); Huerta-specific tone (annual cycle, fruit-set photoperiod, tutorá staking). tomatera ≠ tomate-cherry, romero ≠ romero-rastrero. `npm run check:i18n-keys` + `npx tsc --noEmit` + `node scripts/smoke-phase14.mjs` all green. Locale parity verified for all 18 entries.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:

```bash
# 1. Cumulative catalog content (15 + 21 + 18 = 54 entries)
grep -c "whyRationale" src/data/plantDatabase.ts                                  # ≥ 49 (cumulative)
grep -c "careAction" src/data/plantDatabase.ts                                    # ≥ 54

# 2. i18n keys cumulative
grep -c "whyRationale" src/i18n/locales/es/plants.json                            # ≥ 49
grep -c "whyRationale" src/i18n/locales/en/plants.json                            # ≥ 49

# 3. JSON syntax + gates
cat src/i18n/locales/{en,es}/plants.json | python3 -m json.tool > /dev/null
npm run check:i18n-keys                                                           # exits 0
npx tsc --noEmit                                                                  # exits 0
node scripts/smoke-phase14.mjs                                                    # exits 0

# 4. Coverage check (≥16/18 aromaticas+huerta)
node -e "const j = require('./src/i18n/locales/es/plants.json'); const ids = ['albahaca','ciboulette','cilantro','menta','oregano','perejil','romero','romero-rastrero','tomillo','frutilla','lechuga','pepino','pimiento','rucula','tomate-cherry','tomatera','zanahoria','zapallito']; let n=0; for (const id of ids) { if (j[id]?.whyRationale) n++; } process.exit(n >= 16 ? 0 : 1)"

# 5. Locale parity
node -e "const en = require('./src/i18n/locales/en/plants.json'); const es = require('./src/i18n/locales/es/plants.json'); const ids = ['albahaca','ciboulette','cilantro','menta','oregano','perejil','romero','romero-rastrero','tomillo','frutilla','lechuga','pepino','pimiento','rucula','tomate-cherry','tomatera','zanahoria','zapallito']; let drift = []; for (const id of ids) { const e = Object.keys(en[id] ?? {}).sort(); const s = Object.keys(es[id] ?? {}).sort(); if (JSON.stringify(e) !== JSON.stringify(s)) drift.push(id); } process.exit(drift.length === 0 ? 0 : 1)"

# 6. Phase 10 grep guard
grep -rc "EXPO_PUBLIC_PERENUAL_API_KEY" src/ .env .env.example app.json 2>/dev/null
```
</verification>

<success_criteria>
1. All 18 aromaticas + huerta catalog entries gain 5 EDU-02 educational fields in src/data/plantDatabase.ts.
2. Matching i18n keys in BOTH plants.json files with locale parity.
3. ES voseo discipline preserved; no NEW Castilian forms (grep guard unchanged from Plan 14-05 baseline).
4. whyRationale on ≥16/18 entries; cites Mediterráneo / Frigorial / Tropical / Subtropical origin (aromaticas) OR annual cycle / family-level ecology (huerta).
5. careAction on 18/18 entries; placementRecommended on ≥14/18.
6. tomatera ≠ tomate-cherry; romero ≠ romero-rastrero (distinct strings, no copy-paste).
7. `npm run check:i18n-keys` exits 0 (cumulative 54 entries × 5 fields × 2 locales validated); `npx tsc --noEmit` exits 0; `node scripts/smoke-phase14.mjs` reports 0 FAILs.
8. JSON syntax valid in both plants.json files.
9. Phase 10 grep guard remains clean.
</success_criteria>

<output>
After completion, create `.planning/phases/14-educational-detail-modal/14-06-SUMMARY.md` documenting:
- Per-entry coverage table for 18 entries (aromaticas + huerta)
- Voseo regression grep result vs Plan 14-05 post-plan baseline
- Origin/ecology distribution (how many Mediterráneo / Frigorial / Tropical aromaticas + how many solanáceas / cucurbitáceas / brassicáceas / etc. huerta)
- tomatera/tomate-cherry distinguishing rationale (paste both)
- romero/romero-rastrero distinguishing rationale (paste both)
- Cumulative phase progress: 54/64 entries (84%) have full EDU-02/03 content
- Note that Plan 14-07 (frutales + suculentas, 10 entries) starts next, depends on this plan
</output>
