---
phase: 22-gamification-toasts-haptics
plan: 03
type: execute
wave: 3
depends_on:
  - "22-02"
files_modified: []
autonomous: false
requirements:
  - GAM-01
  - GAM-02
  - GAM-05
must_haves:
  truths:
    - "Full automation gate re-verified GREEN at plan-start time (npx tsc --noEmit + npm run check:i18n-keys + smoke 18+19+20+21+22)"
    - "14-item Blocks A-E device-test checklist presented to the user (Block A = Toast on Today, Block B = Toast on Calendar, Block C = Toast on Plants via fertilize, Block D = Haptic on iOS+Android, Block E = GAM-05 anti-pattern visual sweep)"
    - "User selects Option A (run-now device test) OR Option B (defer to v1.2 milestone-end backlog memory per established Phase 18-05 / 19-07 / 20-10 / 21-06 precedent)"
    - "If Option B selected: 14-item checklist appended verbatim to /Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md mirroring Phase 21-06 entry structure"
    - "Phase 22 CLOSED at code level; all 3 requirement IDs (GAM-01/02/05) reach closing PASS state"
  artifacts:
    - path: ".planning/phases/22-gamification-toasts-haptics/22-03-SUMMARY.md"
      provides: "Closure record documenting Option A vs Option B path + automation gate counts + per-item results (if Option A) or memory append confirmation (if Option B)"
      contains: "Option"
  key_links:
    - from: "Plan 22-03 checkpoint"
      to: "/Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md"
      via: "Option B path: append 14-item checklist verbatim alongside Phase 18 / 20 / 21 entries"
      pattern: "Phase 22"
---

<objective>
Manual checkpoint plan closing Phase 22. Mirror the established v1.2 closing-gate pattern from Plan 18-05 / 19-07 / 20-10 / 21-06: (1) re-verify the full automation suite green at plan-start; (2) present a 14-item Blocks A-E device-test checklist; (3) offer Option A (run-now) vs Option B (defer to v1.2 milestone-end backlog memory).

Per the v1.2 precedent stack (Phase 18-05 = run-now executed; Phase 19-07 / 20-10 / 21-06 = Option B auto-selected via orchestrator), this plan defaults to surfacing both options to the user. The orchestrator can auto-select Option B via the established `--auto --no-transition` chain flag if invoked in autonomous-closure mode.

GAM-01/02/05 reach closing PASS state at code level via the smoke runner. Device-level visual + haptic verification is the only remaining gate. The 14-item checklist captures the manual-only behaviors enumerated in 22-VALIDATION.md § Manual-Only Verifications.

Purpose: Close Phase 22 at code level; document device-test backlog for v1.2 milestone-end batch testing.
Output: 0 source file changes; 1 memory backlog append (Option B path) OR 0 changes if Option A run-now succeeds; SUMMARY.md documents the path taken.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-gamification-toasts-haptics/22-CONTEXT.md
@.planning/phases/22-gamification-toasts-haptics/22-VALIDATION.md
@.planning/phases/22-gamification-toasts-haptics/22-00-PLAN.md
@.planning/phases/22-gamification-toasts-haptics/22-01-PLAN.md
@.planning/phases/22-gamification-toasts-haptics/22-02-PLAN.md

# Precedent — Option A vs Option B closing gate pattern
@.planning/phases/21-plant-journal/21-06-PLAN.md
@.planning/phases/20-fertilization-subsystem/20-10-PLAN.md

# Option B append target
@/Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-checkpoint automation gate — re-verify the full suite green</name>
  <files></files>
  <read_first>
    - .planning/phases/22-gamification-toasts-haptics/22-VALIDATION.md § Manual-Only Verifications
    - .planning/STATE.md (current position should be `Plan 22-02 complete`)
  </read_first>
  <action>
    Run the FULL automation suite and confirm GREEN exit code 0 on every command. Record the exact PASS/FAIL/SKIP counts in the SUMMARY at the end of the plan. The suite (per 22-VALIDATION.md § Full suite command):

    1. `npx tsc --noEmit` (must exit 0)
    2. `npm run check:i18n-keys` (must exit 0)
    3. `npm run smoke:phase18` (must exit 0; FAIL=0)
    4. `npm run smoke:phase19` (must exit 0; FAIL=0)
    5. `npm run smoke:phase20` (must exit 0; FAIL=0)
    6. `npm run smoke:phase21` (must exit 0; FAIL=0)
    7. `npm run smoke:phase22` (must exit 0; FAIL=0)

    Capture stdout for each run. If ANY command fails, STOP and surface the failure to the user — do NOT proceed to checkpoint. Failure here means Plan 22-01 or 22-02 left a regression that must be remediated BEFORE the manual gate via `/gsd:plan-phase 22 --gaps`.

    Expected GREEN state (per 22-VALIDATION.md per-task verification map and prior phase patterns):
    - smoke-phase18 PASS >= 56
    - smoke-phase19 PASS >= 85
    - smoke-phase20 PASS >= 49
    - smoke-phase21 PASS >= 76
    - smoke-phase22 PASS >= 45 (W0 scaffold + cross-phase + all GAM-01/02/05 placeholders now flipped from Plans 22-01/22-02)

    DO NOT proceed past this task if ANY automation command fails.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:i18n-keys &amp;&amp; npm run smoke:phase18 &amp;&amp; npm run smoke:phase19 &amp;&amp; npm run smoke:phase20 &amp;&amp; npm run smoke:phase21 &amp;&amp; npm run smoke:phase22</automated>
  </verify>
  <acceptance_criteria>
    - All 7 commands exit 0
    - Each smoke runner reports FAIL=0
    - smoke-phase22 reports PASS >= 45 with all GAM-01 / GAM-02 / GAM-05 sentinels flipped from SKIP to PASS at this point
    - Cross-phase regression chain green (Phase 18 + 19 + 20 + 21 sentinels in smoke-phase22 all PASS)
  </acceptance_criteria>
  <done>
    Full automation suite confirmed GREEN at plan-start. Counts captured for SUMMARY.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Manual device-test checkpoint — Option A (run now) vs Option B (defer to v1.2 backlog)</name>
  <files></files>
  <read_first>
    - .planning/phases/22-gamification-toasts-haptics/22-VALIDATION.md § Manual-Only Verifications
    - .planning/phases/21-plant-journal/21-06-PLAN.md (Task 2 — Option A/B closing-gate template; mirror its action body shape exactly)
    - .planning/phases/20-fertilization-subsystem/20-10-PLAN.md (prior closing-gate precedent)
    - /Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md (existing v1.2 backlog — Phase 22 entry appends here on Option B path)
  </read_first>
  <action>
    Present the 14-item Blocks A-E checklist to the user verbatim and ask them to choose Option A (run now) or Option B (defer to v1.2 backlog). Claude pauses for user input — DO NOT proceed with any automation in this task.

    ---

    **What was built (summary for user):**

    All 3 GAM-01/02/05 requirement IDs are CLOSED at code level:
    - GAM-01 — Toast component shown on task completion "¡Vas bien! 🌱" / "You're on it! 🌱" (EN); 2 s auto-dismiss; wired in 3 screens (PlantsScreen + TodayScreen + CalendarScreen) via `gamificationToastVisible` state distinct from Phase 18 `toastVisible` + Phase 21 `journalToastVisible`. Single source of truth: `setOnTaskCompleted(cb | null)` setter on StorageContext; each screen registers `setGamificationToastVisible(true)` via useEffect with cleanup.
    - GAM-02 — `triggerHaptic('success')` (Haptics.NotificationFeedbackType.Success) fires inside ALL 4 useStorage task actions (waterPlant, sunPlant, outdoorPlant, fertilizePlant) BEFORE updatePlant. New actions waterPlant/sunPlant/outdoorPlant mirror the existing Plan 20-04 fertilizePlant shape. TodayScreen + CalendarScreen handlers migrated to call the new actions; PlantsScreen does NOT migrate (collection-mode lacks task chips — fertilize flow via MyPlantDetailModal preserved).
    - GAM-05 — STRICT negative-grep over `src/` for `streak | consecutiveDays | dayCount | currentStreak | bestStreak | streakReset` with `CARE_STREAKS` (V2.0 placeholder at features.ts:31) + `gam_anti_patterns.md` whitelist; 4 anti-pattern comment blocks (literal `GAM-05 lock`) anchor the 4 task actions in useStorage.tsx; mood emoji (Phase 18 GAM-03/04) remains the only health affordance — no streak UI anywhere.

    Pitfall 3 (toggle semantics): sun/outdoor actions condition haptic+Toast on `wasUndone` boolean — celebration fires ONLY on transition TO done, NOT on undo. Water + fertilize are not toggles.

    All 7 automation gates green (Task 1 of this plan).

    ---

    **14-item Blocks A-E manual device-test checklist** (12 hard-fail + 2 soft-fail):

    **Block A — Toast on TodayScreen (4 hard-fail)**
    - A1: Open TodayScreen. Plant with water task due TODAY. Tap "Regar" chip. EXPECTED: (a) NotificationFeedbackType.Success haptic (single short distinct buzz); (b) Toast "¡Vas bien! 🌱" centered ~bottom 25% screen, fully visible above tab bar; (c) auto-dismiss ~2 s.
    - A2: Open TodayScreen. Plant with sun task. Tap "Sol" chip. EXPECTED: same as A1.
    - A3: Open TodayScreen. Plant with outdoor task. Tap "Sacar afuera". EXPECTED: same as A1.
    - A4: Tap an already-done sun chip (undo direction). EXPECTED: NO haptic, NO Toast, chip toggles back. (Pitfall 3 toggle semantics — celebration only on TO-done.)

    **Block B — Toast on CalendarScreen (3 hard-fail)**
    - B1: Open CalendarScreen. Tap TODAY's cell. Day-detail modal opens. Tap "Regar" on a plant. EXPECTED: haptic + Toast (CalendarScreen's FIRST Toast surface).
    - B2: Open CalendarScreen. Tap YESTERDAY's cell. Tap "Regar" on a plant. EXPECTED: water back-dated to yesterday; NO haptic, NO Toast (selectedDate !== todayStr branch preserved).
    - B3: Open CalendarScreen. Tap TODAY's cell. Tap "Sacar afuera" on outdoor plant. EXPECTED: haptic + Toast.

    **Block C — Toast on PlantsScreen via Fertilize (2 hard-fail)**
    - C1: Open PlantsScreen. Long-press a plant card -> MyPlantDetailModal opens. Navigate to "¿Qué hacer?". Tap fertilize TaskButton. EXPECTED: haptic + Toast on PlantsScreen (focused screen receives callback).
    - C2: Open PlantsScreen. Trigger Phase 18 swipe-delete. EXPECTED: Phase 18 swipe-undo Toast (4 s, "Plant deleted" + Undo). NO gamification Toast. (Toast independence verification — Phase 18 wiring preserved.)

    **Block D — Haptic Hardware on Both Platforms (2 hard-fail + 1 soft-fail)**
    - D1: iOS device. Complete A1 again. EXPECTED: distinct UINotificationFeedbackType.Success buzz (sharp, distinct from Phase 18 impactMedium swipe-commit haptic). HARD-fail.
    - D2: Android device. Complete A1 again. EXPECTED: haptic fires. SOFT-fail allowed (OEM variability — Pitfall 7). Report OEM + model.
    - D3: iOS device. Tap an already-done outdoor chip (undo). EXPECTED: NO haptic, NO Toast. HARD-fail (toggle semantics on outdoor).

    **Block E — GAM-05 Anti-Pattern Visual Sweep (1 hard-fail + 1 soft-fail)**
    - E1: Scroll every primary screen (Today, Plantas, Calendar, Explore, Settings). EXPECTED: NO "N-day streak", NO "reset count", NO "consecutive days", NO score number visible. Mood emoji is the only health affordance. HARD-fail if any streak-style UI visible.
    - E2: Open MyPlantDetailModal on any plant. Scroll all sections. EXPECTED: NO streak counter inside modal. Diario date headers ("Hoy"/"Ayer"/"Hace N días") are ACCEPTED (they are date labels, not streaks). SOFT-fail if ambiguous label found.

    Total: 14 items — 12 hard-fail (A1-4, B1-3, C1-2, D1, D3, E1) + 2 soft-fail (D2, E2).

    ---

    **Choose ONE option:**

    **Option A — Run all 14 items now on device.**
    User runs the checklist on iOS dev client + Android dev client. Reports back PASS/FAIL per item. If any hard-fail item fails, plan re-enters with `/gsd:plan-phase 22 --gaps`. If all 12 hard-fail items pass and 2 soft-fail items are acceptable, Phase 22 closes fully.

    **Option B — Defer to v1.2 backlog memory** (per Phase 18-05 / 19-07 / 20-10 / 21-06 milestone-end batching precedent).
    The 14-item checklist is appended verbatim to `/Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md` with section heading `## Phase 22 — Gamification (Toasts + Haptics) (deferred YYYY-MM-DD)` + 12-hard / 2-soft classifier, mirroring the Phase 21-06 entry structure exactly. Sub-sections: `### What was built (code-level CLOSED)` + `### Device-test checklist` + `### Why deferred` + `### Pre-submission unblock`. Insert BEFORE the existing pre-submission session structure section in the file. Update the pre-submission comment block to enumerate Phase 22 alongside Phase 18 38-item / Phase 20 14-item / Phase 21 14-item checklists. Phase 22 closes at code level immediately; device tests run as part of end-of-v1.2 batch before App Store / Google Play submission.

    **Resume signals:**
    - `option-a` — I will run the 14-item checklist now and report results.
    - `option-b` — Defer to v1.2 backlog memory; close Phase 22 at code level.
    - Or describe a specific item that needs adjustment.

    **If user selects Option A:**
    Walk through each of the 14 items. Record PASS/FAIL in SUMMARY. If any hard-fail item FAILS, surface to orchestrator for `/gsd:plan-phase 22 --gaps` remediation. Plan completes only after all hard-fail items resolved.

    **If user selects Option B:**
    Append the 14-item checklist verbatim to `/Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md` with section heading + 12-hard / 2-soft classifier following Phase 21-06 entry shape. Plan completes immediately at code level.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-gamification-toasts-haptics/22-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - User responds with `option-a` OR `option-b` (or a specific item adjustment request)
    - If Option A: all 12 hard-fail items report PASS; 2 soft-fail items acceptable or noted; SUMMARY records per-item PASS/FAIL per device (iOS + Android)
    - If Option B: 14-item checklist appended verbatim to `v1_2_test_backlog.md` memory with `## Phase 22 — Gamification (Toasts + Haptics) (deferred <date>)` heading + 12-hard / 2-soft classifier + 4 sub-sections matching Phase 21-06 entry; SUMMARY records timestamp of append + line count delta
    - `.planning/phases/22-gamification-toasts-haptics/22-03-SUMMARY.md` exists and documents the decision lane taken (Option A or Option B) along with automation gate counts from Task 1
  </acceptance_criteria>
  <done>
    User has resolved the Option A vs Option B checkpoint. Phase 22 closes at code level either way; manual device-test results either captured in SUMMARY (Option A) or queued to v1_2_test_backlog.md memory (Option B). GAM-01/02/05 all at closing PASS state.
  </done>
</task>

</tasks>

<verification>
After Task 1 + Task 2 complete:
1. Full automation suite GREEN at start (Task 1)
2. User responds to checkpoint with Option A or Option B selection (Task 2)
3. If Option B: v1_2_test_backlog.md contains a new Phase 22 entry with 14-item checklist + 4 sub-sections matching Phase 21-06 entry structure exactly
4. If Option A run-now: 12/12 hard-fails PASS + 2/2 soft-fails noted; SUMMARY records device results
5. STATE.md updated to mark Phase 22 CLOSED at code level (orchestrator handles this)
</verification>

<success_criteria>
- Pre-checkpoint automation suite re-verified GREEN (Task 1)
- 14-item Blocks A-E checklist presented verbatim with HARD-fail / SOFT-fail markings (Task 2)
- User selects Option A or Option B (or orchestrator auto-selects Option B per v1.2 chain precedent)
- If Option B: v1_2_test_backlog.md append landed verbatim mirroring Phase 21-06 entry structure
- If Option A: 12/12 hard-fails confirmed PASS on both iOS + Android; soft-fails documented
- Phase 22 CLOSED at code level; all 3 requirement IDs (GAM-01/02/05) at closing PASS state
- 0 source file changes in this plan (verification-only / memory-backlog-only)
</success_criteria>

<output>
After completion, create `.planning/phases/22-gamification-toasts-haptics/22-03-SUMMARY.md` covering:
- Path taken (Option A run-now vs Option B defer)
- Automation suite counts at pre-checkpoint gate (each smoke PASS/FAIL/SKIP)
- If Option A: per-item device-test results table (14 items × 2 devices)
- If Option B: backlog append confirmation (file path, line count delta, mirroring Phase 21-06 shape)
- Phase 22 close-state: GAM-01/02/05 all PASS at code level; manual gate either CLEARED (Option A 12/12 HARD-PASS) or BACKLOGGED (Option B)
- Carry-forward: Phase 23 (Polish — UAT Fixes + Brand Voice) ready to plan
</output>
