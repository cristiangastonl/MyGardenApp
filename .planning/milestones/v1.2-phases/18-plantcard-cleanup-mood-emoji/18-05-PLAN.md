---
phase: 18-plantcard-cleanup-mood-emoji
plan: 05
type: execute
wave: 5
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified: []
autonomous: false
requirements:
  - CARD-01
  - CARD-02
  - CARD-03
  - CARD-04
  - CARD-05
  - GAM-03
  - GAM-04
must_haves:
  truths:
    - "User confirms swipe-to-delete feel + haptic on real iOS device"
    - "User confirms swipe-to-delete feel + haptic on real Android device"
    - "User confirms long-press menu opens BottomSheetModal with no z-order conflict against PaywallModal"
    - "User confirms mood emoji is visible across all 4 health states and visually contrasts on dark plant images (Pitfall 7)"
    - "User confirms Toast positioning above tab bar + FAB (Pitfall 8)"
    - "User confirms first-card affordance hint fires once and stays dismissed across kill/relaunch (CARD-04 persistence)"
    - "User confirms FlatList row recycling does not leak swipe state (Pitfall 5) — scroll fast through 20+ plants"
  artifacts: []
  key_links: []
---

<objective>
Manual verification gate for Phase 18. The four prior plans land everything that is automatable; this plan locks the device-only verifications that the smoke runner cannot reach (haptic feedback, gesture feel, real BottomSheetModal z-order, animation timing, screen-reader announcements, FlatList recycling under load).

This plan is `autonomous: false` and gated by `checkpoint:human-verify` per the Phase 13 Plan 13-03 + Phase 14 Plan 14-08 precedent.

Prerequisite — before signoff:
- `npx tsc --noEmit` exits 0
- `npm run check:i18n-keys` exits 0
- `node scripts/smoke-phase18.cjs` exits 0 with zero unexplained SKIPs
- `npm run smoke:phase17` exits 0 (cross-phase regression sentinel)
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-plantcard-cleanup-mood-emoji/18-VALIDATION.md
@.planning/phases/18-plantcard-cleanup-mood-emoji/18-RESEARCH.md
@.planning/phases/18-plantcard-cleanup-mood-emoji/18-CONTEXT.md
@.planning/phases/18-plantcard-cleanup-mood-emoji/18-04-SUMMARY.md

# Manual verification checklist source — VALIDATION.md §"Manual-Only Verifications" + RESEARCH.md §"Common Pitfalls" 1, 5, 7, 8, 10
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pre-checkpoint sanity sweep (automation gate)</name>
  <files>n/a — read-only verification</files>
  <read_first>
    - .planning/phases/18-plantcard-cleanup-mood-emoji/18-04-SUMMARY.md
  </read_first>
  <action>
    Run the full automation gate as a single block before presenting the manual checkpoint. If any of these fail, the manual checkpoint is BLOCKED — surface failure and exit. Do NOT proceed to Task 2 if any fail.

    Commands (run sequentially):
    ```
    npx tsc --noEmit
    npm run check:i18n-keys
    node scripts/smoke-phase18.cjs
    npm run smoke:phase17
    ```

    For `node scripts/smoke-phase18.cjs`: parse the output line `[Phase 18 smoke] PASS=N FAIL=M SKIP=K`. Assert `FAIL === 0`. Assert `SKIP === 0` OR enumerate any remaining SKIPs as INTENTIONAL manual-only deferrals (cross-reference 18-VALIDATION.md §"Manual-Only Verifications" — those rows do not have automated commands).

    Output a one-paragraph summary block with:
    - Toolchain status (4 commands × exit code)
    - Phase 18 smoke counts
    - Cross-phase smoke (Phase 17) count
    - Any unexpected SKIPs surfaced with rationale
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run check:i18n-keys &amp;&amp; node scripts/smoke-phase18.cjs &amp;&amp; npm run smoke:phase17</automated>
  </verify>
  <acceptance_criteria>
    - All four commands exit 0.
    - `[Phase 18 smoke] FAIL=0` in output.
    - Phase 17 smoke is unchanged (catalog routing intact at 118 entries).
    - Summary paragraph created in execution output explicitly enumerating any remaining SKIPs (expected: 0 file-content SKIPs; manual-only checks are out of band).
  </acceptance_criteria>
  <done>Automation gate green; ready for human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual device verification — Phase 18 acceptance</name>
  <files>n/a — manual checkpoint, no file edits</files>
  <read_first>
    - .planning/phases/18-plantcard-cleanup-mood-emoji/18-VALIDATION.md §"Manual-Only Verifications"
    - .planning/phases/18-plantcard-cleanup-mood-emoji/18-CONTEXT.md (full file — frame the user's expectation set)
  </read_first>
  <action>
    PAUSE for human verification. Present the user with the build instructions + device test checklist below; wait for an explicit "approved" or per-block issue report. Do NOT auto-resolve — this is a blocking checkpoint.

    **What was built (summarize for the user):**
    - 5-element card: image (with mood emoji overlay) + name + 1 task or none + water badge + (mood emoji as the 5th).
    - Swipe-left to reveal red Eliminar button at ~30% width; full swipe past ~70% commits delete.
    - Swipe completion fires haptic (`Haptics.ImpactFeedbackStyle.Medium`) once at threshold-cross.
    - Optimistic delete: plant disappears immediately; Toast at bottom shows "{name} eliminada" + "Deshacer" action; 4-second window to undo; tap restores plant via `addPlant`.
    - Long-press on a card opens BottomSheetModal with three items: Favorite (toggles), Edit (opens MyPlantDetailModal), Delete (re-uses optimistic + Toast undo path).
    - Mood emoji (🌱/😊/😐/😟) overlaid on bottom-right of the 48×48 plant image; ALWAYS visible regardless of health score; tap opens existing PlantHealthDetail modal.
    - PlantHealthBadge fully removed from PlantCard; PRESERVED in MyPlantDetailModal:203 + index.ts re-export + component file.
    - Tip italic moved from PlantCard to MyPlantDetailModal "¿Qué hacer?" section.
    - First-card affordance hint on PlantsScreen: chevron-peek animation fires on first launch until user completes their first swipe; persists via `@plantcard_swipe_discovered` AsyncStorage flag.
    - Generic Toast component shipped at src/components/Toast.tsx (will be reused by Phase 22 for celebration toasts).

    **How to verify:** Build dev clients on BOTH iOS and Android (or load Expo Go if RN Reanimated v4 + RNGH 2.28 + Bottom Sheet 5.2.13 are compatible — verify on first launch). Then run the following checklist on EACH platform; record observations per row.

    **Block A — Smoke (iOS + Android, Plantas tab):**
    1. Open the Plantas tab. Confirm at least one plant card visible.
    2. Verify the FIRST card animates a chevron-peek (≈12px slide left, springs back) on first launch. (CARD-04)
    3. Swipe a card slowly to ~30%; the red "Eliminar" button reveals; release. Card snaps to revealed position.
    4. Tap the revealed "Eliminar" button. Plant disappears immediately; Toast appears at the bottom with "{name} eliminada" + "Deshacer". (CARD-01 small-swipe path)
    5. Tap "Deshacer" within 4 seconds. Plant restored to the list. Toast slides out. (CARD-01 undo)
    6. Swipe a card aggressively past ~70%. Plant disappears immediately, no reveal step. Toast appears with same UX. (CARD-01 full-swipe path)
    7. Wait 4+ seconds without tapping Deshacer. Toast auto-dismisses; plant deletion is final. Confirm by killing the app and re-opening — plant remains absent. (CARD-01 4s window)

    **Block B — Haptic (iOS + Android, real device — NOT simulator):**
    8. Swipe a card past the COMMIT_THRESHOLD (≈70% width). Confirm a single thump haptic at threshold-cross. NO continuous buzzing. (CARD-05 + Pitfall 2)
    9. Swipe slowly past REVEAL_THRESHOLD only (≈30%). Confirm NO haptic fires at the reveal point. (CARD-05 — only commit threshold fires)
    10. Long-press a card. Confirm a single LIGHT haptic when the menu opens. (CARD-02 long-press feedback)

    **Block C — Long-press menu (iOS + Android):**
    11. Long-press a non-favorited plant. BottomSheetModal rises. Items: "Favorito", "Editar", "Eliminar" (red).
    12. Tap "Favorito". Sheet dismisses. Plant moves to top of list (favorites-first sort). (CARD-02 favorite + sort)
    13. Long-press a favorited plant. Top item now reads "Sacar de favoritos" (i18n parity check).
    14. Tap "Editar". Sheet dismisses. MyPlantDetailModal opens for that plant. (CARD-02 edit)
    15. Long-press another plant; tap "Eliminar". Sheet dismisses; immediately Toast appears with the same undo UX as swipe-Delete. (CARD-02 menu-Delete reuses undo flow)
    16. Open the Plantas tab. Long-press a card. While the sheet is open, somehow trigger a paywall (e.g., try to add a plant beyond the free quota). Confirm sheet auto-dismisses; paywall presents on top with no z-order conflict. (Pitfall 10)

    **Block D — Mood emoji (iOS + Android):**
    17. Confirm every plant card shows a small circular mood emoji at bottom-right of the image. (GAM-03 always-visible)
    18. Find a plant with `excellent` health (recently watered, no overdue tasks). Confirm 🌱.
    19. Find or create a plant with `warning` health (overdue water 2+ days). Confirm 😐. (Mood emoji updates with state — MANUAL test: mark watering overdue 3+ days, kill app, reopen.)
    20. Find or create a plant with `danger` health (overdue 4+ days + sun overdue). Confirm 😟.
    21. Tap the mood emoji on any card. Existing PlantHealthDetail modal opens. (GAM-03 tap target)
    22. Visual check: on a card with a DARK plant image, the mood emoji's 2px white ring keeps it visible. (Pitfall 7)
    23. Confirm NO numeric health score appears on the card itself (only inside HealthDetail modal). (GAM-04)

    **Block E — Toast positioning (iOS + Android):**
    24. Trigger a swipe-delete. Confirm Toast appears CLEARLY ABOVE the bottom tab bar AND above the ExpandedFAB (does not overlap). (Pitfall 8)
    25. Tap the "Deshacer" action button. Confirm it is tappable (not behind FAB or bezel).

    **Block F — FlatList recycling (Pitfall 5):**
    26. Add 20+ plants (or use existing test data). Scroll fast up and down through the list. Confirm NO half-swiped reveals appear on rows other than the one user swiped.
    27. Type in the search bar to filter the list. Confirm no card retains a leftover translateX offset.
    28. Delete a plant near the top via swipe. Confirm no other card visibly shifts or shows the reveal action.

    **Block G — TodayScreen mode parity:**
    29. Switch to Hoy tab. Confirm at least one plant card visible (must have a task today, e.g., water).
    30. Swipe-delete a card on Hoy. Same UX as Plantas: reveal/commit/Toast/Deshacer. (CARD-01 mode parity)
    31. Long-press a card on Hoy. BottomSheetModal opens with same items. (CARD-02 mode parity)
    32. Confirm Hoy does NOT show a chevron-peek hint on the first card. (CARD-04 mode parity — PlantsScreen only)

    **Block H — Persistence:**
    33. Kill the app after performing one successful swipe in Plantas. Reopen. Confirm the chevron-peek hint does NOT fire on the first card. (CARD-04 persistence — `@plantcard_swipe_discovered` flag persisted)
    34. (Optional dev) Reset the flag via dev tools or by clearing AsyncStorage; confirm the hint reappears.

    **Block I — i18n parity (both screens):**
    35. Switch app language to ES (Settings or system). Confirm Toast says "{name} eliminada" + "Deshacer".
    36. Long-press menu in ES: "Favorito" / "Editar" / "Eliminar".
    37. Switch to EN. Confirm Toast says "{name} deleted" + "Undo".
    38. Long-press menu in EN: "Favorite" / "Edit" / "Delete".

    **Pass criteria:** ALL items above produce the expected behavior on BOTH iOS and Android. If any item fails, classify:
    - HARD FAIL (blocking): items 1-15, 17-23, 24-25, 26-28, 30-31, 33, 35-38.
    - SOFT FAIL (note + ship): items 16, 22, 32, 34. Soft fails get an issue raised but do not block Phase 18 closure.

    **Resume signal:** Type "approved" to close Phase 18, or describe issues per block (e.g., "Block B item 8 — no haptic on Android Pixel 6").
  </action>
  <verify>
    <automated>echo "MANUAL CHECKPOINT — awaiting human signoff. No automated verification."</automated>
  </verify>
  <acceptance_criteria>
    - User responds with explicit "approved" string OR a structured per-block issue report.
    - If issues are reported, each is classified as HARD FAIL (block phase closure) or SOFT FAIL (carry to v1.2 device-test backlog memory file per CLAUDE.md milestone-end batching pattern).
    - No HARD FAILs at signoff time.
  </acceptance_criteria>
  <done>Human signoff received; all 38 manual items reviewed; HARD FAILs (if any) escalated.</done>
</task>

</tasks>

<verification>
- Task 1 automation gate green.
- Task 2 user signoff with explicit "approved" OR a documented set of issues that the user accepts as ship-blockers / soft-fails.
</verification>

<success_criteria>
- All 7 Phase 18 requirement IDs (CARD-01..05, GAM-03, GAM-04) verified on both iOS and Android.
- All 5 phase-level Success Criteria (per ROADMAP) verified:
  1. Swipe-left reveals delete affordance; full swipe triggers haptic + confirmation; not instant delete (verified via Block A + Block B).
  2. Long-press reveals overflow menu with favorite, delete, edit (Block C).
  3. Mood emoji always visible (Block D, items 17-23).
  4. Conditional `PlantHealthBadge` removed from PlantCard (verified by grep in Plan 03 acceptance + Block D item 23).
  5. First-render swipe affordance hint shown on first card in PlantsScreen, dismissible (Block A item 2 + Block H items 33-34).
- Manual-only deferrals from 18-VALIDATION.md §"Manual-Only Verifications" all signed off.
- Phase 18 closes with no v1.2 device-test backlog regression (existing v1.2 backlog continues forward to Phase 19+).
</success_criteria>

<output>
After completion, create `.planning/phases/18-plantcard-cleanup-mood-emoji/18-05-SUMMARY.md` documenting:
- Per-block pass/fail counts (i.e., Block A: 7/7 PASS; Block C: 6/6 PASS).
- Any soft-fails carried forward to v1.2 device-test backlog (per CLAUDE.md milestone-end batching pattern — append to v1_2_test_backlog memory if new items emerge).
- Confirmation that `PlantHealthBadge.tsx` file + `MyPlantDetailModal:203` usage + `src/components/index.ts` re-export remain intact at phase end.
- Final smoke runner output paste (`[Phase 18 smoke] PASS=N FAIL=0 SKIP=K`).
- Cumulative phase metrics: total plans (5), total tasks (≈9), files modified across phase, LOC delta estimate.
- Phase 18 closing statement: "Phase 18 complete; PlantCard restructure landed; Toast primitive shipped for Phase 22 reuse."
</output>
