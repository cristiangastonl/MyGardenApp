---
phase: 08-catalog-rebalance
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - src/data/plantDatabase.ts
autonomous: true
requirements: [LIGHT-04, WATER-07, CAT-01, CAT-08]

must_haves:
  truths:
    - "All 38 existing PLANT_DATABASE entries carry expert-vetted lightLevel + waterSchedule + waterMode after this plan"
    - "Phase-4 mechanical mappers (applyColdFactor) are NOT re-invoked on these entries — direct edits are SSOT"
    - "Mediterranean / drought-tolerant outdoor entries (lavanda, romero, tomillo, jazmin, bougainvillea, etc.) have correct 2-3× cold ratios per RESEARCH expert table"
    - "Tropical interior entries (monstera, potus, filodendro, palmera-interior) have correct 1:2 warm:cold ratio"
    - "aloe-vera and jade have cold=30 (not 21); higuera has cold=14 (not 11); dracaena warm=14 cold=21 (not 10/15)"
    - "smoke-phase08 still passes (A1–A8 all green) after every commit"
  artifacts:
    - path: "src/data/plantDatabase.ts"
      provides: "38 entries with expert-vetted lightLevel/waterSchedule/waterMode"
      contains: "PLANT_DATABASE"
  key_links:
    - from: "src/data/plantDatabase.ts entries"
      to: "RESEARCH §Expert-Override Table — All 38 Existing Entries"
      via: "manual data edit per entry"
      pattern: "waterSchedule: \\{ warm:"
    - from: "scripts/smoke-phase08.mjs"
      to: "src/data/plantDatabase.ts"
      via: "A1+A2+A3 assertions over PLANT_DATABASE"
      pattern: "assert.*waterSchedule"
---

<objective>
Wave 1 — replace Phase-4 mechanical `applyColdFactor` heuristic values with the expert-vetted per-entry warm/cold/lightLevel values for all 38 existing catalog entries. Direct edits in `src/data/plantDatabase.ts` (single source of truth). NO mappers re-run.

The override list is divided into 3 commit-friendly tasks by category. Each task corresponds to a logical commit (one task = one category cluster) so that bisecting later is straightforward. Smoke runner re-verifies after each task.

**lavanda is INTENTIONALLY NOT TOUCHED in this plan** — its rename + alias + 2 new variants is Plan 03's atomic unit (RESEARCH Pitfall 1: i18n key rename must be atomic with id rename). The override values for `lavanda-angustifolia` (cold 17→21) land in Plan 03 Task 3 alongside the rename.

Purpose: lock the catalog horticultural truth before downstream consumers (Plans 04+) start reading it.

Output: src/data/plantDatabase.ts with 38 entries carrying expert-vetted values; smoke-phase08 still PASS.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-catalog-rebalance/08-CONTEXT.md
@.planning/phases/08-catalog-rebalance/08-RESEARCH.md
@.planning/phases/08-catalog-rebalance/08-VALIDATION.md
@.planning/phases/08-catalog-rebalance/08-01-SUMMARY.md
@CLAUDE.md

# Source surfaces this plan touches
@src/data/plantDatabase.ts

# Smoke runner that re-verifies after every commit
@scripts/smoke-phase08.mjs

<interfaces>
<!-- The PLANT_DATABASE entry shape executor will edit. From src/data/plantDatabase.ts. -->

Entry shape (current Phase 4 baseline — Plan 02 changes ONLY the v1.1 trio: lightLevel + waterSchedule + waterMode):
```typescript
{
  id: "monstera",
  name: "Monstera",
  scientificName: "Monstera deliciosa",
  category: "interior",
  // ... other static fields ...
  lightLevel: "bright_indirect",                // ← may change
  waterSchedule: { warm: 7, cold: 11 },         // ← may change (cold 11→14 for monstera)
  waterMode: "fixed",                           // ← rarely changes (locked allowlist)
}
```

The exact textual location of each entry's `lightLevel` / `waterSchedule` / `waterMode` lines was set by `scripts/migrate-catalog.mjs` (Phase 4 codemod) — the lines exist as a 3-line block at the END of each entry literal. Edit those 3 lines per entry; do NOT touch the other fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: INTERIOR category — apply expert overrides to 15 entries</name>
  <read_first>
    - src/data/plantDatabase.ts (lines 1–300 to find INTERIOR entries; confirm category-grouped entry order)
    - .planning/phases/08-catalog-rebalance/08-RESEARCH.md §INTERIOR table (lines 156–175)
    - scripts/smoke-phase08.mjs (assertions A1, A2, A3 — entries must remain compliant after edits)
  </read_first>
  <files>
    - src/data/plantDatabase.ts
  </files>
  <action>
    Apply the following per-entry edits to all INTERIOR category entries. These values are copied verbatim from `08-RESEARCH.md` §INTERIOR table. **Only the v1.1 trio is touched** — `lightLevel`, `waterSchedule.warm`, `waterSchedule.cold`, `waterMode`. Do NOT touch other fields.

    **INTERIOR override table (final values; entries with no change still listed for clarity):**

    | id | lightLevel | warm | cold | mode | Change |
    |----|------------|------|------|------|--------|
    | potus | medium_indirect | 7 | **14** | fixed | cold 11→14 (tropical 1:2) |
    | monstera | bright_indirect | 7 | **14** | fixed | cold 11→14 |
    | ficus | bright_indirect | 10 | 15 | fixed | unchanged |
    | sansevieria | medium_indirect | 14 | 21 | fixed | unchanged |
    | orquidea | bright_indirect | 7 | **14** | fixed | cold 11→14 |
    | calathea | medium_indirect | 4 | **7** | fixed | cold 6→7 |
    | cinta | bright_indirect | 5 | **10** | fixed | cold 8→10 |
    | palmera-interior | bright_indirect | 7 | **14** | fixed | cold 11→14 |
    | aloe-vera | direct | 14 | **30** | soil_check | cold 21→30 |
    | espatifilo | medium_indirect | 5 | 8 | fixed | unchanged |
    | dracaena | medium_indirect | **14** | **21** | fixed | warm 10→14, cold 15→21 |
    | filodendro | bright_indirect | 7 | **14** | fixed | cold 11→14 |
    | jade | bright_indirect | 14 | **30** | soil_check | cold 21→30 |
    | peperomia | medium_indirect | 10 | 15 | fixed | unchanged |
    | yuca | direct | 10 | **21** | fixed | cold 15→21 |

    Procedure for each entry that has at least one change:
    1. Find the entry's literal block by `id: "<entry-id>"`.
    2. Locate the 3-line block (`lightLevel: ...`, `waterSchedule: { warm: N, cold: M },`, `waterMode: "..."`) at the end of the entry.
    3. Replace `cold:` with the new value (or warm/lightLevel/mode as the table requires).
    4. Leave all other fields untouched.

    **Entries with `unchanged` need NO edit** — list shown for completeness; verify they still match the table during the verify step.

    After all edits, run:
    - `npx tsc --noEmit` — exit 0 required
    - `node scripts/smoke-phase08.mjs` — PASS N/N required (A2 cold≥warm catches any inversion typo)

    AVOID:
    - Do NOT edit `lavanda` here — Plan 03 owns it (rename + alias must be atomic).
    - Do NOT touch `tip`, `description`, `problems`, `nutrients` — those are i18n-bridged (Plan 04 read-side migration is the right place).
    - Do NOT touch deprecated `sunHours` / `waterDays` fields — leave them for v1.2 cleanup per SCHEMA-08.
    - Do NOT change `category`, `tempMin`, `tempMax`, `humidity`, `outdoor` — orthogonal axes.
    - Do NOT change `waterMode` for interior entries (locked allowlist already correct: aloe-vera, jade are soil_check; rest are fixed).
  </action>
  <verify>
    <automated>
bash -c 'set -e
F=src/data/plantDatabase.ts
# Spot-check key overrides — interior cluster
# potus cold=14
awk "/id: \"potus\"/,/waterMode/" "$F" | grep -q "cold: 14"
# monstera cold=14
awk "/id: \"monstera\"/,/waterMode/" "$F" | grep -q "cold: 14"
# orquidea cold=14
awk "/id: \"orquidea\"/,/waterMode/" "$F" | grep -q "cold: 14"
# calathea cold=7
awk "/id: \"calathea\"/,/waterMode/" "$F" | grep -q "cold: 7"
# cinta cold=10
awk "/id: \"cinta\"/,/waterMode/" "$F" | grep -q "cold: 10"
# palmera-interior cold=14
awk "/id: \"palmera-interior\"/,/waterMode/" "$F" | grep -q "cold: 14"
# aloe-vera cold=30
awk "/id: \"aloe-vera\"/,/waterMode/" "$F" | grep -q "cold: 30"
# dracaena warm=14 AND cold=21
awk "/id: \"dracaena\"/,/waterMode/" "$F" | grep -q "warm: 14"
awk "/id: \"dracaena\"/,/waterMode/" "$F" | grep -q "cold: 21"
# filodendro cold=14
awk "/id: \"filodendro\"/,/waterMode/" "$F" | grep -q "cold: 14"
# jade cold=30
awk "/id: \"jade\"/,/waterMode/" "$F" | grep -q "cold: 30"
# yuca cold=21
awk "/id: \"yuca\"/,/waterMode/" "$F" | grep -q "cold: 21"
# Smoke gate
npx tsc --noEmit
node scripts/smoke-phase08.mjs'
    </automated>
  </verify>
  <acceptance_criteria>
    - All 11 changed interior entries (potus, monstera, orquidea, calathea, cinta, palmera-interior, aloe-vera, dracaena, filodendro, jade, yuca) reflect the new cold/warm values per the table
    - 4 unchanged entries (ficus, sansevieria, espatifilo, peperomia) preserve their Phase 4 values
    - `npx tsc --noEmit` exits 0
    - `node scripts/smoke-phase08.mjs` exits 0 (PASS N/N)
    - No entry has `cold < warm` (A2 gate)
    - `aloe-vera` and `jade` still `waterMode: "soil_check"` (A3 gate)
  </acceptance_criteria>
  <done>
    Interior category fully expert-vetted. Smoke green. Ready for one commit: `feat(08-02): catalog interior expert overrides — 11 entries (potus/monstera/orquidea/calathea/cinta/palmera-interior/aloe-vera/dracaena/filodendro/jade/yuca)`.
  </done>
</task>

<task type="auto">
  <name>Task 2: EXTERIOR + AROMATICAS categories — apply expert overrides to 17 entries (lavanda excluded — Plan 03)</name>
  <read_first>
    - src/data/plantDatabase.ts (find EXTERIOR + AROMATICAS entries)
    - .planning/phases/08-catalog-rebalance/08-RESEARCH.md §EXTERIOR (lines 178–191) and §AROMATICAS (lines 195–204)
    - scripts/smoke-phase08.mjs (assertions A1, A2, A3)
  </read_first>
  <files>
    - src/data/plantDatabase.ts
  </files>
  <action>
    Apply per-entry overrides to EXTERIOR (8 entries — lavanda explicitly EXCLUDED, Plan 03 owns it) and AROMATICAS (8 entries) categories. Verbatim values from RESEARCH.

    **EXTERIOR override table (lavanda OWNED BY PLAN 03 — do NOT touch lavanda here):**

    | id | lightLevel | warm | cold | mode | Change |
    |----|------------|------|------|------|--------|
    | petunia | direct | 2 | **4** | fixed | cold 3→4 |
    | hortensia | bright_indirect | 3 | **7** | fixed | cold 5→7 |
    | jazmin | direct | 4 | **10** | fixed | cold 7→10 (Mediterranean drought) |
    | geranio | direct | 3 | **7** | fixed | cold 5→7 |
    | rosa | direct | 3 | **7** | fixed | cold 5→7 |
    | bougainvillea | direct | 7 | **21** | fixed | cold 12→21 (LARGEST single override; drought 3×) |
    | hibisco | direct | 3 | **7** | fixed | cold 5→7 |
    | margarita | direct | 3 | **7** | fixed | cold 5→7 |

    **AROMATICAS override table:**

    | id | lightLevel | warm | cold | mode | Change |
    |----|------------|------|------|------|--------|
    | albahaca | direct | 2 | **4** | fixed | cold 3→4 |
    | romero | direct | 10 | **21** | fixed | cold 15→21 (Mediterranean) |
    | menta | bright_indirect | 2 | **4** | fixed | cold 3→4 |
    | perejil | bright_indirect | 2 | **4** | fixed | cold 3→4 |
    | oregano | direct | 7 | **14** | fixed | cold 11→14 (Mediterranean) |
    | cilantro | bright_indirect | 2 | **4** | fixed | cold 3→4 |
    | tomillo | direct | 10 | **21** | fixed | cold 15→21 (Mediterranean) |
    | ciboulette | bright_indirect | 3 | **6** | fixed | cold 5→6 |

    Procedure (per entry):
    1. Locate entry by `id: "<entry-id>"`.
    2. Replace the cold value (and warm if listed) in the existing `waterSchedule: { warm: N, cold: M },` line.
    3. lightLevel and waterMode remain unchanged for all 16 entries in this task.

    After all edits:
    - `npx tsc --noEmit` exit 0
    - `node scripts/smoke-phase08.mjs` PASS N/N (A2 catches any cold<warm typo)

    AVOID:
    - **lavanda is owned by Plan 03** — leave it untouched here. Verify `awk '/id: "lavanda"/,/waterMode/' src/data/plantDatabase.ts` still shows `cold: 17` (Phase 4 mechanical value) at end of this task.
    - Do NOT touch tip/description/problems/nutrients (Plan 04 territory).
    - Do NOT change waterMode (all 16 stay 'fixed').
  </action>
  <verify>
    <automated>
bash -c 'set -e
F=src/data/plantDatabase.ts
# EXTERIOR (8 entries — lavanda EXCLUDED)
awk "/id: \"petunia\"/,/waterMode/" "$F" | grep -q "cold: 4"
awk "/id: \"hortensia\"/,/waterMode/" "$F" | grep -q "cold: 7"
awk "/id: \"jazmin\"/,/waterMode/" "$F" | grep -q "cold: 10"
awk "/id: \"geranio\"/,/waterMode/" "$F" | grep -q "cold: 7"
awk "/id: \"rosa\"/,/waterMode/" "$F" | grep -q "cold: 7"
awk "/id: \"bougainvillea\"/,/waterMode/" "$F" | grep -q "cold: 21"
awk "/id: \"hibisco\"/,/waterMode/" "$F" | grep -q "cold: 7"
awk "/id: \"margarita\"/,/waterMode/" "$F" | grep -q "cold: 7"
# lavanda NOT touched (still Phase 4 value)
awk "/id: \"lavanda\"/,/waterMode/" "$F" | grep -q "cold: 17"
# AROMATICAS (8 entries)
awk "/id: \"albahaca\"/,/waterMode/" "$F" | grep -q "cold: 4"
awk "/id: \"romero\"/,/waterMode/" "$F" | grep -q "cold: 21"
awk "/id: \"menta\"/,/waterMode/" "$F" | grep -q "cold: 4"
awk "/id: \"perejil\"/,/waterMode/" "$F" | grep -q "cold: 4"
awk "/id: \"oregano\"/,/waterMode/" "$F" | grep -q "cold: 14"
awk "/id: \"cilantro\"/,/waterMode/" "$F" | grep -q "cold: 4"
awk "/id: \"tomillo\"/,/waterMode/" "$F" | grep -q "cold: 21"
awk "/id: \"ciboulette\"/,/waterMode/" "$F" | grep -q "cold: 6"
# Smoke gate
npx tsc --noEmit
node scripts/smoke-phase08.mjs'
    </automated>
  </verify>
  <acceptance_criteria>
    - All 8 EXTERIOR entries (petunia/hortensia/jazmin/geranio/rosa/bougainvillea/hibisco/margarita) reflect new cold values
    - bougainvillea cold === 21 (largest single override)
    - lavanda still shows `cold: 17` (UNTOUCHED — Plan 03 will rewrite as part of rename)
    - All 8 AROMATICAS entries (albahaca/romero/menta/perejil/oregano/cilantro/tomillo/ciboulette) reflect new cold values
    - romero and tomillo cold === 21 (Mediterranean)
    - `npx tsc --noEmit` exits 0
    - `node scripts/smoke-phase08.mjs` exits 0 (A2 invariant: cold ≥ warm holds for all 16)
  </acceptance_criteria>
  <done>
    Exterior + Aromaticas categories fully expert-vetted (lavanda excluded, owned by Plan 03). Smoke green. Commit: `feat(08-02): catalog exterior+aromaticas expert overrides — 16 entries (lavanda deferred to 08-03)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: HUERTA + FRUTALES + SUCULENTAS categories — apply 1 override (higuera) + verify all others unchanged</name>
  <read_first>
    - src/data/plantDatabase.ts (find HUERTA, FRUTALES, SUCULENTAS entries)
    - .planning/phases/08-catalog-rebalance/08-RESEARCH.md §HUERTA (lines 207–219), §FRUTALES (lines 222–230), §SUCULENTAS (lines 233–240)
    - scripts/smoke-phase08.mjs
  </read_first>
  <files>
    - src/data/plantDatabase.ts
  </files>
  <action>
    HUERTA, FRUTALES, and SUCULENTAS clusters are mostly unchanged from Phase 4 mechanical mapping. Only ONE category-specific override is required: `higuera` cold 11→14.

    **HUERTA — all 8 entries unchanged from Phase 4** (verify only):

    | id | lightLevel | warm | cold | mode |
    |----|------------|------|------|------|
    | tomatera | direct | 2 | 3 | fixed |
    | pimiento | direct | 2 | 3 | fixed |
    | frutilla | direct | 2 | 3 | fixed |
    | lechuga | bright_indirect | 2 | 3 | fixed |
    | pepino | direct | 2 | 3 | fixed |
    | zanahoria | direct | 3 | 4 | fixed |
    | rucula | bright_indirect | 2 | 3 | fixed |
    | zapallito | direct | 2 | 3 | fixed |

    **FRUTALES — 4 unchanged + 1 override (higuera):**

    | id | lightLevel | warm | cold | mode | Change |
    |----|------------|------|------|------|--------|
    | limonero | direct | 5 | 10 | fixed | unchanged |
    | naranjo | direct | 5 | 10 | fixed | unchanged |
    | mandarino | direct | 5 | 10 | fixed | unchanged |
    | aguacate | direct | 5 | 10 | fixed | unchanged |
    | higuera | direct | 7 | **14** | fixed | cold 11→14 (winter dormancy) |

    **SUCULENTAS — all 5 entries unchanged from Phase 4** (verify only):

    | id | lightLevel | warm | cold | mode |
    |----|------------|------|------|------|
    | suculenta-generica | direct | 12 | 24 | soil_check |
    | cactus | direct | 21 | 30 | soil_check |
    | echeveria | direct | 10 | 20 | soil_check |
    | haworthia | bright_indirect | 14 | 28 | soil_check |
    | sedum | direct | 14 | 28 | soil_check |

    Procedure:
    1. Edit `higuera` ONLY: change `cold: 11` to `cold: 14`.
    2. For all other 17 entries (8 huerta + 4 frutales + 5 suculentas): VERIFY current values match the tables above. If any differ from Phase 4 baseline, the prior plan (or human edit) drifted — flag and stop.
    3. Run `npx tsc --noEmit` and `node scripts/smoke-phase08.mjs`.

    Note: Phase 4 codemod set `cactus` to `waterMode: "soil_check"` (RESEARCH Pitfall 5 confirms). Verify here; do NOT "fix" it.

    AVOID:
    - Do NOT touch huerta entries (1.3× factor is correct for shoulder-season annuals).
    - Do NOT touch suculentas (Phase 4 already correct; soil_check mode locked).
    - Do NOT touch citrus (limonero/naranjo/mandarino/aguacate — 1:2 already correct).
    - Do NOT change cactus mode from soil_check (Pitfall 5).
  </action>
  <verify>
    <automated>
bash -c 'set -e
F=src/data/plantDatabase.ts
# higuera cold=14 (the one change)
awk "/id: \"higuera\"/,/waterMode/" "$F" | grep -q "cold: 14"
# HUERTA spot checks
awk "/id: \"tomatera\"/,/waterMode/" "$F" | grep -q "cold: 3"
awk "/id: \"zanahoria\"/,/waterMode/" "$F" | grep -q "cold: 4"
# FRUTALES spot checks (unchanged 1:2 ratio)
awk "/id: \"limonero\"/,/waterMode/" "$F" | grep -q "cold: 10"
awk "/id: \"naranjo\"/,/waterMode/" "$F" | grep -q "cold: 10"
# SUCULENTAS spot checks
awk "/id: \"cactus\"/,/waterMode/" "$F" | grep -q "soil_check"
awk "/id: \"echeveria\"/,/waterMode/" "$F" | grep -q "cold: 20"
awk "/id: \"haworthia\"/,/waterMode/" "$F" | grep -q "lightLevel: \"bright_indirect\""
awk "/id: \"sedum\"/,/waterMode/" "$F" | grep -q "cold: 28"
# A3 contract: aloe-vera + jade soil_check (Plan 02 T1 already set)
awk "/id: \"aloe-vera\"/,/waterMode/" "$F" | grep -q "soil_check"
awk "/id: \"jade\"/,/waterMode/" "$F" | grep -q "soil_check"
# Smoke gate
npx tsc --noEmit
node scripts/smoke-phase08.mjs'
    </automated>
  </verify>
  <acceptance_criteria>
    - higuera cold === 14 (only override in this task)
    - All 8 HUERTA entries match expected values (tomatera/pimiento/frutilla/pepino/zapallito cold=3; lechuga/rucula cold=3; zanahoria cold=4)
    - All 4 unchanged FRUTALES entries (limonero/naranjo/mandarino/aguacate) preserve cold=10
    - All 5 SUCULENTAS entries preserve Phase 4 values; cactus mode is soil_check; haworthia lightLevel is bright_indirect
    - aloe-vera and jade still soil_check (carried from Task 1)
    - `npx tsc --noEmit` exits 0
    - `node scripts/smoke-phase08.mjs` exits 0 (PASS N/N — all 38 entries pass A1/A2/A3)
  </acceptance_criteria>
  <done>
    Huerta+Frutales+Suculentas verified+higuera overridden. All 38 existing entries now horticulturally correct. Smoke green. Commit: `feat(08-02): catalog huerta+frutales+suculentas — higuera cold 11→14, others unchanged (verified)`.
  </done>
</task>

</tasks>

<verification>
**Plan 02 gate (must pass before Plan 03 starts):**

```bash
npx tsc --noEmit                                                    # types green
node scripts/smoke-phase08.mjs                                       # PASS N/N (A1+A2+A3 cover all 38)
# Spot-verify the 5 most consequential overrides
grep -c "cold: 30" src/data/plantDatabase.ts                        # ≥ 2 (aloe-vera, jade)
grep -c "cold: 21" src/data/plantDatabase.ts                        # ≥ 5 (dracaena, yuca, bougainvillea, romero, tomillo)
grep -c "cold: 14" src/data/plantDatabase.ts                        # ≥ 6 (potus, monstera, orquidea, palmera-interior, filodendro, oregano, higuera)
# lavanda still untouched (Plan 03 owns it)
awk '/id: "lavanda"/,/waterMode/' src/data/plantDatabase.ts | grep -c "cold: 17"  # === 1
```

**Cross-phase regression check:**

```bash
node scripts/smoke-phase07.mjs    # still PASS 100/100
node scripts/smoke-phase06.mjs    # still PASS 82/82
node scripts/migration-smoke-test.mjs   # still PASS 106/106
```

If any regression, an override drift broke a downstream contract — diff and fix.
</verification>

<success_criteria>
- All 38 existing PLANT_DATABASE entries reflect the expert-override table from RESEARCH §Expert-Override Table
- lavanda is untouched in this plan (still `cold: 17` — Plan 03 will rename + override as one atomic change)
- bougainvillea cold === 21 (the most dramatic single override)
- Mediterranean entries (romero, tomillo, lavanda-once-renamed, jazmin) carry cold values 14–21 reflecting drought tolerance
- Tropical interior entries (monstera/potus/filodendro/palmera-interior) carry 1:2 warm:cold
- aloe-vera and jade carry cold === 30
- soil_check waterMode preserved for all 7 entries (suculenta-generica, cactus, echeveria, haworthia, sedum, aloe-vera, jade)
- smoke-phase08 exits 0 after each task commit
- No regression in Phase 4/5/6/7 smoke runners
</success_criteria>

<output>
After completion, create `.planning/phases/08-catalog-rebalance/08-02-SUMMARY.md` documenting:
- Decisions: any deviations from RESEARCH table (none expected — RESEARCH is the SSOT); per-task commit boundaries
- Affects: Plan 03 (lavanda owned by 03; will follow same edit pattern), Plan 04 (read-side consumers will surface these new values)
- Patterns: per-entry awk-bounded edit verified by smoke; reusable for Plan 03 lavanda edit
- Pitfalls observed (if any) — particularly around edit drift if the entry lookup span (`/id:/, /waterMode/`) fails for a multi-line entry literal
</output>
