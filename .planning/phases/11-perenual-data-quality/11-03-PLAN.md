---
phase: 11-perenual-data-quality
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified: []
autonomous: false
requirements:
  - DATA-04
gap_closure: false

user_setup:
  - service: supabase
    why: "Deploy the Plan 11-01 changes to the live get-plant-care edge function. The Phase 10 deploy already exists; Plan 11-03 redeploys with the new isGoodMatch validator + family/type schema. Claude has no read access to .envrc (per CLAUDE.md security: '.env / .envrc never committed') so it cannot run `supabase functions deploy` itself."
    env_vars: []
    dashboard_config:
      - task: "Redeploy get-plant-care edge function with Plan 11-01 changes"
        location: "Local terminal — `source .envrc && supabase functions deploy get-plant-care`"
      - task: "Verify edge function logs for [get-plant-care] Mismatch:"
        location: "Supabase Dashboard → Functions → get-plant-care → Logs"
      - task: "Run 5-species DATA-04 fixture in Expo Go and record results"
        location: "Local Expo Go device — identify each fixture species, observe enriched data in app, capture tempMax + humidity"

must_haves:
  truths:
    - "supabase/functions/get-plant-care is redeployed with Plan 11-01's isGoodMatch + family/type schema (visible in Supabase Dashboard → Functions; deploy timestamp post-Plan-11-01)"
    - "After redeploy, querying a known-mismatch (e.g., 'qwerty fictional plant') returns `{ data: null }` and emits a `[get-plant-care] Mismatch:` log line in Supabase Functions logs"
    - "After redeploy, the 5-species DATA-04 fixture (Monstera deliciosa, Echinopsis pachanoi, Rosa canina, Phalaenopsis amabilis, Aloe vera) produces ≥4/5 cases with `tempMax !== 35 AND humidity !== null` (≥80% threshold per ROADMAP success criterion #4)"
    - "Phase 10 grep guard (`EXPO_PUBLIC_PERENUAL_API_KEY` absent from src/.env*/app.json) is still clean post-deploy"
    - "TypeScript strict still passes (`npx tsc --noEmit`) and the smoke runner still exits 0 (`node scripts/smoke-phase11.mjs`) post-deploy — no regression from the redeploy"
    - "If <4/5 fixture cases pass, the deviation is documented in 11-03-SUMMARY.md with the failing species' raw Perenual response (so a follow-up phase can decide whether to tighten the validator or add more category fallback rules)"
  artifacts: []
  key_links:
    - from: "supabase/functions/get-plant-care/index.ts (Plan 11-01 source — isGoodMatch + schema expansion)"
      to: "Supabase project (production edge function)"
      via: "supabase functions deploy get-plant-care — atomically swaps the live function to the new source; secrets (PERENUAL_API_KEY from Phase 10) are preserved"
      pattern: "Deployed Function"
    - from: "Live edge function with isGoodMatch"
      to: "Client convertPerenualToKnowledge with inferHumidity + classifyTempMaxFallback (Plan 11-02)"
      via: "Edge function returns family/type in the data envelope; client reads them via the expanded PerenualPlantDetail interface; 5-species fixture validates the end-to-end chain"
      pattern: "PerenualPlantDetail.family"
---

<objective>
Manual checkpoint: redeploy the `get-plant-care` edge function with Plan 11-01's changes (isGoodMatch validator + family/type schema), then run the 5-species fixture verification in Expo Go to confirm DATA-04 (≥80% of cases produce non-default tempMax + non-null humidity). This plan modifies ZERO files — the executor cannot run `supabase functions deploy` (no `.envrc` token access) and cannot run the live device fixture (requires a real device + Perenual round-trip). Mirrors Phase 10 Plan 10-03's `checkpoint:human-action` pattern.

Purpose: closes the Phase 11 success-criteria loop. Plans 11-00, 11-01, 11-02 ship the source; without this redeploy + fixture verification, the live app still runs the Phase 10 edge function (no validator, no schema fields) and DATA-04 is unverified. The 5-species fixture is the only test that exercises the full Perenual round-trip — unit-level smoke tests with mocked payloads cannot catch real schema drift or family/type field absence in live Perenual responses.

Output: ZERO files modified. One redeploy + one fixture verification round + 11-03-SUMMARY.md capturing the per-species tempMax/humidity results. After this plan, Phase 11 is verifiable via `/gsd:verify-work` — all 4 DATA requirements have either an automated assertion (DATA-01/02/03 via smoke) or a manual fixture record (DATA-04 in this plan's SUMMARY).
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-perenual-data-quality/11-CONTEXT.md
@.planning/phases/11-perenual-data-quality/11-RESEARCH.md
@.planning/phases/11-perenual-data-quality/11-VALIDATION.md
@.planning/phases/11-perenual-data-quality/11-01-SUMMARY.md
@.planning/phases/11-perenual-data-quality/11-02-SUMMARY.md
@.planning/phases/10-perenual-security/10-03-SUMMARY.md
@CLAUDE.md
@supabase/functions/get-plant-care/index.ts
@src/services/plantKnowledgeService.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Redeploy get-plant-care edge function with Plan 11-01 changes + verify mismatch log</name>
  <files></files>
  <read_first>
    - CLAUDE.md (Supabase Edge Functions section: `source .envrc && supabase functions deploy <name>` command pattern)
    - .envrc (verify SUPABASE_ACCESS_TOKEN + project link present — DO NOT cat the file; verify existence only via `test -f .envrc`)
    - supabase/functions/get-plant-care/index.ts (Plan 11-01 deliverable — confirm `isGoodMatch` is present before attempting deploy: `grep -c "function isGoodMatch(" supabase/functions/get-plant-care/index.ts` MUST return 1)
    - .planning/phases/11-perenual-data-quality/11-01-SUMMARY.md (Plan 11-01 outcome — confirm Plan 11-01 actually merged before triggering redeploy)
  </read_first>
  <what-built>
    Plans 11-00, 11-01, 11-02 are merged. The edge function source (`supabase/functions/get-plant-care/index.ts`) now contains the `isGoodMatch` validator + the expanded `PerenualPlantDetail` interface (family?, type?). The client service (`src/services/plantKnowledgeService.ts`) now contains `inferHumidity`, `classifyTempMaxFallback`, the rewritten `parseHardiness`, and the matching schema fields. NEITHER plan deploys to Supabase — the redeploy requires `.envrc` access (Supabase CLI token) which Claude does not have.
  </what-built>
  <action>
    EXECUTOR INSTRUCTION: This is a `checkpoint:human-action` task. Surface to the user that 2 commands must be run from the project root (the cwd that contains `.envrc` and `supabase/`), in this exact order. Pause execution and wait for the user to reply with confirmation before proceeding to Task 2.

    Pre-flight check the executor CAN run before pausing (no `.envrc` needed): confirm Plan 11-01 source actually contains the validator. Run: `grep -c "function isGoodMatch(" supabase/functions/get-plant-care/index.ts` — MUST return 1. If 0, abort and notify the user that Plan 11-01 hasn't merged yet.

    USER INSTRUCTIONS — copy-paste these into your local terminal:

    ```bash
    # Step 1: Redeploy the edge function (uploads supabase/functions/get-plant-care/index.ts to Supabase)
    # The PERENUAL_API_KEY secret from Phase 10 is preserved — secrets are not affected by redeploy.
    source .envrc && supabase functions deploy get-plant-care
    ```

    Expected output: a line containing `Deployed Function get-plant-care` (or similar). If you see `401 Unauthorized` or `project not linked`, re-run `supabase login` and `supabase link --project-ref <ref>` first, then retry.

    ```bash
    # Step 2: Verify the new validator path by triggering a known-mismatch query.
    # Open Expo Go (or EAS dev client) on a device. Use the in-app catalog search OR
    # the in-app plant identification flow with a deliberately fake species name.
    # Easier alternative: invoke the edge function directly via curl from your terminal:

    source .envrc
    curl -X POST "$SUPABASE_URL/functions/v1/get-plant-care" \
      -H "Content-Type: application/json" \
      -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
      -d '{"plantName": "qwerty fictional plant xyzzy"}'
    ```

    Expected response body: `{"data":null}` (HTTP 200). If the response contains `{"data": { ... }}` with details, the validator is NOT running — re-check the deploy actually swapped to the new source.

    Then verify in Supabase Dashboard:
    - Open: https://supabase.com/dashboard/project/<your_project_ref>/functions
    - Click on `get-plant-care`
    - Click "Logs" tab
    - You should see a fresh log line:
      - `[get-plant-care] Mismatch: query="qwerty fictional plant xyzzy" vs result="<some real plant Perenual returned>" / "<scientific name>"`
    - This proves: (a) the deploy actually replaced the old code, (b) the validator is invoked, (c) the mismatch path correctly returns null without traversing to the details fetch.

    REPLY TO CLAUDE WHEN DONE: Type `deployed and validator verified` in chat to continue to Task 2 (5-species fixture). Type `failed: <reason>` if anything fails — Claude will help diagnose. Type `partial: deployed but mismatch log not seen` if Step 1 succeeds but Step 2 fails (Claude will help debug — most likely cause: edge function logs lag by ~30s, retry the curl).
  </action>
  <how-to-verify>
    1. Open a terminal at `/Users/gaston/Documents/Personal/MiJardinApp/`.
    2. Run `source .envrc && supabase functions deploy get-plant-care` — confirm "Deployed Function" output line.
    3. Run the curl mismatch test above — confirm response is `{"data":null}`.
    4. Open Supabase Dashboard → Functions → `get-plant-care` → Logs. Confirm a fresh `[get-plant-care] Mismatch:` log line appears with the qwerty query and a Perenual-returned plant name as the result.
    5. (Optional sanity check) Run a curl with a real plant query: `curl ... -d '{"plantName": "monstera"}'` — expected response is `{"data": {"id": ..., "common_name": "Swiss Cheese Plant" or similar, "family": "Araceae", "type": "...", ...}}`. The presence of `family` and `type` keys in the response body confirms the schema expansion in Plan 11-01 is live.
  </how-to-verify>
  <verify>
    <automated>grep -c "function isGoodMatch(" supabase/functions/get-plant-care/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - User confirms `supabase functions deploy get-plant-care` exited with `Deployed Function` message (paste the output line in 11-03-SUMMARY.md)
    - User confirms a curl with a fictional plantName returned HTTP 200 with body `{"data":null}` (paste the curl output)
    - User confirms Supabase Functions logs show a fresh `[get-plant-care] Mismatch:` line for the curl query (paste the log line — masking any real plant names returned by Perenual is fine)
    - (Optional) User confirms a curl with `plantName: "monstera"` returns a body containing `"family":"Araceae"` (or similar) — proves the schema expansion is live
    - Pre-flight grep before pause: `grep -c "function isGoodMatch(" supabase/functions/get-plant-care/index.ts` returns 1
  </acceptance_criteria>
  <resume-signal>Type `deployed and validator verified` to continue to Task 2 (5-species fixture). Type `failed: <reason>` to debug. Type `partial: <details>` if deploy succeeded but log/curl verification was flaky (Claude will help diagnose; if the redeploy itself succeeded, the validator IS active — the ambiguity is just observation, not behavior).</resume-signal>
  <done>
    - Edge function redeployed with Plan 11-01 source (isGoodMatch + schema expansion)
    - Mismatch curl test returned `{ data: null }` HTTP 200
    - Supabase Functions logs show the `[get-plant-care] Mismatch:` line confirming the validator path executes
    - DATA-01 success criterion is now LIVE-verified (REQUIREMENTS.md DATA-01 + ROADMAP success criterion #3)
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run 5-species DATA-04 fixture and record per-species tempMax + humidity</name>
  <files></files>
  <read_first>
    - .planning/phases/11-perenual-data-quality/11-RESEARCH.md (Test Fixture (DATA-04) — 5 Species Selection table at lines 257-277 — the 5 species, expected humidity, expected tempMax, and the rationale per species)
    - .planning/phases/11-perenual-data-quality/11-VALIDATION.md (Manual-Only Verifications section — DATA-04 row gives the exact verification protocol)
    - .planning/REQUIREMENTS.md DATA-04 — "≥80% of cases (verified via test fixture of 5 known species)" threshold
  </read_first>
  <what-built>
    Task 1 redeployed the edge function. The full Phase 11 chain is now LIVE: client (with inferHumidity/classifyTempMaxFallback/expanded interface from Plan 11-02) calls deployed edge function (with isGoodMatch/expanded schema from Plan 11-01) which returns family/type/hardiness data which the client maps to humidity + tempMax. Task 2 exercises this chain end-to-end with 5 well-known species.
  </what-built>
  <action>
    EXECUTOR INSTRUCTION: This is a `checkpoint:human-action` task. Surface to the user the 5-species fixture protocol below. Pause execution and wait for the user to reply with the per-species results. The results MUST be recorded in 11-03-SUMMARY.md so Phase 11 verification has a paper trail.

    USER INSTRUCTIONS — perform the fixture verification:

    For each of the 5 fixture species, identify (or directly query) via the deployed edge function and record `tempMax` (heat tolerance, °C) and `humidity` ('alta' | 'media' | 'baja' | null). Two ways to obtain the data, in order of preference:

    **Option A — In-app identification flow (most realistic; closest to a user experience):**
    1. Open Expo Go or EAS dev client.
    2. For each of the 5 species, find a clear photo (search Wikipedia or Google Images, screenshot to camera roll).
    3. Use the in-app PlantNet identification flow with that photo.
    4. After identification + Perenual enrichment completes, observe the resulting plant card.
    5. Record `tempMax` (visible in plant detail or by reading the AsyncStorage row via React DevTools) and `humidity` (likewise).

    **Option B — Direct curl invocation of the deployed edge function (faster; bypasses PlantNet):**

    For each species, run this curl from your local terminal (the one with `.envrc`):

    ```bash
    source .envrc
    curl -s -X POST "$SUPABASE_URL/functions/v1/get-plant-care" \
      -H "Content-Type: application/json" \
      -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
      -d '{"plantName": "<scientific name>"}' | jq .
    ```

    Then compute what `convertPerenualToKnowledge` would produce:
    - `tempMax`: if response has `data.hardiness.max`, look up zone in zoneToTempMax (zones 1-4→25, 5-6→28, 7-8→32, 9→35, 10→38, 11-13→40); else apply category fallback from `data.family` + `data.type` (Araceae/Orchidaceae/Bromeliaceae→32; Cactaceae/Crassulaceae→40; type contains 'cactus'/'succulent'→40; type contains 'tropical'/'fern'/'moss'→32; indoor true→32; default 35).
    - `humidity`: family Araceae/Orchidaceae/Bromeliaceae→'alta'; family Cactaceae/Crassulaceae→'baja'; type contains 'cactus'/'succulent'→'baja'; type contains 'tropical'/'fern'/'moss'→'alta'; default 'media'.

    The 5 species (verbatim from RESEARCH.md):

    | # | Scientific Name | Common Name | Family | Expected humidity | Expected tempMax |
    |---|----------------|-------------|--------|-------------------|------------------|
    | 1 | Monstera deliciosa | Monstera | Araceae | alta | 32 (or higher if hardiness.max=11→40) |
    | 2 | Echinopsis pachanoi | Cactus San Pedro | Cactaceae | baja | 40 |
    | 3 | Rosa canina | Rosa silvestre | Rosaceae | media | 28-35 (zone 5-9) |
    | 4 | Phalaenopsis amabilis | Orquídea mariposa | Orchidaceae | alta | 32 (or higher if hardiness.max≥10) |
    | 5 | Aloe vera | Aloe Vera | Asphodelaceae (succulent) | baja | 38-40 |

    For EACH species, record into 11-03-SUMMARY.md a row with: scientific name, returned `family` (raw from Perenual), returned `type`, returned `hardiness.max`, computed `tempMax`, computed `humidity`, PASS/FAIL on the criterion `tempMax !== 35 AND humidity !== null`.

    DATA-04 SUCCESS THRESHOLD: ≥4/5 species PASS the criterion (≥80%).

    INTERPRETATION NOTES (per RESEARCH.md):
    - Aloe vera is in family Asphodelaceae which is NOT in the primary classifier — it must rely on `type` containing "succulent". If Perenual returns `type: null` or `type: "grass"`, Aloe contributes a FAIL (humidity='media', tempMax=35). Document the raw response for follow-up.
    - "rose" → "rosemary" collision documented in RESEARCH.md is NOT relevant here because we're querying the FULL scientific name "Rosa canina" — the validator passes (Rosa canina includes "rosa canina" via the bidirectional check).
    - If Perenual returns `data: null` for any species (e.g., it's not in their catalog), record as N/A and substitute the next-best species per RESEARCH.md: replace Aloe vera with Echeveria elegans (Crassulaceae — guaranteed family match).

    REPLY TO CLAUDE WHEN DONE: Paste the 5-row results table into chat. Use this format:

    ```
    | # | Scientific Name | family | type | hardiness.max | tempMax | humidity | PASS? |
    |---|----------------|--------|------|---------------|---------|----------|-------|
    | 1 | Monstera deliciosa | Araceae | tree | 11 | 40 | alta | YES |
    | 2 | Echinopsis pachanoi | Cactaceae | (null) | (null) | 40 | baja | YES |
    | 3 | Rosa canina | Rosaceae | (null) | 7 | 32 | media | YES (tempMax !=35) |
    | 4 | Phalaenopsis amabilis | Orchidaceae | (null) | 10 | 38 | alta | YES |
    | 5 | Aloe vera | Asphodelaceae | succulent | (null) | 40 | baja | YES |
    Threshold: 5/5 ≥ 4/5 — DATA-04 PASS
    ```

    Type `fixture verified: <pass-count>/5` to continue. Type `fixture failed: <count>/5 — <reasons>` if <4/5 — Claude will help triage (likely outcomes: replace 1 species, OR document an edge case for follow-up phase, OR widen the classifier rules).
  </action>
  <how-to-verify>
    1. For each of the 5 species, the user runs Option A or Option B and records the per-species outputs.
    2. The results table contains 5 rows (or 4 + 1 N/A with substitution noted).
    3. PASS count is computed: ≥4/5 means DATA-04 PASS.
    4. The results table is pasted into 11-03-SUMMARY.md verbatim.
    5. Failures (if any) include the raw Perenual response payload (so a follow-up phase can decide whether to tighten the validator, add fallback rules, or accept the deviation).
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit && node scripts/smoke-phase11.mjs</automated>
  </verify>
  <acceptance_criteria>
    - User pasted the 5-species results table into chat AND into 11-03-SUMMARY.md
    - The table shows ≥4/5 species PASS (`tempMax !== 35 AND humidity !== null`) — DATA-04 success threshold met
    - For any FAIL row, the raw Perenual response (`family`, `type`, `hardiness`) is recorded so the deviation can be triaged in a follow-up
    - `npx tsc --noEmit` still exits 0 (no regression from the live Plan 11-01/02 changes)
    - `node scripts/smoke-phase11.mjs` still exits 0 (unit-level chain still healthy)
    - `grep -rc "EXPO_PUBLIC_PERENUAL_API_KEY" src/ .env .env.example app.json 2>/dev/null` shows count 0 on every line (Phase 10 grep guard preserved)
  </acceptance_criteria>
  <resume-signal>Type `fixture verified: <count>/5` to mark Phase 11 done (Claude will help finalize 11-03-SUMMARY.md). Type `fixture failed: <count>/5 — <reasons>` if <4/5 — this triggers a triage discussion: accept the deviation as documented limitation, OR replace a fixture species, OR scope a follow-up phase to widen the classifier.</resume-signal>
  <done>
    - 5-species fixture results recorded in 11-03-SUMMARY.md
    - DATA-04 verified: ≥4/5 species produce `tempMax !== 35 AND humidity !== null`
    - Phase 11 success criteria 1, 2, 3, 4 from ROADMAP now all live-verified or unit-verified
    - Phase 11 ready for `/gsd:verify-work` (all 4 DATA requirements have a verification record)
  </done>
</task>

</tasks>

<verification>
After both checkpoints complete:

```bash
# 1. Source-level reverification (executor-runnable)
grep -c "function isGoodMatch(" supabase/functions/get-plant-care/index.ts                       # 1
grep -c "family?: string" supabase/functions/get-plant-care/index.ts                             # 1
grep -c "type?: string" supabase/functions/get-plant-care/index.ts                               # 1
grep -c "function inferHumidity(" src/services/plantKnowledgeService.ts                          # 1
grep -c "function classifyTempMaxFallback(" src/services/plantKnowledgeService.ts                # 1
grep -c "humidity: inferHumidity(plant)" src/services/plantKnowledgeService.ts                   # 1
grep -c "tempMax ?? classifyTempMaxFallback(plant)" src/services/plantKnowledgeService.ts        # 1

# 2. Type and smoke pass
npx tsc --noEmit
node scripts/smoke-phase11.mjs

# 3. Phase 10 grep guard still clean
grep -rc "EXPO_PUBLIC_PERENUAL_API_KEY" src/ .env .env.example app.json 2>/dev/null

# 4. Live verification (user-driven, captured in 11-03-SUMMARY.md):
#    - Supabase Dashboard → Functions → get-plant-care → Logs shows fresh [get-plant-care] Mismatch: line
#    - 5-species fixture results table pasted with ≥4/5 PASS
```
</verification>

<success_criteria>
1. Edge function `get-plant-care` is REDEPLOYED with Plan 11-01's `isGoodMatch` validator + `family?: string` + `type?: string` schema fields (Task 1).
2. Mismatch curl test returns `{"data":null}` HTTP 200 with a `[get-plant-care] Mismatch:` log line in Supabase Functions logs — confirms DATA-01 path is live (Task 1).
3. 5-species DATA-04 fixture executed; results table recorded in 11-03-SUMMARY.md; ≥4/5 species PASS the `tempMax !== 35 AND humidity !== null` criterion (Task 2).
4. ROADMAP success criteria #1 (tempMax derived from zone), #2 (humidity 'alta' for Araceae / 'baja' for cactus), #3 (mismatch returns null), #4 (≥80% fixture PASS) are all VERIFIED (1-2 unit-level via smoke + 3-4 live-fixture).
5. TypeScript strict still passes; smoke runner still exits 0; Phase 10 grep guard preserved.
6. (If <4/5 PASS) — DATA-04 deferred with a documented triage in 11-03-SUMMARY.md; remaining 3 DATA-* requirements still complete; phase marked partial.
</success_criteria>

<output>
After completion, create `.planning/phases/11-perenual-data-quality/11-03-SUMMARY.md` documenting:
- Task 1 outcome: deploy command output (paste the `Deployed Function` line); curl mismatch test response body; Supabase Functions logs `[get-plant-care] Mismatch:` line (paste — sanitize any real plant names if desired)
- Task 2 outcome: 5-species results table verbatim; PASS count; per-species notes (especially for Aloe vera type-fallback edge case)
- Final state: which DATA-* requirements are CLOSED (DATA-01, DATA-02, DATA-03 always; DATA-04 if ≥4/5 PASS) vs DEFERRED (DATA-04 if <4/5 with triage rationale)
- Phase 11 ready for `/gsd:verify-work`: all 4 DATA-* requirements have an assertion record (smoke or fixture)
</output>
