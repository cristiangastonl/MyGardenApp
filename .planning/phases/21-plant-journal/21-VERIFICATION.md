---
phase: 21-plant-journal
verified: 2026-05-11T23:30:00Z
status: passed
score: 5/5 must-haves verified
---

# Phase 21: Plant Journal — Verification Report

**Phase Goal:** "Users can log text notes and photos for any plant in a reverse-chronological journal; journal data is stored safely in the file system (not AsyncStorage base64); deleting a plant cleans up its journal"

**Verified:** 2026-05-11T23:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| #   | Truth                                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                                                                              |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A user can add a journal entry (text + optional photo + optional care tag) via a bottom sheet from MyPlantDetailModal in 2 taps or fewer                             | ✓ VERIFIED | `JournalQuickAddSheet.tsx` renders with `snapPoints={['60%']}` (L123); both `Cámara`/`Galería` photo buttons (L141–146); 6-chip `CARE_TAGS` care-tag row (L42, L161–177); empty-entry-tolerant `handleSave` (date defaults today; text/photo/tag optional — L89–113); Guardar/Cancelar footer (L178–189); 2-tap floor mechanically enforceable (open + save). |
| 2   | Journal entries survive an app restart and appear in reverse-chronological order                                                                                     | ✓ VERIFIED | `AppData.journals?: Record<string, JournalEntry[]>` typed (`src/types/index.ts:171`); `useStorage` load-path `data.journals \|\| {}` default at `useStorage.tsx:312` + setJournals hydrated; snapshotFromRef includes `journals: d.journals` at L128; both mutation actions persist via `dataRef.current` + scheduleSave; `JournalSection` sorts via `b.date.localeCompare(a.date)` at L83 (reverse-chronological). |
| 3   | Journal photo URIs point to `documentDirectory` paths — no base64 strings in AsyncStorage (verified by startup size log)                                              | ✓ VERIFIED | `journalService.ts` uses modern `Paths.document.uri` API (L15 imports `Paths, File, Directory`; L22 builds `${Paths.document.uri}journal/${plantId}/`); JPEG 0.7 / 1080px pipeline (L37–43); `__DEV__` payload-bytes log at `useStorage.tsx:188-192` (`'[useStorage] AsyncStorage payload bytes:'`). No `FileSystem.documentDirectory` string-concat usage in code paths (single match is documentation negation in JSDoc). |
| 4   | Deleting a plant also removes its journal entries from storage (no orphans)                                                                                          | ✓ VERIFIED | `useStorage.deletePlant` calls `deleteJournalDirectory(id).catch(...)` BEFORE state mutation at `useStorage.tsx:413` (fire-and-forget, idempotent); journals[id] map entry removed via `delete newJournals[id]; setJournals(newJournals); dataRef.current.journals = newJournals;` at L428–431; `deleteJournalDirectory` in journalService recursively removes per-plant subdir (L103–106, idempotent). |
| 5   | Journal entries are readable without a premium subscription — no paywall at read level                                                                                | ✓ VERIFIED | Negative-grep `usePremiumGate|isPremium|canReadJournal` returns 0 matches across `JournalSection.tsx`, `JournalEntryRow.tsx`, `JournalQuickAddSheet.tsx`. MyPlantDetailModal Diario block (L502–509) renders unconditionally. The modal's line-83 `usePremiumGate()` destructures `canDiagnose`/`isPremium` for Phase 8 diagnosis flow only (lines 212/304/308), NOT wrapping Diario. `JOURNAL-05.modal.diario-block-not-premium-gated` window-grep sentinel green. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact                                                              | Expected                                                                                                                  | Status     | Details                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/types/index.ts`                                                  | `CareTag` 6-literal union + `JournalEntry` interface + `AppData.journals?: Record<string, JournalEntry[]>`              | ✓ VERIFIED | L139 `CareTag = 'riego' \| 'fertilizar' \| 'sol' \| 'poda' \| 'problema' \| 'otro'`; L146 `JournalEntry` interface with id/date required + text/photoUri/careTag optional; L171 `journals?: Record<string, JournalEntry[]>` additive optional.                                                  |
| `src/services/journalService.ts`                                      | Real impl with `pickJournalPhoto/saveJournalPhoto/deleteJournalPhoto/deleteJournalDirectory` using modern Paths API     | ✓ VERIFIED | 105 lines; imports `{ Paths, File, Directory }` from `expo-file-system` (L15); 4 exports + 3 helpers; JPEG `compress: 0.7` + `width: 1080` (L37–43); idempotent delete guards (L95, L105).                                                                                                       |
| `src/hooks/useStorage.tsx`                                            | `StorageState.journals` + snapshotFromRef inclusion + load-path default + `__DEV__` payload log + `addJournalEntry`/`deleteJournalEntry` actions + `deletePlant` cascade | ✓ VERIFIED | L46 StorageState.journals; L128 snapshotFromRef; L149 useState setter; L170 dataRef init; L224/353 brand-new-user property assignments; L312 load-path `data.journals \|\| {}` + L345 rebuild + L188-192 `__DEV__` payload log; L548-554 addJournalEntry / L556-562 deleteJournalEntry useCallback impls; L413 deletePlant calls deleteJournalDirectory BEFORE mutation; L428-431 journals[id] map cleanup. |
| `src/components/MyPlantDetailModal.tsx`                               | `ModalSectionId` extended to `'diario'`; Diario as 6th section AFTER Mascotas; `onJournalEntrySaved` callback prop      | ✓ VERIFIED | L42 union `... | 'mascotas' | 'diario'`; L494 Mascotas (5th); L502-509 Diario JSX as 6th with `onSectionLayout('diario')` + JournalSection wired; L58-61 `onJournalEntrySaved?: () => void` prop.                                                                                                  |
| `src/components/plant-detail/JournalSection.tsx`                      | Real impl with 180ms `Easing.out(Easing.cubic)` collapse + empty-state CTA + FlatList                                   | ✓ VERIFIED | L32 `COLLAPSE_DURATION = 180`; L57 `Easing.out(Easing.cubic)`; L96 chevron rotation 0°→90°; L119-126 empty-state CTA via `ListEmptyComponent`; L83 reverse-chronological sort; L112 FlatList timeline.                                                                                            |
| `src/components/plant-detail/JournalQuickAddSheet.tsx`                | `snapPoints=['60%']` + Cámara/Galería buttons + `CARE_TAGS` literal + 6 chips + Guardar/Cancelar                        | ✓ VERIFIED | L123 `snapPoints={['60%']}`; L141-146 Cámara/Galería buttons; L42 `CARE_TAGS = ['riego','fertilizar','sol','poda','problema','otro'] as const satisfies readonly CareTag[]`; L161-177 chip render; L178-189 Guardar/Cancelar footer.                                                              |
| `src/components/plant-detail/JournalEntryRow.tsx`                     | Date header w/ Hoy/Ayer/Hace N días + careTag chip + ~80px thumbnail + long-press → delete BottomSheetModal             | ✓ VERIFIED | L47 `getRelativeDateLabel` returns today/yesterday/daysAgo per delta; L100 onLongPress presents sheet; L113-115 thumbnail (80x80 via styles.thumbnail at L168-173); L119-132 BottomSheetModal with `Eliminar entrada` option + Alert.alert confirm + deleteJournalPhoto best-effort.                |
| `src/screens/PlantsScreen.tsx`                                        | Separate type-only NAMED `ModalSectionId` import; `journalToastVisible` state; second `<Toast>` sibling                | ✓ VERIFIED | L43 `import type { ModalSectionId } from '../components/MyPlantDetailModal';` separate line; L78 `useState<ModalSectionId | undefined>(undefined)`; L146 `journalToastVisible` distinct state; L458 `onJournalEntrySaved={() => setJournalToastVisible(true)}`; L496-503 second `<Toast>` sibling.  |
| `src/screens/TodayScreen.tsx`                                         | Mirror of PlantsScreen wiring                                                                                             | ✓ VERIFIED | L46 type-only NAMED import; L138 ModalSectionId state widened; L260 `journalToastVisible` state; L666 callback wired; L704-711 second `<Toast>` sibling.                                                                                                                                          |
| `src/i18n/locales/{en,es}/common.json`                                | ≥22 `journal.*` keys EN+ES parity (ES voseo); bare `t()` calls (no `??` fallbacks)                                      | ✓ VERIFIED | EN: `journal.{header, emptyState, addEntry, savedToast, photoCamera, photoGallery, careTag.{6}, deleteConfirm, dateLabel.{today, yesterday, daysAgo}, save, cancel, delete, deleteEntry, textPlaceholder, error.photoSaveFailed}` (22 leaf keys). ES mirrors with voseo (`Agregá`, `Escribí`, `No pudimos`). |
| `scripts/smoke-phase21.cjs`                                           | Three-tier runner with JOURNAL-01..05 sentinels + STRICT cross-phase Phase 18/19/20 regression                          | ✓ VERIFIED | Smoke run reports `PASS=76 FAIL=0 SKIP=0`. Phase 18 (56) / 19 (85) / 20 (49) preserved.                                                                                                                                                                                                          |

### Key Link Verification

| From                                  | To                                          | Via                                                                                                            | Status | Details                                                                                                                                                                  |
| ------------------------------------- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `MyPlantDetailModal`                  | `useStorage`                                | destructures `journals`, `addJournalEntry`, `deleteJournalEntry`                                              | WIRED  | Modal renders `JournalSection` with `entries={journals[plant.id] || []}` (L505) and passes `onDeleteEntry` to invoke `deleteJournalEntry` (L507).                       |
| `MyPlantDetailModal`                  | `JournalQuickAddSheet`                      | rendered via `setQuickAddSheetVisible(true)` from `onAddEntry`                                                | WIRED  | `onAddEntry={() => setQuickAddSheetVisible(true)}` at L506; `<JournalQuickAddSheet>` mounted at modal root.                                                            |
| `JournalQuickAddSheet`                | `journalService.pickJournalPhoto`           | `handlePhotoCamera`/`handlePhotoGallery` callbacks                                                            | WIRED  | L73 `pickJournalPhoto('camera')`; L78 `pickJournalPhoto('gallery')`; URI stored in `rawPhotoUri` state.                                                                |
| `JournalQuickAddSheet`                | `journalService.saveJournalPhoto`           | `handleSave` (atomic-write: photo save FIRST → compose entry → onSave)                                         | WIRED  | L96 `await saveJournalPhoto(plantId, entryId, rawPhotoUri)` inside try/catch; failure path Alerts and aborts before entry composition (atomic-write invariant).         |
| `JournalQuickAddSheet`                | `useStorage.addJournalEntry`                | `onSave` callback → parent invokes `addJournalEntry(plant.id, entry)`                                          | WIRED  | Parent `MyPlantDetailModal` passes `onSave={(entry) => { addJournalEntry(plant.id, entry); onJournalEntrySaved?.(); }}` (confirmed by grep + functional flow).         |
| `JournalEntryRow`                     | `journalService.deleteJournalPhoto`         | Alert.alert confirm `onPress` → `deleteJournalPhoto(plantId, entry.id)`                                        | WIRED  | L86 try/catch wrapped delete inside the Alert "Eliminar" handler (best-effort photo cleanup).                                                                          |
| `JournalEntryRow`                     | `useStorage.deleteJournalEntry`             | `onDelete(entry.id)` prop → parent `JournalSection.onDeleteEntry` → modal's `deleteJournalEntry(plant.id, …)` | WIRED  | L91 `onDelete(entry.id)`; threads through JournalSection L116 onto modal L507.                                                                                          |
| `useStorage.deletePlant`              | `journalService.deleteJournalDirectory`     | fire-and-forget `.catch(...)` BEFORE state mutation                                                            | WIRED  | L413 `deleteJournalDirectory(id).catch(e => ...)` invoked synchronously before plants/diagnosisHistory/journals map mutation.                                          |
| `useStorage.scheduleSave`             | `AsyncStorage`                              | `JSON.stringify(persisted)` payload sized via `__DEV__` log                                                    | WIRED  | L188-192 `__DEV__` console.log emits `'[useStorage] AsyncStorage payload bytes:'` ahead of `AsyncStorage.setItem`; instrumentation evidences NON-base64 size (Block A3 manual). |
| `PlantsScreen`/`TodayScreen` Toast    | `onJournalEntrySaved` callback              | `setJournalToastVisible(true)` flips the dedicated state; sibling `<Toast>` renders                            | WIRED  | PlantsScreen L458/L498-503; TodayScreen L666/L706-711. Distinct identifier from Phase 18 `toastVisible` (no collision).                                                |

### Requirements Coverage

| Requirement  | Source Plan(s)         | Description                                                                                                                                            | Status      | Evidence                                                                                                                                                                                                       |
| ------------ | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| JOURNAL-01   | 21-00, 21-01           | `AppData.journals?` additive optional + migration default `{}`                                                                                          | ✓ SATISFIED | Type at types/index.ts:171 (additive optional); load-path default at useStorage.tsx:312; dataRef init + brand-new-user property assignments at L170/L224/L353; snapshotFromRef inclusion at L128.            |
| JOURNAL-02   | 21-00, 21-01, 21-02    | `JournalEntry` type + photos saved to file system (documentDirectory), not base64-in-AsyncStorage                                                       | ✓ SATISFIED | types/index.ts:146 `JournalEntry`; journalService.ts uses `Paths.document.uri` (modern API); JPEG 0.7/1080px pipeline; `__DEV__` payload-bytes log for runtime verification (Block A3 manual).               |
| JOURNAL-03   | 21-00, 21-03           | `useStorage.addJournalEntry` + `deleteJournalEntry` actions                                                                                             | ✓ SATISFIED | Both useCallback impls at useStorage.tsx:548-562; StorageActions interface signatures at L70/L73; provider value + useMemo deps array wired (4-site mutation each); smoke `JOURNAL-03.public-interface-exposes-3-names` PASS. |
| JOURNAL-04   | 21-00, 21-03, 21-04    | Diario section + reverse-chrono timeline + quick-add bottom sheet (2-tap max) + orphan cleanup on plant delete                                          | ✓ SATISFIED | ModalSectionId extended to 'diario'; JournalSection 180ms collapse + reverse-chrono sort + empty-state CTA; JournalQuickAddSheet 60% snap + Cámara/Galería + 6 chips + Guardar; JournalEntryRow long-press delete; deletePlant fire-and-forget deleteJournalDirectory + journals[id] map cleanup. |
| JOURNAL-05   | 21-00, 21-04, 21-05    | Journal entries NEVER premium-gated at read level (Blossom cautionary tale)                                                                            | ✓ SATISFIED | Zero `usePremiumGate`/`isPremium`/`canReadJournal` references at JournalSection / JournalEntryRow / JournalQuickAddSheet; Diario JSX block in MyPlantDetailModal unconditional (verified via window-grep sentinel ±5 lines around onSectionLayout('diario')). |

**No orphaned requirements detected** — all 5 JOURNAL-01..05 IDs in REQUIREMENTS.md (lines 246-250) map to phase plans declaring them.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |

None detected. Scan covered the new and modified files (journalService.ts, JournalSection.tsx, JournalQuickAddSheet.tsx, JournalEntryRow.tsx, useStorage.tsx, MyPlantDetailModal.tsx, PlantsScreen.tsx, TodayScreen.tsx) for TODO/FIXME/XXX/HACK/PLACEHOLDER/placeholder/coming-soon/will-be-here + empty implementations + console-only handlers. All TODOs found are either pre-existing (out of scope) or non-blocking documentation comments. Pre-existing `home.emptyGardenText` tuteo string at `es/common.json:270` is OUT OF SCOPE per Phase 21-05 deviation-rule scope boundary; logged in 21-05 SUMMARY for future i18n hygiene work.

### Human Verification Required

Per phase prompt instruction, the 14-item Blocks A–E device-test checklist is DEFERRED to v1.2 milestone-end backlog per the established Option B precedent (Phase 18-05 / 19-07 / 20-10). The deferral is documented in `/Users/gaston/.claude/projects/-Users-gaston-Documents-Personal-MiJardinApp/memory/v1_2_test_backlog.md`.

This verifier explicitly does NOT flag `human_needed` for the deferred items — they are tracked in the v1.2 device-test backlog and are a v1.2 submission-gate blocker, NOT a Phase 21 closure blocker.

### Gaps Summary

None. All five Success Criteria from ROADMAP.md (Phase 21) map to verified observable truths backed by substantive, wired artifacts. All five requirement IDs (JOURNAL-01..05) close at the code level with smoke-runner enforcement (PASS=76 FAIL=0 SKIP=0). Cross-phase regression sentinels for Phase 18 (PASS=56) + Phase 19 (PASS=85) + Phase 20 (PASS=49) preserved. `tsc --noEmit` exit 0. `check:i18n-keys` PASS 118 catalog ids.

The Blossom cautionary tale invariant (JOURNAL-05) is locked at the smoke-runner level via two sentinels: (a) `JOURNAL-05.negative-grep.no-premium-gate-at-read-sites` OR-chain over all 3 Wave 3 components, (b) `JOURNAL-05.modal.diario-block-not-premium-gated` window-grep ±5 lines around the `onSectionLayout('diario')` anchor.

---

*Verified: 2026-05-11T23:30:00Z*
*Verifier: Claude (gsd-verifier)*
