---
phase: 21-plant-journal
plan: 05
type: execute
wave: 4
depends_on: ["21-04"]
files_modified:
  - src/components/plant-detail/JournalSection.tsx
  - src/components/plant-detail/JournalEntryRow.tsx
  - src/components/plant-detail/JournalQuickAddSheet.tsx
autonomous: true
requirements: [JOURNAL-05]
must_haves:
  truths:
    - "NO `usePremiumGate()` / `isPremium` / `canReadJournal` calls at journal read sites (JournalSection / JournalEntryRow / JournalQuickAddSheet / MyPlantDetailModal Diario render)"
    - "All `journal.*` i18n keys used by Wave 3 components have full EN+ES parity in common.json (Wave 0 already shipped the FULL ≥22-key namespace per Blocker 4; this plan only confirms parity via negative-grep — no JSON additions, no `??` fallback removals)"
    - "ES locale voseo discipline confirmed (no tuteo verb forms in journal.* namespace)"
    - "Plan 21-05 is now PURELY a negative-grep + parity verification plan — no copy additions; the i18n-finalization heavy lifting was completed at Wave 0 per Blocker 4 (22 keys including save/cancel/delete/deleteEntry/textPlaceholder/error.photoSaveFailed)"
  artifacts:
    - path: "src/components/plant-detail/JournalSection.tsx"
      provides: "(read-only verification target — no source mutation expected) NO premium-gate references"
      contains: "JournalSection"
    - path: "src/components/plant-detail/JournalEntryRow.tsx"
      provides: "(read-only verification target — no source mutation expected) NO premium-gate references"
      contains: "JournalEntryRow"
    - path: "src/components/plant-detail/JournalQuickAddSheet.tsx"
      provides: "(read-only verification target — no source mutation expected) NO premium-gate references"
      contains: "JournalQuickAddSheet"
  key_links:
    - from: "JournalSection.tsx + JournalEntryRow.tsx + JournalQuickAddSheet.tsx t('journal.*') calls"
      to: "common.json journal.* keyset (≥22 keys shipped at Wave 0)"
      via: "react-i18next resolution — bare t() calls (no `??` fallbacks)"
      pattern: "t\\(['\"]journal\\.[a-zA-Z_.]+['\"]\\)"
    - from: "JOURNAL-05 negative-grep sentinel"
      to: "absence of usePremiumGate / isPremium / canReadJournal at journal-read sites"
      via: "smoke runner negative-grep on JournalSection.tsx + JournalEntryRow.tsx + JournalQuickAddSheet.tsx"
      pattern: "!/usePremiumGate\\|isPremium\\|canReadJournal/"
---

<objective>
JOURNAL-05 negative-guard + i18n-parity confirmation. Run a grep audit at journal-read sites confirming NO premium-gate calls leak in (Blossom cautionary tale — historical user content is NEVER paywalled). Confirm the `journal.*` i18n namespace in `common.json` already covers ALL keys referenced by Wave 3 components — no JSON additions needed (Wave 0 shipped the full 22-key namespace per Blocker 4; Wave 3 used BARE `t()` calls with no `??` fallbacks).

Purpose: Journal entries are readable without any premium subscription check anywhere in the read path. i18n parity is intact end-to-end with no missing-key runtime warnings.

Output: 0 JSON files modified (Wave 0 already shipped the full namespace). 0 source files modified IF audit clean. JOURNAL-05 negative-grep + i18n parity SKIPs flip to PASS.

**Plan scope simplification (per Blocker 4 followup):** This plan was previously responsible for both adding 6 missing keys AND removing `??` fallback literals. Since Wave 0 now ships ALL 22 keys upfront (Blocker 4 fix in Plan 21-00) AND Plan 21-04 components use bare `t()` calls (no `??` fallbacks introduced), this plan reduces to a negative-grep audit + voseo discipline check + i18n parity confirmation. No copy edits, no fallback removals.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-plant-journal/21-CONTEXT.md
@.planning/phases/21-plant-journal/21-RESEARCH.md § Anti-Patterns § Don't Hand-Roll (premium gating)
@.planning/phases/21-plant-journal/21-04-SUMMARY.md
@src/i18n/locales/en/common.json
@src/i18n/locales/es/common.json
@src/components/plant-detail/JournalSection.tsx
@src/components/plant-detail/JournalEntryRow.tsx
@src/components/plant-detail/JournalQuickAddSheet.tsx

<interfaces>
Wave 0 (Plan 21-00, post-Blocker-4) shipped these 22 keys (EN+ES parity):
- `journal.header`, `journal.emptyState`, `journal.addEntry`, `journal.savedToast`
- `journal.photoCamera`, `journal.photoGallery`
- `journal.careTag.{riego,fertilizar,sol,poda,problema,otro}` (6 keys)
- `journal.deleteConfirm`
- `journal.dateLabel.{today,yesterday,daysAgo}` (3 keys)
- `journal.save`, `journal.cancel`, `journal.delete`, `journal.deleteEntry` (4 keys)
- `journal.textPlaceholder` (1 key)
- `journal.error.photoSaveFailed` (1 key)

Wave 3 (Plan 21-04) introduced exactly these `t('journal.*')` calls — ALL bare, no `??` fallbacks:
- JournalSection: `journal.header`, `journal.emptyState`, `journal.addEntry`
- JournalEntryRow: `journal.dateLabel.*`, `journal.careTag.*`, `journal.deleteEntry`, `journal.deleteConfirm`, `journal.cancel`, `journal.delete`
- JournalQuickAddSheet: `journal.textPlaceholder`, `journal.photoCamera`, `journal.photoGallery`, `journal.careTag.*`, `journal.cancel`, `journal.save`, `journal.error.photoSaveFailed`

Every Wave 3 t() call resolves to a Wave 0 key — no gaps.

JOURNAL-05 read sites (NEVER premium-gated):
- src/components/plant-detail/JournalSection.tsx — `entries={journals[plant.id] || []}` is the read site
- src/components/plant-detail/JournalEntryRow.tsx — renders entry data directly
- src/components/plant-detail/JournalQuickAddSheet.tsx — write path, but DO NOT premium-gate write either (per CONTEXT)
- src/components/MyPlantDetailModal.tsx — Diario section render

Premium-gate forbidden patterns (RESEARCH § Anti-Patterns):
- `usePremiumGate()` hook call
- `isPremium` boolean check
- `canReadJournal` / `canRead*` props
- `Features.PREMIUM_GATE` referenced at journal read paths (catalog-only)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confirm i18n parity + voseo discipline (verification-only — no JSON edits, no fallback removals; all heavy lifting was at Wave 0 per Blocker 4)</name>
  <files>src/components/plant-detail/JournalSection.tsx, src/components/plant-detail/JournalEntryRow.tsx, src/components/plant-detail/JournalQuickAddSheet.tsx</files>
  <read_first>
    - src/i18n/locales/en/common.json (current `journal.*` namespace — should already have ALL 22 keys from Wave 0)
    - src/i18n/locales/es/common.json (current `journal.*` namespace — should already have ALL 22 keys from Wave 0, voseo-correct)
    - src/components/plant-detail/JournalSection.tsx (verify all `t('journal.*')` calls resolve to existing keys — read-only check)
    - src/components/plant-detail/JournalEntryRow.tsx (same)
    - src/components/plant-detail/JournalQuickAddSheet.tsx (same)
    - CLAUDE.md § Internationalization (voseo: regá / sacá / podés / agregá / guardá; friendly casual tone)
  </read_first>
  <behavior>
    - Test 1: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/common.json','utf8'))"` exits 0 (valid JSON)
    - Test 2: Same for es/common.json
    - Test 3: Every `t('journal.X')` call site in the 3 Wave 3 components has a corresponding key in BOTH en and es common.json (no missing-key warnings at runtime)
    - Test 4: NO `??` fallback literals remain in any of the 3 Wave 3 component files (Blocker 4 — Wave 3 was specified to use bare t() calls; this just confirms)
    - Test 5: ES locale uses voseo for any action verb: `guardá` / `agregá` / `eliminá` / `escribí`; NOT `guarda` / `agrega` / `elimina` / `escribe`
  </behavior>
  <action>
    **Step 1: Run a key-coverage audit.** Verify every Wave 3 `t('journal.*')` call site has a corresponding key in BOTH common.json files:
    ```bash
    grep -rohE "t\\(['\"]journal\\.[a-zA-Z_.]+['\"]\\)" src/components/plant-detail/Journal*.tsx
    ```
    Cross-reference each output against `en/common.json` and `es/common.json`. Expected: ALL keys resolve (no gaps; Wave 0 shipped all 22 keys per Blocker 4).

    **If a key is missing from common.json (unexpected — Wave 0 should have covered it):**
    - Add the key to EN with a sensible English copy
    - Add the matching key to ES with voseo-correct copy
    - Re-run the audit
    
    But the expected outcome is: NO additions needed; Wave 0 already covers everything.

    **Step 2: Confirm NO `??` fallback literals remain in the 3 Wave 3 component files (Blocker 4 enforcement).**
    ```bash
    grep -E "t\\([^)]+\\)\\s*\\?\\?\\s*'" src/components/plant-detail/Journal*.tsx
    ```
    Expected: ZERO matches. Wave 3 was specified to use bare t() calls only (per Blocker 4). If any `??` fallback literals are found, REMOVE them — replace `t('journal.X') ?? 'literal'` with bare `t('journal.X')`. But the expected outcome is: NO removals needed; Plan 21-04 already used bare t() calls.

    **Note: theme-token `??` fallbacks like `colors.border ?? '#ccc'` are NOT i18n fallbacks and are exempt** — the grep pattern above only matches `t(...)`-prefixed `??` constructs.

    **Step 3: Confirm ES voseo discipline.**
    ```bash
    grep -E "(Agrega|Guarda|Elimina|Escribe) [a-z]" src/i18n/locales/es/common.json
    ```
    Expected: ZERO matches (no tuteo verb forms before a lowercase next word). If matches found, replace with voseo equivalents (`Agregá`, `Guardá`, `Eliminá`, `Escribí`).

    **Note:** Static button labels like `Guardar` / `Cancelar` / `Eliminar` (infinitives) are acceptable in ES — voseo only applies to imperative verb forms in copy text like "Agregá tu primera entrada" or "Escribí tu nota...". Plan 21-00 Wave 0 already shipped the correct verb forms (Blocker 4 keys: `journal.textPlaceholder` = "Escribí tu nota...", `journal.emptyState` = "Agregá tu primera entrada 📔").

    **Step 4: This task is verification-only.** Source code should require NO mutation if Plan 21-00 (Blocker 4) and Plan 21-04 (bare t() calls) were executed correctly. If mutation IS required (e.g., a `??` fallback slipped through), make the surgical fix and document in SUMMARY.
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/common.json','utf8'))"` exits 0
    - `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/es/common.json','utf8'))"` exits 0
    - `node -e "const en=require('./src/i18n/locales/en/common.json'); const es=require('./src/i18n/locales/es/common.json'); const flat=['header','emptyState','addEntry','savedToast','photoCamera','photoGallery','deleteConfirm','save','cancel','delete','deleteEntry','textPlaceholder']; flat.forEach(k=>{if(!en.journal[k])throw new Error('EN missing '+k); if(!es.journal[k])throw new Error('ES missing '+k);}); ['riego','fertilizar','sol','poda','problema','otro'].forEach(k=>{if(!en.journal.careTag[k]||!es.journal.careTag[k])throw new Error('careTag missing '+k);}); ['today','yesterday','daysAgo'].forEach(k=>{if(!en.journal.dateLabel[k]||!es.journal.dateLabel[k])throw new Error('dateLabel missing '+k);}); if(!en.journal.error?.photoSaveFailed||!es.journal.error?.photoSaveFailed)throw new Error('error.photoSaveFailed missing');"` exits 0
    - `grep -E "t\\([^)]+\\)\\s*\\?\\?\\s*'" src/components/plant-detail/Journal*.tsx` returns NO matches (Blocker 4 — bare t() only)
    - `grep -E "(Agrega|Guarda|Elimina|Escribe) [a-z]" src/i18n/locales/es/common.json` returns NO matches (voseo discipline — no tuteo verb forms before lowercase next word)
    - `npx tsc --noEmit` exits 0
    - `node scripts/smoke-phase21.cjs` shows all i18n.parity.* SKIPs flipped to PASS (from Wave 0 onward — this plan just confirms)
    - `npm run check:i18n-keys` exits 0 (catalog-content parity not affected by our common.json changes)
    - `npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/common.json','utf8'));JSON.parse(require('fs').readFileSync('src/i18n/locales/es/common.json','utf8'));" && npx tsc --noEmit && node scripts/smoke-phase21.cjs && npm run check:i18n-keys && npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20</automated>
  </verify>
  <done>EN + ES `journal.*` i18n namespaces cover all Wave 3 `t()` call sites (Wave 0 already shipped the full 22-key namespace per Blocker 4); ES voseo discipline confirmed; NO `??` fallback literals remain (Plan 21-04 used bare t() throughout per Blocker 4); smoke runner i18n.parity.* SKIPs flipped to PASS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JOURNAL-05 negative-grep guard (NO premium-gate at journal-read sites)</name>
  <files>src/components/plant-detail/JournalSection.tsx, src/components/plant-detail/JournalEntryRow.tsx, src/components/plant-detail/JournalQuickAddSheet.tsx</files>
  <read_first>
    - src/components/plant-detail/JournalSection.tsx (current contents — verify NO premium-gate imports or calls)
    - src/components/plant-detail/JournalEntryRow.tsx (same)
    - src/components/plant-detail/JournalQuickAddSheet.tsx (same — even though it's the write site, NO premium gate is allowed per CONTEXT)
    - src/components/MyPlantDetailModal.tsx Diario render block (verify no premium-gate wrapping the JournalSection render)
    - src/config/features.ts (PREMIUM_GATE flag context — should NOT be referenced at journal sites)
    - src/config/premium.ts (usePremiumGate hook — confirm NOT imported in any journal file)
    - .planning/phases/21-plant-journal/21-RESEARCH.md § Anti-Patterns "Reading entries through `usePremiumGate()`"
    - REQUIREMENTS.md JOURNAL-05 (Blossom cautionary tale)
  </read_first>
  <behavior>
    - Test 1: `grep -rE "usePremiumGate|isPremium|canReadJournal|canRead\\*|Features\\.PREMIUM_GATE" src/components/plant-detail/Journal*.tsx` returns NO matches
    - Test 2: `grep -E "usePremiumGate|isPremium|canReadJournal" src/components/MyPlantDetailModal.tsx` — IF matches exist, they MUST NOT be wrapping the JournalSection render (visual line-context check)
    - Test 3: `npx tsc --noEmit` exits 0
    - Test 4: Smoke runner JOURNAL-05.negative-grep.no-premium-gate-at-read-sites flips to PASS
  </behavior>
  <action>
    **Step 1: Audit the 3 component files for premium-gate references.**

    Run:
    ```bash
    grep -rE "usePremiumGate|isPremium|canReadJournal|canRead.*Journal" src/components/plant-detail/Journal*.tsx
    ```

    Expected: ZERO matches.

    **If any matches found** (unexpected — Plan 21-04 should not have introduced these):
    - Remove the import / hook call / conditional
    - The Diario section read path renders unconditionally; entries are ALWAYS visible (Blossom cautionary tale per JOURNAL-05)

    **Step 2: Audit MyPlantDetailModal Diario render block.**

    Verify the Diario section render does NOT have a premium-gate wrap. The block should look like:
    ```tsx
    <View onLayout={onSectionLayout('diario')}>
      <JournalSection .../>
    </View>
    ```

    NOT:
    ```tsx
    {isPremium && (   // ← FORBIDDEN
      <View onLayout={onSectionLayout('diario')}>
        <JournalSection .../>
      </View>
    )}
    ```

    **Step 3: NO new code is added in this task.** The audit is verification-only. If the audit passes (no premium-gate references at any journal site), the JOURNAL-05 sentinel flips to PASS automatically.

    **Step 4 (if audit fails):** Remove offending lines and commit the negative diff.

    **Why this is a "task" not just "audit":** the smoke runner's JOURNAL-05.negative-grep sentinel currently SKIPs (no journal files exist yet at Wave 0). After Plan 21-04 lands real impls, the sentinel becomes evaluable. This task's purpose is to ensure the sentinel evaluates to PASS (not FAIL). If audit reveals an unexpected premium-gate addition, fix it. If audit reveals zero premium-gate references, this task is a no-op confirming the invariant.
  </action>
  <acceptance_criteria>
    - `grep -rE "usePremiumGate|isPremium|canReadJournal" src/components/plant-detail/JournalSection.tsx src/components/plant-detail/JournalEntryRow.tsx src/components/plant-detail/JournalQuickAddSheet.tsx` returns NO matches (exit code 1 with empty output)
    - `grep -rE "import.*usePremiumGate.*from.*['\"]\\.\\./config/premium['\"]" src/components/plant-detail/Journal*.tsx` returns NO matches
    - `npx tsc --noEmit` exits 0
    - `node scripts/smoke-phase21.cjs` flips `JOURNAL-05.negative-grep.no-premium-gate-at-read-sites` SKIP to PASS
    - `npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>! grep -rE "usePremiumGate|isPremium|canReadJournal" src/components/plant-detail/JournalSection.tsx src/components/plant-detail/JournalEntryRow.tsx src/components/plant-detail/JournalQuickAddSheet.tsx && npx tsc --noEmit && node scripts/smoke-phase21.cjs && npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20</automated>
  </verify>
  <done>Journal read sites contain NO premium-gate references; JOURNAL-05 negative-grep sentinel flipped to PASS; Blossom cautionary tale invariant preserved (journal entries are always readable, regardless of subscription).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `node scripts/smoke-phase21.cjs` reports ALL JOURNAL-01..05 SKIPs flipped to PASS, all i18n.parity.* SKIPs flipped to PASS, ALL W0.scaffold.* PASSes still PASS, Phase 18/19/20 STRICT cross-phase regression all PASS
- `npm run check:i18n-keys` exits 0 (no plants.json drift introduced)
- `npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20 && npm run smoke:phase21` all exit 0
- No `??` fallback literals remain in Wave 3 component files (Plan 21-04 used bare t() per Blocker 4)
- No premium-gate references in any journal component
</verification>

<success_criteria>
- [x] EN + ES `journal.*` i18n namespace covers all Wave 3 t() call sites (Wave 0 shipped all 22 keys per Blocker 4)
- [x] ES locale voseo discipline confirmed (no tuteo verb forms in copy text)
- [x] NO `??` fallback literals in JournalSection / JournalEntryRow / JournalQuickAddSheet (Blocker 4 enforced at Wave 3)
- [x] JOURNAL-05 negative-grep sentinel evaluates PASS (no premium-gate at journal-read sites)
- [x] `npx tsc --noEmit` exits 0
- [x] All cross-phase regression remains PASS
- [x] Smoke runner reports 0 FAIL with all JOURNAL-* SKIPs flipped to PASS
</success_criteria>

<output>
After completion, create `.planning/phases/21-plant-journal/21-05-SUMMARY.md` per template.
</output>
