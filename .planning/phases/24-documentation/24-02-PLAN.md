---
phase: 24-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements:
  - DOCS-02
must_haves:
  truths:
    - "A future Claude session reading PROJECT.md Key Decisions table understands the v1.2 recommendation-first pivot rationale"
    - "A future Claude session reading PROJECT.md Key Decisions table sees the deep-merge guard for `updatePlant` as a locked architectural decision (CRIT-1)"
    - "A future Claude session reading PROJECT.md Key Decisions table sees the derived-only streaks / no-streak-counter anti-pattern lock (GAM-05)"
    - "A future Claude session reading PROJECT.md Key Decisions table sees journal photos in FileSystem (NOT base64 AsyncStorage)"
    - "A future Claude session reading PROJECT.md Key Decisions table sees the BottomSheetProvider single App-root wrap covering both AppContent paths"
    - "A future Claude session reading PROJECT.md Key Decisions table sees the three-tier smoke runner discipline and the milestone-end Option B device-test deferral pattern (5 consecutive precedents)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions table extended with 7 v1.2 architectural decisions"
      contains: "recommendation-first, deep-merge, derived-only streaks, journal photos in FileSystem, BottomSheetProvider, ModalSectionId, three-tier smoke, Option B deferral"
  key_links:
    - from: ".planning/PROJECT.md Key Decisions table"
      to: "Phase 14 EDU-06 (deep-merge guard)"
      via: "table row referencing useStorage.updatePlant PROTECTED_USER_FIELDS"
      pattern: "deep-merge|PROTECTED_USER_FIELDS"
    - from: ".planning/PROJECT.md Key Decisions table"
      to: "Phase 22 GAM-05 (anti-streak lock)"
      via: "table row referencing derived-only mood emoji + STRICT negative-grep"
      pattern: "GAM-05|derived-only"
    - from: ".planning/PROJECT.md Key Decisions table"
      to: "Phase 13 INFRA-02 (BottomSheetProvider single wrap)"
      via: "table row referencing single App-root wrap covering both AppContent paths"
      pattern: "BottomSheetModalProvider|two-AppContent-paths"
---

<objective>
DOCS-02 — Extend the Key Decisions table in `.planning/PROJECT.md` with v1.2 architectural decisions so future Claude sessions can reason about the locked patterns without re-deriving rationale.

Purpose: The Key Decisions table currently ends at row 22 (Perenual server-side move — Phase 10) and the "Last updated" footer still reads 2026-05-02. The table is the canonical surface for "what's locked and why" — Phases 11-23 introduced 6-7 additional architectural decisions worth surfacing. This plan extends the table append-only and bumps the footer date.

Output: Updated `.planning/PROJECT.md` with 7 new table rows + footer date refresh. ATOMIC commit.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-documentation/24-CONTEXT.md
@.planning/phases/24-documentation/24-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOCS-02 surgical PROJECT.md Key Decisions table extension (append 7 rows + bump footer)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    1. Read `.planning/PROJECT.md` in full (~125 lines).
    2. Confirm the Key Decisions table structure has not drifted:
       - Table header `| Decision | Rationale | Outcome |` exists
       - Last row references "Perenual API key moved server-side via get-plant-care edge function" (Phase 10)
       - Footer line `*Last updated: 2026-05-02 ...*` exists immediately after the table
    3. If structure has drifted (table reshaped or footer renamed), STOP and report — do not guess.
  </read_first>
  <action>
**Make TWO surgical edits to `.planning/PROJECT.md` using the Edit tool. Append-only — preserve all existing rows verbatim.**

---

### EDIT 1 — Append 7 v1.2 decision rows to the Key Decisions table

**Anchor (find this old_string verbatim — last existing row + closing horizontal rule):**

```
| Perenual API key moved server-side via get-plant-care edge function (was in client bundle since v1.0 as EXPO_PUBLIC_PERENUAL_API_KEY; rotation deferred for trusted-test-build context — to be done before public ship) | Key was accidentally exposed in client bundle since v1.0; Phase 10 moves it exclusively to Supabase secrets (Deno.env.get). Rotation deferred: build is distributed to 5 trusted testers only, not a public APK — risk bounded. Rotate before any public store release. | ✓ Done (v1.2 Phase 10) |

---
```

**Replace with (append the 7 new rows BEFORE the `---` separator; preserve the separator and the footer that follows):**

```
| Perenual API key moved server-side via get-plant-care edge function (was in client bundle since v1.0 as EXPO_PUBLIC_PERENUAL_API_KEY; rotation deferred for trusted-test-build context — to be done before public ship) | Key was accidentally exposed in client bundle since v1.0; Phase 10 moves it exclusively to Supabase secrets (Deno.env.get). Rotation deferred: build is distributed to 5 trusted testers only, not a public APK — risk bounded. Rotate before any public store release. | ✓ Done (v1.2 Phase 10) |
| Recommendation-first pivot — app recommends what to do with horticultural rationale; user adjusts to their reality (4-section MyPlantDetailModal: ¿Qué hacer? / ¿Dónde ponerla? / ¿Por qué? / Tus ajustes) | Pivot away from passive tracking. Catalog extended with 5 EDU fields (`careAction`, `placementRecommended`, `placementAlternatives`, `placementAvoid`, `whyRationale`); identification picker pre-selects the species' recommended `lightLevel`. "Tus ajustes" surfaces a soft override note when user differs from recommendation. Anchors the entire v1.2 milestone direction. | ✓ Good (v1.2 Phase 14) |
| Deep-merge guard for `useStorage.updatePlant` (PROTECTED_USER_FIELDS tuple) | CRIT-1 pitfall: catalog-source values silently overwriting user customizations on detail-modal re-open. Phase 14 EDU-06 introduced the PROTECTED_USER_FIELDS tuple guard; Phase 20 Plan 01 extended it to cover `Plant.fertilizeSchedule` (FERT-01). Any new user-editable field on `Plant` MUST be added to PROTECTED_USER_FIELDS at the same time. | ✓ Good (v1.2 Phase 14 + Phase 20) |
| Derived-only mood emoji / NO persistent streak counters in UI (GAM-05 anti-pattern lock) | Industry research: streak counters create anxiety and infantilize plant-serious users; missing a watering day is sometimes horticulturally correct. Mood emoji on PlantCard is DERIVED from `calculatePlantHealth(plant).healthLevel` — no per-plant streak history, no consecutive-day counters, no "N-day streak" UI anywhere. STRICT negative-grep enforced in `smoke-phase22.cjs` against `samplePlants/mockPlants/seedPlants/streakCount/CARE_STREAKS` tokens with documented whitelist (`gam_anti_patterns.md`). Blossom cautionary tale — sustained-engagement gamification was harmful at scale. Persistent streaks deferred to v2 (`STREAK-01/02` opt-in). | ✓ Good (v1.2 Phase 18 + Phase 22) |
| Journal photos in FileSystem `documentDirectory` (modern Paths/File/Directory API) — NEVER base64 in AsyncStorage | Phase 21 JOURNAL-02 lock: AsyncStorage has a ~6 MB practical ceiling on Android; base64 photo strings would saturate it within ~20 journal entries. `journalService.ts` saves to `<documentDirectory>/journals/<plantId>/<entryId>.jpg` (1080px @ 0.7 JPEG via `expo-image-manipulator`). `deletePlant` invokes `deleteJournalDirectory(plantId)` for orphan cleanup. `JournalEntry.photoUri` always holds a filesystem URI. Verified by startup size log + negative-grep against `data:image/.*;base64` tokens in journal code paths. | ✓ Good (v1.2 Phase 21) |
| Single App-root `BottomSheetModalProvider` wrap above the `Features.AUTH` branch (covers both AppContent paths via React context) | Phase 13 INFRA-02: the app has two AppContent paths (AUTH-on and AUTH-off) gated by `Features.AUTH`. Provider wraps at App.tsx root (above the branch) so both paths inherit the same `BottomSheetModalProvider` + `GestureHandlerRootView` context. Avoids double-wrapping (which would break sheet stacking) AND avoids per-branch wrapping (which would drift between AUTH-on/off). Same two-AppContent-paths discipline as the V1.1 `AuthProvider` skip pattern — preserved verbatim. PaywallModal Z-order coexistence verified in Phase 13 Plan 03 manual gate. | ✓ Good (v1.2 Phase 13) |
| ModalSectionId controlled extension precedent (Phase 19 `mascotas` + Phase 21 `diario`; Phase 20 deliberately did NOT extend) | `MyPlantDetailModal.ModalSectionId` is the discriminated union over scrollable sections. Phase 19 TOX-04 extended with `'mascotas'` (initialSection prop + ScrollView scrollTo-section mechanism). Phase 21 JOURNAL-04 extended with `'diario'`. Phase 20 FERT-06 deliberately did NOT extend — fertilize content lives inside the existing `'que-hacer'` section via orthogonal `initialExpanded` prop, avoiding union bloat for non-navigation expansion. Precedent: extend ModalSectionId ONLY when the new section requires a scroll-to-section navigation entry-point from outside the modal; use `initialExpanded` for in-section expansion. | ✓ Good (v1.2 Phase 19 + Phase 21) |
| Three-tier smoke runner discipline + STRICT cross-phase regression sentinels (Phase 19+); Option B end-of-milestone device-test deferral pattern (5 consecutive precedents — Phase 18-05 / 20-10 / 21-06 / 22-03 / 23-04; Phase 19-07 ONE outlier) | Three-tier pattern: (1) W0 scaffold STRICT PASS — files MUST exist; (2) SKIP→PASS placeholders — flip as later plans land code; (3) STRICT cross-phase regression — forked verbatim from prior phase, locks invariants. Each `smoke-phase<N>.cjs` runner ships in Wave 0 of its phase, is NOT modified after, and the latest runner in the chain (Phase 23 = `smoke-phase23.cjs`) is sufficient pre-submit coverage for Phase 18-22 invariants. Manual-checkpoint Option B (defer device-test to `v1_2_test_backlog.md` memory) is the established v1.2 closure pattern for non-source-code device-test gates — 5 consecutive precedents at the manual-checkpoint structural threshold. Phase 19-07 was the ONE Option A (run-now) outlier. Full manual device-test batch runs end-of-milestone before v1.2 App Store / Play Store submit, alongside the 69-entry image-upload backlog. | ✓ Good (v1.2 Phase 19-23) |

---
```

**CHECK after edit:**
- `grep -c "Recommendation-first pivot" .planning/PROJECT.md` returns 1
- `grep -c "PROTECTED_USER_FIELDS" .planning/PROJECT.md` returns 1
- `grep -c "GAM-05 anti-pattern lock" .planning/PROJECT.md` returns 1
- `grep -c "documentDirectory" .planning/PROJECT.md` returns 1
- `grep -c "BottomSheetModalProvider" .planning/PROJECT.md` returns 1
- `grep -c "ModalSectionId controlled extension" .planning/PROJECT.md` returns 1
- `grep -c "Three-tier smoke runner discipline" .planning/PROJECT.md` returns 1
- `grep -c "Option B" .planning/PROJECT.md` returns 1
- `grep -c "v1.2 Phase 10" .planning/PROJECT.md` returns 1 (pre-existing — verify NOT lost)
- Existing rows (camera-in-chat, AI-decides, severity-mapping, AppData envelope, mode-as-dispatcher, `getEffectiveSeason`, lookup-by-id catalog, App-level PaywallModal context, smoke runner v1.1, dual-payload, manual-ops deferral, Perenual server-side) ALL still present.

---

### EDIT 2 — Update footer "Last updated" date

**Anchor (find this old_string verbatim — footer line at end of file):**

```
*Last updated: 2026-05-02 after v1.2 Recommendation-First Plant Guide milestone defined (6 themes from UAT findings + UX diagnostic). Previous: v1.1 Precision Care shipped (39 plans / 6 phases).*
```

**Replace with:**

```
*Last updated: 2026-05-12 after v1.2 Recommendation-First Plant Guide milestone Phase 24 docs closure — Key Decisions table extended with 7 v1.2 architectural decisions (recommendation-first pivot, deep-merge guard, derived-only streaks, journal in FileSystem, single BottomSheetProvider wrap, ModalSectionId controlled extension, three-tier smoke + Option B deferral). Previous: 2026-05-02 v1.2 milestone defined. v1.1 Precision Care shipped (39 plans / 6 phases).*
```

**CHECK after edit:**
- `grep -c "Last updated: 2026-05-12" .planning/PROJECT.md` returns 1
- `grep -c "Last updated: 2026-05-02" .planning/PROJECT.md` returns 0 (replaced)

---

### POST-EDIT VERIFICATION (run after both edits)

```bash
# New v1.2 decision row sentinels (8 new content phrases)
grep -c "Recommendation-first pivot" .planning/PROJECT.md          # expect: 1
grep -c "PROTECTED_USER_FIELDS" .planning/PROJECT.md               # expect: 1
grep -c "GAM-05 anti-pattern lock" .planning/PROJECT.md            # expect: 1
grep -c "documentDirectory" .planning/PROJECT.md                   # expect: 1
grep -c "BottomSheetModalProvider" .planning/PROJECT.md            # expect: 1
grep -c "ModalSectionId controlled extension" .planning/PROJECT.md # expect: 1
grep -c "Three-tier smoke runner discipline" .planning/PROJECT.md  # expect: 1
grep -c "Option B" .planning/PROJECT.md                            # expect: 1

# Footer date refresh
grep -c "Last updated: 2026-05-12" .planning/PROJECT.md            # expect: 1
grep -c "Last updated: 2026-05-02" .planning/PROJECT.md            # expect: 0

# Pre-existing row preservation (12 existing decisions — must all survive)
grep -c "Camera in chat follows existing message gate" .planning/PROJECT.md     # expect: 1
grep -c "AI decides follow-up frequency" .planning/PROJECT.md                    # expect: 1
grep -c "AI suggests resolution" .planning/PROJECT.md                            # expect: 1
grep -c "Problem timeline in plant detail" .planning/PROJECT.md                  # expect: 1
grep -c "Push + Hoy tasks" .planning/PROJECT.md                                  # expect: 1
grep -c "Extend SavedDiagnosis in-place" .planning/PROJECT.md                    # expect: 1
grep -c "NotificationContext for deep-link" .planning/PROJECT.md                 # expect: 1
grep -c "Emoji + text severity labels" .planning/PROJECT.md                      # expect: 1
grep -c "Three-zone seasonality" .planning/PROJECT.md                            # expect: 1
grep -c "versioned envelope" .planning/PROJECT.md                                # expect: 1
grep -c "Mode-as-dispatcher" .planning/PROJECT.md                                # expect: 1
grep -c "getEffectiveSeason" .planning/PROJECT.md                                # expect: 1
grep -c "Lookup-by-id catalog content" .planning/PROJECT.md                      # expect: 1
grep -c "App-level PaywallModal context" .planning/PROJECT.md                    # expect: 1
grep -c "Single-compile-path smoke runner" .planning/PROJECT.md                  # expect: 1
grep -c "Edge function dual-payload" .planning/PROJECT.md                        # expect: 1
grep -c "Defer manual ops" .planning/PROJECT.md                                  # expect: 1
grep -c "v1.2 Phase 10" .planning/PROJECT.md                                     # expect: 1

# Structure preservation — top-level headers unchanged
grep -c "^## Key Decisions" .planning/PROJECT.md                                 # expect: 1
grep -c "^| Decision | Rationale | Outcome |" .planning/PROJECT.md               # expect: 1
grep -c "^|----------|-----------|---------|" .planning/PROJECT.md               # expect: 1
```

If ANY sentinel fails, fix before commit. Do not commit partial edits.

---

### COMMIT (atomic — single commit, single file)

```bash
node "/Users/gaston/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(24-02): extend PROJECT.md Key Decisions table with v1.2 architectural decisions (DOCS-02 — 7 new rows + footer date)" --files .planning/PROJECT.md
```
  </action>
  <verify>
    <automated>bash -c "grep -c 'Recommendation-first pivot' .planning/PROJECT.md && grep -c 'PROTECTED_USER_FIELDS' .planning/PROJECT.md && grep -c 'GAM-05 anti-pattern lock' .planning/PROJECT.md && grep -c 'documentDirectory' .planning/PROJECT.md && grep -c 'BottomSheetModalProvider' .planning/PROJECT.md && grep -c 'ModalSectionId controlled extension' .planning/PROJECT.md && grep -c 'Three-tier smoke runner discipline' .planning/PROJECT.md && grep -c 'Option B' .planning/PROJECT.md && grep -c 'Last updated: 2026-05-12' .planning/PROJECT.md && test \$(grep -c 'Last updated: 2026-05-02' .planning/PROJECT.md) -eq 0"</automated>
  </verify>
  <done>
- `.planning/PROJECT.md` Key Decisions table has 7 new rows appended (decisions 23-29 by sequence): recommendation-first pivot, deep-merge guard, derived-only streaks (GAM-05 lock), journal in FileSystem, single BottomSheetProvider wrap, ModalSectionId controlled extension, three-tier smoke + Option B deferral
- All 12 pre-existing decision rows preserved verbatim
- Footer "Last updated" line refreshed to 2026-05-12 with v1.2 closure note
- Existing PROJECT.md structure preserved: `## What This Is` / `## Core Value` / `## Requirements` / `## Context` / `## Constraints` / `## Key Decisions` sections all intact
- Single atomic commit landed on `.planning/PROJECT.md` only
  </done>
</task>

</tasks>

<verification>
After this plan completes:
- `git diff HEAD~1 -- .planning/PROJECT.md | wc -l` shows a clearly-bounded additive diff (7 new table rows + 1 footer line replacement; expected ~10-15 lines added net of the footer swap)
- Cross-phase Phase 18-23 smoke runners remain green (this plan touches no source code):
  - `node scripts/smoke-phase18.cjs` exits 0
  - `node scripts/smoke-phase19.cjs` exits 0
  - `node scripts/smoke-phase20.cjs` exits 0
  - `node scripts/smoke-phase21.cjs` exits 0
  - `node scripts/smoke-phase22.cjs` exits 0
  - `node scripts/smoke-phase23.cjs` exits 0
- `npx tsc --noEmit` exits 0 (this plan touches no `.ts`/`.tsx`)
- `npm run check:i18n-keys` exits 0 (this plan touches no `plants.json`)
- `npm run lint:voseo` exits 0 (this plan touches no `common.json` ES microcopy)
- Plan 24-01 already landed (CLAUDE.md extensions) — this plan's `depends_on: ["24-01"]` is satisfied
</verification>

<success_criteria>
DOCS-02 is complete when a future Claude session reading `.planning/PROJECT.md` Key Decisions table can answer ALL of the following without re-reading phase summaries:
1. "Why is v1.2 not just more tracking features?" — Answer: Recommendation-first pivot (Phase 14) — app recommends, user adjusts; 4-section modal
2. "Why does `updatePlant` use a deep-merge guard?" — Answer: CRIT-1 — catalog re-open would silently overwrite user customizations; PROTECTED_USER_FIELDS tuple lock
3. "Why no streak counters?" — Answer: GAM-05 anti-pattern lock — mood emoji is derived; persistent streaks deferred to v2 (STREAK-01/02 opt-in)
4. "Where do journal photos live?" — Answer: `documentDirectory` (FileSystem); never base64 in AsyncStorage
5. "Why does App.tsx wrap BottomSheetModalProvider at root?" — Answer: Two-AppContent-paths discipline — single wrap above `Features.AUTH` branch covers both paths
6. "When do I extend ModalSectionId vs use initialExpanded?" — Answer: Extend ModalSectionId only for scroll-to-section navigation; use `initialExpanded` for in-section expansion (Phase 20 precedent)
7. "What's the smoke runner discipline?" — Answer: Three-tier (W0 STRICT / SKIP→PASS / cross-phase STRICT); latest runner suffices pre-submit
8. "Why are device-tests deferred?" — Answer: Option B end-of-milestone batch pattern — 5 consecutive precedents (Phase 18-05/20-10/21-06/22-03/23-04)
</success_criteria>

<output>
After completion, create `.planning/phases/24-documentation/24-02-SUMMARY.md` per the standard SUMMARY template:
- What was done (Key Decisions table extended with 7 v1.2 rows + footer date refresh)
- Files modified (.planning/PROJECT.md only)
- Commit SHA + message
- Verification: grep sentinel counts (new rows + pre-existing rows preservation) + cross-phase smoke runner exit codes
- Phase 24 closure: both DOCS-01 (Plan 24-01) and DOCS-02 (Plan 24-02) complete; v1.2 milestone fully landed at code+docs level; only manual ops backlog (device-tests + 69 image uploads + Perenual key rotation before public ship) remain before v1.2 App Store / Play Store submit
</output>
