---
phase: 24-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - "A future Claude session reading CLAUDE.md learns about the `get-plant-care` edge function and how to deploy it"
    - "A future Claude session reading CLAUDE.md learns to run `npm run lint:voseo` + the smoke-phase18..23 chain before any v1.2 submit"
    - "A future Claude session reading CLAUDE.md learns where journal photos live (FileSystem documentDirectory) and that they are NOT base64 in AsyncStorage"
    - "A future Claude session reading CLAUDE.md learns that `unknownPlantTracker.ts` exists and logs catalog misses to `@unknown_plants` AsyncStorage"
    - "A future Claude session reading CLAUDE.md learns the three-tier smoke runner pattern (W0 scaffold / SKIP→PASS / STRICT cross-phase regression) used by Phases 18-23"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Authoritative handoff context: Pre-submit Checks extended, Architecture §Key Patterns extended, Services Layer extended, Security section verified current"
      contains: "lint:voseo, smoke:phase18, smoke:phase23, journalService, documentDirectory, unknownPlantTracker, three-tier smoke runner discipline"
  key_links:
    - from: "Pre-submit Checks section"
      to: "voseo-lint.mjs + smoke-phase{18..23}.cjs chain"
      via: "shell command literals in the bash code block"
      pattern: "npm run lint:voseo|smoke:phase18"
    - from: "Architecture §Key Patterns"
      to: "journalService.ts (Phase 21 JOURNAL-02)"
      via: "bullet describing documentDirectory + per-plant subdirectory + orphan cleanup"
      pattern: "documentDirectory|journalService"
    - from: "Architecture §Services Layer"
      to: "unknownPlantTracker.ts (Phase 12 TRACK-01)"
      via: "bullet describing fire-and-forget catalog-miss logger"
      pattern: "unknownPlantTracker"
---

<objective>
DOCS-01 — Extend `CLAUDE.md` with v1.2 architecture decisions so future Claude sessions start with correct context. Surgical additions to four sections; preserve existing structure. No source-code edits, no behavioral changes.

Purpose: `CLAUDE.md` is the authoritative handoff document. After 14 v1.2 phases (10-23), the file currently documents `get-plant-care` deploy command (line 20) and the Perenual server-side guard (line 233), but does not yet surface:
  1. The `lint:voseo` + `smoke-phase{18..23}.cjs` pre-submit chain (Phase 23 POLISH-06 + the three-tier runner pattern established since Phase 18)
  2. The journal photo storage strategy (Phase 21 — `documentDirectory` + modern Paths API + per-plant subdirectory + orphan cleanup; explicitly NOT base64 in AsyncStorage)
  3. The `unknownPlantTracker.ts` service (Phase 12 — fire-and-forget catalog-miss logger, future expansion prioritization)
  4. The three-tier smoke runner discipline (Phase 19+ pattern, STRICT cross-phase regression sentinels)

Output: Updated `CLAUDE.md` with 4 surgical additions. ATOMIC commit.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-documentation/24-CONTEXT.md
@CLAUDE.md
@scripts/smoke-phase23.cjs
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOCS-01 surgical CLAUDE.md extensions (4 sections)</name>
  <files>CLAUDE.md</files>
  <read_first>
    1. Read CLAUDE.md in full (use offset/limit if needed; file is ~234 lines).
    2. Confirm structure has not drifted from this plan's assumptions:
       - "## Pre-submit Checks" section starts around line 168
       - "### Key Patterns" subsection starts around line 64 within "## Architecture"
       - "### Services Layer" subsection starts around line 75 within "## Architecture"
       - "## Security" section starts around line 229
    3. If structure has drifted (sections renamed/reordered), STOP and report — do not guess.
  </read_first>
  <action>
**Make FOUR surgical edits to CLAUDE.md using the Edit tool. Each edit is an append-style insertion that preserves existing structure. Do NOT rewrite full sections; do NOT reflow paragraphs. Use the Edit tool's old_string/new_string pattern with sufficient unique anchors.**

---

### EDIT 1 — Extend "### Key Patterns" subsection (Architecture)

**Anchor (find this old_string verbatim — last bullet in the Key Patterns list):**

```
- **Feature-gated rendering** — Components check `Features.*` and `usePremiumGate()` before rendering gated UI
```

**Replace with (append 2 new bullets after the Feature-gated rendering one — preserve trailing blank line):**

```
- **Feature-gated rendering** — Components check `Features.*` and `usePremiumGate()` before rendering gated UI
- **Journal photo storage** — `journalService.ts` (Phase 21) saves journal photos to `expo-file-system` `documentDirectory` via the modern `Paths`/`File`/`Directory` API under per-plant subdirectories (`<documentDirectory>/journals/<plantId>/<entryId>.jpg`, compressed to 1080px @ 0.7 JPEG). Photo URIs in `JournalEntry.photoUri` are filesystem paths, NEVER base64 strings in AsyncStorage. `deletePlant` calls `deleteJournalDirectory(plantId)` for orphan cleanup
- **Three-tier smoke runner discipline (Phase 19+)** — Each non-trivial phase ships a `scripts/smoke-phase<N>.cjs` runner with three tiers: (1) **W0 scaffold STRICT PASS** asserting Wave-0 files exist; (2) **SKIP→PASS placeholders** that flip from SKIP to PASS as later plans land concrete code; (3) **STRICT cross-phase regression sentinels** forked verbatim from the prior phase's runner (NEVER SKIP) — locks Phase 18-23 invariants like `PlantCard.moodEmoji`, `MyPlantDetailModal.ModalSectionId`, `useStorage.triggerHaptic`, `journals?: Record<string, JournalEntry[]>`, `petToxicity` symptoms validation, `fertilizer.industrial/homemade` validation, and GAM-05 streak-counter negative-grep
```

**CHECK after edit:** `grep -c "Journal photo storage" CLAUDE.md` returns 1. `grep -c "Three-tier smoke runner" CLAUDE.md` returns 1.

---

### EDIT 2 — Extend "### Services Layer" subsection (Architecture)

**Anchor (find this old_string verbatim — last bullet in the Services Layer list):**

```
- `imageService.ts` — (V1.1) Upload/delete plant photos to Supabase Storage bucket `plant-images`
```

**Replace with (append 2 new bullets after imageService — preserve trailing blank line + the next "### Supabase Configuration" header):**

```
- `imageService.ts` — (V1.1) Upload/delete plant photos to Supabase Storage bucket `plant-images`
- `unknownPlantTracker.ts` — (v1.2 Phase 12) Fire-and-forget tracker writing catalog misses to AsyncStorage key `@unknown_plants` (Record<scientificName, UnknownPlantEntry>). Called from `getEnrichedPlantData()` BEFORE the Perenual fallback when `getPlantById(scientificName)` returns no curated entry. `getUnknownPlantsReport()` reads back sorted desc by count for Settings → Dev tools display. Future v2 (`TRACK-V01`) migrates to a Supabase `unknown_plant_requests` table once AUTH ships
- `journalService.ts` — (v1.2 Phase 21) Per-plant journal photo I/O against `expo-file-system` `documentDirectory` via the modern `Paths`/`File`/`Directory` API: `pickJournalPhoto()`, `saveJournalPhoto(plantId, entryId, srcUri)`, `deleteJournalPhoto(uri)`, `deleteJournalDirectory(plantId)`. Photos are saved as 1080px @ 0.7 JPEG (`expo-image-manipulator`) into `<documentDirectory>/journals/<plantId>/<entryId>.jpg`. `deletePlant` invokes `deleteJournalDirectory` for orphan cleanup — no base64 strings in AsyncStorage
```

**CHECK after edit:** `grep -c "unknownPlantTracker" CLAUDE.md` returns 1. `grep -c "journalService" CLAUDE.md` returns at least 2 (Key Patterns + Services Layer references).

---

### EDIT 3 — Extend "## Pre-submit Checks" section

**Anchor (find this old_string verbatim — the existing bash code block intro):**

```
## Pre-submit Checks

Two npm scripts must be run before submitting builds to catch catalog drift:

```bash
npm run check:i18n-keys   # sync; ~2s; fails on missing en/es plants.json keys
npm run check:images      # async network HEAD; ~30-60s; fails on 404 imageUrl
```
```

**Replace with (extend the bash block + add a new paragraph explaining the v1.2 chain — preserve everything after this section verbatim):**

````
## Pre-submit Checks

Before submitting builds, run the following commands to catch catalog drift, ES microcopy drift, and cross-phase regressions:

```bash
npm run check:i18n-keys     # sync; ~2s; fails on missing en/es plants.json keys (incl. conditional petToxicity.symptoms + fertilizer.industrial/homemade sub-fields)
npm run check:images        # async network HEAD; ~30-60s; fails on 404 imageUrl
npm run lint:voseo          # STRICT; ~1s; fails on ES action buttons missing voseo + emoji
node scripts/smoke-phase18.cjs    # Phase 18 — PlantCard 5-element layout + mood emoji + Gesture.Pan + Toast
node scripts/smoke-phase19.cjs    # Phase 19 — Pet toxicity badges + Mascotas section + initialSection prop
node scripts/smoke-phase20.cjs    # Phase 20 — Fertilize task type 5-site discriminator + FertilizeCard
node scripts/smoke-phase21.cjs    # Phase 21 — Journal data layer + FileSystem service + UI surfaces
node scripts/smoke-phase22.cjs    # Phase 22 — Gamification toasts + haptics + GAM-05 anti-streak negative-grep
node scripts/smoke-phase23.cjs    # Phase 23 — POLISH-01..08 + STRICT cross-phase regression Phase 18-22 sentinels
```

The smoke-phase{18..23}.cjs chain enforces the **three-tier discipline** (W0 scaffold / SKIP→PASS / STRICT cross-phase regression) described in Architecture §Key Patterns. Each runner forks the prior phase's STRICT cross-phase block verbatim, so running the latest (`smoke-phase23.cjs`) is sufficient to cover Phase 18-22 invariants. The full chain is recommended pre-submit to localize regressions to the originating phase.
````

**Then ALSO update the "Commands" block at the top of CLAUDE.md (around line 11-22)** to add the new pre-submit lines under the existing commands. Find old_string:

```
npm run check:i18n-keys       # Pre-submit: catalog id ↔ i18n keyset parity
npm run check:images          # Pre-submit: catalog imageUrl 200 OK (network)
```

Replace with:

```
npm run check:i18n-keys       # Pre-submit: catalog id ↔ i18n keyset parity
npm run check:images          # Pre-submit: catalog imageUrl 200 OK (network)
npm run lint:voseo            # Pre-submit: ES action buttons voseo + emoji discipline (STRICT)
node scripts/smoke-phase23.cjs  # Pre-submit: STRICT cross-phase regression (forks Phase 18-22 sentinels)
```

**CHECK after edits:** `grep -c "lint:voseo" CLAUDE.md` returns at least 3 (Commands block + Pre-submit Checks block + paragraph reference). `grep -c "smoke-phase23" CLAUDE.md` returns at least 2 (Commands block + Pre-submit Checks block). `grep -c "smoke-phase18" CLAUDE.md` returns 1.

---

### EDIT 4 — Verify Security section text current (NO content change expected)

**Anchor (find this old_string verbatim — Security paragraph that already exists at the end of CLAUDE.md):**

```
**Pre-submit grep guard (Phase 10 SEC-01):** `EXPO_PUBLIC_PERENUAL_API_KEY` MUST NOT appear in `.env`, `.env.example`, `app.json`, or anywhere under `src/`. The Perenual API key lives ONLY in Supabase secrets (`PERENUAL_API_KEY`) and is accessed server-side via `Deno.env.get` in `supabase/functions/get-plant-care/index.ts`. Verify with: `grep -rc "EXPO_PUBLIC_PERENUAL_API_KEY" src/ .env .env.example app.json` — every line must show count 0. The pre-Phase-10 leaked key has been rotated as of v1.2.
```

**Status check (no edit needed if text matches verbatim above):**
- Run: `grep -n "EXPO_PUBLIC_PERENUAL_API_KEY" CLAUDE.md` — confirm it appears exactly in the Security section paragraph quoted above and nowhere else in CLAUDE.md (the file's own documentation of the guard is OK; the guard's negative-grep target is `src/` / `.env` / `.env.example` / `app.json`, NOT CLAUDE.md itself).
- If the paragraph already states "has been rotated as of v1.2", NO edit is needed for this section.
- If the paragraph still says "pending rotation" or any non-rotated language, update to the verbatim text above.

This step is verification-only by design — Phase 10 already authored this paragraph and STATE.md confirms it has stood since 2026-05-03. Skip the edit if text is already current.

---

### POST-EDIT VERIFICATION (run after all 4 edits)

Run these greps and confirm ALL are non-zero / as specified:

```bash
# Edit 1 sentinels (Key Patterns extension)
grep -c "Journal photo storage" CLAUDE.md          # expect: 1
grep -c "Three-tier smoke runner" CLAUDE.md        # expect: 1
grep -c "documentDirectory" CLAUDE.md              # expect: at least 2 (Key Patterns + Services Layer)

# Edit 2 sentinels (Services Layer extension)
grep -c "unknownPlantTracker" CLAUDE.md            # expect: 1
grep -c "@unknown_plants" CLAUDE.md                # expect: 1
grep -c "journalService" CLAUDE.md                 # expect: at least 2 (Key Patterns + Services Layer)
grep -c "deleteJournalDirectory" CLAUDE.md         # expect: at least 1

# Edit 3 sentinels (Pre-submit Checks extension + Commands block)
grep -c "lint:voseo" CLAUDE.md                     # expect: at least 3
grep -c "smoke-phase23" CLAUDE.md                  # expect: at least 2
grep -c "smoke-phase18" CLAUDE.md                  # expect: 1
grep -c "smoke-phase19" CLAUDE.md                  # expect: 1
grep -c "smoke-phase20" CLAUDE.md                  # expect: 1
grep -c "smoke-phase21" CLAUDE.md                  # expect: 1
grep -c "smoke-phase22" CLAUDE.md                  # expect: 1
grep -c "three-tier discipline" CLAUDE.md          # expect: 1

# Edit 4 sentinel (Security section verification — no edit if already current)
grep -c "has been rotated as of v1.2" CLAUDE.md    # expect: 1 (already present per STATE.md)

# Structure-preservation invariants
grep -c "^## About" CLAUDE.md                      # expect: 1 (unchanged)
grep -c "^## Architecture" CLAUDE.md               # expect: 1 (unchanged)
grep -c "^### Key Patterns" CLAUDE.md              # expect: 1 (unchanged)
grep -c "^### Services Layer" CLAUDE.md            # expect: 1 (unchanged)
grep -c "^## Pre-submit Checks" CLAUDE.md          # expect: 1 (unchanged)
grep -c "^## Security" CLAUDE.md                   # expect: 1 (unchanged)
```

If ANY sentinel fails, fix before commit. Do not commit partial edits.

---

### COMMIT (atomic — single commit, single file)

```bash
node "/Users/gaston/.claude/get-shit-done/bin/gsd-tools.cjs" commit "docs(24-01): extend CLAUDE.md with v1.2 architecture (DOCS-01 — journal/tracker/voseo-lint/smoke-chain/three-tier discipline)" --files CLAUDE.md
```
  </action>
  <verify>
    <automated>bash -c "grep -c 'lint:voseo' CLAUDE.md && grep -c 'unknownPlantTracker' CLAUDE.md && grep -c 'journalService' CLAUDE.md && grep -c 'Three-tier smoke runner' CLAUDE.md && grep -c 'smoke-phase23' CLAUDE.md && grep -c 'documentDirectory' CLAUDE.md && grep -c '@unknown_plants' CLAUDE.md && grep -c 'has been rotated as of v1.2' CLAUDE.md"</automated>
  </verify>
  <done>
- `CLAUDE.md` contains the 4 surgical additions (Key Patterns +2 bullets, Services Layer +2 bullets, Pre-submit Checks extended + Commands block extended, Security section verified current)
- All post-edit grep sentinels pass (counts as listed above)
- Existing CLAUDE.md structure preserved: 6 top-level `##` section headers unchanged, 2 targeted `###` subsection headers unchanged, no paragraphs deleted, no behavioral docs altered
- Single atomic commit landed on `CLAUDE.md` only
  </done>
</task>

</tasks>

<verification>
After this plan completes:
- `git diff HEAD~1 -- CLAUDE.md | wc -l` shows a clearly-bounded additive diff (no full-file rewrite; expected ~30-50 lines added)
- Cross-phase Phase 18-23 smoke runners remain green (this plan touches no source code):
  - `node scripts/smoke-phase18.cjs` exits 0
  - `node scripts/smoke-phase19.cjs` exits 0
  - `node scripts/smoke-phase20.cjs` exits 0
  - `node scripts/smoke-phase21.cjs` exits 0
  - `node scripts/smoke-phase22.cjs` exits 0
  - `node scripts/smoke-phase23.cjs` exits 0
- `npx tsc --noEmit` exits 0 (this plan touches no `.ts`/`.tsx`)
- `npm run check:i18n-keys` exits 0 (this plan touches no `plants.json`)
- `npm run lint:voseo` exits 0 (this plan touches no `common.json` ES microcopy)
</verification>

<success_criteria>
DOCS-01 is complete when a future Claude session reading `CLAUDE.md` can answer ALL of the following without grepping the codebase:
1. "Where do journal photos live?" — Answer: FileSystem documentDirectory under `journals/<plantId>/<entryId>.jpg`; NOT base64 in AsyncStorage
2. "How do I deploy `get-plant-care`?" — Answer: `source .envrc && supabase functions deploy get-plant-care` (already documented at line 20, preserved)
3. "What pre-submit checks must pass?" — Answer: `check:i18n-keys` + `check:images` + `lint:voseo` + `smoke-phase{18..23}.cjs` chain
4. "What's the three-tier smoke runner pattern?" — Answer: W0 scaffold / SKIP→PASS / STRICT cross-phase regression
5. "Where do catalog misses get logged?" — Answer: `unknownPlantTracker.ts` writes to `@unknown_plants` AsyncStorage key
6. "Is the Perenual key still in the client bundle?" — Answer: No, server-side only since Phase 10; rotated; pre-submit grep guard active
</success_criteria>

<output>
After completion, create `.planning/phases/24-documentation/24-01-SUMMARY.md` per the standard SUMMARY template:
- What was done (4 surgical additions enumerated)
- Files modified (CLAUDE.md only)
- Commit SHA + message
- Verification: grep sentinel counts + cross-phase smoke runner exit codes
- No follow-up work; clean phase plan handoff to Plan 24-02
</output>
