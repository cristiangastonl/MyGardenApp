---
phase: 15-catalog-wave-a-interior-tropicals
plan: 04
type: execute
wave: 3
depends_on:
  - "15-00"
  - "15-01"
  - "15-02"
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - CAT-12
gap_closure: false

must_haves:
  truths:
    - "CLAUDE.md 'Accepted-known failures' section is extended with a Phase 15 Wave A block listing all 23 ids"
    - "The new block follows the v1.1 LATAM 15-entry precedent (single grouped block, NOT 23 individual lines) and references the milestone-end batch upload pattern"
    - "Phase 15 smoke runner W4.CAT-12 placeholder flips from SKIP to PASS (CLAUDE.md mentions ≥20 of 23 ids)"
    - "`npm run check:images` is EXPECTED to fail for the 23 new entries (their imageUrls 404 until manual upload) — this failure is now documented and accepted, NOT a Phase 15 ship blocker"
    - "Phase 15 closes — all 4 requirements (CAT-09/10/11/12) are PASSing in smoke + check:i18n-keys + tsc --noEmit"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Accepted-known failures registry entry for Phase 15 Wave A — single grouped block listing all 23 ids"
      contains: "Phase 15 Wave A"
  key_links:
    - from: "CLAUDE.md 'Accepted-known failures' section"
      to: "scripts/check-images.mjs failure list"
      via: "documented exception — failures expected for the 23 ids until manual upload"
      pattern: "Phase 15 Wave A"
    - from: "Phase 15 ship gate"
      to: "milestone-end image upload backlog"
      via: "manual ops batched at milestone end (mirrors v1.1 + v1.2 device-test pattern)"
      pattern: "milestone-end batch upload"
---

<objective>
Document the 23 Phase 15 Wave A entries as accepted-known image failures in CLAUDE.md, mirroring the v1.1 LATAM 15-entry precedent. Close CAT-12 — the final Phase 15 requirement.

Purpose: Per RESEARCH §Pattern 5 + CONTEXT.md decision, image upload is deferred to milestone-end batch operations. The smoke gate (`npm run check:images`) intentionally fails for these 23 ids, but the failure is now declared and accepted in project-level documentation — Phase 15 ships green on every other gate.

Output:
- `CLAUDE.md` — appended "Phase 15 Wave A (v1.2, 23 entries — image upload pending)" block under the existing "Accepted-known failures (Phase 8, v1.1)" section.
- Phase 15 smoke runner: W4.CAT-12 SKIP flips to PASS.
- Phase 15 fully closed: CAT-09 (Plan 15-02 ✓) + CAT-10 (Plan 15-02 ✓) + CAT-11 (Plan 15-03 ✓) + CAT-12 (this plan ✓).
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-catalog-wave-a-interior-tropicals/15-CONTEXT.md
@.planning/phases/15-catalog-wave-a-interior-tropicals/15-RESEARCH.md
@.planning/phases/15-catalog-wave-a-interior-tropicals/15-VALIDATION.md
@.planning/phases/15-catalog-wave-a-interior-tropicals/15-00-PLAN.md
@.planning/phases/15-catalog-wave-a-interior-tropicals/15-01-PLAN.md
@.planning/phases/15-catalog-wave-a-interior-tropicals/15-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Existing CLAUDE.md "Accepted-known failures" block (line 179-187): -->
```markdown
**`check:images`** — HEAD-requests every `imageUrl` in PLANT_DATABASE at concurrency 8. Exits 1 with itemised URL list on any non-200. **Accepted-known failures (Phase 8, v1.1):** the following 15 entries point at imageUrls that 404 until manual image upload to Supabase Storage:

- jacaranda, ceibo, glicina, gardenia, camelia, dalia
- salvia-ornamental, cala, copete, verbena
- lavanda-stoechas, lavanda-dentada
- romero-rastrero, tomate-cherry
- lavanda-angustifolia (renamed from lavanda; old lavanda.jpg needs re-upload as lavanda-angustifolia.jpg)

These failures are documented in the v1.1 device-test backlog. Image upload steps:
1. Source images (CC0/public domain or user-shot photos).
2. Upload to Supabase Storage bucket `plant-images/catalog/<id>.jpg`.
3. Re-run `npm run check:images` — should now pass.
```

<!-- The 23 Phase 15 ids to document (sub-batch order matches Plans 15-01/02 append order): -->
<!-- Sub-batch A (Aroceous): anthurium, alocasia, caladium, singonio, aglaonema, costilla-adan, difenbaquia -->
<!-- Sub-batch A (Foliage especial): begonia-rex, croton, fitonia, ficus-lyrata, maranta -->
<!-- Sub-batch B (CAM): zamioculca, cola-burro -->
<!-- Sub-batch B (Trepadora): hiedra -->
<!-- Sub-batch B (Palmera): palmera-areca, palmera-kentia -->
<!-- Sub-batch B (Helecho): helecho-boston, helecho-nido -->
<!-- Sub-batch B (All-rounder): pilea, tradescantia, cheflera, arbol-dinero -->

<!-- Phase 15 smoke runner CAT-12 sentinel (from 15-00-PLAN.md): -->
<!-- "Phase 15" substring + at least 20 of the 23 ids must appear in CLAUDE.md text. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append Phase 15 Wave A accepted-known block to CLAUDE.md</name>
  <files>CLAUDE.md</files>

  <read_first>
    - CLAUDE.md (lines 175-200 — confirm existing "Accepted-known failures (Phase 8, v1.1)" block format and exact location)
    - .planning/phases/15-catalog-wave-a-interior-tropicals/15-RESEARCH.md §Example 6 (CLAUDE.md update format draft)
    - .planning/phases/15-catalog-wave-a-interior-tropicals/15-CONTEXT.md (image plan strategy decision: defer all 23 to accepted-known pattern)
  </read_first>

  <action>
Insert a new accepted-known failures block IMMEDIATELY AFTER the existing v1.1 LATAM 15-entry list and BEFORE the "Image upload steps" numbered list. This keeps the cross-cutting "image upload steps" applicable to BOTH the Phase 8 and Phase 15 backlogs.

**Step A — locate insertion point in CLAUDE.md:**

The existing block looks like:
```markdown
**Accepted-known failures (Phase 8, v1.1):** ...
- jacaranda, ceibo, ...
- ...
- lavanda-angustifolia (renamed from lavanda; ...)

These failures are documented in the v1.1 device-test backlog. Image upload steps:
1. Source images...
```

Insert the new block AFTER the `lavanda-angustifolia` line and BEFORE the `These failures are documented...` paragraph.

**Step B — emit this exact text:**

```markdown
**Accepted-known failures (Phase 15 Wave A, v1.2 — 23 entries — image upload pending):** the following 23 interior-tropical entries are accepted-known failures until manual image upload to Supabase Storage:

- anthurium, alocasia, caladium, singonio, aglaonema, costilla-adan, difenbaquia
- begonia-rex, croton, fitonia, ficus-lyrata, maranta
- zamioculca, cola-burro, hiedra
- palmera-areca, palmera-kentia
- helecho-boston, helecho-nido
- pilea, tradescantia, cheflera, arbol-dinero

These follow the same milestone-end batch upload pattern as the v1.1 LATAM 15-entry list. Image upload tracked alongside the v1.2 device-test backlog.
```

**Step C — verify the existing v1.1 block is NOT modified:** the 15 v1.1 entries (jacaranda...lavanda-angustifolia) and the surrounding text remain byte-identical. Only the NEW Phase 15 block is added.

**Step D — verify no other CLAUDE.md content is touched:** the rest of CLAUDE.md (architecture, commands, design system, i18n, App Store, Android Build, iOS Build, Pre-submit Checks, Landing Page, Security sections) must not be modified.

**Step E — markdown lint sanity check:** preserve blank-line separation between paragraphs; the new block opens with `**Accepted-known failures (Phase 15 Wave A, ...):**` matching the existing block's `**Accepted-known failures (Phase 8, v1.1):**` style.
  </action>

  <verify>
    <automated>node scripts/phase15-smoke.cjs && grep -c "Phase 15 Wave A" CLAUDE.md && grep -c "^- anthurium, alocasia" CLAUDE.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "Phase 15 Wave A" CLAUDE.md` returns ≥ 1 (block header present)
    - `grep -c "anthurium, alocasia, caladium" CLAUDE.md` returns 1 (first sub-batch line present)
    - `grep -c "zamioculca, cola-burro, hiedra" CLAUDE.md` returns 1
    - All 23 ids are mentioned in CLAUDE.md (verify with: `for id in anthurium alocasia caladium singonio aglaonema costilla-adan difenbaquia begonia-rex croton fitonia ficus-lyrata maranta zamioculca cola-burro hiedra palmera-areca palmera-kentia helecho-boston helecho-nido pilea tradescantia cheflera arbol-dinero; do grep -q "$id" CLAUDE.md || echo "MISSING: $id"; done` produces NO output)
    - The existing Phase 8 v1.1 block is byte-identical except for the addition of the new block after it — `grep -c "jacaranda, ceibo" CLAUDE.md` returns 1; `grep -c "lavanda-angustifolia" CLAUDE.md` returns 1
    - The "Image upload steps" numbered list is preserved AFTER the new block: `grep -A 4 "These failures are documented" CLAUDE.md | grep "Source images"` succeeds
    - `node scripts/phase15-smoke.cjs` exits 0 — W4.CAT-12 SKIP has flipped to PASS (the runner asserts ≥20 of 23 ids appear in CLAUDE.md)
    - `git diff CLAUDE.md | grep "^-" | grep -v "^---" | wc -l` returns 0 (only ADDS — no existing lines removed)
  </acceptance_criteria>

  <done>CLAUDE.md documents the 23 Phase 15 Wave A entries as accepted-known image failures; CAT-12 closes; Phase 15 fully complete except for the natural ship-time `npm run check:images` failure (which is now expected and documented).</done>
</task>

</tasks>

<verification>
End-of-plan verification:

```bash
# Smoke runner — full Phase 15 closure
node scripts/phase15-smoke.cjs                                          # → exit 0; ALL CAT-09/10/11/12 PASS
node scripts/phase15-smoke.cjs --identification                         # → exit 0

# i18n + tsc remain green
npm run check:i18n-keys                                                 # → exit 0
npx tsc --noEmit                                                        # → exit 0

# Voseo regression preserved
grep -cE '\b(riega|saca|pon|ten|haz|quieres|toca|mueve|puedes)\b' src/i18n/locales/es/plants.json  # → ≤ 2

# Catalog count = 87
grep -cE "^\s{4}id:\s*['\"]" src/data/plantDatabase.ts                  # → 87

# Phase 15 in CLAUDE.md
grep -c "Phase 15 Wave A" CLAUDE.md                                     # → ≥ 1

# Expected: check:images fails for the 23 new ids (NOT a ship blocker)
npm run check:images || true   # → expected to exit 1 with the 23 new urls + the 15 v1.1 urls listed
```
</verification>

<success_criteria>
- CLAUDE.md gains a "Phase 15 Wave A" accepted-known block listing all 23 ids
- All 23 ids appear in CLAUDE.md (≥ 20 needed by smoke runner; we provide 23)
- Existing Phase 8 v1.1 block + Image upload steps + rest of CLAUDE.md NOT modified
- Phase 15 smoke runner: ALL CAT-09/10/11/12 SKIPs are now PASS
- `npm run check:i18n-keys`, `npx tsc --noEmit`, voseo regression all green
- `npm run check:images` is EXPECTED to fail for the 23 (and the 15 v1.1) ids — but this is documented and accepted
- Phase 15 fully closed: 4/4 requirements pass
</success_criteria>

<output>
Create `.planning/phases/15-catalog-wave-a-interior-tropicals/15-04-SUMMARY.md` capturing:
- File modified: `CLAUDE.md` (+~12 LOC for the new block)
- All 23 ids documented (verified via grep loop)
- Phase 15 final status:
  - CAT-09 ✓ (Plan 15-02 closure — 87 catalog entries)
  - CAT-10 ✓ (Plan 15-02 closure — full i18n keysets EN+ES)
  - CAT-11 ✓ (Plan 15-03 closure — COMMON_NAMES_ES + scientificName routing)
  - CAT-12 ✓ (this plan — accepted-known image registry)
- Smoke runner final state: 0 SKIPs, 0 FAILs (or noted SKIPs as accepted)
- Carry-forward to milestone close: image upload backlog (23 Phase 15 + 15 v1.1 = 38 entries pending) — to be batched alongside device-test backlog
- Any deviations from the plan (with rationale)
</output>
