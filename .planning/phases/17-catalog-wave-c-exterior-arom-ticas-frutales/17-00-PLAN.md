---
phase: 17-catalog-wave-c-exterior-arom-ticas-frutales
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/phase17-smoke.cjs
  - scripts/.tmp-phase17/async-storage.cjs
  - scripts/.tmp-phase17/i18n.cjs
  - package.json
  - .gitignore
autonomous: true
requirements:
  - CAT-17
  - CAT-18
  - CAT-19
  - CAT-20
  - CAT-21
gap_closure: false

must_haves:
  truths:
    - "`node scripts/phase17-smoke.cjs` exits 0 at Wave 0 baseline (scaffold PASSes + SKIP placeholders for CAT-17/18/19/20/21)"
    - "`node scripts/phase17-smoke.cjs --identification` exits 0 at Wave 0 (placeholder SKIPs flip in Plan 17-03)"
    - "`node scripts/phase17-smoke.cjs --routing-fix` exits 0 at Wave 0 (refactor sentinel ALREADY landed in Phase 16 Plan 16-00; routing probes for the 14 Phase 17 species SKIP until Plans 17-01/02 land their entries)"
    - "`npm run smoke:phase17` runs the smoke runner via the new package.json script"
    - "All 14 Phase 17 ids encoded as runner constants (PHASE_17_IDS)"
    - "All 14 species-qualified scientificNames encoded as PHASE_17_SCIENTIFIC_NAMES Record (id → scientificName)"
    - "scripts/.tmp-phase17/ is gitignored — auto-written stubs never appear in git status"
    - "Mid-band count window encoded: idMatches > 104 && idMatches < 118 → SKIP; final assertion idMatches === 118 closes CAT-21 (and the entire v1.2 catalog expansion)"
    - "Voseo regex baseline assertion (≤2) PASSes against current src/i18n/locales/es/plants.json"
    - "NO findPlantInDatabase refactor needed — Phase 16 Plan 16-00 already landed exact-match-first; verified via `grep -c 'const exactMatch = PLANT_DATABASE.find' src/utils/plantIdentification.ts === 1`"
  artifacts:
    - path: "scripts/phase17-smoke.cjs"
      provides: "Phase 17 smoke runner — file-content asserts (CAT-17/18/19/20 per-id + i18n keysets + COMMON_NAMES_ES + image plan) + ts.transpileModule routing assertions (--identification, --routing-fix flags) + voseo regression + final CAT-21 count gate at 118"
      contains: "PHASE_17_IDS"
      min_lines: 280
    - path: "scripts/.tmp-phase17/async-storage.cjs"
      provides: "In-memory Map-backed AsyncStorage stub (CommonJS) — auto-written at runtime"
      contains: "_store = new Map()"
    - path: "scripts/.tmp-phase17/i18n.cjs"
      provides: "i18n stub returning defaultValue or key (CommonJS) — auto-written at runtime"
      contains: "module.exports"
    - path: "package.json"
      provides: "smoke:phase17 npm-script entry"
      contains: "\"smoke:phase17\""
    - path: ".gitignore"
      provides: "Excludes scripts/.tmp-phase17/ from git tracking"
      contains: "scripts/.tmp-phase17"
  key_links:
    - from: "package.json scripts.smoke:phase17"
      to: "scripts/phase17-smoke.cjs"
      via: "node command invocation"
      pattern: "node scripts/phase17-smoke.cjs"
    - from: "scripts/phase17-smoke.cjs (--routing-fix and --identification flags)"
      to: "src/utils/plantIdentification.ts findPlantInDatabase (Phase 16 exact-match-first refactor)"
      via: "ts.transpileModule + dynamic require — actual function call, not file-content regex"
      pattern: "ts\\.transpileModule|findPlantInDatabase"
    - from: "scripts/phase17-smoke.cjs (file-content asserts)"
      to: "src/data/plantDatabase.ts, src/utils/plantIdentification.ts, src/i18n/locales/{en,es}/plants.json, CLAUDE.md"
      via: "fs.readFileSync + regex"
      pattern: "readFileSync.*plantDatabase\\.ts|readFileSync.*plantIdentification\\.ts|readFileSync.*plants\\.json|readFileSync.*CLAUDE\\.md"
---

<open_questions>
The following 4 items were surfaced by 17-RESEARCH.md as deserving user review BEFORE Plan 17-01/02 author the 14 entries. Each has a researcher-recommended default; planning proceeds with defaults until user confirms or flips. None block Plan 17-00 (Wave 0 is content-agnostic).

**Q1. Aromáticas outdoor flag (HIGH IMPACT)** — CONTEXT.md locks 3 new aromáticas at `outdoor: false` claiming this matches the existing albahaca/romero/menta/cilantro/perejil/ciboulette precedent. Direct grep verification shows ALL 6 existing aromáticas use `outdoor: true` (albahaca:598, romero:634, menta:670, perejil:1292, cilantro:1364, ciboulette:1768). The locked decision diverges from precedent, not matches it.
- Default in Plan 17-02: HONOR THE LOCK (`outdoor: false` for salvia-officinalis, eneldo, stevia) — kitchen-container framing. Document divergence in 17-02-SUMMARY.md.
- User can flip to `outdoor: true` (matching actual existing aromáticas) before Plan 17-02 executes — change is single-token per entry.
- **Recommendation:** flip to `outdoor: true` to match real precedent — the lock was based on a research-stage misread of the catalog state.

**Q2. PlantCategory enum count (LOW IMPACT, doc correction)** — CONTEXT.md says "existing 8 PlantCategory values"; src/types/index.ts:152 declares `PlantCategory = "interior" | "exterior" | "aromaticas" | "huerta" | "frutales" | "suculentas"` — 6 values. Phase 17 entries map cleanly to the 6 actual values. No code action needed in this phase; doc correction can land in Phase 24.

**Q3. arandano category (CONTENT-LEVEL)** — Existing frutilla (line 780) is `category: "huerta"` despite being a perennial berry. Existing limonero (line 818) is `category: "frutales"` (woody perennial citrus). arandano is a woody perennial Vaccinium corymbosum.
- Default in Plan 17-02: `category: "frutales"` (matches limonero precedent — woody perennial).
- Alternative: `category: "huerta"` (matches frutilla precedent — edible berry).
- **Recommendation:** keep `frutales` (woody-perennial Vaccinium > annual frutilla).

**Q4. Magnolia variant choice (MEDIUM IMPACT)** — RHS small-garden guide explicitly cites *Magnolia stellata* (3m) as the small-garden choice; *Magnolia grandiflora* is the iconic 20m evergreen. CONTEXT.md says "researcher confirms at planning time."
- Default in Plan 17-01: `scientificName: 'Magnolia stellata'` (compact dwarf — Argentine apartment/small-garden context). Add `Magnolia grandiflora` as legacy alias in Plan 17-03 for PlantNet drift coverage.
- Alternative: flip canonical to `Magnolia grandiflora` (iconic-large framing); copy adjusts accordingly.
- **Recommendation:** keep stellata canonical with grandiflora as legacy alias.

These defaults are encoded throughout Plans 17-01/02/03/04. Surface for user review at plan-checker stage; user changes propagate trivially (per-token edits in author plans).
</open_questions>

<objective>
Build the Phase 17 Wave 0 smoke runner scaffold. Locks every CAT-17/18/19/20/21 verification surface as SKIP placeholders that later waves (17-01..04) flip to PASS without modifying the runner. Final assertion `idMatches === 118` closes the entire v1.2 catalog expansion.

Purpose: Per-task feedback loop in <15s (`node scripts/phase17-smoke.cjs && npx tsc --noEmit`). Mirrors Phase 11/12/13/14/15/16 Nyquist-gate pattern. Phase 17 is a textual delta from Phase 16's runner — same shape (CJS + ts.transpileModule routing path), same partial-landing-tolerance window, same flag set. No findPlantInDatabase refactor needed (Phase 16 Plan 16-00 already landed exact-match-first; this plan inherits its protection unchanged).

Output:
- `scripts/phase17-smoke.cjs` (CommonJS — VALIDATION.md locks `.cjs`; uses `ts.transpileModule` for routing tests; mid-band SKIP `> 104 && < 118`; final `=== 118`)
- Auto-written `scripts/.tmp-phase17/` stubs (gitignored)
- `package.json` script entry: `"smoke:phase17": "node scripts/phase17-smoke.cjs"`
- `.gitignore` updated to exclude `scripts/.tmp-phase17/`

Wave 0 baseline expected report: `[phase17-smoke] PASS N/N (M placeholders skipped — Waves 1-3 flip)` exit 0. Per-flag default invocation = scaffold (~9) + voseo (1) + ROADMAP success-criterion #1 (mid-band SKIP at idMatches=104) + 14 CAT-17/18/19 SKIPs + 14 CAT-20.keyset SKIPs + 14 CAT-20.commonNames SKIPs + 1 CAT-20.imagePlan SKIP ≈ ~10 PASS + ~43 SKIP. Under `--identification`: +14 IDENT.* SKIPs. Under `--routing-fix`: +14 W2.ROUTING-FIX.* SKIPs (refactor already landed → only landing-state gate keeps them SKIP at Wave 0).
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-catalog-wave-c-exterior-arom-ticas-frutales/17-CONTEXT.md
@.planning/phases/17-catalog-wave-c-exterior-arom-ticas-frutales/17-RESEARCH.md
@.planning/phases/17-catalog-wave-c-exterior-arom-ticas-frutales/17-VALIDATION.md
@.planning/phases/16-catalog-wave-b-suculentas-cactus-trepadoras-trending/16-00-PLAN.md
@.planning/phases/16-catalog-wave-b-suculentas-cactus-trepadoras-trending/16-00-SUMMARY.md
@scripts/phase16-smoke.cjs
@scripts/check-i18n-keys.mjs
@src/utils/plantIdentification.ts
@package.json
@.gitignore

<interfaces>
<!-- Concrete contracts. Use these verbatim. -->

THE 14 PHASE 17 ENTRY IDS (locked by REQUIREMENTS.md CAT-17/18/19):
```js
const PHASE_17_IDS = [
  // CAT-17 (8): exterior flores — Wave 1 (Plan 17-01)
  'azalea', 'ciclamen', 'fucsia', 'clavel', 'crisantemo', 'tulipan', 'girasol', 'magnolia',
  // CAT-18 (3): aromáticas — Wave 2 (Plan 17-02)
  'salvia-officinalis', 'eneldo', 'stevia',
  // CAT-19 (3): frutales/huerta — Wave 2 (Plan 17-02)
  'olivo', 'arandano', 'espinaca',
];
```

THE 14 CANONICAL SCIENTIFIC NAMES (per 17-RESEARCH.md §Standard Stack — POWO 2024 + Wikipedia + RHS/NCSU cross-verified):
```js
const PHASE_17_SCIENTIFIC_NAMES = {
  // CAT-17 (8) — Wave 1
  'azalea': 'Rhododendron simsii',
  'ciclamen': 'Cyclamen persicum',
  'fucsia': 'Fuchsia magellanica',
  'clavel': 'Dianthus caryophyllus',
  'crisantemo': 'Chrysanthemum × morifolium',
  'tulipan': 'Tulipa gesneriana',
  'girasol': 'Helianthus annuus',
  'magnolia': 'Magnolia stellata',
  // CAT-18 (3) — Wave 2
  'salvia-officinalis': 'Salvia officinalis',
  'eneldo': 'Anethum graveolens',
  'stevia': 'Stevia rebaudiana',
  // CAT-19 (3) — Wave 2
  'olivo': 'Olea europaea',
  'arandano': 'Vaccinium corymbosum',
  'espinaca': 'Spinacia oleracea',
};
```

CURRENT findPlantInDatabase IS ALREADY exact-match-first (Phase 16 Plan 16-00 landed it; verified at src/utils/plantIdentification.ts:206 — line numbers may shift slightly, but `const exactMatch = PLANT_DATABASE.find(p => p.scientificName.toLowerCase() === searchName)` pattern is in place):
```typescript
export function findPlantInDatabase(scientificName: string): PlantDBEntry | undefined {
  const searchName = scientificName.toLowerCase();
  // First pass: exact match (case-insensitive) — fixes genus-prefix collision (Dracaena/Sedum/etc.)
  const exactMatch = PLANT_DATABASE.find(p => p.scientificName.toLowerCase() === searchName);
  if (exactMatch) return exactMatch;
  // Second pass: genus prefix (legacy fuzzy fallback)
  return PLANT_DATABASE.find(plant => {
    const dbName = plant.scientificName.toLowerCase();
    return dbName.startsWith(searchName.split(' ')[0]) ||
           searchName.startsWith(dbName.split(' ')[0]);
  });
}
```

**Phase 17 inherits this protection unchanged. NO refactor task in this plan.** All 14 Phase 17 species-qualified scientificNames will route correctly via exact-match-first once Plans 17-01/02 land them, and once Plan 17-03 adds COMMON_NAMES_ES species-qualified keys.

CURRENT VOSEO BASELINE (verified 2026-05-08):
```bash
grep -cE '\b(riega|saca|pon|ten|haz|quieres|toca|mueve|puedes)\b' src/i18n/locales/es/plants.json
# => 2 (preserved through Phases 14/15/16)
```

CURRENT PLANT_DATABASE COUNT (verified 2026-05-08):
```bash
grep -cE "^\s{4}id:\s*['\"]" src/data/plantDatabase.ts
# => 104 (post-Phase-16)
```

CURRENT EXISTING package.json scripts (verified 2026-05-08):
```json
"scripts": {
  "start": "expo start",
  "android": "expo run:android",
  "check:legacy-fields": "node scripts/check-no-legacy-reads.js",
  "check:i18n-keys": "node scripts/check-i18n-keys.mjs",
  "check:images": "node scripts/check-images.mjs",
  "ios": "expo run:ios",
  "smoke:migration": "node scripts/migration-smoke-test.mjs",
  "smoke:phase14": "node scripts/smoke-phase14.mjs",
  "smoke:phase15": "node scripts/phase15-smoke.cjs",
  "smoke:phase16": "node scripts/phase16-smoke.cjs",
  "typecheck": "tsc --noEmit",
  "web": "expo start --web"
}
```

CURRENT .gitignore smoke-runner block (verified 2026-05-08 — last 8 lines):
```
# Smoke-runner temp artifacts (generated by scripts/smoke-phase*.mjs and check-*.mjs)
scripts/.tmp-*/
scripts/.tmp-*
scripts/.tmp-phase14/
scripts/.tmp-phase15/
scripts/.tmp-phase16/
scripts/*.compiled.mjs
scripts/*.compiled-*.mjs
```

**SENTINEL FOR CAT-20.commonNames SKIP→PASS (Plan 17-03 lands these):**
The runner detects Plan 17-03 has run when ANY of the 14 species-qualified scientificNames OR the new genus aliases appear in idSrc:
```js
const W3_SENTINEL_PRESENT = /Rhododendron simsii|Cyclamen persicum|Fuchsia magellanica|Dianthus caryophyllus|Chrysanthemum × morifolium|Tulipa gesneriana|Helianthus annuus|Magnolia stellata|Salvia officinalis|Anethum graveolens|Stevia rebaudiana|Olea europaea|Vaccinium corymbosum|Spinacia oleracea/.test(idSrc);
```

**SENTINEL FOR CAT-20.imagePlan SKIP→PASS (Plan 17-04 lands this):**
```js
if (!/Phase 17 Wave C/i.test(claudeMd)) return undefined; // SKIP until Plan 17-04 lands
const found = PHASE_17_IDS.filter(id => claudeMd.includes(id)).length;
return found >= 12;  // ≥12 of 14 — accommodates substring overlap with other CLAUDE.md text
```

**FINAL CAT-21 COUNT GATE (closes v1.2 catalog expansion):**
```js
assertSkippable(() => {
  const idMatches = (dbSrc.match(/^\s{4}id:\s*['"][^'"]+['"]/gm) || []).length;
  if (idMatches < 104) return undefined;       // pre-baseline catalog drift — SKIP
  if (idMatches === 104) return undefined;     // baseline = pre-W1 — SKIP
  if (idMatches > 104 && idMatches < 118) return undefined;  // mid-band Plan 17-01 → 17-02 — SKIP
  return idMatches === 118;
}, 'W2.CAT-counts.total: plantDatabase.ts has exactly 118 entry id declarations (104 + 14 net-new; closes v1.2 catalog expansion)');
```

PATTERN REFERENCE — phase16-smoke.cjs full template (scripts/phase16-smoke.cjs, 396 LOC):
- Lines 1-25: header + ROOT/TMP_DIR setup
- Lines 27-48: stub auto-write (async-storage.cjs + i18n.cjs)
- Lines 50-68: assertion harness (assert/assertSkippable/readSafe)
- Lines 70-108: PHASE_16_IDS, PHASE_16_NEW_IDS, PHASE_16_SCIENTIFIC_NAMES (Phase 17 has NO upgrades — PHASE_17_NEW_IDS === PHASE_17_IDS)
- Lines 110-126: read source files + W0.* scaffold PASSes
- Lines 128-130: voseo regression
- Lines 132-194: CAT-13/14/15 per-id presence with partial-landing tolerance
- Lines 196-206: CAT-counts.total with mid-band SKIP
- Lines 208-220: CAT-16 i18n keysets
- Lines 222-238: CAT-16 COMMON_NAMES_ES
- Lines 240-245: CAT-16 image plan (CLAUDE.md sentinel)
- Lines 247-268: --identification flag
- Lines 270-380: --routing-fix flag (ts.transpileModule + Module._resolveFilename intercept)
- Lines 382-396: report + exit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/phase17-smoke.cjs as a textual delta from scripts/phase16-smoke.cjs</name>
  <files>scripts/phase17-smoke.cjs, scripts/.tmp-phase17/async-storage.cjs (auto-written), scripts/.tmp-phase17/i18n.cjs (auto-written)</files>

  <read_first>
    - scripts/phase16-smoke.cjs (full 396 LOC — DIRECT template; copy verbatim and apply the textual deltas below)
    - .planning/phases/17-catalog-wave-c-exterior-arom-ticas-frutales/17-RESEARCH.md §"Example 3: phase17-smoke.cjs delta from phase16-smoke.cjs" (the exact delta diff — copy source for the substitution table)
    - .planning/phases/17-catalog-wave-c-exterior-arom-ticas-frutales/17-RESEARCH.md §"Final Manifest Assertion" (the `=== 118` closing gate code block)
    - .planning/phases/17-catalog-wave-c-exterior-arom-ticas-frutales/17-VALIDATION.md (Wave 0 requirements section — locks .cjs filename + flag names + sampling rate)
    - src/data/plantDatabase.ts (lines 1-50 — confirm CATALOG_BASE_URL constant + first id declarations to ensure regex `^\s{4}id:\s*['"]` matches; current count = 104)
    - src/utils/plantIdentification.ts (lines 200-220 — confirm `const exactMatch = PLANT_DATABASE.find` already exists from Phase 16 Plan 16-00; refactor sentinel reuse)
    - src/i18n/locales/es/plants.json (last 30 lines — confirm voseo baseline = 2 from `grep -cE '\b(riega|saca|pon|ten|haz|quieres|toca|mueve|puedes)\b'`)
  </read_first>

  <action>
Create `scripts/phase17-smoke.cjs` as a Node CommonJS smoke runner. **Start from scripts/phase16-smoke.cjs as the template** (copy the entire 396-LOC file as your starting point) and apply the substitution deltas below. Do NOT rewrite from scratch — the textual delta is small and well-defined.

**Step 1 — Copy scripts/phase16-smoke.cjs verbatim to scripts/phase17-smoke.cjs**, then apply the following substitutions (from 17-RESEARCH.md §Example 3):

```diff
- scripts/phase16-smoke.cjs
+ scripts/phase17-smoke.cjs
- Phase 16 Catalog Wave B — Suculentas/Cactus + Trepadoras + Trending
+ Phase 17 Catalog Wave C — Exterior + Aromáticas + Frutales
- // Mid-band exit-0 guarantee (Wave 1 lands 10 ids, Wave 2 lands the remaining 9):
+ // Mid-band exit-0 guarantee (Wave 1 lands 8 ids, Wave 2 lands the remaining 6):
- TMP_DIR = .tmp-phase16
+ TMP_DIR = .tmp-phase17
- async storage stub comment "Phase 16"
+ async storage stub comment "Phase 17"
- i18n stub comment "Phase 16"
+ i18n stub comment "Phase 17"
- voseo label `post-Phase-15 baseline`
+ voseo label `post-Phase-16 baseline`
- count gate floor `87`, mid-band `> 87 && < 104`, final `=== 104`
+ count gate floor `104`, mid-band `> 104 && < 118`, final `=== 118`
- count assertion label includes "87 + 17 net-new; potus/filodendro upgrades counted in 87"
+ count assertion label includes "104 + 14 net-new; closes v1.2 catalog expansion"
- W0/W1/W2/W3 labels using CAT-13/CAT-14/CAT-15/CAT-16
+ W0/W1/W2/W3 labels using CAT-17/CAT-18/CAT-19/CAT-20 (CAT-21 = the count gate label)
- imagePlan sentinel `/Phase 16 Wave B/i.test(claudeMd)` + `≥17 of 19` threshold
+ imagePlan sentinel `/Phase 17 Wave C/i.test(claudeMd)` + `≥12 of 14` threshold
- W3_SENTINEL_PRESENT regex matching Phase 16 species
+ W3_SENTINEL_PRESENT regex matching Phase 17 species (see code below)
- runner banner `[phase16-smoke]` (in 2 places)
+ runner banner `[phase17-smoke]` (in 2 places)
```

**Step 2 — REPLACE the constants block (lines 70-108 in phase16) with Phase 17 values:**

```js
// ─── Phase 17 constants (locked by REQUIREMENTS.md CAT-17/18/19 + 17-CONTEXT.md) ───
const PHASE_17_IDS = [
  // CAT-17 (8): exterior flores — Wave 1
  'azalea', 'ciclamen', 'fucsia', 'clavel', 'crisantemo', 'tulipan', 'girasol', 'magnolia',
  // CAT-18 (3): aromáticas — Wave 2
  'salvia-officinalis', 'eneldo', 'stevia',
  // CAT-19 (3): frutales/huerta — Wave 2
  'olivo', 'arandano', 'espinaca',
];

// Phase 17 has NO in-place upgrades (unlike Phase 16's potus/filodendro). All 14 ids are net-new.
const PHASE_17_NEW_IDS = PHASE_17_IDS;

const PHASE_17_SCIENTIFIC_NAMES = {
  // CAT-17 (8) — Wave 1
  'azalea': 'Rhododendron simsii',
  'ciclamen': 'Cyclamen persicum',
  'fucsia': 'Fuchsia magellanica',
  'clavel': 'Dianthus caryophyllus',
  'crisantemo': 'Chrysanthemum × morifolium',
  'tulipan': 'Tulipa gesneriana',
  'girasol': 'Helianthus annuus',
  'magnolia': 'Magnolia stellata',
  // CAT-18 (3) — Wave 2
  'salvia-officinalis': 'Salvia officinalis',
  'eneldo': 'Anethum graveolens',
  'stevia': 'Stevia rebaudiana',
  // CAT-19 (3) — Wave 2
  'olivo': 'Olea europaea',
  'arandano': 'Vaccinium corymbosum',
  'espinaca': 'Spinacia oleracea',
};
```

**Step 3 — REPLACE the per-id assertion blocks (lines 132-194 in phase16) with Phase 17 CAT-17/18/19 splits:**

The Phase 16 runner uses 3 separate arrays (`slice(0,10)` for CAT-13, `['potus', 'filodendro']` + `['hoya', 'mini-monstera']` for CAT-14, and `['strelitzia', 'eucalipto', 'bambu-suerte', 'sansevieria-cilindrica', 'cactus-san-pedro']` for CAT-15). Phase 17 uses 3 simpler slices — NO upgrade special-casing because Phase 17 has no in-place upgrades:

```js
// CAT-17: 8 exterior flores (all NEW; partial-landing tolerant)
PHASE_17_IDS.slice(0, 8).forEach((id, i) => {
  assertSkippable(() => {
    const present = PHASE_17_LANDED_FLAGS[i];
    if (!anyLanded) return undefined;
    if (anyLanded && !allLanded && !present) return undefined;
    return present;
  }, `W1.CAT-17.${id}: entry id '${id}' present in plantDatabase.ts`);
});

// CAT-18: 3 aromáticas (all NEW)
['salvia-officinalis', 'eneldo', 'stevia'].forEach(id => {
  const i = PHASE_17_IDS.indexOf(id);
  assertSkippable(() => {
    const present = PHASE_17_LANDED_FLAGS[i];
    if (!anyLanded) return undefined;
    if (anyLanded && !allLanded && !present) return undefined;
    return present;
  }, `W2.CAT-18.${id}: entry id '${id}' present in plantDatabase.ts`);
});

// CAT-19: 3 frutales/huerta (all NEW)
['olivo', 'arandano', 'espinaca'].forEach(id => {
  const i = PHASE_17_IDS.indexOf(id);
  assertSkippable(() => {
    const present = PHASE_17_LANDED_FLAGS[i];
    if (!anyLanded) return undefined;
    if (anyLanded && !allLanded && !present) return undefined;
    return present;
  }, `W2.CAT-19.${id}: entry id '${id}' present in plantDatabase.ts`);
});
```

**Step 4 — REPLACE the count gate block (lines 196-206 in phase16) with the CAT-21 final assertion:**

```js
// ─── CAT-21 final manifest assertion (PARTIAL-LANDING TOLERANT) — closes the entire v1.2 catalog expansion ───
// Wave 1 lands 8 net-new (azalea..magnolia) → 104+8 = 112 (mid-band SKIP).
// Wave 2 lands 6 net-new (salvia-officinalis, eneldo, stevia, olivo, arandano, espinaca) → 112+6 = 118 (PASS).
// All 14 are net-new — Phase 17 has no in-place upgrades.
assertSkippable(() => {
  const idMatches = (dbSrc.match(/^\s{4}id:\s*['"][^'"]+['"]/gm) || []).length;
  if (idMatches < 104) return undefined;       // pre-baseline catalog drift — SKIP
  if (idMatches === 104) return undefined;     // baseline = pre-W1 — SKIP
  if (idMatches > 104 && idMatches < 118) return undefined;  // mid-band Plan 17-01 → 17-02 — SKIP
  return idMatches === 118;
}, 'W2.CAT-counts.total: plantDatabase.ts has exactly 118 entry id declarations (104 + 14 net-new; closes v1.2 catalog expansion — CAT-21)');
```

**Step 5 — REPLACE the i18n keyset block (lines 208-220 in phase16) with the simpler Phase 17 form (no potus/filodendro special-case):**

```js
// ─── CAT-20 (i18n keysets — Plan 17-01 + 17-02 land 14 entry blocks) ───
PHASE_17_IDS.forEach(id => {
  assertSkippable(() => {
    const idEsc = id.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    const enHas = new RegExp(`"${idEsc}"\\s*:\\s*\\{`).test(enJson);
    const esHas = new RegExp(`"${idEsc}"\\s*:\\s*\\{`).test(esJson);
    if (!enHas && !esHas) return undefined; // SKIP at baseline for new ids
    return enHas && esHas;
  }, `W2.CAT-20.${id}.keyset: '${id}' keyset present in BOTH en + es plants.json`);
});
```

**Step 6 — REPLACE the COMMON_NAMES_ES block (lines 222-238 in phase16) with the Phase 17 sentinel + per-id gate:**

```js
// ─── CAT-20 (COMMON_NAMES_ES placeholders — Plan 17-03 lands the species-qualified keys) ───
// Sentinel: any of the 14 Phase 17 species-qualified scientificNames appears in idSrc.
// (Some Phase 17 genera — Rhododendron, Cyclamen, Fuchsia — already exist in COMMON_NAMES_ES at the genus
// level; Plan 17-03 ADDS species-qualified keys on top. The sentinel uses species-level matches only so
// the SKIP→PASS flip happens precisely when Plan 17-03 lands its block.)
const W3_SENTINEL_PRESENT = /Rhododendron simsii|Cyclamen persicum|Fuchsia magellanica|Dianthus caryophyllus|Chrysanthemum × morifolium|Tulipa gesneriana|Helianthus annuus|Magnolia stellata|Salvia officinalis|Anethum graveolens|Stevia rebaudiana|Olea europaea|Vaccinium corymbosum|Spinacia oleracea/.test(idSrc);

PHASE_17_IDS.forEach(id => {
  assertSkippable(() => {
    if (!W3_SENTINEL_PRESENT) return undefined;
    const sn = PHASE_17_SCIENTIFIC_NAMES[id];
    const genus = sn.split(' ')[0].split('×')[0].trim();
    const snEsc = sn.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    const genusEsc = genus.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    const speciesQualified = new RegExp(`['"]${snEsc}['"]\\s*:`).test(idSrc);
    const genusOnly = new RegExp(`['"]${genusEsc}['"]\\s*:`).test(idSrc);
    return speciesQualified || genusOnly;
  }, `W3.CAT-20.${id}: '${PHASE_17_SCIENTIFIC_NAMES[id]}' (or its genus) mapped in COMMON_NAMES_ES`);
});
```

**Step 7 — REPLACE the image plan block (lines 240-245 in phase16) with Phase 17 thresholds:**

```js
// ─── CAT-20 image plan (CLAUDE.md "Phase 17 Wave C" mention of ≥12 of 14 ids) ───
assertSkippable(() => {
  if (!/Phase 17 Wave C/i.test(claudeMd)) return undefined; // SKIP until Plan 17-04 lands
  const found = PHASE_17_IDS.filter(id => claudeMd.includes(id)).length;
  return found >= 12;
}, 'W3.CAT-20.imagePlan: CLAUDE.md "Phase 17 Wave C" block mentions >=12 of 14 Phase 17 ids');
```

**Step 8 — REPLACE the --identification flag block (lines 247-268 in phase16) with Phase 17 form (NO potus/filodendro special-case):**

```js
// ─── --identification flag (file-content co-occurrence; same partial-landing tolerance) ───
const isIdentificationMode = process.argv.includes('--identification');
if (isIdentificationMode) {
  PHASE_17_IDS.forEach(id => {
    const i = PHASE_17_IDS.indexOf(id);
    const sn = PHASE_17_SCIENTIFIC_NAMES[id];
    assertSkippable(() => {
      const idLanded = PHASE_17_LANDED_FLAGS[i];
      if (!anyLanded) return undefined;
      if (anyLanded && !allLanded && !idLanded) return undefined;
      const idIdxDouble = dbSrc.indexOf(`id: "${id}"`);
      const idIdxSingle = dbSrc.indexOf(`id: '${id}'`);
      const idIdx = idIdxDouble >= 0 ? idIdxDouble : idIdxSingle;
      if (idIdx < 0) return undefined;
      const block = dbSrc.slice(idIdx, idIdx + 5000);
      const snEsc = sn.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
      return new RegExp(`scientificName:\\s*['"]${snEsc}['"]`).test(block);
    }, `IDENT.CAT-20.${id}: scientificName '${sn}' co-occurs with id '${id}' in plantDatabase.ts`);
  });
}
```

**Step 9 — REPLACE the --routing-fix flag block (lines 270-380 in phase16). KEEP the loadFindPlantInDatabase helper VERBATIM.** Phase 17 inherits Phase 16's exact-match-first refactor unchanged. REMOVE the W0.ROUTING-FIX.* probe array (Phase 16 used it as a sentinel that the refactor landed within Plan 16-00; for Phase 17 it's already landed before Wave 0). Phase 17 keeps ONLY the per-Phase-17-id W2.ROUTING-FIX gates:

```js
// ─── --routing-fix flag (ts.transpileModule + runtime function call) ───
const isRoutingFixMode = process.argv.includes('--routing-fix');
if (isRoutingFixMode) {
  // Phase 16 Plan 16-00 already landed the exact-match-first refactor. Phase 17 inherits
  // its protection unchanged — verify the refactor is still in place as a regression sentinel.
  const refactorLanded = /const exactMatch = PLANT_DATABASE\.find/.test(idSrc);

  // Helper: loadFindPlantInDatabase — COPY VERBATIM from phase16-smoke.cjs lines 278-333.
  // Stubs i18n + AsyncStorage via Module._resolveFilename intercept, swap TMP_DIR refs to .tmp-phase17.
  function loadFindPlantInDatabase() {
    /* COPY phase16-smoke.cjs lines 278-333 verbatim — only TMP_DIR path strings change (.tmp-phase16 → .tmp-phase17).
       The 6 Module._resolveFilename intercepts (i18n, async-storage, plantDatabase, types, migration, supabase)
       are unchanged. */
    /* paste phase16's loadFindPlantInDatabase body here, unchanged except STUB_AS / STUB_I18N
       references which already resolve to scripts/.tmp-phase17/ via the TMP_DIR constant at top of file. */
  }

  // Phase 17 species routing tests (post-Plan 17-01/02 landing — uses anyLanded/allLanded gate):
  let findFn = null;
  PHASE_17_NEW_IDS.forEach(id => {
    const sn = PHASE_17_SCIENTIFIC_NAMES[id];
    assertSkippable(() => {
      if (!refactorLanded) return undefined;  // regression sentinel — should never SKIP post-Phase-16
      const idLanded = PHASE_17_LANDED_FLAGS[PHASE_17_IDS.indexOf(id)];
      if (!anyLanded) return undefined;
      if (anyLanded && !allLanded && !idLanded) return undefined;
      try {
        if (!findFn) findFn = loadFindPlantInDatabase();
        const result = findFn(sn);
        return result?.id === id;
      } catch (e) {
        return undefined;
      }
    }, `W2.ROUTING-FIX.${id}: findPlantInDatabase('${sn}')?.id === '${id}'`);
  });
}
```

**Step 10 — Report banner (lines 382-396 in phase16):** swap `phase16-smoke` → `phase17-smoke` in the 2 console messages.

**NOTES:**
- Phase 17 has NO `hasEduFieldsForId` helper (only Phase 16 needed it for potus/filodendro upgrade detection). REMOVE the helper function entirely from your Phase 17 runner.
- Phase 17 has NO `PHASE_17_NEW_IDS = filter(...)` line — `PHASE_17_NEW_IDS = PHASE_17_IDS` because all 14 ids are net-new.
- Voseo regex baseline label: change "post-Phase-15 baseline" → "post-Phase-16 baseline" (the count is still ≤2; only the label evolves per phase).
- Total Wave 0 PASSes expected (default invocation, no flags): ~9 scaffold + 1 voseo = ~10 PASS. Total SKIPs at Wave 0 (default): 8 (CAT-17) + 3 (CAT-18) + 3 (CAT-19) + 1 (count) + 14 (CAT-20 keyset) + 14 (CAT-20 commonNames) + 1 (CAT-20 imagePlan) ≈ ~44 SKIPs. Under `--identification`: +14 IDENT.* SKIPs. Under `--routing-fix`: +14 W2.ROUTING-FIX.* SKIPs.
- ts.transpileModule path is INSIDE the `--routing-fix` flag branch — default invocation has zero TS overhead.
- The runner is NEVER edited again after this plan. All future SKIP→PASS flips happen via Plans 17-01/02/03/04 modifying source files (plantDatabase.ts, plants.json, plantIdentification.ts, CLAUDE.md).
  </action>

  <verify>
    <automated>node scripts/phase17-smoke.cjs && node scripts/phase17-smoke.cjs --identification && node scripts/phase17-smoke.cjs --routing-fix</automated>
  </verify>

  <acceptance_criteria>
    - File `scripts/phase17-smoke.cjs` exists and is ≥280 lines
    - `node scripts/phase17-smoke.cjs` exits 0 (success) at Wave 0 baseline
    - `node scripts/phase17-smoke.cjs --identification` exits 0 (extra IDENT placeholder SKIPs, no FAILs)
    - `node scripts/phase17-smoke.cjs --routing-fix` exits 0 (extra W2.ROUTING-FIX placeholder SKIPs at Wave 0; refactor regression sentinel PASSes because Phase 16 Plan 16-00 already landed it)
    - Output contains `[phase17-smoke] PASS` and lists ≥9 PASSes (scaffold + voseo)
    - Output contains 0 FAILs at Wave 0 baseline
    - `grep -c "PHASE_17_IDS" scripts/phase17-smoke.cjs` returns ≥1
    - `grep -c "PHASE_17_SCIENTIFIC_NAMES" scripts/phase17-smoke.cjs` returns ≥1
    - `grep -c "PHASE_17_NEW_IDS" scripts/phase17-smoke.cjs` returns ≥1
    - All 14 Phase 17 species-qualified scientificNames present: `grep -cE "(Rhododendron simsii|Cyclamen persicum|Fuchsia magellanica|Dianthus caryophyllus|Chrysanthemum × morifolium|Tulipa gesneriana|Helianthus annuus|Magnolia stellata|Salvia officinalis|Anethum graveolens|Stevia rebaudiana|Olea europaea|Vaccinium corymbosum|Spinacia oleracea)" scripts/phase17-smoke.cjs` returns ≥14
    - All 14 ids appear literally in the file: `grep -cE "(azalea|ciclamen|fucsia|clavel|crisantemo|tulipan|girasol|magnolia|salvia-officinalis|eneldo|stevia|olivo|arandano|espinaca)" scripts/phase17-smoke.cjs` returns ≥14
    - File `scripts/.tmp-phase17/async-storage.cjs` exists after first run, contains `_store = new Map()`
    - File `scripts/.tmp-phase17/i18n.cjs` exists after first run, contains `module.exports`
    - Voseo regression assertion PASSes (not SKIP) at Wave 0 — exit code 0 confirms
    - Partial-landing gates encoded: `grep -c "anyLanded" scripts/phase17-smoke.cjs` returns ≥3; `grep -c "allLanded" scripts/phase17-smoke.cjs` returns ≥2
    - Mid-band count gate encoded: `grep -c "idMatches > 104 && idMatches < 118" scripts/phase17-smoke.cjs` returns 1
    - Final count assertion encoded: `grep -c "idMatches === 118" scripts/phase17-smoke.cjs` returns 1
    - `grep -c "ts.transpileModule\\|require('typescript')" scripts/phase17-smoke.cjs` returns ≥1 (ts-transpile path encoded)
    - `grep -c "exactMatch" scripts/phase17-smoke.cjs` returns ≥1 (refactor sentinel inherited from Phase 16)
    - `grep -c "Module._resolveFilename" scripts/phase17-smoke.cjs` returns ≥1 (stub-resolution intercept encoded)
    - `grep -c "hasEduFieldsForId" scripts/phase17-smoke.cjs` returns 0 (Phase 16-specific helper REMOVED — Phase 17 has no upgrades)
    - `grep -c "Phase 17 Wave C" scripts/phase17-smoke.cjs` returns ≥1 (image plan sentinel encoded)
    - `grep -c "phase16-smoke\\|phase16" scripts/phase17-smoke.cjs` returns 0 (no leftover references to Phase 16 banner/path)
    - `grep -c "post-Phase-16 baseline" scripts/phase17-smoke.cjs` returns ≥1 (voseo label updated from Phase 16's "post-Phase-15")
    - `npx tsc --noEmit` exits 0 (Wave 0 touches no .ts source — the smoke runner is .cjs)
  </acceptance_criteria>

  <done>Wave 0 smoke runner is fully scaffolded; all CAT-17/18/19/20/21 verifications encoded as SKIPs that auto-flip to PASS as Plans 17-01/02/03/04 land. ts-transpile + runtime function-call routing path encoded behind `--routing-fix` flag, inheriting Phase 16's exact-match-first refactor as a regression sentinel. Mid-band partial-landing window (104..118) keeps the runner exit-0 between Plans 17-01 and 17-02. Final CAT-21 gate at `idMatches === 118` closes the entire v1.2 catalog expansion.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire smoke:phase17 npm script + .gitignore exclusion for scripts/.tmp-phase17/</name>
  <files>package.json, .gitignore</files>

  <read_first>
    - package.json (full file — confirm existing scripts block; Task 1 created `scripts/phase17-smoke.cjs` but NOT yet wired to npm; the existing `smoke:phase16` line at line 15 is the alphabetical anchor for the new `smoke:phase17` line)
    - .gitignore (full file — confirm existing `scripts/.tmp-*` patterns + the explicit `.tmp-phase14/`, `.tmp-phase15/`, `.tmp-phase16/` lines; Phase 17 follows the same explicit-line precedent)
  </read_first>

  <action>
**Step A — package.json:** Add a single new entry to the `"scripts"` object IMMEDIATELY AFTER the existing `"smoke:phase16"` line (alphabetical placement). Do NOT modify any other field.

EXACT new line to add after `"smoke:phase16": "node scripts/phase16-smoke.cjs",`:
```json
    "smoke:phase17": "node scripts/phase17-smoke.cjs",
```

Final ordered scripts block (verify visually):
```json
"scripts": {
  "start": "expo start",
  "android": "expo run:android",
  "check:legacy-fields": "node scripts/check-no-legacy-reads.js",
  "check:i18n-keys": "node scripts/check-i18n-keys.mjs",
  "check:images": "node scripts/check-images.mjs",
  "ios": "expo run:ios",
  "smoke:migration": "node scripts/migration-smoke-test.mjs",
  "smoke:phase14": "node scripts/smoke-phase14.mjs",
  "smoke:phase15": "node scripts/phase15-smoke.cjs",
  "smoke:phase16": "node scripts/phase16-smoke.cjs",
  "smoke:phase17": "node scripts/phase17-smoke.cjs",
  "typecheck": "tsc --noEmit",
  "web": "expo start --web"
}
```

Preserve existing 2-space indentation. Preserve existing quoting style. No reordering of existing entries.

**Step B — .gitignore:** Append a single new line under the existing "Smoke-runner temp artifacts" block, after the existing `scripts/.tmp-phase16/` line (Phase 14 + 15 + 16 precedent).

Insert AFTER the existing `scripts/.tmp-phase16/` line:
```
scripts/.tmp-phase17/
```

Final relevant block:
```
# Smoke-runner temp artifacts (generated by scripts/smoke-phase*.mjs and check-*.mjs)
scripts/.tmp-*/
scripts/.tmp-*
scripts/.tmp-phase14/
scripts/.tmp-phase15/
scripts/.tmp-phase16/
scripts/.tmp-phase17/
scripts/*.compiled.mjs
scripts/*.compiled-*.mjs
```

The existing wildcards `scripts/.tmp-*/` and `scripts/.tmp-*` already cover `scripts/.tmp-phase17/`, but the explicit line is added for grep-discoverability per Phase 14/15/16 precedent. Do NOT touch any other section of .gitignore.
  </action>

  <verify>
    <automated>npm run smoke:phase17 && git check-ignore scripts/.tmp-phase17/foo.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '"smoke:phase17"' package.json` returns 1
    - `grep -c "node scripts/phase17-smoke.cjs" package.json` returns 1
    - `grep -c "scripts/.tmp-phase17/" .gitignore` returns 1
    - `npm run smoke:phase17` exits 0 (runs the runner via npm)
    - `npm run smoke:phase17 -- --identification` exits 0 (npm --argv pass-through)
    - `npm run smoke:phase17 -- --routing-fix` exits 0
    - `git check-ignore scripts/.tmp-phase17/dummy.cjs` succeeds (returns the path, exit 0)
    - `git status --short scripts/.tmp-phase17/ 2>&1 | grep -c "tmp-phase17"` returns 0 (auto-written stubs not in git status after running smoke)
    - `npx tsc --noEmit` exits 0 (no TS regressions)
    - The `smoke:phase16` line is still present in package.json (verify with `grep -c '"smoke:phase16"' package.json` returns 1) — no displacement of existing Phase 16 wiring
    - The `.tmp-phase16/` line is still present in .gitignore — no displacement of existing Phase 16 line
  </acceptance_criteria>

  <done>The Phase 17 smoke runner is callable via `npm run smoke:phase17`, and its auto-generated stubs are gitignored. Per-task feedback loop fully wired. Plan 17-01/02/03/04 inherit unchanged.</done>
</task>

</tasks>

<verification>
End-of-plan verification:

```bash
node scripts/phase17-smoke.cjs                          # exit 0; PASS report with ≥9 PASSes + ~44 SKIPs
node scripts/phase17-smoke.cjs --identification         # exit 0; --identification flag adds IDENT.* SKIPs
node scripts/phase17-smoke.cjs --routing-fix            # exit 0; --routing-fix flag has 14 W2.ROUTING-FIX.* SKIPs at Wave 0 (refactor regression sentinel PASSes)
npm run smoke:phase17                                    # alias works; same output
npx tsc --noEmit                                         # exit 0 (Wave 0 touches no .ts source)
npm run check:i18n-keys                                  # exit 0 (no i18n changes)
git check-ignore scripts/.tmp-phase17/                   # exit 0 — gitignore correctly excludes
git status --short                                       # scripts/.tmp-phase17/ NOT listed (gitignored)

# Phase 15 + Phase 16 regression checks (CRITICAL — no cross-phase regression introduced)
node scripts/phase15-smoke.cjs                           # exit 0
node scripts/phase15-smoke.cjs --identification          # exit 0
node scripts/phase16-smoke.cjs                           # exit 0
node scripts/phase16-smoke.cjs --identification          # exit 0
node scripts/phase16-smoke.cjs --routing-fix             # exit 0

# Voseo regression
grep -cE '\b(riega|saca|pon|ten|haz|quieres|toca|mueve|puedes)\b' src/i18n/locales/es/plants.json  # ≤2

# Catalog count unchanged at 104 (Wave 0 doesn't touch plantDatabase.ts)
grep -cE "^\s{4}id:\s*['\"]" src/data/plantDatabase.ts                  # 104

# Phase 16 refactor sentinel still in place (regression check)
grep -c "const exactMatch = PLANT_DATABASE\\.find" src/utils/plantIdentification.ts  # 1
```
</verification>

<success_criteria>
- `scripts/phase17-smoke.cjs` exists, is CJS, ≥280 lines, encodes all 14 ids + 14 species-qualified scientificNames + ts-transpile routing path
- `npm run smoke:phase17` is a valid script and exits 0
- `scripts/.tmp-phase17/` is gitignored (explicit line + wildcard cover)
- NO findPlantInDatabase refactor in this plan — Phase 16 Plan 16-00 already landed exact-match-first
- Wave 0 baseline report: ≥9 PASS + ~44 SKIP + 0 FAIL (default invocation); --routing-fix flag reports 14 W2.ROUTING-FIX.* SKIPs at Wave 0 (refactor sentinel PASSes — regression check on Phase 16 work)
- Voseo regression assertion is a PASS (not SKIP) at Wave 0 — proves baseline = 2
- Phase 15 + Phase 16 smoke runners remain fully green — no cross-phase regression
- All Wave 1-3 verification surfaces are encoded as SKIPs that future waves flip; the runner itself is NEVER edited again after this plan
- Partial-landing tolerance encoded for CAT-17/18/19 per-id, count, IDENT, ROUTING-FIX modes — runner stays exit-0 at the Plan 17-01 → 17-02 midpoint (idMatches=112)
- Final CAT-21 gate at `idMatches === 118` closes the entire v1.2 catalog expansion when Plan 17-02 lands the last 6 entries
- Mid-band SKIP window: `idMatches > 104 && idMatches < 118 → undefined`; idMatches === 118 → PASS
- 4 open questions (aromáticas outdoor, PlantCategory enum count, arandano category, magnolia variant) surfaced at plan level with researcher-recommended defaults
</success_criteria>

<output>
After completion, create `.planning/phases/17-catalog-wave-c-exterior-arom-ticas-frutales/17-00-SUMMARY.md` capturing:
- Files created/modified (paths + LOC; expected: scripts/phase17-smoke.cjs ~280 LOC + 2-line .gitignore + 1-line package.json)
- Smoke runner Wave 0 baseline numbers (PASS / SKIP / FAIL counts for default + --identification + --routing-fix invocations)
- Voseo baseline confirmed (count = 2)
- Phase 15 + Phase 16 cross-phase regression check (both green)
- Sentinel patterns documented (Phase 17 species substring sentinel for COMMON_NAMES_ES; "Phase 17 Wave C" substring for image plan; `const exactMatch = PLANT_DATABASE.find` for routing-fix; partial-landing anyLanded/allLanded windowing)
- 4 open questions surfaced — note which were resolved by user feedback before Plan 17-01/02 execute
</output>
