---
phase: 20-fertilization-subsystem
plan: 01
type: execute
wave: 1
depends_on: ["20-00"]
files_modified:
  - src/hooks/useStorage.tsx
autonomous: true
requirements: [FERT-01, FERT-02]
must_haves:
  truths:
    - "PROTECTED_USER_FIELDS tuple in useStorage.tsx includes 'fertilizeSchedule' so that catalog-source updatePlant({ fertilizeSchedule: ... }) calls without { fromUserEdit: true } are dropped per CRIT-1 deep-merge guard"
    - "Existing 3 protected field names ('waterSchedule', 'lightLevel', 'waterMode') stay verbatim — the extension is APPEND-ONLY (no reorder, no rename)"
    - "tsc remains green; the tuple is `as const` so TypeScript still resolves the literal-union narrowing for the loop"
  artifacts:
    - path: src/hooks/useStorage.tsx
      provides: extended PROTECTED_USER_FIELDS tuple guarding fertilizeSchedule from catalog overwrite
      contains: "PROTECTED_USER_FIELDS"
  key_links:
    - from: src/hooks/useStorage.tsx
      to: src/types/index.ts (Plant.fertilizeSchedule)
      via: PROTECTED_USER_FIELDS literal-array key narrowing
      pattern: "fertilizeSchedule"
---

<objective>
Wave 1 type-side hardening: extend the CRIT-1 deep-merge guard in `useStorage.updatePlant` to protect the new `Plant.fertilizeSchedule` field. This is a single-line tuple extension — the hardest part is preserving `as const` semantics so TypeScript narrows the loop correctly.

Purpose: closes FERT-01 type-side guarantee — once a user manually overrides their plant's fertilize cadence (rare in this phase since no UI exposes it; reserved for v1.3+), no catalog-source code path can silently clobber it. Mirrors Phase 14 EDU-06 guard pattern that protects waterSchedule/lightLevel/waterMode.
Output: 1 file modified; 1 atomic commit.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-fertilization-subsystem/20-CONTEXT.md
@.planning/phases/20-fertilization-subsystem/20-RESEARCH.md
@.planning/phases/20-fertilization-subsystem/20-00-PLAN.md

<interfaces>
<!-- Existing PROTECTED_USER_FIELDS tuple at src/hooks/useStorage.tsx:23 -->

```typescript
// CURRENT
const PROTECTED_USER_FIELDS = ['waterSchedule', 'lightLevel', 'waterMode'] as const;

// USED INSIDE updatePlant (lines 402-414):
// for (const key of PROTECTED_USER_FIELDS) {
//   if (existing[key] !== undefined && key in normalizedUpdates) {
//     delete normalizedUpdates[key];
//   }
// }
```

<!-- Plant.fertilizeSchedule landed in 20-00 Task 2 — Plant interface in src/types/index.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 'fertilizeSchedule' to PROTECTED_USER_FIELDS tuple in useStorage.tsx</name>
  <files>src/hooks/useStorage.tsx</files>
  <read_first>
    - src/hooks/useStorage.tsx (lines 18-30 — JSDoc comment + tuple declaration; lines 397-414 — guard loop usage)
    - src/types/index.ts (verify Plant.fertilizeSchedule landed in 20-00 Task 2)
    - .planning/phases/20-fertilization-subsystem/20-RESEARCH.md §"Architecture Pattern 7 — Deep-Merge Guard" (lines 363-378)
  </read_first>
  <action>
Single edit: change line 23 of `src/hooks/useStorage.tsx` from:

```typescript
const PROTECTED_USER_FIELDS = ['waterSchedule', 'lightLevel', 'waterMode'] as const;
```

to:

```typescript
const PROTECTED_USER_FIELDS = ['waterSchedule', 'lightLevel', 'waterMode', 'fertilizeSchedule'] as const;
```

**Rules:**
- APPEND ONLY — `'fertilizeSchedule'` is the LAST element. Do NOT reorder existing entries.
- Keep `as const` at end (preserves literal-union narrowing — without it, `key in normalizedUpdates` loses type safety).
- Do NOT modify the JSDoc comment block above the tuple (lines 18-22) — it documents the CRIT-1 contract and is still accurate verbatim.
- Do NOT modify the guard loop body (lines 402-414) — it iterates the tuple agnostically.

**Why this is safe:** `Plant.fertilizeSchedule` was added as `?:` (optional) in Plan 20-00 Task 2. The guard's existing-value check (`existing[key] !== undefined`) correctly skips plants where the user hasn't set fertilizeSchedule yet — so no false-positive drops. When/if a future UI lets the user set fertilizeSchedule manually, the guard kicks in. Any future migration script that derives fertilizeSchedule from catalog at first launch must use raw `setPlants()` (bypasses the guard) OR pass `{ fromUserEdit: true }` (explicitly opts in) — both paths are documented in Phase 14 EDU-06 SUMMARY.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -qE "PROTECTED_USER_FIELDS = \['waterSchedule', 'lightLevel', 'waterMode', 'fertilizeSchedule'\] as const" src/hooks/useStorage.tsx && node scripts/smoke-phase20.cjs && npm run smoke:phase18 && npm run smoke:phase19</automated>
  </verify>
  <done>PROTECTED_USER_FIELDS tuple includes 'fertilizeSchedule' as the last entry; tsc-green; smoke-phase20 still exits 0; cross-phase regression (Phase 18 + 19) preserved.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0
- `node scripts/smoke-phase20.cjs` exits 0 (no new sentinel — this guard is encoded by Plan 20-00 W0.scaffold.types.* + Plan 20-04 modal-side fertilize override path; this plan ensures the existing guard doesn't break)
- `npm run smoke:phase18 && npm run smoke:phase19` still PASS
- `grep -c "PROTECTED_USER_FIELDS" src/hooks/useStorage.tsx` returns 2 (1 declaration + 1 loop usage) — unchanged from baseline
</verification>

<success_criteria>
- Single-line surgical edit; tuple extended APPEND-ONLY
- `as const` preserved (literal-union narrowing intact)
- Cross-phase regression unaffected (Phase 14 EDU-06 + Phase 18 + Phase 19)
</success_criteria>

<output>
After completion, create `.planning/phases/20-fertilization-subsystem/20-01-SUMMARY.md`
</output>
