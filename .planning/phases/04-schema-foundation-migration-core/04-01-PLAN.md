---
phase: 04-schema-foundation-migration-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/check-no-legacy-reads.js
  - scripts/migration-smoke-test.mjs
  - tests/fixtures/v0-app-data.json
  - .planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md
  - package.json
autonomous: true
requirements:
  - SCHEMA-08
must_haves:
  truths:
    - "Running `npm run check:legacy-fields` exits 0 today; exits 1 when a non-allowlisted file reads `plant.sunHours` or `plant.waterEvery`"
    - "Running `node scripts/migration-smoke-test.mjs` exits 0 once mappers exist (Wave 1) and prints PASS for each assertion"
    - "v0 fixture covers the edge cases the planner needs to verify migration correctness (1 plant, 50 plants, undefined sunHours, suculenta, interior, allowlist soil_check)"
    - "SMOKE-TEST.md is the single hand-runnable device protocol for phase gate"
  artifacts:
    - path: "scripts/check-no-legacy-reads.js"
      provides: "CI grep guard for SCHEMA-08; allowlist of files in transition"
      contains: "ALLOWLIST"
    - path: "scripts/migration-smoke-test.mjs"
      provides: "Node-only smoke test that loads fixture, runs runMigrations, asserts shape + idempotency + backup"
      contains: "runMigrations"
    - path: "tests/fixtures/v0-app-data.json"
      provides: "Synthetic v1.0 AppData blob for fixture loading"
      contains: "plants"
    - path: ".planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md"
      provides: "Manual device test protocol for phase gate"
      contains: "v0 fixture"
    - path: "package.json"
      provides: "scripts.check:legacy-fields and scripts.smoke:migration entries"
      contains: "check:legacy-fields"
  key_links:
    - from: "package.json scripts.check:legacy-fields"
      to: "scripts/check-no-legacy-reads.js"
      via: "node invocation"
      pattern: "check-no-legacy-reads"
    - from: "scripts/migration-smoke-test.mjs"
      to: "src/utils/migration.ts"
      via: "ESM dynamic import (runs after Wave 1)"
      pattern: "migration"
---

<objective>
Build the test infrastructure that the rest of Phase 4 depends on: a CI grep guard for legacy field reads (SCHEMA-08), a node-only smoke test runner, a synthetic v1.0 fixture, and the manual device-test protocol document. No production source files are modified — all artifacts are scripts, fixtures, and docs.

Purpose: Establish verifiable feedback loops before any migration code is written. Wave 1+ will use `npm run check:legacy-fields` to enforce SCHEMA-08 on every commit, and `node scripts/migration-smoke-test.mjs` to verify mapper correctness without a test runner.

Output: One CI guard script, one smoke-test runner, one fixture JSON, one SMOKE-TEST.md, two new package.json scripts.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-schema-foundation-migration-core/04-CONTEXT.md
@.planning/phases/04-schema-foundation-migration-core/04-RESEARCH.md
@.planning/phases/04-schema-foundation-migration-core/04-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create CI grep guard for legacy field reads (SCHEMA-08)</name>
  <files>scripts/check-no-legacy-reads.js, package.json</files>
  <read_first>
    - .planning/phases/04-schema-foundation-migration-core/04-RESEARCH.md (Example 5 — full grep guard implementation)
    - .planning/phases/04-schema-foundation-migration-core/04-CONTEXT.md (decisions §Legacy Field Retention)
    - package.json (current scripts block — additive only)
    - src/types/index.ts (confirm Plant.sunHours and Plant.waterEvery field names)
  </read_first>
  <action>
    Create `scripts/check-no-legacy-reads.js` (CommonJS, node-runnable, no dependencies). It MUST:

    1. Import `child_process.execSync` only.
    2. Define `const ALLOWLIST = new Set([...])` containing these EXACT file paths (transitional readers — Phase 4-7 will migrate them):
       - `src/types/index.ts`
       - `src/types/database.ts`
       - `src/utils/migration.ts`
       - `src/utils/notificationScheduler.ts`
       - `src/utils/plantInfo.ts`
       - `src/utils/plantLogic.ts`
       - `src/utils/plantHealth.ts`
       - `src/utils/plantAlerts.ts`
       - `src/utils/plantIdentification.ts`
       - `src/screens/OnboardingScreen.tsx`
       - `src/screens/ExploreScreen.tsx`
       - `src/screens/SettingsScreen.tsx`
       - `src/components/AddPlantModal.tsx`
       - `src/components/PlantCard.tsx`
       - `src/components/PlantDetailModal.tsx`
       - `src/components/MyPlantDetailModal.tsx`
       - `src/components/PlantDatabaseCard.tsx`
       - `src/components/PlantHealthDetail.tsx`
       - `src/components/PlantDiagnosis`
       - `src/components/PlantIdentifier`
       - `src/data/plantDatabase.ts`
       - `src/data/constants.ts`
       - `src/services/syncService.ts`
       - `src/services/plantKnowledgeService.ts`
    3. Define `const FORBIDDEN_PATTERNS = ['\\.sunHours', '\\.waterEvery']`.
    4. For each pattern, run `git grep -nE "${pattern}" -- 'src/**/*.ts' 'src/**/*.tsx'`. Catch exit code 1 (no matches = OK). Re-throw any other exit code.
    5. Split output by newline, filter empty lines, parse `file:line:content` (split on first `:`). For any line whose file is NOT in `ALLOWLIST` AND whose file does NOT START WITH any allowlisted DIRECTORY prefix (so `src/components/PlantDiagnosis/Foo.tsx` matches the `src/components/PlantDiagnosis` directory entry), push to `violations`.
    6. If `violations.length > 0`: print "❌ Legacy field reads found outside allowlist:", print each violation, print "Migrate these to plant.lightLevel / plant.waterSchedule, or add the file to ALLOWLIST in scripts/check-no-legacy-reads.js if it is in transition.", `process.exit(1)`.
    7. Else print "✅ No new legacy-field reads outside allowlist." and `process.exit(0)`.

    Then EDIT `package.json` `"scripts"` block to ADD (do NOT remove existing keys):
    ```
    "check:legacy-fields": "node scripts/check-no-legacy-reads.js",
    "typecheck": "tsc --noEmit"
    ```
    Place these alphabetically after existing `"android"` entry; preserve trailing comma rules so JSON stays valid.
  </action>
  <verify>
    <automated>node scripts/check-no-legacy-reads.js; echo "EXIT=$?"</automated>
    Expected: prints "✅ No new legacy-field reads outside allowlist." and EXIT=0 (because every existing reader is allowlisted).
    Sad-path manual check (do NOT commit): temporarily insert `const x = plant.sunHours;` into `src/screens/TodayScreen.tsx` (NOT in allowlist), re-run, expect EXIT=1 with the violation printed; revert.
  </verify>
  <acceptance_criteria>
    - `scripts/check-no-legacy-reads.js` exists and is executable via `node scripts/check-no-legacy-reads.js`
    - `grep -c "ALLOWLIST" scripts/check-no-legacy-reads.js` returns >= 1
    - `grep -c "FORBIDDEN_PATTERNS\|forbidden" scripts/check-no-legacy-reads.js` returns >= 1
    - `node scripts/check-no-legacy-reads.js` exits 0 on the current codebase
    - `grep -c "check:legacy-fields" package.json` returns 1
    - `grep -c "typecheck" package.json` returns 1
    - `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0 (valid JSON)
    - `npm run check:legacy-fields` exits 0
  </acceptance_criteria>
  <done>Grep guard script committed, runs green on the current codebase, package.json has both `check:legacy-fields` and `typecheck` scripts, script fails (exit 1) when an unallowlisted file reads `plant.sunHours` or `plant.waterEvery`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create v0 fixture + node smoke-test runner</name>
  <files>tests/fixtures/v0-app-data.json, scripts/migration-smoke-test.mjs</files>
  <read_first>
    - src/types/index.ts (Plant + AppData shape — fixture must match v1.0 shape EXACTLY: include sunHours, waterEvery; do NOT include lightLevel, waterSchedule, waterMode)
    - src/data/plantDatabase.ts (lines 27-200 — for valid databaseId + category combos used by fixture)
    - .planning/phases/04-schema-foundation-migration-core/04-CONTEXT.md (decisions §Mapper Thresholds — for assertion targets)
    - .planning/phases/04-schema-foundation-migration-core/04-RESEARCH.md (Pitfall 2 MOD-3 — fixture must include user-customized sunHours overriding catalog default)
  </read_first>
  <action>
    **Step 1:** Create `tests/fixtures/v0-app-data.json` with the EXACT v1.0 unwrapped `AppData` shape (NO `schemaVersion` envelope). Required fixture content:
    ```json
    {
      "plants": [
        { "id": "p1",  "name": "Mi Monstera",     "typeId": "trepa",      "typeName": "Monstera",     "icon": "🌿", "databaseId": "monstera",    "waterEvery": 7,  "sunHours": 5, "sunDays": [1,3,5], "outdoorDays": [],    "lastWatered": "2026-04-01", "sunDoneDate": null, "outdoorDoneDate": null },
      { "id": "p2",  "name": "Cactus de la abuela", "typeId": "cactus",  "typeName": "Cactus",       "icon": "🌵", "databaseId": "cactus",      "waterEvery": 14, "sunHours": 6, "sunDays": [1,2,3,4,5,6,0], "outdoorDays": [], "lastWatered": "2026-03-20", "sunDoneDate": null, "outdoorDoneDate": null },
      { "id": "p3",  "name": "Aloe del balcón",   "typeId": "suculenta", "typeName": "Aloe Vera",    "icon": "🪴", "databaseId": "aloe",        "waterEvery": 10, "sunHours": 4, "sunDays": [], "outdoorDays": [1,3,5], "lastWatered": null, "sunDoneDate": null, "outdoorDoneDate": null },
      { "id": "p4",  "name": "Helecho",            "typeId": "helecho",  "typeName": "Helecho",      "icon": "🌿", "databaseId": "helecho",     "waterEvery": 3,  "sunHours": 1, "sunDays": [], "outdoorDays": [], "lastWatered": "2026-04-15", "sunDoneDate": null, "outdoorDoneDate": null },
      { "id": "p5",  "name": "Albahaca",           "typeId": "aromatica","typeName": "Albahaca",     "icon": "🌱", "databaseId": "albahaca",    "waterEvery": 2,  "sunHours": 6, "sunDays": [1,3,5], "outdoorDays": [], "lastWatered": null, "sunDoneDate": null, "outdoorDoneDate": null },
      { "id": "p6",  "name": "Tomate",             "typeId": "frutal",   "typeName": "Tomate",       "icon": "🍅", "databaseId": "tomate",      "waterEvery": 2,  "sunHours": 8, "sunDays": [], "outdoorDays": [], "lastWatered": "2026-04-20", "sunDoneDate": null, "outdoorDoneDate": null },
      { "id": "p7",  "name": "Sin sunHours",       "typeId": "otra",     "typeName": "Otra",         "icon": "🌱", "waterEvery": 7,  "sunDays": [], "outdoorDays": [], "lastWatered": null, "sunDoneDate": null, "outdoorDoneDate": null }
      ],
      "notes": {},
      "reminders": {},
      "location": null,
      "onboardingCompleted": true,
      "userName": "Gaston",
      "notificationSettings": null,
      "plantNetApiKey": null,
      "installDate": "2026-03-19",
      "identificationCount": 0,
      "diagnosisCount": 0,
      "diagnosisHistory": {},
      "shoppingList": []
    }
    ```
    Note: plant `p7` deliberately omits `sunHours` to test the `?? 3` fallback in `migratePlant_0to1`. Plants p3 (aloe) and p2 (cactus) MUST end up `waterMode === 'soil_check'` per the allowlist + suculenta-category rule.

    **Step 2:** Create `scripts/migration-smoke-test.mjs` (ES module, node-runnable, no dependencies). It MUST:

    1. Use top-level await; import via `const fs = await import('node:fs/promises')`.
    2. Read fixture: `const fixture = JSON.parse(await fs.readFile('tests/fixtures/v0-app-data.json', 'utf8'))`.
    3. Dynamic import migration module compiled-on-the-fly via `tsx` is NOT available; instead, the smoke runner ASSUMES Wave 1 has built `src/utils/migration.ts` as plain TS. To run from node, we will COMPILE TS source to JS in-memory using `typescript` API. Project already has `typescript` as devDep. Implementation:
       ```js
       const ts = (await import('typescript')).default;
       const tsSource = await fs.readFile('src/utils/migration.ts', 'utf8');
       // Strip imports of types-only modules; migration.ts must be runtime-pure to be smoke-testable
       const compiled = ts.transpileModule(tsSource, { compilerOptions: { module: ts.ModuleKind.ESNext, target: ts.ScriptTarget.ES2022 } }).outputText;
       // Write to tmp file then dynamic import (eval cannot import)
       const tmp = `${process.cwd()}/scripts/.tmp-migration.mjs`;
       await fs.writeFile(tmp, compiled);
       const mod = await import(tmp);
       const { runMigrations, migratePlant_0to1, sunHoursToLightLevel, applyColdFactor, inferWaterMode, CURRENT_SCHEMA_VERSION } = mod;
       ```
       NOTE TO EXECUTOR: This step requires that `src/utils/migration.ts` (Wave 1) imports `findDatabaseEntry` from `'./plantInfo'` ONLY behind a try/catch fallback that defaults to `undefined` when the import fails (so smoke runner can run without the entire RN module graph). Wave 1 task explicitly handles this pattern.

       If dynamic compile is brittle, fallback: USE `node --experimental-strip-types` on `src/utils/migration.ts` directly (Node 22+ supports this). Try compile path first; if it throws "ERR_INVALID_MODULE_SPECIFIER" or similar, document fallback in script header. Add a comment block at top of the script: `// If this script breaks on a future Node version, switch to: node --experimental-strip-types scripts/migration-smoke-test.mjs (requires Node 22+)`.

    4. Assertions (use plain `if` + `console.log('PASS:' / 'FAIL:')` + `process.exitCode = 1` on FAIL — NO assert library):
       - **Mapper unit checks (LIGHT-03):** `sunHoursToLightLevel(5) === 'direct'`, `sunHoursToLightLevel(3) === 'bright_indirect'`, `sunHoursToLightLevel(2) === 'medium_indirect'`, `sunHoursToLightLevel(1) === 'low'`, `sunHoursToLightLevel(4.999) === 'bright_indirect'`.
       - **Mapper unit checks (WATER-04):** `applyColdFactor(7, 'suculentas') === 14`, `applyColdFactor(7, 'interior') === 11` (round of 10.5), `applyColdFactor(7, 'exterior') === 12`, `applyColdFactor(7, 'aromaticas') === 11`, `applyColdFactor(7, 'huerta') === 9`, `applyColdFactor(7, 'frutales') === 11`, `applyColdFactor(20, 'suculentas') === 30` (clamp), `applyColdFactor(0.4, 'interior') === 1` (clamp lower).
       - **inferWaterMode:** `inferWaterMode('suculentas', 'aloe') === 'soil_check'`, `inferWaterMode('interior', 'aloe') === 'soil_check'` (allowlist), `inferWaterMode('interior', 'monstera') === 'fixed'`, `inferWaterMode('frutales', 'tomate') === 'fixed'`.
       - **Migration shape (SCHEMA-01):** Run `const persisted = { schemaVersion: 0, data: fixture }; const migrated = runMigrations(persisted);`. Assert `migrated.plants.length === 7`. For every plant: `typeof p.lightLevel === 'string'`, `typeof p.waterSchedule?.warm === 'number'`, `typeof p.waterSchedule?.cold === 'number'`, `p.waterMode === 'fixed' || p.waterMode === 'soil_check'`, `p._migratedFromV0 === true`.
       - **Per-plant truths:** p1 (Monstera, sunHours 5) → `lightLevel === 'direct'`, `waterSchedule.warm === 7`, `waterSchedule.cold === 11`, `waterMode === 'fixed'`. p3 (Aloe) → `waterMode === 'soil_check'`. p2 (Cactus) → `waterMode === 'soil_check'`. p7 (no sunHours) → `lightLevel === 'bright_indirect'` (default 3 fallback).
       - **Idempotency (SCHEMA-03):** Wrap migrated in envelope: `const second = runMigrations({ schemaVersion: 1, data: migrated });`. Assert `JSON.stringify(second) === JSON.stringify(migrated)`.
       - **Defensive per-plant guard:** `migratePlant_0to1(migrated.plants[0])` returns plant unchanged (`JSON.stringify` matches).
    5. Print final summary: `${pass}/${total} PASS`. `process.exit(process.exitCode || 0)`.
    6. Cleanup tmp: `await fs.unlink(tmp).catch(() => {})` in a finally block.

    Add `package.json` script: `"smoke:migration": "node scripts/migration-smoke-test.mjs"` (ADD to scripts block; preserve JSON validity).
  </action>
  <verify>
    <automated>ls tests/fixtures/v0-app-data.json && node -e "JSON.parse(require('fs').readFileSync('tests/fixtures/v0-app-data.json','utf8'))" && ls scripts/migration-smoke-test.mjs</automated>
    Expected: fixture parses as valid JSON, smoke script exists. Full smoke run is verified in Wave 1 once `src/utils/migration.ts` exists; the script will exit 1 (cannot find `src/utils/migration.ts`) until then — that is EXPECTED at Wave 0.
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/v0-app-data.json` exists and parses as valid JSON
    - `node -e "const f=JSON.parse(require('fs').readFileSync('tests/fixtures/v0-app-data.json','utf8')); console.log(f.plants.length)"` prints `7`
    - Fixture contains plants p1-p7 with these exact ids: `node -e "const f=JSON.parse(require('fs').readFileSync('tests/fixtures/v0-app-data.json','utf8')); console.log(f.plants.map(p=>p.id).join(','))"` prints `p1,p2,p3,p4,p5,p6,p7`
    - Plant p7 has no `sunHours` field: `node -e "const f=JSON.parse(require('fs').readFileSync('tests/fixtures/v0-app-data.json','utf8')); console.log('sunHours' in f.plants[6])"` prints `false`
    - `scripts/migration-smoke-test.mjs` exists
    - `grep -c "runMigrations" scripts/migration-smoke-test.mjs` returns >= 1
    - `grep -c "sunHoursToLightLevel" scripts/migration-smoke-test.mjs` returns >= 5 (one per assertion case)
    - `grep -c "smoke:migration" package.json` returns 1
  </acceptance_criteria>
  <done>v0 fixture exists with 7 plants covering all migration edge cases; smoke runner script exists and is wired to `npm run smoke:migration`; full execution will be validated in Wave 1 once mappers are written.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create SMOKE-TEST.md phase-gate manual protocol</name>
  <files>.planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md</files>
  <read_first>
    - .planning/phases/04-schema-foundation-migration-core/04-VALIDATION.md (Manual-Only Verifications table — every row becomes a section)
    - .planning/phases/04-schema-foundation-migration-core/04-RESEARCH.md (§Validation Architecture — Phase Gate scenarios 1-5)
    - .planning/phases/04-schema-foundation-migration-core/04-CONTEXT.md (success criteria from ROADMAP)
  </read_first>
  <action>
    Create `.planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md` with this exact section structure:

    ```
    # Phase 4 — Manual Smoke Test Protocol

    Run before declaring Phase 4 ship-ready. Tests run on a real low-end Android device (Pixel 3a-class or older) AND a real iOS device. Sign off below per scenario.

    ## Pre-flight
    - [ ] `npx tsc --noEmit` exits 0
    - [ ] `npm run check:legacy-fields` exits 0
    - [ ] `npm run smoke:migration` exits 0
    - [ ] Build a v1.0 fixture build (use git checkout of last v1.0 tag) and install on test device
    - [ ] Populate 50 plants in v1.0 build (use Settings dev tool OR sideload `tests/fixtures/v0-app-data.json` via dev console: `AsyncStorage.setItem('plant-agenda-v2', JSON.stringify(fixture))`)

    ## Scenario 1 — Happy-path migration (SCHEMA-01, SCHEMA-04, SCHEMA-05, UX-01)
    Steps:
    1. With v1.0 fixture data populated, OTA-upgrade to v1.1 build (or sideload v1.1 over v1.0).
    2. Cold launch the app from device launcher.
    3. Observe launch — there should be NO white flash, NO splash beyond the existing one, NO error banner.
    Expected:
    - [ ] App opens directly to Hoy / Plantas with all 50 plants visible
    - [ ] Open any plant → MyPlantDetailModal shows the migration tooltip ONCE
    - [ ] Dismiss tooltip → reopen same plant → tooltip is gone
    - [ ] Open a different plant → tooltip shows again (per-plant)
    - [ ] In Supabase analytics_events table (or `__DEV__` console): `migration_started` event fired ONCE with `{ plantCount: 50 }`
    - [ ] `migration_completed` event fired ONCE with `{ plantCount: 50, durationMs: <200, schemaVersionFrom: 0, schemaVersionTo: 1 }`

    ## Scenario 2 — Kill mid-migration (SCHEMA-02, SCHEMA-07)
    Steps:
    1. Same fixture setup as Scenario 1.
    2. Cold launch v1.1 build.
    3. Within 1 second of launch (before splash dismisses), force-kill the app from recent-apps.
    4. Cold launch again.
    Expected:
    - [ ] App opens normally — either fully migrated OR shows the legacy-data banner
    - [ ] If banner shown: tap into a plant, edit a name, kill app, reopen — change persisted (legacy reads still working)
    - [ ] AsyncStorage backup key `plant-agenda-v2.backup-pre-v1.1` exists (verify via dev console: `AsyncStorage.getItem('plant-agenda-v2.backup-pre-v1.1')` returns the original v1.0 blob exactly)

    ## Scenario 3 — Idempotency (SCHEMA-03)
    Steps:
    1. After Scenario 1 completes successfully, kill app.
    2. Cold launch v1.1 build again.
    3. Check analytics events.
    Expected:
    - [ ] NO `migration_started` event on this second launch (envelope short-circuits)
    - [ ] App opens with same plant data, no UI change
    - [ ] Bytes-on-disk for `plant-agenda-v2` are identical to before launch (within JSON key-order stability tolerance)

    ## Scenario 4 — Notification reschedule (SCHEMA-06)
    Steps:
    1. Pre-migration: in v1.0 build, ensure morning reminder is enabled and a few sun-needing plants exist. Run `getAllScheduledNotificationsAsync()` via dev console; record count and dump payloads.
    2. Upgrade to v1.1 and launch (Scenario 1 setup).
    3. After app settles (~2s post-launch), run `getScheduledNotificationCounts()` via dev console.
    Expected:
    - [ ] Total scheduled count is non-zero (morning + sun reminders rescheduled)
    - [ ] No notification's `data` payload contains `sunHours` references
    - [ ] `isNotificationsAvailable()` returns `true` (proves no silent disable from CRIT-3)

    ## Scenario 5 — Forced migration failure (SCHEMA-07)
    Steps:
    1. Build v1.1 with `__DEV__ && process.env.FORCE_MIGRATION_FAIL === '1'` flag enabled (set via `npx expo start --dev-client` env var). The dev build must throw inside `migratePlant_0to1` when this flag is set (Wave 2 task adds this dev-only hook).
    2. Cold launch.
    Expected:
    - [ ] Failure banner visible above tabs ("Tu jardín está cargando con datos antiguos" / "Your garden is loading with older data")
    - [ ] App is fully usable — can open plants, create plants, edit, diagnose
    - [ ] AsyncStorage live key `plant-agenda-v2` is UNTOUCHED (still v1.0 unwrapped shape)
    - [ ] `migration_failed` analytics event fired with `{ stage: 'transform' }` payload

    ## Performance Gate (SCHEMA-04)
    - [ ] On Pixel 3a-class device with 50 plants: `migration_completed.durationMs` < 200ms
    - [ ] No splash flash visible to the human eye
    - [ ] Launch-to-first-paint not perceptibly slower than v1.0 build

    ## Sign-off
    - [ ] All scenarios pass on iOS
    - [ ] All scenarios pass on Android (low-end)
    - [ ] Phase gate approved by: ________ on ________
    ```

    Use the EXACT section structure and checklist items above. The document is a working device-test checklist — do NOT add prose commentary beyond the section headers and steps.
  </action>
  <verify>
    <automated>ls .planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md && grep -c "Scenario" .planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md</automated>
    Expected: file exists, "Scenario" appears at least 5 times.
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md` exists
    - `grep -c "^## Scenario" .planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md` returns 5
    - `grep -c "SCHEMA-0" .planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md` returns >= 6
    - `grep -c "Performance Gate" .planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md` returns 1
    - `grep -c "Sign-off" .planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md` returns 1
    - `grep -c "FORCE_MIGRATION_FAIL" .planning/phases/04-schema-foundation-migration-core/SMOKE-TEST.md` returns 1
  </acceptance_criteria>
  <done>SMOKE-TEST.md exists with 5 scenarios + performance gate + sign-off; covers SCHEMA-01..07, UX-01; references force-fail dev flag that Wave 2 will implement.</done>
</task>

</tasks>

<verification>
Wave 0 establishes test infrastructure with no production code changes. Verify:
- `npm run check:legacy-fields` exits 0 today
- `tests/fixtures/v0-app-data.json` parses and has 7 plants with id sequence p1..p7
- `scripts/migration-smoke-test.mjs` exists (full smoke run validated Wave 1)
- `SMOKE-TEST.md` has 5 scenarios + perf gate + sign-off
- `npx tsc --noEmit` still passes (no source changes)
</verification>

<success_criteria>
- All three task acceptance_criteria pass
- `npm run check:legacy-fields` exits 0
- `npx tsc --noEmit` exits 0
- No production source files modified (only scripts/, tests/, .planning/, package.json)
- Wave 1 unblocked: mappers can be smoke-tested as soon as `src/utils/migration.ts` lands
</success_criteria>

<output>
After completion, create `.planning/phases/04-schema-foundation-migration-core/04-01-SUMMARY.md` documenting:
- Files created (full paths)
- Allowlist contents (so Wave 2-7 know what files are pre-approved for transitional reads)
- Smoke runner approach (compile-on-the-fly TS) and known fragility points
- Wave 0 sign-off: ready for Wave 1
</output>
