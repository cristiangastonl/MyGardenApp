---
phase: 22-gamification-toasts-haptics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/smoke-phase22.cjs
  - package.json
  - .gitignore
  - src/i18n/locales/en/common.json
  - src/i18n/locales/es/common.json
autonomous: true
requirements:
  - GAM-01
  - GAM-02
  - GAM-05
must_haves:
  truths:
    - "scripts/smoke-phase22.cjs exists and runs (exit code 0 with non-zero PASS)"
    - "npm run smoke:phase22 wired in package.json"
    - "gamification.toastSuccess i18n key resolves in EN + ES (voseo)"
    - "GAM-01/02/05 placeholders SKIP at baseline and will flip to PASS as Plans 22-01/02 land"
    - "Cross-phase STRICT sentinels for Phase 18 + 19 + 20 + 21 are preserved (regression gate)"
  artifacts:
    - path: "scripts/smoke-phase22.cjs"
      provides: "Three-tier smoke runner (PASS scaffold + SKIP placeholders + STRICT cross-phase regression)"
      min_lines: 250
    - path: "package.json"
      provides: "smoke:phase22 npm script"
      contains: 'smoke:phase22'
    - path: ".gitignore"
      provides: ".tmp-phase22/ ignore line (precautionary parity with Phase 21)"
      contains: "scripts/.tmp-phase22/"
    - path: "src/i18n/locales/en/common.json"
      provides: "gamification namespace with toastSuccess key in EN"
      contains: 'gamification'
    - path: "src/i18n/locales/es/common.json"
      provides: "gamification namespace with toastSuccess key in ES (voseo)"
      contains: 'gamification'
  key_links:
    - from: "scripts/smoke-phase22.cjs"
      to: "src/i18n/locales/{en,es}/common.json"
      via: "JSON.parse + getNested('gamification.toastSuccess')"
      pattern: "gamification\\.toastSuccess"
    - from: "scripts/smoke-phase22.cjs"
      to: "src/hooks/useStorage.tsx, src/screens/{PlantsScreen,TodayScreen,CalendarScreen}.tsx"
      via: "readSafe + regex sentinels (SKIP until W1/W2 lands code)"
      pattern: "gamificationToastVisible|setOnTaskCompleted|waterPlant|sunPlant|outdoorPlant"
    - from: "scripts/smoke-phase22.cjs"
      to: "src/ tree"
      via: "STRICT negative-grep with CARE_STREAKS + gam_anti_patterns.md whitelist"
      pattern: "streak|consecutiveDays|dayCount|currentStreak|bestStreak|streakReset"
---

<objective>
Wave 0 Nyquist scaffold for Phase 22 (Gamification — Toasts + Haptics).

Lands the smoke runner harness, the npm script, the i18n key skeleton (single gamification.toastSuccess key in EN + ES with voseo for ES), and the .gitignore line. The smoke runner ships three-tier sentinels: (1) STRICT W0 scaffold PASS asserts, (2) SKIP-to-PASS placeholders for GAM-01 (Toast wiring across 3 screens), GAM-02 (haptic firing in 4 useStorage actions), and GAM-05 (negative-grep), (3) STRICT cross-phase regression sentinels preserving Phase 18 + 19 + 20 + 21 invariants verbatim (forked from scripts/smoke-phase21.cjs L219-244).

GAM-05 negative-grep MUST whitelist (a) CARE_STREAKS (V2.0 placeholder flag at src/config/features.ts:31) AND (b) any line containing the literal gam_anti_patterns.md (allows code-comment references to the anti-pattern memory file).

The runner is NEVER modified after Wave 0 lands (Phase 14-00 / 19-00 / 20-00 / 21-00 precedent). Later plans flip SKIPs to PASSes by landing concrete code/JSX/data.

Purpose: Lock the validation contract before any source code lands. Smoke runner is the deterministic gate per VALIDATION.md.
Output: 5 file changes (1 new script, 1 npm script edit, 1 .gitignore edit, 2 i18n JSON edits).
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-gamification-toasts-haptics/22-CONTEXT.md
@.planning/phases/22-gamification-toasts-haptics/22-RESEARCH.md
@.planning/phases/22-gamification-toasts-haptics/22-VALIDATION.md

# Reference implementations
@scripts/smoke-phase21.cjs
@src/i18n/locales/en/common.json
@src/i18n/locales/es/common.json
@.gitignore
@package.json
@src/config/features.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/smoke-phase22.cjs (fork from smoke-phase21.cjs)</name>
  <files>scripts/smoke-phase22.cjs</files>
  <read_first>
    - scripts/smoke-phase21.cjs (full file — fork target; preserves CommonJS harness, readSafe/assert/assertSkippable/getNested helpers, and the three-tier pattern at L66-244)
    - src/config/features.ts (line 31 — confirm CARE_STREAKS: false is the ONLY existing streak-token line in src/; verifies the whitelist regex is necessary)
    - .planning/phases/22-gamification-toasts-haptics/22-RESEARCH.md Validation Architecture section (per-task verification map + smoke runner sample code at L373-420)
    - .planning/phases/22-gamification-toasts-haptics/22-VALIDATION.md Per-Task Verification Map (W0/W1/W2 sentinels enumerated)
  </read_first>
  <action>
    Create a NEW file scripts/smoke-phase22.cjs by forking the structure of scripts/smoke-phase21.cjs. Preserve verbatim the assertion harness (L23-44), readSafe helper, and getNested helper. Replace ALL Phase 21 / JOURNAL-* logic with Phase 22 GAM-* logic.

    File MUST start with a shebang + header comment block describing the three-tier pattern (PASS scaffold + SKIP-to-PASS placeholders for GAM-01/02/05 + STRICT cross-phase regression for Phase 18 + 19 + 20 + 21). Usage: `node scripts/smoke-phase22.cjs`.

    Read these source files ONCE at top of runner:
    - src/hooks/useStorage.tsx (useStorageSrc)
    - src/screens/PlantsScreen.tsx (plantsScreenSrc)
    - src/screens/TodayScreen.tsx (todayScreenSrc)
    - src/screens/CalendarScreen.tsx (calendarScreenSrc)
    - src/components/PlantCard.tsx (plantCardSrc)
    - src/components/MyPlantDetailModal.tsx (modalSrc)
    - src/utils/plantLogic.ts (plantLogicSrc)
    - src/utils/notificationScheduler.ts (schedulerSrc)
    - src/i18n/locales/en/common.json (parsed to enCommon)
    - src/i18n/locales/es/common.json (parsed to esCommon)
    - src/services/journalService.ts (journalServiceSrc — cross-phase only)
    - src/components/plant-detail/JournalSection.tsx (journalSectionSrc — cross-phase only)
    - src/components/plant-detail/JournalEntryRow.tsx (journalEntryRowSrc — cross-phase only)
    - src/components/plant-detail/JournalQuickAddSheet.tsx (journalQuickAddSrc — cross-phase only)
    - src/types/index.ts (typesSrc — cross-phase only)

    Implement the THREE-TIER sentinel block in this exact order:

    TIER 1 — W0 Scaffold (STRICT PASS — files MUST exist after Wave 0):
      assert(readSafe('scripts/smoke-phase22.cjs') !== null, 'W0.scaffold.smoke-runner-exists');
      assert(/smoke:phase22/.test(readSafe('package.json') || ''), 'W0.scaffold.npm-script-exists');
      assert(/scripts\/\.tmp-phase22\//.test(readSafe('.gitignore') || ''), 'W0.scaffold.gitignore-tmp-dir');
      assert(getNested(enCommon, 'gamification.toastSuccess') !== undefined, 'W0.scaffold.i18n.en.gamification.toastSuccess');
      assert(getNested(esCommon, 'gamification.toastSuccess') !== undefined, 'W0.scaffold.i18n.es.gamification.toastSuccess');
      assert(getNested(esCommon, 'gamification.toastSuccess') === '¡Vas bien! 🌱', 'W0.scaffold.i18n.es.voseo-literal');
      assert(getNested(enCommon, 'gamification.toastSuccess') === "You're on it! 🌱", 'W0.scaffold.i18n.en.literal');

    TIER 2 — GAM-01 / GAM-02 / GAM-05 SKIP-to-PASS placeholders (all assertSkippable returning undefined until the new identifier appears):
      - GAM-02.useStorage.waterPlant-action: regex /const\s+waterPlant\s*=\s*useCallback/
      - GAM-02.useStorage.sunPlant-action: regex /const\s+sunPlant\s*=\s*useCallback/
      - GAM-02.useStorage.outdoorPlant-action: regex /const\s+outdoorPlant\s*=\s*useCallback/
      - GAM-02.useStorage.waterPlant.triggerHaptic-success: proximity regex /const\s+waterPlant\s*=\s*useCallback[\s\S]{0,400}triggerHaptic\(['"]success['"]\)/ (SKIP if waterPlant not yet present)
      - GAM-02.useStorage.sunPlant.triggerHaptic-success: analogous proximity regex
      - GAM-02.useStorage.outdoorPlant.triggerHaptic-success: analogous proximity regex
      - GAM-02.useStorage.fertilizePlant.triggerHaptic-success: proximity regex /const\s+fertilizePlant\s*=\s*useCallback[\s\S]{0,400}triggerHaptic\(['"]success['"]\)/ (SKIP if /triggerHaptic/ test on useStorageSrc is false)
      - GAM-02.useStorage.triggerHaptic-import: regex /import\s*\{[^}]*triggerHaptic[^}]*\}\s*from\s*['"][^'"]*utils\/haptics['"]/
      - GAM-01.useStorage.setOnTaskCompleted-in-interface: regex /setOnTaskCompleted\s*:\s*\(/
      - GAM-01.useStorage.setOnTaskCompleted-implementation: regex /const\s+setOnTaskCompleted\s*=\s*useCallback/
      - GAM-01.useStorage.onTaskCompletedRef-useRef: regex /onTaskCompletedRef\s*=\s*useRef/
      - GAM-01.useStorage.waterPlant.onTaskCompleted-call: proximity regex /const\s+waterPlant\s*=\s*useCallback[\s\S]{0,400}onTaskCompletedRef\.current\?\.\(\)/ (SKIP if onTaskCompletedRef absent)
      - GAM-01.useStorage.sunPlant.onTaskCompleted-call: analogous
      - GAM-01.useStorage.outdoorPlant.onTaskCompleted-call: analogous
      - GAM-01.useStorage.fertilizePlant.onTaskCompleted-call: analogous
      - GAM-01.PlantsScreen.gamificationToastVisible-state: regex /gamificationToastVisible/ on plantsScreenSrc
      - GAM-01.TodayScreen.gamificationToastVisible-state: regex /gamificationToastVisible/ on todayScreenSrc
      - GAM-01.CalendarScreen.gamificationToastVisible-state: regex /gamificationToastVisible/ on calendarScreenSrc
      - GAM-01.Toast.vas-bien-wired-3-screens: STRICT proximity regex /gamificationToastVisible[\s\S]{0,500}gamification\.toastSuccess|gamification\.toastSuccess[\s\S]{0,500}gamificationToastVisible/ MUST match in ALL THREE screens (SKIP when none match)
      - GAM-01.Toast.durationMs-2000-3-screens: proximity regex /visible=\{gamificationToastVisible\}[\s\S]{0,300}durationMs=\{2000\}|durationMs=\{2000\}[\s\S]{0,300}visible=\{gamificationToastVisible\}/ MUST match in ALL THREE screens
      - GAM-01.PlantsScreen.useEffect-registers-callback: proximity regex /setOnTaskCompleted\([\s\S]{0,200}setGamificationToastVisible|setGamificationToastVisible[\s\S]{0,200}setOnTaskCompleted/
      - GAM-01.TodayScreen.useEffect-registers-callback: analogous
      - GAM-01.CalendarScreen.useEffect-registers-callback: analogous
      - GAM-02.TodayScreen.handlers-migrated-to-actions: requires /waterPlant\(/ AND /sunPlant\(/ AND /outdoorPlant\(/ on todayScreenSrc
      - GAM-02.CalendarScreen.handlers-migrated-to-actions: requires /waterPlant\(/ AND /sunPlant\(/ AND /outdoorPlant\(/ on calendarScreenSrc
      - GAM-05.useStorage.anti-pattern-comment-x4: count of 'GAM-05 lock' literal in useStorageSrc must equal 4 (SKIP if 0 — Wave 0 baseline)

    GAM-05 STRICT negative-grep block (NEVER SKIP — runs unconditionally at W0 and beyond):
      const STREAK_TOKENS_RE = /\b(streak|consecutiveDays|dayCount|currentStreak|bestStreak|streakReset)\b/i;
      const WHITELIST_LINE_RE = /CARE_STREAKS|gam_anti_patterns\.md/;
      Walk src/ recursively for .ts/.tsx/.js/.jsx files. For each line, if STREAK_TOKENS_RE matches AND WHITELIST_LINE_RE does NOT match, record violation. Final assert: violations.length === 0 with label 'GAM-05.negative-grep.no-streak-tokens-in-src'. At W0 baseline this MUST PASS (only false-positive is src/config/features.ts:31 CARE_STREAKS line, absorbed by whitelist).

    TIER 3 — STRICT cross-phase regression sentinels (NEVER SKIP — fork verbatim from smoke-phase21.cjs L219-244 AND add Phase 21 sentinels):
      - CROSS.GAM-04.healthBadge.MODAL-USAGE-PRESERVED: modalSrc.includes('PlantHealthBadge')
      - CROSS.GAM-04.healthBadge.FILE-NOT-DELETED: readSafe('src/components/PlantHealthBadge.tsx') !== null
      - CROSS.CARD-01.plantcard.gesture-pan-preserved: /Gesture\.Pan|GestureDetector/.test(plantCardSrc)
      - CROSS.GAM-03.plantcard.mood-emoji-preserved: plantCardSrc.includes('moodEmoji') && plantCardSrc.includes('moodBadge')
      - CROSS.Toast.FILE-NOT-DELETED: readSafe('src/components/Toast.tsx') !== null
      - CROSS.TOX-03.plantcard.toxicity-badges-preserved: plantCardSrc.includes('PetToxicityBadge')
      - CROSS.TOX-04.modal.mascotas-section-preserved: modalSrc.includes('MascotasContent') || modalSrc.includes('emoji="🐾"')
      - CROSS.TOX-04.modal.initialSection-preserved: modalSrc.includes('initialSection?: ModalSectionId')
      - CROSS.TOX-06.checkScript.symptoms-extension-preserved: /petToxicity[^]*?symptoms/.test(checkScript) where checkScript = readSafe('scripts/check-i18n-keys.mjs') || ''
      - CROSS.FERT-03.plantLogic.fertilize-emit: plantLogicSrc contains 'fertilize' literal
      - CROSS.FERT-03.scheduler.fertilize-filter: schedulerSrc contains 'fertilize' literal or 'fertilizeTasks'
      - CROSS.FERT-06.FertilizeCard.FILE-NOT-DELETED: readSafe('src/components/plant-detail/FertilizeCard.tsx') !== null
      - CROSS.FERT-07.checkScript.fertilizer-extension-preserved: /fertilizer\.(industrial|homemade)/.test(checkScript)
      - CROSS.JOURNAL-01.types.AppData.journals: /journals\?:\s*Record<string,\s*JournalEntry\[\]>/.test(typesSrc)
      - CROSS.JOURNAL-02.journalService.FILE-NOT-DELETED: readSafe('src/services/journalService.ts') !== null
      - CROSS.JOURNAL-04.ModalSectionId.diario-extension: /'que-hacer'\s*\|\s*'donde'\s*\|\s*'por-que'\s*\|\s*'tus-ajustes'\s*\|\s*'mascotas'\s*\|\s*'diario'/.test(modalSrc)
      - CROSS.JOURNAL-04.modal.JournalSection-rendered: modalSrc.includes('JournalSection')
      - CROSS.JOURNAL-04.PlantsScreen.journalToastVisible-preserved: STRICT proximity regex /journalToastVisible[\s\S]{0,500}journal\.savedToast|journal\.savedToast[\s\S]{0,500}journalToastVisible/ tested on plantsScreenSrc
      - CROSS.JOURNAL-04.TodayScreen.journalToastVisible-preserved: same proximity regex on todayScreenSrc
      - CROSS.JOURNAL-05.JournalSection.no-premium-gate: GATE_RE = /usePremiumGate|isPremium|canReadJournal/ MUST NOT match on JournalSection.tsx contents
      - CROSS.JOURNAL-05.JournalEntryRow.no-premium-gate: same GATE_RE MUST NOT match on JournalEntryRow.tsx contents
      - CROSS.JOURNAL-05.JournalQuickAddSheet.no-premium-gate: same GATE_RE MUST NOT match on JournalQuickAddSheet.tsx contents

    Finish with the standard report block (verbatim from smoke-phase21.cjs:246-259): prints SKIP list (if any), FAIL list (if any), then summary line `[Phase 22 smoke] PASS=N FAIL=N SKIP=N`. Exit code = `fail > 0 ? 1 : 0`.

    DO NOT add any logic that requires Phase 22 source files (useStorage actions, screen state) to exist at W0 — all such sentinels MUST be in assertSkippable blocks that return undefined when the new identifiers are absent.
  </action>
  <verify>
    <automated>node scripts/smoke-phase22.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f scripts/smoke-phase22.cjs` exits 0
    - Runner runs and exits 0: `node scripts/smoke-phase22.cjs` exit code is 0
    - W0 scaffold PASSes >= 7 (3 file/script/gitignore assertions + 2 i18n key-exists + 2 i18n literal-equality)
    - Cross-phase STRICT asserts >= 22 (Phase 18 = 5, Phase 19 = 4, Phase 20 = 4, Phase 21 = 9)
    - SKIP count in range 18-30 at baseline
    - 0 FAIL at baseline: `node scripts/smoke-phase22.cjs 2>&1 | grep -c '^FAIL:'` returns 0
    - GAM-05 negative-grep block PASSes at baseline (whitelist absorbs src/config/features.ts:31 CARE_STREAKS line)
    - Final summary line matches regex `^\[Phase 22 smoke\] PASS=[0-9]+ FAIL=0 SKIP=[0-9]+$`
  </acceptance_criteria>
  <done>
    Smoke runner shipped. Baseline: PASS >= 29, FAIL = 0, SKIP >= 18, exit 0. GAM-05 negative-grep returns 0 violations (whitelist absorbs CARE_STREAKS at src/config/features.ts:31). Cross-phase regression sentinels for Phase 18 + 19 + 20 + 21 all PASS unconditionally. Runner is FROZEN — NEVER edited after this task.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire npm script, .gitignore, and i18n skeleton</name>
  <files>
    package.json
    .gitignore
    src/i18n/locales/en/common.json
    src/i18n/locales/es/common.json
  </files>
  <read_first>
    - package.json (locate the "scripts" block — must add "smoke:phase22" entry preserving JSON validity; mirrors "smoke:phase21" entry shape)
    - .gitignore (must add a single line `scripts/.tmp-phase22/` near the existing `scripts/.tmp-phase21/` line for parity, even though no transpileModule stubs are planned)
    - src/i18n/locales/en/common.json (file already has top-level keys; add "gamification" namespace as a new top-level object alongside existing "journal" namespace at L9)
    - src/i18n/locales/es/common.json (same structure as EN; Spanish translation uses voseo per CLAUDE.md i18n rules)
  </read_first>
  <action>
    Make FOUR file changes in this exact shape:

    1. package.json — add the following line inside the "scripts" object, immediately after the existing `"smoke:phase21": "node scripts/smoke-phase21.cjs"` line (preserve JSON validity, trailing comma discipline):
       `"smoke:phase22": "node scripts/smoke-phase22.cjs",`

    2. .gitignore — add a new line `scripts/.tmp-phase22/` immediately after the existing `scripts/.tmp-phase21/` line. Precautionary parity with prior phases.

    3. src/i18n/locales/en/common.json — add a new top-level "gamification" namespace with a single key "toastSuccess". Insert BEFORE the existing "journal" namespace (around L9). The new namespace object must be exactly:
       ```
       "gamification": {
         "toastSuccess": "You're on it! 🌱"
       },
       ```

    4. src/i18n/locales/es/common.json — add the same namespace shape with voseo-friendly Spanish (LITERAL match for smoke runner sentinel `W0.scaffold.i18n.es.voseo-literal`):
       ```
       "gamification": {
         "toastSuccess": "¡Vas bien! 🌱"
       },
       ```

    CRITICAL: the literals MUST match exactly — including the trailing 🌱 emoji and the inverted exclamation `¡` in ES. The smoke runner checks these as `===` equality, not substring matches.

    DO NOT modify any other keys, namespaces, or whitespace in the i18n JSON files. After each JSON edit run `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/common.json','utf8'))"` to verify parse validity.
  </action>
  <verify>
    <automated>node scripts/smoke-phase22.cjs &amp;&amp; npm run check:i18n-keys</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "smoke:phase22" package.json` returns 1
    - `grep -c "scripts/.tmp-phase22/" .gitignore` returns 1
    - JSON validity: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/en/common.json','utf8'))"` exits 0
    - JSON validity: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/es/common.json','utf8'))"` exits 0
    - EN literal: `node -e "const j=require('./src/i18n/locales/en/common.json'); process.exit(j.gamification.toastSuccess===\"You're on it! 🌱\"?0:1)"` exits 0
    - ES voseo literal: `node -e "const j=require('./src/i18n/locales/es/common.json'); process.exit(j.gamification.toastSuccess==='¡Vas bien! 🌱'?0:1)"` exits 0
    - npm run smoke:phase22 exits 0 (no FAIL)
    - npm run check:i18n-keys exits 0 (no parity drift)
    - W0 scaffold assertions in smoke runner now all PASS (no longer would-FAIL)
  </acceptance_criteria>
  <done>
    npm script `smoke:phase22` wired; .gitignore extended; both EN and ES common.json contain `gamification.toastSuccess` with the exact literals. `node scripts/smoke-phase22.cjs` exits 0 with all 7 W0 scaffold sentinels PASSing.
  </done>
</task>

</tasks>

<verification>
After Tasks 1+2 complete:
1. `node scripts/smoke-phase22.cjs` exits 0
2. Final summary line: `[Phase 22 smoke] PASS=N FAIL=0 SKIP=M` with N >= 29, M >= 18
3. `npm run check:i18n-keys` exits 0
4. `npx tsc --noEmit` exits 0 (no TS regression from i18n edits)
5. Full cross-phase regression chain green: `npm run smoke:phase18 && npm run smoke:phase19 && npm run smoke:phase20 && npm run smoke:phase21 && npm run smoke:phase22` all exit 0
</verification>

<success_criteria>
- scripts/smoke-phase22.cjs exists and is executable as `node scripts/smoke-phase22.cjs`
- W0 scaffold tier passes (>=7 PASS asserts)
- GAM-01/02/05 placeholder tier ships in SKIP state at baseline (will flip as Plans 22-01/02 land)
- GAM-05 STRICT negative-grep block PASSes at baseline (whitelist absorbs CARE_STREAKS false-positive)
- Cross-phase STRICT tier preserves Phase 18 + 19 + 20 + 21 invariants (>=22 PASS asserts, never SKIP)
- npm run smoke:phase22 in package.json
- gamification.toastSuccess in both en/common.json and es/common.json with exact-literal voseo for ES
- Baseline exit code 0 with FAIL=0 and non-zero SKIP
- Runner is FROZEN after this plan — no edits to scripts/smoke-phase22.cjs in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/22-gamification-toasts-haptics/22-00-SUMMARY.md` summarizing:
- Smoke runner shipped (scripts/smoke-phase22.cjs, file size, PASS/FAIL/SKIP at baseline)
- npm script + .gitignore + i18n key landings
- Baseline metrics (PASS=N, FAIL=0, SKIP=M)
- Cross-phase regression coverage (Phase 18+19+20+21 sentinel list)
- Carry-forward: SKIPs that will flip to PASS as Plans 22-01 (useStorage actions + setOnTaskCompleted) and 22-02 (3-screen integration) land
</output>
