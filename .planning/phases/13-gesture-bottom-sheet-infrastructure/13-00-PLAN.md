---
phase: 13-gesture-bottom-sheet-infrastructure
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/smoke-phase13.mjs
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
gap_closure: false

must_haves:
  truths:
    - "scripts/smoke-phase13.mjs exists and is executable via `node scripts/smoke-phase13.mjs`"
    - "Wave 0 baseline runner exits 0 with PASS report — INFRA-01/02/03 placeholder assertions are wired via assertSkippable so they SKIP gracefully until Plans 01/02 land"
    - "INFRA-02 success-criterion semantics encoded as: `grep -c 'BottomSheetModalProvider' App.tsx === 3` (1 import + 1 JSX opening tag + 1 JSX closing tag — single App-root wrap, locked JSX shape) AND `grep -c 'GestureHandlerRootView' App.tsx === 3` (same shape)"
    - "babel.config.js conditional assertion: either file is absent OR (file exists AND uses `react-native-worklets/plugin` AND does NOT use deprecated `react-native-reanimated/plugin`)"
    - "Phase 13 runner is file-content asserts only — NO ts.transpileModule (this is a config phase, not a runtime-logic phase)"
    - "Future Plans 01/02 tasks reference `node scripts/smoke-phase13.mjs` in their `<verify><automated>` blocks — placeholders silently flip from SKIP to PASS as App.tsx, package.json, and the new src files land"
  artifacts:
    - path: "scripts/smoke-phase13.mjs"
      provides: "Phase 13 smoke runner; harness + Wave 0 scaffold PASSes + Wave 1/2 placeholders for INFRA-01/02/03 file-content assertions"
      contains: "readFileSync"
  key_links:
    - from: "scripts/smoke-phase13.mjs"
      to: "package.json"
      via: "JSON.parse(readFileSync) + dep version-range checks for the 4 new deps"
      pattern: "react-native-reanimated"
    - from: "scripts/smoke-phase13.mjs"
      to: "App.tsx"
      via: "readFileSync + .match(/BottomSheetModalProvider/g) count + .match(/GestureHandlerRootView/g) count"
      pattern: "BottomSheetModalProvider"
    - from: "scripts/smoke-phase13.mjs"
      to: "babel.config.js"
      via: "existsSync conditional check + readFileSync content check (worklets vs reanimated plugin name)"
      pattern: "react-native-worklets/plugin"
---

<objective>
Wave 0 scaffold for Phase 13. Creates the smoke runner skeleton at `scripts/smoke-phase13.mjs` so Wave 1 (Plan 13-01) and Wave 2 (Plan 13-02) tasks can wire their file-content assertions against a runnable harness on first push.

Purpose: enforces the Nyquist sampling rule from `13-VALIDATION.md` — every Wave 1/2 task's `<verify><automated>` references `node scripts/smoke-phase13.mjs`, so this file must exist and exit 0 BEFORE Wave 1 runs. This phase is config + JSX wrapping (not runtime logic) so the runner uses pure `readFileSync` + regex matches — NO `ts.transpileModule`, NO module compilation, NO stub modules. Mirrors `smoke-phase11.mjs`/`smoke-phase12.mjs` shape minus the compile path.

Output: 1 new file (~150 LOC). No source files touched. After this plan, `node scripts/smoke-phase13.mjs` exits 0 with a PASS report containing scaffold-level assertions; Plan 13-01 and Plan 13-02 tasks make the SKIP placeholders flip to PASS as App.tsx, package.json, and the new src files (`Skeleton.tsx`, `haptics.ts`, `useDismissOnPaywall.ts`) land.
</objective>

<execution_context>
@/Users/gaston/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gaston/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-gesture-bottom-sheet-infrastructure/13-CONTEXT.md
@.planning/phases/13-gesture-bottom-sheet-infrastructure/13-RESEARCH.md
@.planning/phases/13-gesture-bottom-sheet-infrastructure/13-VALIDATION.md
@.planning/phases/12-unknown-plant-tracking/12-00-PLAN.md
@scripts/smoke-phase12.mjs
@scripts/smoke-phase11.mjs

<interfaces>
<!-- The Phase 13 smoke runner is intentionally simpler than Phase 11/12 — it is file-content asserts only. No ts.transpileModule, no stub modules, no .tmp-phase13/ dir. -->

Wave 1/2 placeholder targets (each `assertSkippable` returns `undefined` until the relevant file/JSX/import lands):

INFRA-01 (Plan 13-01) — package.json deps:
- `react-native-reanimated` matches `^4` or `~4` or `4.x.x`
- `react-native-gesture-handler` present (any version pinned by `npx expo install`)
- `@gorhom/bottom-sheet` matches `^5` or `~5` or `5.x.x`
- `expo-haptics` present (any version pinned by `npx expo install`)

INFRA-02 (Plan 13-01) — App.tsx content:
- `grep -c "BottomSheetModalProvider" App.tsx === 3` (1 import + 1 JSX opening tag + 1 JSX closing tag — locked JSX shape per ROADMAP success criterion)
- `grep -c "GestureHandlerRootView" App.tsx === 3` (same shape)
- `App.tsx` import block contains `from '@gorhom/bottom-sheet'` and `from 'react-native-gesture-handler'`
- The single `<GestureHandlerRootView>` JSX usage has `style={{ flex: 1 }}` (or equivalent that resolves to flex:1)
- The single `<BottomSheetModalProvider>` JSX usage is INSIDE `<GestureHandlerRootView>` and OUTSIDE the `Features.AUTH ?` ternary (i.e., wraps the entire SafeAreaProvider/StorageProvider/PremiumProvider chain)

INFRA-02 (Plan 13-01) — babel.config.js conditional:
- Either `babel.config.js` does NOT exist (preferred — repo state today; babel-preset-expo auto-manages worklets plugin)
- OR `babel.config.js` exists AND its contents contain `react-native-worklets/plugin` AND do NOT contain `react-native-reanimated/plugin` (the deprecated v3 name)

INFRA-03 (Plan 13-02) — new src files:
- `src/components/Skeleton.tsx` exists AND contains `export function Skeleton` or `export const Skeleton` or `export default function Skeleton`
- `src/utils/haptics.ts` exists AND contains `export function triggerHaptic` AND `export type HapticKind` (or `export { type HapticKind }`)
- `src/hooks/useDismissOnPaywall.ts` exists AND contains `export function useDismissOnPaywall`

INFRA-03/04 (Plan 13-02) — SettingsScreen dev block:
- `src/screens/SettingsScreen.tsx` contains `import { BottomSheetModal` (or `import BottomSheetModal`) from `@gorhom/bottom-sheet`
- `src/screens/SettingsScreen.tsx` contains `import { Skeleton }` from a Skeleton path
- `src/screens/SettingsScreen.tsx` contains `settings.devTestBottomSheet` reference (i18n key)
- `src/i18n/locales/en/common.json` contains `"devTestBottomSheet"` AND `"devTestBottomSheetContent"` keys
- `src/i18n/locales/es/common.json` contains the same two keys (locale parity)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/smoke-phase13.mjs runner with assertion harness, scaffold PASSes, and Plan 13-01/02 placeholders</name>
  <files>scripts/smoke-phase13.mjs</files>
  <read_first>
    - scripts/smoke-phase12.mjs (entire file — canonical pattern; copy assertion-harness shape `assert` + `assertSkippableAsync`, final PASS/FAIL report, `globalThis.__DEV__ = false` shim, ROOT path resolution. Phase 13 SHEDS the `ts.transpileModule` block + tmp-dir stub creation because this phase has no runtime-logic asserts — only file-content reads.)
    - scripts/smoke-phase11.mjs (compare structure; Phase 11 also uses transpileModule. Phase 13 follows the same import/__dirname/ROOT scaffolding but skips the compile + stub path entirely.)
    - .planning/phases/13-gesture-bottom-sheet-infrastructure/13-RESEARCH.md (Validation Architecture section, especially "Per-Task Verification Map" + "No transpileModule import-execution needed — Phase 13 is config + JSX wrapping, not runtime logic. File-content asserts via readFileSync are sufficient.")
    - .planning/phases/13-gesture-bottom-sheet-infrastructure/13-VALIDATION.md (Per-Task Verification Map — gives the exact `task ID → assertion` mapping that this runner must wire as placeholders)
    - .planning/phases/13-gesture-bottom-sheet-infrastructure/13-CONTEXT.md (§"Smoke runner (Claude's discretion)" — exact 6 coverage points + the babel.config.js adjustment captured in §"Standard Stack" of RESEARCH.md)
    - App.tsx (lines 1-50, 373-383 — current import block + current default export which has 0 BottomSheetModalProvider/GestureHandlerRootView refs; the runner's grep counts must START AT 0 in Wave 0 baseline so the placeholders SKIP, then flip to 3 once Plan 13-01 lands)
    - package.json (lines 14-50 — current `dependencies`; verify the 4 new deps are absent today; the runner's package.json placeholders return `undefined` while any dep is missing)
  </read_first>
  <behavior>
    - Test 1: `node scripts/smoke-phase13.mjs` exits 0 on the Wave 0 baseline (when App.tsx has 0 provider refs, package.json has 0 of the 4 new deps, and the 4 new src files do not exist yet).
    - Test 2: stdout contains a final summary line `[smoke-phase13] PASS N/N` where N >= 4 scaffold PASSes.
    - Test 3: stderr contains no `FAIL:` lines on Wave 0 baseline (placeholders SKIP, not FAIL).
    - Test 4: When package.json gains the 4 new deps (Plan 13-01), `INFRA-01.*` placeholders flip from SKIP to PASS — no runner edit required.
    - Test 5: When App.tsx gains the 2 imports + 2 JSX usages with explicit opening + closing tags (Plan 13-01), `INFRA-02.*` placeholders flip from SKIP to PASS (count goes from 0 → 3 for each provider).
    - Test 6: When the 3 new src files (`Skeleton.tsx`, `haptics.ts`, `useDismissOnPaywall.ts`) and SettingsScreen __DEV__ edits + i18n keys land (Plan 13-02), `INFRA-03.*` placeholders flip from SKIP to PASS.
    - Test 7: Re-running the script (idempotent) produces the same exit 0 result; no side-effect file writes.
  </behavior>
  <action>
    Create `scripts/smoke-phase13.mjs` with the following EXACT content (~150 LOC). Mirror `smoke-phase12.mjs` structure and the harness shape but skip ts.transpileModule + the tmp-dir stubs entirely (Phase 13 needs file-content reads only).

    ```javascript
    #!/usr/bin/env node
    // scripts/smoke-phase13.mjs
    // Phase 13 Gesture + Bottom-Sheet Infrastructure smoke runner.
    // Wave 0: harness + scaffold PASSes + Plan 13-01/02 placeholders.
    // Plan 13-01 wires INFRA-01 (deps) + INFRA-02 (App.tsx providers + babel).
    // Plan 13-02 wires INFRA-03 (Skeleton + haptics + useDismissOnPaywall + SettingsScreen + i18n).
    //
    // FILE-CONTENT ASSERTS ONLY. NO ts.transpileModule. Phase 13 is config + JSX wrapping
    // (not runtime logic) so module compilation is unnecessary — readFileSync + regex is sufficient.

    import { readFileSync, existsSync } from 'node:fs';
    import { fileURLToPath } from 'node:url';
    import { dirname, resolve } from 'node:path';

    globalThis.__DEV__ = false;

    const __filename = fileURLToPath(import.meta.url);
    const __dirname = dirname(__filename);
    const ROOT = resolve(__dirname, '..');

    // ─── Helpers ───
    function readSafe(relPath) {
      const abs = resolve(ROOT, relPath);
      return existsSync(abs) ? readFileSync(abs, 'utf8') : null;
    }
    function countMatches(text, pattern) {
      if (!text) return 0;
      const m = text.match(pattern);
      return m ? m.length : 0;
    }

    // ─── Assertion harness (mirrors smoke-phase12.mjs) ───
    let pass = 0, fail = 0, skip = 0;
    const errors = [];
    const skips = [];
    function assert(cond, label) {
      if (cond) { pass++; }
      else { fail++; errors.push(`FAIL: ${label}`); }
    }
    function assertSkippable(condFn, label) {
      try {
        const cond = condFn();
        if (cond === undefined) { skip++; skips.push(`SKIP: ${label}`); }
        else if (cond) { pass++; }
        else { fail++; errors.push(`FAIL: ${label}`); }
      } catch (e) {
        skip++; skips.push(`SKIP: ${label} (threw: ${e.message})`);
      }
    }

    // ─── Wave 0: scaffold PASSes (always pass — verify the harness itself) ───
    assert(typeof readFileSync === 'function', 'W0.1: node:fs.readFileSync available');
    assert(typeof existsSync === 'function', 'W0.2: node:fs.existsSync available');
    assert(typeof globalThis.__DEV__ === 'boolean', 'W0.3: __DEV__ shim present (false in smoke runner)');
    assert(existsSync(resolve(ROOT, 'package.json')), 'W0.4: package.json present at repo root');
    assert(existsSync(resolve(ROOT, 'App.tsx')), 'W0.5: App.tsx present at repo root');

    // ─── Read source files ONCE for placeholder evaluation ───
    let pkgJson = null;
    try {
      const raw = readSafe('package.json');
      pkgJson = raw ? JSON.parse(raw) : null;
    } catch { pkgJson = null; }
    const appTsx = readSafe('App.tsx');
    const babelConfig = readSafe('babel.config.js');
    const skeletonSrc = readSafe('src/components/Skeleton.tsx');
    const hapticsSrc = readSafe('src/utils/haptics.ts');
    const dismissHookSrc = readSafe('src/hooks/useDismissOnPaywall.ts');
    const settingsSrc = readSafe('src/screens/SettingsScreen.tsx');
    const enCommon = readSafe('src/i18n/locales/en/common.json');
    const esCommon = readSafe('src/i18n/locales/es/common.json');

    // ─── INFRA-01 placeholders (Plan 13-01 — package.json deps) ───
    // Each returns undefined → SKIP until the dep lands; then flips to true → PASS.
    const deps = pkgJson && pkgJson.dependencies ? pkgJson.dependencies : {};

    assertSkippable(() => {
      const v = deps['react-native-reanimated'];
      if (!v) return undefined;
      return /(\^|~)?4\.|(\^|~)4(\b|\.)/.test(v);
    }, 'INFRA-01.reanimated: package.json includes react-native-reanimated at v4+ [Plan 13-01]');

    assertSkippable(() => {
      const v = deps['react-native-gesture-handler'];
      if (!v) return undefined;
      return v.length > 0;
    }, 'INFRA-01.gesture-handler: package.json includes react-native-gesture-handler [Plan 13-01]');

    assertSkippable(() => {
      const v = deps['@gorhom/bottom-sheet'];
      if (!v) return undefined;
      return /(\^|~)?5\.|(\^|~)5(\b|\.)/.test(v);
    }, 'INFRA-01.bottom-sheet: package.json includes @gorhom/bottom-sheet at v5+ [Plan 13-01]');

    assertSkippable(() => {
      const v = deps['expo-haptics'];
      if (!v) return undefined;
      return v.length > 0;
    }, 'INFRA-01.expo-haptics: package.json includes expo-haptics [Plan 13-01]');

    // ─── INFRA-02 placeholders (Plan 13-01 — App.tsx provider wrap + babel) ───
    assertSkippable(() => {
      if (!appTsx) return undefined;
      const c = countMatches(appTsx, /BottomSheetModalProvider/g);
      if (c === 0) return undefined; // baseline before Plan 13-01 — SKIP
      return c === 3;
    }, 'INFRA-02.bottom-sheet-provider-count: App.tsx contains exactly 3 BottomSheetModalProvider refs (1 import + 1 JSX opening tag + 1 JSX closing tag) — single App-root wrap, locked JSX shape [Plan 13-01]');

    assertSkippable(() => {
      if (!appTsx) return undefined;
      const c = countMatches(appTsx, /GestureHandlerRootView/g);
      if (c === 0) return undefined; // baseline before Plan 13-01 — SKIP
      return c === 3;
    }, 'INFRA-02.gesture-root-view-count: App.tsx contains exactly 3 GestureHandlerRootView refs (1 import + 1 JSX opening tag + 1 JSX closing tag) — single App-root wrap, locked JSX shape [Plan 13-01]');

    assertSkippable(() => {
      if (!appTsx) return undefined;
      const hasGorhomImport = /from ['"]@gorhom\/bottom-sheet['"]/.test(appTsx);
      const hasRNGHImport = /from ['"]react-native-gesture-handler['"]/.test(appTsx);
      if (!hasGorhomImport && !hasRNGHImport) return undefined; // baseline — SKIP
      return hasGorhomImport && hasRNGHImport;
    }, 'INFRA-02.imports-present: App.tsx imports BOTH @gorhom/bottom-sheet AND react-native-gesture-handler [Plan 13-01]');

    assertSkippable(() => {
      if (!appTsx) return undefined;
      // Verify JSX nesting: GestureHandlerRootView must wrap BottomSheetModalProvider
      // (i.e., GHRV opens before BSMP opens; BSMP closes before GHRV closes).
      const ghrvOpen = appTsx.indexOf('<GestureHandlerRootView');
      const bsmpOpen = appTsx.indexOf('<BottomSheetModalProvider');
      const bsmpClose = appTsx.indexOf('</BottomSheetModalProvider>');
      const ghrvClose = appTsx.indexOf('</GestureHandlerRootView>');
      if (ghrvOpen === -1 || bsmpOpen === -1) return undefined; // baseline — SKIP
      return ghrvOpen < bsmpOpen && bsmpOpen < bsmpClose && bsmpClose < ghrvClose;
    }, 'INFRA-02.jsx-nesting: <GestureHandlerRootView> wraps <BottomSheetModalProvider> in App.tsx [Plan 13-01]');

    assertSkippable(() => {
      if (!appTsx) return undefined;
      const ghrvOpen = appTsx.indexOf('<GestureHandlerRootView');
      if (ghrvOpen === -1) return undefined; // baseline — SKIP
      // Slice from <GHRV opener to next > and assert style flex 1
      const sliceEnd = appTsx.indexOf('>', ghrvOpen);
      const ghrvTag = appTsx.slice(ghrvOpen, sliceEnd + 1);
      return /style=\{\{\s*flex:\s*1\s*\}\}/.test(ghrvTag);
    }, 'INFRA-02.flex-1: <GestureHandlerRootView> has style={{ flex: 1 }} [Plan 13-01]');

    assertSkippable(() => {
      // babel.config.js conditional: either absent OR uses react-native-worklets/plugin (NOT the deprecated react-native-reanimated/plugin).
      // This assertion ALWAYS evaluates (no SKIP) because absence is a valid pass state.
      if (!babelConfig) return true; // ABSENT: PASS (preferred — babel-preset-expo auto-manages plugin under SDK 54)
      const hasWorklets = /react-native-worklets\/plugin/.test(babelConfig);
      const hasDeprecated = /react-native-reanimated\/plugin/.test(babelConfig);
      return hasWorklets && !hasDeprecated;
    }, 'INFRA-02.babel-conditional: babel.config.js absent OR uses worklets/plugin (NOT deprecated reanimated/plugin) [Plan 13-01]');

    // ─── INFRA-03 placeholders (Plan 13-02 — Skeleton + haptics + useDismissOnPaywall + SettingsScreen + i18n) ───

    assertSkippable(() => {
      if (skeletonSrc === null) return undefined; // file absent — SKIP
      // Accept any of: `export function Skeleton`, `export const Skeleton`, `export default function Skeleton`, `export { Skeleton }`
      return /export\s+(?:default\s+)?(?:function|const)\s+Skeleton\b/.test(skeletonSrc)
        || /export\s*\{[^}]*\bSkeleton\b[^}]*\}/.test(skeletonSrc);
    }, 'INFRA-03.skeleton-export: src/components/Skeleton.tsx exports a Skeleton component [Plan 13-02]');

    assertSkippable(() => {
      if (skeletonSrc === null) return undefined;
      // Verify Skeleton uses Reanimated v4 + LinearGradient
      return /from\s+['"]react-native-reanimated['"]/.test(skeletonSrc)
        && /from\s+['"]expo-linear-gradient['"]/.test(skeletonSrc)
        && /useSharedValue|withRepeat|withTiming/.test(skeletonSrc);
    }, 'INFRA-03.skeleton-impl: Skeleton.tsx uses react-native-reanimated + expo-linear-gradient + shimmer worklet [Plan 13-02]');

    assertSkippable(() => {
      if (hapticsSrc === null) return undefined;
      return /export\s+function\s+triggerHaptic\b/.test(hapticsSrc)
        && /export\s+type\s+HapticKind\b/.test(hapticsSrc);
    }, 'INFRA-03.haptics-exports: src/utils/haptics.ts exports triggerHaptic + HapticKind type [Plan 13-02]');

    assertSkippable(() => {
      if (hapticsSrc === null) return undefined;
      // Verify all 7 HapticKind values are handled (impactLight/Medium/Heavy + success/warning/error + selection)
      const kinds = ['impactLight', 'impactMedium', 'impactHeavy', 'success', 'warning', 'error', 'selection'];
      return kinds.every((k) => new RegExp(`['"]${k}['"]`).test(hapticsSrc));
    }, 'INFRA-03.haptics-kinds: triggerHaptic handles all 7 HapticKind values [Plan 13-02]');

    assertSkippable(() => {
      if (dismissHookSrc === null) return undefined;
      return /export\s+function\s+useDismissOnPaywall\b/.test(dismissHookSrc)
        && /isPaywallVisible/.test(dismissHookSrc)
        && /\.dismiss\(\)/.test(dismissHookSrc);
    }, 'INFRA-03.dismiss-hook: useDismissOnPaywall.ts exports hook + subscribes to isPaywallVisible + calls .dismiss() [Plan 13-02]');

    assertSkippable(() => {
      if (settingsSrc === null) return undefined;
      const hasBSMImport = /from\s+['"]@gorhom\/bottom-sheet['"]/.test(settingsSrc);
      const hasSkeletonImport = /from\s+['"][^'"]*Skeleton['"]/.test(settingsSrc) || /\bSkeleton\b.*from/.test(settingsSrc);
      const hasDevTestKey = /settings\.devTestBottomSheet/.test(settingsSrc);
      // SKIP only if NONE of these landed (Plan 13-02 baseline)
      if (!hasBSMImport && !hasSkeletonImport && !hasDevTestKey) return undefined;
      return hasBSMImport && hasSkeletonImport && hasDevTestKey;
    }, 'INFRA-03/04.settings-test-sheet: SettingsScreen imports BottomSheetModal + Skeleton + uses settings.devTestBottomSheet key [Plan 13-02]');

    assertSkippable(() => {
      if (!enCommon || !esCommon) return undefined;
      const enHas = /"devTestBottomSheet"\s*:/.test(enCommon) && /"devTestBottomSheetContent"\s*:/.test(enCommon);
      const esHas = /"devTestBottomSheet"\s*:/.test(esCommon) && /"devTestBottomSheetContent"\s*:/.test(esCommon);
      if (!enHas && !esHas) return undefined; // baseline — SKIP
      return enHas && esHas;
    }, 'INFRA-03/04.i18n-parity: en/common.json AND es/common.json contain devTestBottomSheet + devTestBottomSheetContent keys [Plan 13-02]');

    // ─── Final report ───
    if (skip > 0) {
      console.log(`[smoke-phase13] Skipped ${skip} (Plan 13-01/02 placeholders):\n${skips.join('\n')}`);
    }
    if (fail > 0) {
      console.error(`\n[smoke-phase13] FAIL ${fail}/${pass + fail}\n${errors.join('\n')}`);
      process.exit(1);
    }
    console.log(`[smoke-phase13] PASS ${pass}/${pass}${skip > 0 ? ` (+${skip} skipped)` : ''}`);
    ```

    Requirements for the file:
    - First line is the shebang `#!/usr/bin/env node`
    - Then `chmod +x scripts/smoke-phase13.mjs` to set the executable bit (mirrors smoke-phase11/12 convention).
    - Use ONLY `readFileSync` + `existsSync` for source-file access. Do NOT import `typescript`. Do NOT create a `.tmp-phase13/` directory. Do NOT write any output files. The runner is read-only.
    - The harness shape (`assert` + `assertSkippable`) MUST be byte-identical to smoke-phase12.mjs's harness for consistency, with the only difference being `assertSkippable` is synchronous here (no `Async` suffix) because all condition functions are synchronous file-content checks — no awaits needed.
    - The 16 placeholders (4 INFRA-01 + 6 INFRA-02 + 6 INFRA-03/04) cover every Plan 13-01/02 deliverable exactly once. Do not add or remove placeholders.
    - The babel.config.js placeholder is the ONLY non-skippable Plan 13-01 placeholder — it always evaluates (absence is a valid pass state per the locked decision in CONTEXT.md §"Standard Stack" of RESEARCH.md). On Wave 0 baseline (file absent today), it returns `true` and counts as PASS.
    - The two grep-count placeholders (`INFRA-02.bottom-sheet-provider-count` and `INFRA-02.gesture-root-view-count`) MUST assert `c === 3` to match the ROADMAP-locked success criterion (1 import + 1 JSX opening tag + 1 JSX closing tag = 3 substring matches via `/Pattern/g`). These placeholders SKIP when count is 0 (Wave 0 baseline) and PASS only when count is exactly 3 (after Plan 13-01 lands the locked JSX shape with explicit opening + closing tags).

    Wave 0 expected report: `[smoke-phase13] PASS 6/6 (+15 skipped)` — 5 W0.* scaffolds + 1 babel-conditional (absent → PASS) = 6 PASS; 4 INFRA-01 + 5 INFRA-02 (excluding babel) + 6 INFRA-03/04 = 15 SKIP.
  </action>
  <verify>
    <automated>node scripts/smoke-phase13.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/smoke-phase13.mjs` exits 0
    - `test -x scripts/smoke-phase13.mjs` exits 0 (executable bit set)
    - `node scripts/smoke-phase13.mjs` exits 0
    - `node scripts/smoke-phase13.mjs 2>&1 | grep -c "^\[smoke-phase13\] PASS"` returns 1
    - `node scripts/smoke-phase13.mjs 2>&1 | grep -E "PASS [0-9]+/[0-9]+"` shows count >= 4 (Wave 0 baseline reports `PASS 6/6 (+15 skipped)` minimum)
    - `node scripts/smoke-phase13.mjs 2>&1 | grep -c "^FAIL:"` returns 0
    - `grep -c "ts.transpileModule" scripts/smoke-phase13.mjs` returns 0 (Phase 13 is file-content asserts only — NO compile path)
    - `grep -c "esbuild\|swc" scripts/smoke-phase13.mjs` returns 0 (no fallback compile path either)
    - `grep -c "assertSkippable" scripts/smoke-phase13.mjs` returns at least 16 (1 helper definition + 15 placeholders)
    - `grep -c "INFRA-01\." scripts/smoke-phase13.mjs` returns 4 (one per dep)
    - `grep -c "INFRA-02\." scripts/smoke-phase13.mjs` returns 6 (provider count x2, imports, nesting, flex-1, babel)
    - `grep -c "INFRA-03" scripts/smoke-phase13.mjs` returns at least 6 (Skeleton x2, haptics x2, dismiss hook, SettingsScreen+i18n)
    - `grep -c "BottomSheetModalProvider" scripts/smoke-phase13.mjs` returns at least 1 (the regex for App.tsx counting)
    - `grep -c "GestureHandlerRootView" scripts/smoke-phase13.mjs` returns at least 1 (the regex for App.tsx counting)
    - `grep -E "return\s+c\s+===\s+3" scripts/smoke-phase13.mjs | wc -l` returns at least 2 (both provider-count placeholders assert `c === 3` to match the ROADMAP-locked JSX shape: 1 import + opening + closing tag)
    - `grep -c "react-native-worklets/plugin" scripts/smoke-phase13.mjs` returns at least 1 (babel positive check)
    - `grep -c "react-native-reanimated/plugin" scripts/smoke-phase13.mjs` returns at least 1 (babel deprecated-name negative check)
    - `grep -c "@gorhom/bottom-sheet" scripts/smoke-phase13.mjs` returns at least 2 (deps check + import check)
    - `grep -c "react-native-gesture-handler" scripts/smoke-phase13.mjs` returns at least 2 (deps check + import check)
    - `grep -c "expo-haptics" scripts/smoke-phase13.mjs` returns at least 1
    - `grep -c "expo-linear-gradient" scripts/smoke-phase13.mjs` returns at least 1
    - `grep -c "useSharedValue\|withRepeat\|withTiming" scripts/smoke-phase13.mjs` returns at least 1
    - `grep -c "isPaywallVisible" scripts/smoke-phase13.mjs` returns at least 1
    - `grep -c "devTestBottomSheet" scripts/smoke-phase13.mjs` returns at least 2 (en + es i18n key checks)
    - The file contains a `#!/usr/bin/env node` shebang as line 1
    - `node scripts/smoke-phase13.mjs 2>&1 | grep -c "Skipped"` returns 1 (placeholder summary present on Wave 0 baseline)
  </acceptance_criteria>
  <done>
    Smoke runner exists, is executable, exits 0, and reports `PASS 6/6 (+15 skipped)` on Wave 0 baseline (when App.tsx has 0 BSM/GHRV refs, package.json has 0 of the 4 new deps, and the 3 new src files do not exist). The two provider-count placeholders assert `c === 3` to match the ROADMAP-locked JSX shape (1 import + 1 JSX opening tag + 1 JSX closing tag). Plans 13-01 and 13-02 can now reference `node scripts/smoke-phase13.mjs` in their `<verify><automated>` blocks — the harness is operational and the 15 placeholders silently flip from SKIP to PASS as their respective deliverables land.
  </done>
</task>

</tasks>

<verification>
After Task 1 completes:

```bash
# 1. File exists and is executable
test -f scripts/smoke-phase13.mjs
test -x scripts/smoke-phase13.mjs

# 2. Runner exits 0
node scripts/smoke-phase13.mjs

# 3. Output contains the canonical PASS report
node scripts/smoke-phase13.mjs 2>&1 | grep -E "^\[smoke-phase13\] PASS [0-9]+/[0-9]+"

# 4. No transpile path (file-content asserts only — Phase 13 is config + JSX wrapping, not runtime logic)
grep -c "ts.transpileModule" scripts/smoke-phase13.mjs       # 0
grep -c "esbuild\|swc" scripts/smoke-phase13.mjs              # 0

# 5. No FAIL output
node scripts/smoke-phase13.mjs 2>&1 | grep -c "^FAIL:"        # 0

# 6. Pre-submit grep guard still clean (Phase 10 SEC-01 preserved — Wave 0 should not touch any client source)
grep -rc "EXPO_PUBLIC_PERENUAL_API_KEY" src/ .env .env.example app.json 2>/dev/null
```
</verification>

<success_criteria>
1. `scripts/smoke-phase13.mjs` exists, is executable, exits 0, and reports `PASS 6/6 (+15 skipped)` on first run (Wave 0 baseline; App.tsx has 0 BSM/GHRV refs, package.json has 0 of the 4 new deps, the 3 new src files do not exist, babel.config.js does not exist).
2. The runner uses ONLY `readFileSync` + `existsSync` for source-file access. NO `ts.transpileModule`, NO module compilation, NO stub modules, NO `.tmp-phase13/` directory.
3. The 15 Plan 13-01/02 placeholders (4 INFRA-01 + 5 INFRA-02 conditional + 6 INFRA-03/04) are wired with `assertSkippable`; they SKIP gracefully on Wave 0 baseline and silently flip to PASS as Plans 13-01 and 13-02 land their deliverables.
4. The 1 always-evaluating placeholder (babel.config.js conditional) returns PASS on Wave 0 baseline because the file is absent (preferred state under Expo SDK 54 + babel-preset-expo auto-management).
5. The two provider-count placeholders (`INFRA-02.bottom-sheet-provider-count`, `INFRA-02.gesture-root-view-count`) assert `c === 3` to match the ROADMAP-locked JSX shape (1 import + 1 JSX opening tag + 1 JSX closing tag = 3 substring matches via `/Pattern/g`).
6. Phase 10 grep guard remains clean: `grep -rc "EXPO_PUBLIC_PERENUAL_API_KEY" src/ .env .env.example app.json` shows count 0 on every line (Wave 0 does not touch any client source).
7. The runner's harness shape (`assert` + `assertSkippable`) is consistent with `smoke-phase12.mjs` — single source of truth for the smoke-runner pattern across phases.
</success_criteria>

<output>
After completion, create `.planning/phases/13-gesture-bottom-sheet-infrastructure/13-00-SUMMARY.md` documenting:
- Final PASS count from `node scripts/smoke-phase13.mjs` (Wave 0 baseline; record exact `PASS N/N (+M skipped)` line)
- Files created (1): the runner — note no stub module is needed (Phase 13 is config + JSX wrapping, NOT runtime-logic — file-content asserts via readFileSync are sufficient)
- Confirmation that the 15 Plan 13-01/02 placeholders are wired (paste the 15 `assertSkippable` labels)
- Note that Plans 13-01 and 13-02 (Wave 1, Wave 2) can now reference `node scripts/smoke-phase13.mjs` in their `<verify><automated>` blocks — the harness is operational, placeholders silently flip from SKIP to PASS as the deliverables land
- Architectural note: Phase 13 deliberately sheds the `ts.transpileModule` block from smoke-phase11/12 because Phase 13 has no runtime-logic asserts. This is a config + JSX wrapping phase. File-content reads + regex matches are sufficient and faster (~1s vs ~5s with transpile).
- Note the two provider-count placeholders are wired at `c === 3` (1 import + 1 JSX opening tag + 1 JSX closing tag) per ROADMAP success-criterion lock — Plan 13-01 lands the locked JSX shape and these placeholders flip from SKIP to PASS without any smoke-runner edits.
</output>
</output>
